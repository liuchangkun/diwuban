-- =====================================================================
-- 回滚脚本: 110_create_curve_fit_tables_ROLLBACK.sql
-- 用途: 回滚曲线拟合三表分离设计
-- 创建日期: 2025-12-02
-- 版本: v1.0
-- 说明: 删除所有由110_create_curve_fit_tables.sql创建的对象
-- =====================================================================

\encoding UTF8
SET client_encoding = 'UTF8';

BEGIN;

-- ============================================================
-- 1. 删除视图（先删除视图，因为它们依赖于表）
-- ============================================================

DROP VIEW IF EXISTS public.v_curve_fit_full CASCADE;
DROP VIEW IF EXISTS public.curve_fit_latest CASCADE;

-- ============================================================
-- 2. 删除约束变更历史表
-- ============================================================

DROP TABLE IF EXISTS public.constraint_change_history CASCADE;

-- ============================================================
-- 3. 删除学习约束参数表
-- ============================================================

DROP TABLE IF EXISTS public.learned_constraints CASCADE;

-- ============================================================
-- 4. 删除历史评估表
-- ============================================================

DROP TABLE IF EXISTS public.curve_fit_history CASCADE;

-- ============================================================
-- 5. 删除指标表（子表，需要先删除）
-- ============================================================

DROP TABLE IF EXISTS public.curve_fit_metrics CASCADE;

-- ============================================================
-- 6. 删除参数表（子表，需要先删除）
-- ============================================================

DROP TABLE IF EXISTS public.curve_fit_params CASCADE;

-- ============================================================
-- 7. 删除主表
-- ============================================================

DROP TABLE IF EXISTS public.curve_fit_results CASCADE;

-- ============================================================
-- 完成回滚
-- ============================================================

DO $$
BEGIN
    RAISE NOTICE '✅ 曲线拟合三表分离设计回滚完成';
    RAISE NOTICE '   所有表和视图已删除';
END $$;

COMMIT;

