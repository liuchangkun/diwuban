-- ============================================
-- 自适应SQL脚本：calculation_parameters（已废弃）
-- ============================================
-- 文件：scripts/sql/adaptive/03_calculation_parameters.sql
-- 状态：已废弃（2025-11-10）
-- 原因：calculation_parameters 表改为永久配置表，不再清空和重新生成
-- 说明：
--   - calculation_parameters 表包含优化后的参数（optimization_history、confidence_score等）
--   - 清空并重新生成会导致所有优化成果丢失
--   - 设备级参数和泵站级参数会被清空
--   - 因此，此脚本不再执行任何操作
-- ============================================

BEGIN;

-- ============================================
-- calculation_parameters（计算参数）- 已废弃
-- ============================================

-- 不再清空表（2025-11-10）
-- TRUNCATE TABLE calculation_parameters CASCADE;

-- ============================================
-- 以下所有 INSERT 语句已废弃（2025-11-10）
-- ============================================
-- 原因：calculation_parameters 表改为永久配置表，不再重新生成
-- 说明：如需初始化全局参数，请手动执行 INSERT 语句或使用迁移脚本
-- ============================================

-- 类型0：全局物理常数和高级方法参数（零硬编码）- 已废弃
-- HEAD_COEF_V1 方法的物理常数和参数 - 已废弃
-- INSERT INTO calculation_parameters (...) ...

-- 所有 INSERT 语句已废弃，不再执行

COMMIT;

