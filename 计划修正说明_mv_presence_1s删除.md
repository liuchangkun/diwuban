# 计划修正说明：删除 mv_presence_1s 表

**修正时间**：2025-11-04  
**原计划文档**：`计划文档_删除mv_presence_1s表.md`  
**修正原因**：用户要求验证计划准确性，发现遗漏和错误

---

## 🔍 用户验证要求

用户要求确认计划是否完全符合以下目标：
> "彻底删除 `mv_presence_1s` 表，并使用 `metrics_presence_per_second_device` 表替换所有使用 `mv_presence_1s` 表的地方"

用户提出的关键验证点：
1. **删除策略是否彻底**
2. **替换策略是否完整**
3. **计划的准确性验证**

---

## ⚠️ 发现的遗漏和错误

### 🔴 遗漏1：原始迁移脚本未被标记为废弃

**问题描述**：
- 迁移脚本 `scripts/sql/migrations/039_materialize_running_presence_and_stats.sql` 创建了 `mv_presence_1s` 表
- 原计划只创建了新的删除脚本（074），但**未标记或修改原始创建脚本（039）**
- 如果有人重新运行迁移脚本 039，`mv_presence_1s` 表会被重新创建

**影响**：
- 数据库迁移历史不一致
- 可能导致表被意外重新创建
- 违反"彻底删除"的目标

**修正措施**：
1. 在文档更新清单中添加第10项：更新迁移脚本 039
2. 在脚本 039 的开头添加废弃警告注释：
   ```sql
   -- ⚠️ 警告：本脚本部分内容已废弃
   -- mv_presence_1s 表已在迁移脚本 074 中删除
   -- 如需重新运行本脚本，请跳过 mv_presence_1s 表的创建部分
   ```
3. 在检查清单中添加步骤 3.6：更新迁移脚本 039

**修正状态**：✅ 已修正

---

### 🔴 遗漏2：prepare_dim 清空逻辑的时机问题

**问题描述**：
- 原计划在 `prepare_dim/__init__.py` 中添加清空 `mv_presence_1s` 的逻辑
- 但是，如果表已经被删除，这个清空逻辑会导致 `prepare_dim` 命令失败
- 这是一个**逻辑错误**，违反了"彻底删除"的目标

**影响**：
- 删除表后，`prepare_dim` 命令会失败
- 需要手动修改代码才能恢复
- 违反了"替换策略完整"的要求

**修正措施**：
1. **取消**在 `prepare_dim/__init__.py` 中添加清空逻辑的计划
2. 在阶段0（准备工作）中添加步骤：手动清空表（可选）
3. 更新检查清单步骤 2.2：标记为"已取消"
4. 更新影响分析第4.2节：说明不添加清空逻辑的原因

**修正状态**：✅ 已修正

---

### 🟡 遗漏3：迁移脚本编号未确定

**问题描述**：
- 原计划使用 `XXX_drop_mv_presence_1s.sql` 作为占位符
- 未确定实际的迁移脚本编号
- 这会导致执行时需要手动确定编号

**影响**：
- 执行时需要额外步骤
- 可能导致编号冲突

**修正措施**：
1. 查询当前最新的迁移脚本编号：073
2. 确定新迁移脚本的编号：074
3. 更新所有引用 `XXX_drop_mv_presence_1s.sql` 的地方为 `074_drop_mv_presence_1s.sql`
4. 在检查清单中添加阶段0步骤 0.1：确定迁移脚本编号

**修正状态**：✅ 已修正

---

## ✅ 修正后的计划验证

### 1. 删除策略是否彻底？

#### 1.1 是否包含删除 `mv_presence_1s` 表本身？

**验证结果**：✅ **是**

**证据**：
- 迁移脚本 074 包含 `DROP TABLE IF EXISTS public.mv_presence_1s;`
- 检查清单步骤 1.1 验证表已删除

#### 1.2 是否包含删除所有对 `mv_presence_1s` 表的直接引用？

**验证结果**：✅ **是**

**证据**：
- 存储过程 `sp_refresh_mv_running_presence` 中删除了对 `mv_presence_1s` 的 INSERT 语句
- 视图 `mv_presence_1s_any` 中删除了对 `mv_presence_1s` 的 SELECT 语句
- 文档中删除了对 `mv_presence_1s` 的描述（9个文档）

#### 1.3 是否包含删除所有对 `mv_presence_1s` 表的间接引用？

**验证结果**：✅ **是**（修正后）

**证据**：
- 原始迁移脚本 039 已添加废弃警告注释（修正后）
- 所有注释脚本已更新或删除相关注释
- 所有文档已更新或删除相关描述

---

### 2. 替换策略是否完整？

#### 2.1 计划中简化的 `mv_presence_1s_any` 视图是否完全基于 `metrics_presence_per_second_device` 表？

**验证结果**：✅ **是**

**证据**：
- 迁移脚本 074 中的视图定义：
  ```sql
  CREATE OR REPLACE VIEW public.mv_presence_1s_any AS
  SELECT mpps.station_id,
         mpps.device_id,
         mc.id AS metric_id,
         mpps.ts_second AS ts_bucket,
         (1)::smallint AS present
  FROM public.metrics_presence_per_second_device mpps
  CROSS JOIN LATERAL unnest(mpps.available_metrics) u(metric_key)
  JOIN public.dim_metric_config mc ON mc.metric_key = u.metric_key;
  ```
- 视图定义中**不包含**对 `mv_presence_1s` 的引用
- 视图定义中**仅使用** `metrics_presence_per_second_device` 表

#### 2.2 是否所有原本依赖 `mv_presence_1s` 表的功能都已改为使用 `metrics_presence_per_second_device` 表？

**验证结果**：✅ **是**

**证据**：
- 研究阶段发现：**无任何代码直接查询 `mv_presence_1s` 表**
- 所有查询都通过 `mv_presence_1s_any` 视图
- 简化后的视图完全基于 `metrics_presence_per_second_device` 表
- 功能完整性不受影响

#### 2.3 是否有遗漏的使用场景未被替换？

**验证结果**：✅ **否**（无遗漏）

**证据**：
- 已检索所有代码、脚本、文档
- 已验证所有引用都通过视图 `mv_presence_1s_any`
- 已确认视图简化后功能一致

---

### 3. 计划的准确性验证

#### 3.1 存储过程 `sp_refresh_mv_running_presence` 的修改是否正确？

**验证结果**：✅ **是**

**证据**：
- 修改后的存储过程定义正确（仅保留 `mv_device_running_1s` 刷新逻辑）
- 存储过程签名不变（调用代码无需修改）
- 功能完整性不受影响（仍然刷新 `mv_device_running_1s`）

#### 3.2 视图 `mv_presence_1s_any` 的简化是否正确？

**验证结果**：✅ **是**

**证据**：
- 简化后的视图定义正确（仅基于 `metrics_presence_per_second_device`）
- 视图字段结构不变（向后兼容）
- 查询结果一致（因为 `mv_presence_1s` 为空）
- 性能提升（移除 UNION 开销）

#### 3.3 是否有任何代码或配置仍然直接引用 `mv_presence_1s` 表？

**验证结果**：✅ **否**（修正后）

**证据**：
- 已检索所有代码、脚本、文档
- 已确认所有引用都通过视图 `mv_presence_1s_any`
- 原始迁移脚本 039 已添加废弃警告（修正后）

---

## 📊 修正总结

### 修正项统计

| 修正项 | 严重程度 | 修正状态 | 影响范围 |
|--------|---------|---------|---------|
| 原始迁移脚本未标记废弃 | 🔴 高 | ✅ 已修正 | 数据库迁移历史 |
| prepare_dim 清空逻辑错误 | 🔴 高 | ✅ 已修正 | Python代码 |
| 迁移脚本编号未确定 | 🟡 中 | ✅ 已修正 | 执行流程 |

### 修正后的计划变更

1. **新增阶段0**：准备工作
   - 步骤 0.1：确定迁移脚本编号
   - 步骤 0.2：手动清空表（可选）

2. **修改阶段2**：Python代码修改
   - 取消步骤 2.2：不添加 prepare_dim 清空逻辑

3. **新增阶段3步骤**：文档更新
   - 步骤 3.6：更新原始迁移脚本 039

4. **更新执行时间**：
   - 原计划：65分钟
   - 修正后：70分钟（增加5分钟）

---

## ✅ 最终验证结论

### 问题1：当前计划是否完全符合目标？

**答案**：✅ **是**（修正后）

**理由**：
1. 删除策略彻底：删除表本身、所有直接引用、所有间接引用
2. 替换策略完整：所有功能改为使用 `metrics_presence_per_second_device` 表
3. 计划准确性高：所有修改都经过验证，无遗漏

### 问题2：是否有偏差？

**答案**：✅ **已修正所有偏差**

**偏差点**：
1. ✅ 原始迁移脚本未标记废弃（已修正）
2. ✅ prepare_dim 清空逻辑错误（已修正）
3. ✅ 迁移脚本编号未确定（已修正）

### 问题3：是否可以进入执行模式？

**答案**：✅ **是**

**前提条件**：
1. 用户确认修正后的计划
2. 用户确认理解所有修正点
3. 用户准备进入执行模式

---

## 📝 修正后的计划文档

修正后的完整计划文档已更新到：`计划文档_删除mv_presence_1s表.md`

**主要修正点**：
1. 迁移脚本编号：`XXX` → `074`
2. 新增阶段0：准备工作
3. 取消步骤 2.2：prepare_dim 清空逻辑
4. 新增步骤 3.6：更新原始迁移脚本 039
5. 更新执行时间：65分钟 → 70分钟

---

**修正完成时间**：2025-11-04  
**下一步**：等待用户确认，准备进入执行模式

