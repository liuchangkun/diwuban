# 深入研究报告：数据写入机制

**研究时间**：2025-11-11
**研究对象**：数据写入机制、批量写入逻辑、事务处理、自适应批量大小

---

## 📋 研究目标

分析如何将计算结果写入fact_measurements表，理解批量写入逻辑、事务处理和性能优化机制。

---

## 🔍 数据写入入口

### write_results方法

**代码位置**：`app/services/calculation/orchestrator.py:946-1059`

```python
def write_results(
    self,
    station_id: int,
    device_id: int,
    metric_key: str,
    timestamps: np.ndarray,
    values: np.ndarray,
    method_id: Optional[str] = None,
    is_cyclic: bool = False
) -> int:
    """
    批量写入计算结果到fact_measurements表
    
    Args:
        station_id: 泵站ID
        device_id: 设备ID
        metric_key: 指标键
        timestamps: 时间戳数组
        values: 值数组
        method_id: 计算方法ID，用于构造 source_hint（可选）
        is_cyclic: 是否为循环依赖指标，默认False
    
    Returns:
        实际写入的数据点数量
    
    Note:
        - source_hint 构造规则：
          * 循环指标：calculation_cyclic_{metric_key}
          * 非循环指标：calculation_{method_id}
          * 其他情况：calculation_orchestrator
    """
```

---

## 📊 数据准备流程

### 步骤1：获取metric_id

```python
# 获取metric_id
metric_id = self.metric_mapper.get_metric_id(metric_key)
if metric_id is None:
    logger.error(f"无法找到指标 {metric_key} 的metric_id")
    return 0
```

---

### 步骤2：构造source_hint

```python
# 构造source_hint
if is_cyclic:
    source_hint = f"calculation_cyclic_{metric_key}"
elif method_id:
    source_hint = f"calculation_{method_id}"
else:
    source_hint = "calculation_orchestrator"
```

**source_hint的作用**：
- 标识数据来源
- 区分循环依赖指标和普通指标
- 便于追溯计算方法

---

### 步骤3：过滤有效数据

```python
# 过滤NaN和Inf值
valid_mask = np.isfinite(values)
valid_timestamps = timestamps[valid_mask]
valid_values = values[valid_mask]

if len(valid_values) == 0:
    logger.warning(f"所有值都是NaN或Inf，跳过写入")
    return 0
```

**关键洞察**：
- ✅ 自动过滤NaN和Inf值
- ✅ 避免写入无效数据
- ✅ 记录警告日志

---

### 步骤4：准备批量插入数据

```python
# 准备批量插入数据
rows = []
for ts, val in zip(valid_timestamps, valid_values):
    # 生成随机ID（类似现有数据的id值）
    row_id = random.randint(1000000000000000, 9999999999999999)
    
    rows.append((
        row_id,
        station_id,
        device_id,
        metric_id,
        ts,  # ts_raw
        ts,  # ts_bucket（已对齐）
        float(val),
        source_hint
    ))
```

**ID生成策略**：
- 使用随机生成的大整数
- 范围：1000000000000000 - 9999999999999999
- 避免ID冲突

---

## 🔄 批量写入逻辑

### INSERT ON CONFLICT策略

**代码位置**：`orchestrator.py:1050-1058`

```python
insert_query = """
    INSERT INTO fact_measurements (
        id, station_id, device_id, metric_id, ts_raw, ts_bucket,
        value, source_hint
    ) VALUES (%s, %s, %s, %s, %s, %s, %s, %s)
    ON CONFLICT (station_id, device_id, metric_id, ts_bucket)
    DO UPDATE SET
        value = EXCLUDED.value,
        source_hint = EXCLUDED.source_hint
"""
```

**关键特性**：
- ✅ 使用ON CONFLICT处理重复数据
- ✅ 冲突时更新value和source_hint
- ✅ 唯一约束：(station_id, device_id, metric_id, ts_bucket)

---

### 自适应批量大小

**代码位置**：`orchestrator.py:1105-1163`

```python
# 获取初始批量大小
if self.adaptive_batch and self.adaptive_batch.is_enabled():
    batch_size = self.adaptive_batch.get_initial_size()
else:
    batch_size = 10000  # 默认批量大小

# 分批写入
i = 0
while i < len(rows):
    batch_number += 1
    batch = rows[i:i + batch_size]
    
    # 使用executemany批量插入
    cur.executemany(insert_query, batch)
    written_count += len(batch)
    
    # 自适应调整批量大小
    if self.adaptive_batch and self.adaptive_batch.is_enabled():
        new_batch, reason = self.adaptive_batch.adjust(
            batch_size,
            performance
        )
        
        if new_batch != batch_size:
            logger.info(
                f"调整批量大小: {batch_size} → {new_batch}, "
                f"原因: {reason}"
            )
            batch_size = new_batch
    
    i += batch_size
```

**自适应策略**：
- 初始批量大小：10000（默认）
- 根据性能指标动态调整
- 调整原因：吞吐量、内存使用、延迟等

---

## 📌 性能监控

### PerformanceMonitor

**代码位置**：`orchestrator.py:1122-1142`

```python
# 开始批次监控
if self.performance_monitor:
    self.performance_monitor.start_batch(
        batch_number=batch_number,
        time_window_minutes=60
    )

batch_start = time.time()
batch = rows[i:i + batch_size]

# 使用executemany批量插入
cur.executemany(insert_query, batch)
written_count += len(batch)

batch_time = time.time() - batch_start

# 结束批次监控并获取性能指标
if self.performance_monitor:
    performance = self.performance_monitor.end_batch(
        batch_size=len(batch),
        data_points=len(batch)
    )
```

**性能指标**：
- duration_ms：批次耗时（毫秒）
- memory_mb：内存使用（MB）
- throughput：吞吐量（数据点/秒）
- batch_size：批量大小
- data_points：数据点数

---

### 性能指标持久化

**代码位置**：`app/services/calculation/performance_monitor.py:251-282`

```python
def _persist_metrics(self, metrics: Dict[str, Any]):
    """
    持久化性能指标到数据库
    
    Args:
        metrics: 性能指标字典
    """
    try:
        with get_connection() as conn:
            with conn.cursor() as cur:
                cur.execute("""
                    INSERT INTO calculation_performance_metrics
                    (run_id, station_id, device_id, batch_number,
                     time_window_minutes, batch_size, data_points,
                     duration_ms, memory_mb, throughput,
                     adjustment_type, adjustment_reason, old_value, new_value)
                    VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s)
                """, (
                    self.run_id,
                    self.station_id,
                    self.device_id,
                    metrics['batch_number'],
                    metrics['time_window_minutes'],
                    metrics['batch_size'],
                    metrics['data_points'],
                    metrics['duration_ms'],
                    metrics['memory_mb'],
                    metrics['throughput'],
                    metrics['adjustment_type'],
                    metrics['adjustment_reason'],
                    metrics['old_value'],
                    metrics['new_value']
                ))
                
                conn.commit()
    except Exception as e:
        logger.warning(f"持久化性能指标失败: {e}")
```

---

## 🔥 事务处理

### 连接池管理

**代码位置**：`app/adapters/db/__init__.py:132-163`

```python
@contextmanager
def get_connection(timeout: Optional[float] = None) -> Iterator[psycopg.Connection]:
    """
    获取数据库连接的便捷函数
    
    这是一个统一的连接获取接口，会根据初始化状态自动选择：
    - 如果连接池已初始化，使用连接池
    - 如果连接池未初始化，抛出错误提示
    
    参数：
        timeout: 获取连接的超时时间（秒）
    
    用法：
        with get_connection() as conn:
            # 使用连接进行数据库操作
            pass
    
    异常：
        DatabaseError: 如果连接池未初始化或获取连接失败
    """
    if not _pool_initialized:
        raise DatabaseError("数据库连接池未初始化，请先调用 init_database(settings)")
    
    try:
        from app.adapters.db.pool import get_connection as pool_get_connection
        
        with pool_get_connection(timeout=timeout) as conn:
            yield conn
    
    except Exception as e:
        raise DatabaseError(f"获取数据库连接失败: {e}") from e
```

**关键特性**：
- ✅ 使用连接池管理连接
- ✅ 自动管理连接生命周期
- ✅ 支持并发访问和连接复用

---

### 事务自动提交

**代码位置**：`orchestrator.py:1114-1133`

```python
with get_connection() as conn:
    with conn.cursor() as cur:
        # 分批写入
        i = 0
        while i < len(rows):
            batch = rows[i:i + batch_size]
            
            # 使用executemany批量插入
            cur.executemany(insert_query, batch)
            written_count += len(batch)
            
            i += batch_size
        
        # 事务自动提交（连接池管理）
```

**关键洞察**：
- ✅ 使用with语句自动管理事务
- ✅ 连接池自动提交事务
- ✅ 异常时自动回滚

---

## 📊 总结

### 数据写入机制的优势

| 优势 | 描述 |
|------|------|
| 批量写入 | 使用executemany批量插入，提高性能 |
| 自适应批量大小 | 根据性能指标动态调整批量大小 |
| 冲突处理 | 使用ON CONFLICT处理重复数据 |
| 性能监控 | 记录每个批次的性能指标 |
| 事务管理 | 自动管理事务提交和回滚 |

### 数据写入的关键点

1. **数据准备**：过滤NaN和Inf值，构造source_hint
2. **批量写入**：使用executemany批量插入
3. **冲突处理**：使用ON CONFLICT更新重复数据
4. **性能优化**：自适应批量大小，性能监控
5. **事务管理**：连接池自动管理事务

### 潜在改进点

1. **质量状态**：根据验证结果设置quality_status
2. **质量码**：记录计算过程中的质量问题到quality_codes
3. **批量提交**：考虑分批提交事务，避免长事务

---

**研究完成时间**：2025-11-11
**结论**：数据写入机制完善，支持批量写入、自适应批量大小和性能监控
**关键洞察**：使用ON CONFLICT处理重复数据，自适应批量大小提高性能

