# 缺失指标计算功能 - 详细执行计划

> **计划日期**: 2025-01-06  
> **计划模式**: RIPER-5 协议 - 计划模式  
> **基于**: 研究模式发现 + 创新模式推荐方案  
> **版本**: v1.0

---

## 📋 计划模式初始化确认

### 已读取规则文档
- ✅ 项目规则.md (751行)
- ✅ 编码规范.md (1406行)
- ✅ 数据库规范.md (825行)
- ✅ 质量标准.md (636行)

### 核心规则摘要
1. **文件大小限制**: 单文件不超过600行 [强制]
2. **代码占位符**: 禁止使用TODO/FIXME (除非计划明确说明) [强制]
3. **SQL参数化**: 必须使用参数化查询 [强制]
4. **日志中文化**: 所有日志必须使用中文 [强制]
5. **测试覆盖率**: 单元测试覆盖率 ≥ 80% [强制]

---

## 🎯 总体目标

修复研究模式发现的5类问题:
1. **P0 (立即修复)**: 移除2处代码占位符 (0.5小时)
2. **P1 (高优先级)**: 拆分4个超大文件 (16小时)
3. **P2 (中优先级)**: 补充4个专项测试 (8小时)
4. **P2 (中优先级)**: 补充种子数据 (2小时)
5. **P3 (低优先级)**: 更新配置表白名单 (0.5小时)

**总工时**: 27小时

---

## 📝 计划1: 代码占位符移除计划 (P0)

### 1.1 目标
移除2处TODO注释,实现完整的功能逻辑。

### 1.2 前置条件
- ✅ 已读取 `app/services/calculation/orchestrator.py`
- ✅ 已理解TODO注释的上下文
- ✅ 已确认数据来源和验证逻辑

### 1.3 详细步骤

#### 步骤1.1: 移除 orchestrator.py:1019 的TODO

**位置**: `app/services/calculation/orchestrator.py` 第1019行

**当前代码**:
```python
result['valid_points'] += len(values)  # TODO: 添加验证
```

**需要实现的功能**:
- 验证计算结果不是NaN或Inf
- 验证计算结果在合理范围内
- 验证计算结果的数据类型正确

**技术规范**:
1. 使用 `np.isnan()` 和 `np.isinf()` 检查无效值
2. 从 `calculation_validation_config` 表读取合理范围
3. 记录验证失败的数据点数量
4. 使用中文日志记录验证结果

**修改后的代码结构** (不实现,仅描述):
```python
# 验证计算结果的有效性
valid_mask = ~(np.isnan(values) | np.isinf(values))
valid_values = values[valid_mask]

# 验证合理范围 (从配置表读取)
if validation_config:
    range_mask = (valid_values >= validation_config['min_value']) & \
                 (valid_values <= validation_config['max_value'])
    valid_values = valid_values[range_mask]

result['valid_points'] += len(valid_values)
result['invalid_points'] += len(values) - len(valid_values)

logger.debug(f"数据验证: 有效={len(valid_values)}, 无效={len(values) - len(valid_values)}")
```

**验证标准**:
- [ ] TODO注释已移除
- [ ] 验证逻辑已实现
- [ ] 日志使用中文
- [ ] 无效数据被正确过滤
- [ ] 验证结果被记录到result字典

**风险评估**:
- 风险: 验证逻辑可能过滤掉合法数据
- 应对: 使用宽松的默认范围,记录详细日志

---

#### 步骤1.2: 移除 orchestrator.py:1047 的TODO

**位置**: `app/services/calculation/orchestrator.py` 第1047行

**当前代码**:
```python
context={'running_count': 2},  # TODO: 从数据中获取实际运行台数
```

**需要实现的功能**:
- 从 `mv_device_running_1s` 表查询当前时间窗口内运行状态为1的设备数量
- 按时间戳聚合,取众数或平均值
- 处理查询失败的情况

**技术规范**:
1. SQL查询必须使用参数化查询
2. 查询必须包含时间范围过滤
3. 查询超时设置为5秒
4. 查询失败时使用默认值2
5. 使用中文日志记录查询结果

**SQL查询结构** (不实现,仅描述):
```sql
SELECT COUNT(DISTINCT device_id) as running_count
FROM mv_device_running_1s
WHERE station_id = %s
  AND ts_second >= %s
  AND ts_second < %s
  AND is_running = 1
```

**修改后的代码结构** (不实现,仅描述):
```python
# 查询实际运行台数
running_count = self._get_running_device_count(
    station_id=station_id,
    start_time=start_time,
    end_time=end_time
)

context={'running_count': running_count}
```

**辅助函数签名** (不实现,仅描述):
```python
def _get_running_device_count(
    self,
    station_id: int,
    start_time: datetime,
    end_time: datetime
) -> int:
    """
    获取实际运行设备数量
    
    Args:
        station_id: 泵站ID
        start_time: 开始时间
        end_time: 结束时间
    
    Returns:
        运行设备数量,查询失败时返回默认值2
    """
    pass
```

**验证标准**:
- [ ] TODO注释已移除
- [ ] 查询逻辑已实现
- [ ] SQL使用参数化查询
- [ ] 包含时间范围过滤
- [ ] 查询失败时有降级策略
- [ ] 日志使用中文

**风险评估**:
- 风险: 数据库查询失败导致计算中断
- 应对: 使用try-except捕获异常,返回默认值

---

### 1.4 检查清单

**代码质量**:
- [ ] 所有TODO注释已移除
- [ ] 代码符合编码规范 (命名、格式、注释)
- [ ] 日志使用中文
- [ ] SQL使用参数化查询
- [ ] 错误处理完整

**功能正确性**:
- [ ] 数据验证逻辑正确
- [ ] 运行台数查询正确
- [ ] 边界条件处理正确
- [ ] 异常情况处理正确

**测试覆盖**:
- [ ] 已添加单元测试
- [ ] 测试覆盖正常流程
- [ ] 测试覆盖边界条件
- [ ] 测试覆盖异常情况

**文档更新**:
- [ ] 函数docstring已更新
- [ ] 代码注释已添加
- [ ] 变更记录已更新

---

## 📝 计划2: 配置表白名单更新计划 (P3)

### 2.1 目标
将2个配置表添加到白名单,防止被清理脚本误删。

### 2.2 前置条件
- ✅ 已读取 `app/services/system/cleanup.py` 第173-196行
- ✅ 已确认白名单格式
- ✅ 已确认需要添加的表名

### 2.3 详细步骤

#### 步骤2.1: 更新白名单配置

**修改文件**: `app/services/system/cleanup.py`

**修改位置**: 第173-196行的 `preserve_by_schema` 字典

**需要添加的表名**:
1. `metric_calculation_order` - 指标计算顺序表
2. `pump_characteristic_curves` - 泵特性曲线表

**修改内容** (不实现,仅描述):
在 `preserve_by_schema["public"]` 集合中添加2个表名:
```python
"public": {
    # ... 现有表名 ...
    "calculation_method_registry",
    "calculation_parameters",
    "calculation_validation_config",
    "metric_calculation_order",        # 新增
    "pump_characteristic_curves",      # 新增
    "dim_metric_config",
    "dim_stations",
    "dim_devices",
}
```

**技术规范**:
1. 保持字典格式一致
2. 表名使用双引号
3. 每个表名后添加逗号
4. 添加注释说明用途

**验证标准**:
- [ ] 2个表名已添加到白名单
- [ ] 格式正确 (双引号、逗号)
- [ ] 注释已添加
- [ ] 代码格式符合规范

---

#### 步骤2.2: 创建验证测试

**测试文件**: `tests/test_cleanup_whitelist.py` (新建)

**测试场景**:
1. 测试白名单中的表不会被清理
2. 测试非白名单表会被清理
3. 测试新增的2个表在白名单中

**测试代码结构** (不实现,仅描述):
```python
def test_whitelist_contains_calculation_tables():
    """测试白名单包含计算功能配置表"""
    from app.services.system.cleanup import preserve_by_schema
    
    whitelist = preserve_by_schema["public"]
    
    assert "metric_calculation_order" in whitelist
    assert "pump_characteristic_curves" in whitelist
    assert "calculation_method_registry" in whitelist
    assert "calculation_parameters" in whitelist
    assert "calculation_validation_config" in whitelist
```

**验证标准**:
- [ ] 测试文件已创建
- [ ] 测试场景覆盖完整
- [ ] 测试可执行且通过
- [ ] 测试命名符合规范

---

### 2.4 检查清单

**代码质量**:
- [ ] 白名单格式正确
- [ ] 代码符合编码规范
- [ ] 注释清晰

**功能正确性**:
- [ ] 2个表已添加到白名单
- [ ] 白名单配置生效
- [ ] 清理脚本不会删除这2个表

**测试覆盖**:
- [ ] 已创建验证测试
- [ ] 测试通过

**文档更新**:
- [ ] 代码注释已添加
- [ ] 变更记录已更新

---

## 📝 计划3: 种子数据补全计划 (P2)

### 3.1 目标
补充4个指标的方法注册数据到 `calculation_method_registry` 表。

### 3.2 前置条件
- ✅ 已读取 `app/services/calculation/calculators.py`
- ✅ 已读取 `scripts/sql/calculation/init_methods.sql`
- ✅ 已确认表结构 (12个字段)
- ✅ 已确认缺失的指标和方法

### 3.3 详细步骤

#### 步骤3.1: 从代码提取方法定义

**目标**: 从 `calculators.py` 提取4个指标的方法定义

**需要提取的指标**:
1. `pump_flow_rate` - 6个方法 (A-F)
2. `pump_head` - 1个方法 (MAIN)
3. `pump_outlet_pressure` - 4个方法 (A-D)
4. `pump_efficiency` - 1个方法

**提取信息**:
- method_id: 函数名 (如 `pump_flow_rate_method_a`)
- metric_key: 指标键 (如 `pump_flow_rate`)
- method_name: 方法名称 (如 `方法A: 流量计直接读取`)
- method_code: 方法代码 (如 `A`)
- priority: 优先级 (从函数注释或代码推导)
- dependencies: 依赖指标 (从函数参数推导)
- conditions: 可用条件 (从函数注释推导)
- formula_ref: 公式引用 (从函数注释推导)
- accuracy_level: 精度等级 (从函数注释推导)

**技术规范**:
1. 使用Python脚本自动提取
2. 验证提取的数据完整性
3. 生成SQL INSERT语句
4. 使用参数化查询格式

**提取脚本结构** (不实现,仅描述):
```python
import re
import ast

def extract_method_definitions(file_path: str) -> list:
    """
    从calculators.py提取方法定义
    
    Returns:
        方法定义列表,每个元素包含12个字段
    """
    pass
```

**验证标准**:
- [ ] 提取脚本已创建
- [ ] 提取的数据完整 (12个字段)
- [ ] 提取的数据准确 (与代码一致)
- [ ] 提取结果可验证

---

#### 步骤3.2: 生成SQL INSERT语句

**目标**: 生成标准的SQL INSERT语句

**SQL格式**:
```sql
INSERT INTO calculation_method_registry (
    method_id, metric_key, method_name, method_code,
    priority, dependencies, conditions, formula_ref,
    accuracy_level, is_enabled, created_at, updated_at
) VALUES (
    %s, %s, %s, %s,
    %s, %s, %s, %s,
    %s, %s, NOW(), NOW()
);
```

**数据示例** (pump_flow_rate_method_a):
```python
{
    'method_id': 'pump_flow_rate_method_a',
    'metric_key': 'pump_flow_rate',
    'method_name': '方法A: 流量计直接读取',
    'method_code': 'A',
    'priority': 100,
    'dependencies': ['pump_flow_rate_sensor'],
    'conditions': {'has_flow_sensor': True},
    'formula_ref': 'Q = Q_sensor',
    'accuracy_level': 'high',
    'is_enabled': True
}
```

**技术规范**:
1. 使用参数化查询
2. 数组字段使用PostgreSQL数组语法
3. JSONB字段使用JSON格式
4. 时间字段使用NOW()函数

**验证标准**:
- [ ] SQL语句格式正确
- [ ] 参数化查询正确
- [ ] 数据类型匹配
- [ ] 外键约束满足

---

#### 步骤3.3: 执行数据导入

**目标**: 将生成的SQL语句导入数据库

**执行方式**:
1. 创建Python脚本 `scripts/python/补充种子数据.py`
2. 使用 `get_connection()` 获取数据库连接
3. 使用事务执行INSERT语句
4. 记录导入结果

**脚本结构** (不实现,仅描述):
```python
from app.adapters.db import get_connection
import logging

logger = logging.getLogger(__name__)

def import_seed_data():
    """导入种子数据"""
    with get_connection() as conn:
        with conn.cursor() as cur:
            try:
                # 开始事务
                cur.execute("BEGIN")
                
                # 执行INSERT语句
                for method_data in methods:
                    cur.execute(sql, method_data)
                
                # 提交事务
                cur.execute("COMMIT")
                logger.info(f"成功导入 {len(methods)} 条种子数据")
                
            except Exception as e:
                cur.execute("ROLLBACK")
                logger.error(f"导入失败: {e}", exc_info=True)
                raise
```

**验证标准**:
- [ ] 脚本已创建
- [ ] 使用连接池
- [ ] 使用事务
- [ ] 错误处理完整
- [ ] 日志使用中文

---

#### 步骤3.4: 验证数据完整性

**目标**: 验证导入的数据正确且完整

**验证SQL**:
```sql
-- 验证pump_flow_rate的6个方法
SELECT method_code, method_name, priority
FROM calculation_method_registry
WHERE metric_key = 'pump_flow_rate'
ORDER BY priority DESC;

-- 验证pump_head的1个方法
SELECT method_code, method_name, priority
FROM calculation_method_registry
WHERE metric_key = 'pump_head';

-- 验证pump_outlet_pressure的4个方法
SELECT method_code, method_name, priority
FROM calculation_method_registry
WHERE metric_key = 'pump_outlet_pressure'
ORDER BY priority DESC;

-- 验证pump_efficiency的1个方法
SELECT method_code, method_name, priority
FROM calculation_method_registry
WHERE metric_key = 'pump_efficiency';
```

**验证标准**:
- [ ] pump_flow_rate有6个方法
- [ ] pump_head有1个方法
- [ ] pump_outlet_pressure有4个方法
- [ ] pump_efficiency有1个方法
- [ ] 所有字段值正确
- [ ] 外键约束满足

---

### 3.4 检查清单

**数据质量**:
- [ ] 数据提取准确
- [ ] SQL语句正确
- [ ] 数据类型匹配
- [ ] 外键约束满足

**功能正确性**:
- [ ] 4个指标的方法已补全
- [ ] 数据导入成功
- [ ] 数据验证通过

**代码质量**:
- [ ] 脚本符合编码规范
- [ ] 日志使用中文
- [ ] 错误处理完整

**文档更新**:
- [ ] 脚本注释完整
- [ ] 变更记录已更新

---

## 📝 计划4: 测试补充计划 (P2 - 重新评估)

### 4.1 目标
为4个缺少专项测试的功能创建**全面深度的测试套件**，包括单元测试、集成测试、性能测试和并发测试。

**重新评估的工时**: 16小时 (原8小时不足)
- 单元测试: 6小时
- 集成测试: 4小时
- 性能测试: 3小时
- 并发测试: 2小时
- 测试数据准备: 1小时

### 4.2 前置条件
- ✅ 已读取相关功能代码
- ✅ 已理解功能逻辑和实现细节
- ✅ 已确认测试场景和覆盖目标
- ✅ 已读取循环依赖求解、物理验证、特性曲线验证的实现
- ✅ 已读取自适应性能管理的配置和实现

### 4.3 测试覆盖率目标

| 覆盖率类型 | 目标 | 验证方法 |
|-----------|------|---------|
| **行覆盖率** | ≥ 80% | pytest-cov --cov-report=term-missing |
| **分支覆盖率** | ≥ 70% | pytest-cov --cov-branch |
| **函数覆盖率** | 100% | pytest-cov --cov-report=html |
| **关键路径覆盖率** | 100% | 手动验证关键功能点 |

**关键路径定义**:
1. 循环依赖迭代求解的收敛路径
2. 物理规律验证的各种验证规则
3. 特性曲线插值和边界处理
4. 自适应批量/窗口的调整逻辑

---

### 4.4 详细步骤

#### 步骤4.1: 创建泵站级数据获取测试 (单元测试 + 集成测试)

**测试文件**: `tests/test_station_data_loading.py` (新建)

**测试场景细化** (共15个场景):

**A. 正常流程测试** (5个场景):
1. `test_load_station_data_single_device_normal` - 单泵站单设备正常数据加载
   - 验证返回数据结构正确
   - 验证数据类型正确 (Dict[str, np.ndarray])
   - 验证时间戳对齐

2. `test_load_station_data_multiple_devices_normal` - 单泵站多设备正常数据加载
   - 验证所有设备数据都被加载
   - 验证设备间数据独立性
   - 验证数据量符合预期

3. `test_load_station_data_with_running_filter` - 运行状态过滤正常流程
   - 验证只加载运行状态=1的数据
   - 验证使用 `mv_device_running_1s` 表
   - 验证过滤后数据量减少

4. `test_load_station_data_with_quality_filter` - 质量状态过滤正常流程
   - 验证只加载 quality_status='GOOD' 的数据
   - 验证使用 `fact_measurements.quality_status` 字段
   - 验证过滤后数据质量提升

5. `test_load_station_data_with_both_filters` - 双重过滤正常流程
   - 验证运行状态和质量状态同时过滤
   - 验证过滤逻辑的AND关系
   - 验证过滤后数据最少

**B. 边界条件测试** (5个场景):
6. `test_load_station_data_empty_result` - 空数据场景
   - 时间范围内无数据
   - 验证返回空字典或空数组
   - 验证不抛出异常

7. `test_load_station_data_single_record` - 单条数据场景
   - 时间范围内只有1条数据
   - 验证数据正确加载
   - 验证数组长度为1

8. `test_load_station_data_large_batch` - 大批量数据场景
   - 时间范围跨度24小时 (约86万条数据)
   - 验证内存使用合理
   - 验证加载时间 < 30秒

9. `test_load_station_data_time_boundary_start` - 时间边界(开始时间)
   - start_time 精确到秒
   - 验证包含边界时间点的数据
   - 验证时间过滤的闭区间特性

10. `test_load_station_data_time_boundary_end` - 时间边界(结束时间)
    - end_time 精确到秒
    - 验证不包含结束时间点的数据
    - 验证时间过滤的左闭右开特性

**C. 异常情况测试** (3个场景):
11. `test_load_station_data_invalid_station_id` - 无效泵站ID
    - station_id 不存在
    - 验证返回空数据或抛出明确异常
    - 验证错误日志记录

12. `test_load_station_data_database_connection_failure` - 数据库连接失败
    - Mock数据库连接失败
    - 验证异常处理机制
    - 验证重试逻辑 (如果有)

13. `test_load_station_data_timeout` - 查询超时
    - Mock查询超时场景
    - 验证超时处理
    - 验证降级策略

**D. 数据缺失测试** (2个场景):
14. `test_load_station_data_partial_metrics_missing` - 部分指标缺失
    - 某些指标在时间范围内无数据
    - 验证返回的字典中缺失指标的处理
    - 验证不影响其他指标加载

15. `test_load_station_data_intermittent_missing` - 间断缺失
    - 数据在时间范围内有间断
    - 验证间断处理 (NaN填充或跳过)
    - 验证时间戳连续性

**测试代码结构** (不实现,仅描述):
```python
import pytest
import numpy as np
from datetime import datetime, timedelta
from unittest.mock import Mock, patch
from app.services.calculation.orchestrator import CalculationOrchestrator

@pytest.fixture
def orchestrator():
    """创建编排器实例"""
    return CalculationOrchestrator()

@pytest.fixture
def test_time_range():
    """创建测试时间范围"""
    start = datetime(2025, 1, 1, 0, 0, 0)
    end = start + timedelta(hours=1)
    return start, end

# A. 正常流程测试
def test_load_station_data_single_device_normal(orchestrator, test_time_range):
    """测试单设备数据加载"""
    start, end = test_time_range
    data = orchestrator.load_station_data(
        station_id=1,
        start_time=start,
        end_time=end
    )

    # 验证返回类型
    assert isinstance(data, dict)
    # 验证数据不为空
    assert len(data) > 0
    # 验证每个值是numpy数组
    for key, value in data.items():
        assert isinstance(value, np.ndarray)
    # 验证时间戳对齐
    if 'timestamps' in data:
        assert len(data['timestamps']) > 0

# B. 边界条件测试
def test_load_station_data_empty_result(orchestrator):
    """测试空数据场景"""
    # 使用未来时间范围
    start = datetime(2099, 1, 1, 0, 0, 0)
    end = start + timedelta(hours=1)

    data = orchestrator.load_station_data(
        station_id=1,
        start_time=start,
        end_time=end
    )

    # 验证返回空字典或空数组
    assert data is not None
    assert len(data) == 0 or all(len(v) == 0 for v in data.values())

# C. 异常情况测试
@patch('app.adapters.db.get_connection')
def test_load_station_data_database_connection_failure(mock_conn, orchestrator, test_time_range):
    """测试数据库连接失败"""
    # Mock连接失败
    mock_conn.side_effect = Exception("数据库连接失败")

    start, end = test_time_range

    # 验证异常处理
    with pytest.raises(Exception) as exc_info:
        orchestrator.load_station_data(
            station_id=1,
            start_time=start,
            end_time=end
        )

    assert "数据库连接失败" in str(exc_info.value)

# D. 数据缺失测试
def test_load_station_data_partial_metrics_missing(orchestrator, test_time_range):
    """测试部分指标缺失"""
    start, end = test_time_range
    data = orchestrator.load_station_data(
        station_id=1,
        start_time=start,
        end_time=end
    )

    # 验证缺失指标的处理
    # 可能返回空数组或不包含该键
    for key, value in data.items():
        if len(value) == 0:
            # 缺失指标返回空数组
            assert isinstance(value, np.ndarray)
```

**测试数据准备策略**:
1. **真实数据** (70%): 使用测试数据库的真实数据
   - 优点: 真实性高,能发现实际问题
   - 缺点: 依赖数据库状态

2. **Mock数据** (30%): 使用Mock模拟数据
   - 优点: 可控性强,测试速度快
   - 缺点: 可能遗漏真实场景

**测试数据生成方法**:
- 使用 `pytest.fixture` 创建可重用的测试数据
- 使用 `factory_boy` 生成复杂数据结构
- 使用数据库事务回滚确保测试隔离

**测试数据清理策略**:
- 使用 `pytest.fixture(scope="function")` 确保每个测试独立
- 使用数据库事务 `BEGIN; ... ROLLBACK;` 自动回滚
- 使用 `teardown` 清理临时文件和缓存

**验证标准**:
- [ ] 测试文件已创建
- [ ] 15个测试场景全部实现
- [ ] 测试可执行且通过
- [ ] 测试命名符合规范 (test_功能_场景)
- [ ] 行覆盖率 ≥ 80%
- [ ] 分支覆盖率 ≥ 70%

---

#### 步骤4.2: 创建自适应批量管理测试 (单元测试 + 性能测试)

**测试文件**: `tests/test_adaptive_batch.py` (新建)

**测试场景细化** (共12个场景):

**A. 正常流程测试** (4个场景):
1. `test_adaptive_batch_initialization` - 初始化测试
   - 验证配置参数正确加载
   - 验证默认值正确
   - 验证参数验证逻辑 (min_size < max_size)

2. `test_adaptive_batch_adjust_stable_performance` - 性能稳定场景
   - 吞吐量在目标范围内
   - 内存使用正常
   - 验证批量大小保持不变

3. `test_adaptive_batch_adjust_increase` - 批量增大场景
   - 吞吐量高 (> 1.5倍目标)
   - 内存充足 (< 50%限制)
   - 验证批量增大2倍

4. `test_adaptive_batch_adjust_decrease` - 批量减小场景
   - 吞吐量低 (< 0.5倍目标)
   - 验证批量减小50%

**B. 边界条件测试** (4个场景):
5. `test_adaptive_batch_min_size_limit` - 最小批量限制
   - 调整后批量 < min_size
   - 验证限制在min_size

6. `test_adaptive_batch_max_size_limit` - 最大批量限制
   - 调整后批量 > max_size
   - 验证限制在max_size

7. `test_adaptive_batch_small_adjustment_ignored` - 小幅调整忽略
   - 调整幅度 < 10%
   - 验证保持不变

8. `test_adaptive_batch_memory_pressure_priority` - 内存压力优先
   - 内存 > 80%限制
   - 即使吞吐量高也减小批量
   - 验证安全优先原则

**C. 异常情况测试** (2个场景):
9. `test_adaptive_batch_invalid_config` - 无效配置
   - min_size >= max_size
   - 验证抛出ValueError

10. `test_adaptive_batch_missing_performance_data` - 性能数据缺失
    - performance字典缺少必要字段
    - 验证使用默认值或保持不变

**D. 性能测试** (2个场景):
11. `test_adaptive_batch_adjustment_speed` - 调整速度测试
    - 1000次调整操作
    - 验证总耗时 < 100ms
    - 验证平均耗时 < 0.1ms/次

12. `test_adaptive_batch_memory_overhead` - 内存开销测试
    - 创建100个AdaptiveBatchManager实例
    - 验证总内存增长 < 10MB

**测试代码结构** (不实现,仅描述):
```python
import pytest
import time
from app.services.calculation.adaptive_batch import AdaptiveBatchManager

@pytest.fixture
def default_config():
    """默认配置"""
    return {
        'enabled': True,
        'initial_size': 10000,
        'min_size': 1000,
        'max_size': 50000,
        'memory_limit_mb': 1000,
        'target_throughput': 10000
    }

@pytest.fixture
def manager(default_config):
    """创建管理器实例"""
    return AdaptiveBatchManager(default_config)

# A. 正常流程测试
def test_adaptive_batch_adjust_stable_performance(manager):
    """测试性能稳定场景"""
    performance = {
        'memory_mb': 500,  # 50%限制
        'throughput': 10000,  # 目标值
        'duration_ms': 1000
    }

    new_batch, reason = manager.adjust(10000, performance)

    assert new_batch == 10000
    assert "性能稳定" in reason or "保持不变" in reason

# B. 边界条件测试
def test_adaptive_batch_min_size_limit(manager):
    """测试最小批量限制"""
    performance = {
        'memory_mb': 900,  # 高内存压力
        'throughput': 1000,  # 低吞吐量
        'duration_ms': 5000
    }

    new_batch, reason = manager.adjust(1500, performance)

    # 即使需要减小,也不能低于min_size
    assert new_batch >= manager.min_size
    assert new_batch == 1000

# D. 性能测试
def test_adaptive_batch_adjustment_speed(manager):
    """测试调整速度"""
    performance = {
        'memory_mb': 500,
        'throughput': 10000,
        'duration_ms': 1000
    }

    start = time.time()
    for _ in range(1000):
        manager.adjust(10000, performance)
    duration = time.time() - start

    assert duration < 0.1  # 100ms
    assert duration / 1000 < 0.0001  # 0.1ms/次
```

**验证标准**:
- [ ] 测试文件已创建
- [ ] 12个测试场景全部实现
- [ ] 测试可执行且通过
- [ ] 行覆盖率 ≥ 80%
- [ ] 性能测试通过

---

#### 步骤4.3: 创建自适应窗口管理测试 (单元测试 + 性能测试)

**测试文件**: `tests/test_adaptive_window.py` (新建)

**测试场景细化** (共12个场景):

**A. 正常流程测试** (4个场景):
1. `test_adaptive_window_initialization` - 初始化测试
2. `test_adaptive_window_adjust_stable` - 性能稳定场景
3. `test_adaptive_window_adjust_increase` - 窗口增大场景 (处理快)
4. `test_adaptive_window_adjust_decrease` - 窗口减小场景 (处理慢)

**B. 边界条件测试** (4个场景):
5. `test_adaptive_window_min_limit` - 最小窗口限制 (15分钟)
6. `test_adaptive_window_max_limit` - 最大窗口限制 (240分钟)
7. `test_adaptive_window_small_adjustment_ignored` - 小幅调整忽略 (< 5分钟)
8. `test_adaptive_window_ema_smoothing` - EMA平滑效果验证

**C. 数据密度测试** (2个场景):
9. `test_adaptive_window_high_density` - 高密度数据 (> 100点/分钟)
10. `test_adaptive_window_low_density` - 低密度数据 (< 10点/分钟)

**D. 性能测试** (2个场景):
11. `test_adaptive_window_adjustment_speed` - 调整速度测试
12. `test_adaptive_window_ema_calculation_accuracy` - EMA计算精度测试

**测试代码结构** (类似步骤4.2,不重复描述)

**验证标准**:
- [ ] 测试文件已创建
- [ ] 12个测试场景全部实现
- [ ] 测试可执行且通过
- [ ] EMA平滑逻辑正确
- [ ] 行覆盖率 ≥ 80%

---

#### 步骤4.4: 创建性能监控器测试 (单元测试 + 集成测试)

**测试文件**: `tests/test_performance_monitor.py` (新建)

**测试场景细化** (共13个场景):

**A. 正常流程测试** (5个场景):
1. `test_performance_monitor_initialization` - 初始化测试
2. `test_performance_monitor_start_end_batch` - 批次监控流程
3. `test_performance_monitor_memory_usage` - 内存使用监控
4. `test_performance_monitor_statistics` - 统计信息计算
5. `test_performance_monitor_save_to_db` - 数据库持久化

**B. 边界条件测试** (4个场景):
6. `test_performance_monitor_empty_history` - 空历史记录
7. `test_performance_monitor_history_size_limit` - 历史记录大小限制 (deque maxlen)
8. `test_performance_monitor_zero_duration` - 零耗时场景
9. `test_performance_monitor_large_batch` - 大批量场景

**C. 异常情况测试** (2个场景):
10. `test_performance_monitor_psutil_unavailable` - psutil不可用
11. `test_performance_monitor_db_save_failure` - 数据库保存失败

**D. 集成测试** (2个场景):
12. `test_performance_monitor_with_orchestrator` - 与编排器集成
13. `test_performance_monitor_trend_detection` - 趋势检测准确性

**测试代码结构** (不实现,仅描述):
```python
import pytest
from unittest.mock import Mock, patch
from app.services.calculation.performance_monitor import PerformanceMonitor

@pytest.fixture
def monitor():
    """创建监控器实例"""
    return PerformanceMonitor(
        station_id=1,
        device_id=1,
        run_id=1,
        history_size=20
    )

# A. 正常流程测试
def test_performance_monitor_start_end_batch(monitor):
    """测试批次监控流程"""
    # 开始批次
    monitor.start_batch()
    assert monitor.current_batch is not None
    assert 'start_time' in monitor.current_batch

    # 模拟处理
    import time
    time.sleep(0.1)

    # 结束批次
    performance = monitor.end_batch(batch_size=1000, data_points=10000)

    assert performance is not None
    assert 'duration_ms' in performance
    assert 'throughput' in performance
    assert 'memory_mb' in performance
    assert performance['duration_ms'] >= 100  # 至少100ms

# B. 边界条件测试
def test_performance_monitor_history_size_limit(monitor):
    """测试历史记录大小限制"""
    # 添加30个批次 (超过maxlen=20)
    for i in range(30):
        monitor.start_batch()
        monitor.end_batch(batch_size=1000, data_points=10000)

    # 验证只保留最近20个
    assert len(monitor.history) == 20

# C. 异常情况测试
@patch('psutil.Process')
def test_performance_monitor_psutil_unavailable(mock_process):
    """测试psutil不可用"""
    mock_process.side_effect = Exception("psutil不可用")

    monitor = PerformanceMonitor(station_id=1, device_id=1)

    # 验证降级处理
    assert monitor.process is None
    memory = monitor.get_memory_usage()
    assert memory == 0.0  # 返回默认值
```

**验证标准**:
- [ ] 测试文件已创建
- [ ] 13个测试场景全部实现
- [ ] 测试可执行且通过
- [ ] deque历史记录逻辑正确
- [ ] 行覆盖率 ≥ 80%

---

#### 步骤4.5: 创建集成测试套件 (端到端测试)

**测试文件**: `tests/integration/test_missing_metrics_e2e.py` (新建)

**测试目标**: 验证缺失指标计算功能的完整流程

**测试场景细化** (共8个场景):

**A. 完整流程测试** (3个场景):
1. `test_e2e_single_device_single_metric` - 单设备单指标计算
   - 从数据加载到结果写入的完整流程
   - 验证每个阶段的输出
   - 验证最终结果正确性

2. `test_e2e_single_device_multiple_metrics` - 单设备多指标计算
   - 包含依赖关系的多个指标
   - 验证计算顺序正确
   - 验证依赖数据传递正确

3. `test_e2e_multiple_devices_batch` - 多设备批量计算
   - 13个设备并行计算
   - 验证批量处理逻辑
   - 验证结果隔离性

**B. 循环依赖测试** (2个场景):
4. `test_e2e_circular_dependency_convergence` - 循环依赖收敛测试
   - 包含循环依赖的指标组 (如pump_head ↔ pump_outlet_pressure)
   - 验证迭代求解收敛
   - 验证收敛精度 (相对误差 < 1%)
   - 验证迭代次数 ≤ 10

5. `test_e2e_circular_dependency_non_convergence` - 循环依赖不收敛测试
   - 构造不收敛场景 (如初值偏差过大)
   - 验证达到最大迭代次数后的处理
   - 验证警告日志记录

**C. 验证流程测试** (2个场景):
6. `test_e2e_with_physics_validation` - 物理规律验证集成
   - 计算结果包含物理不合理的值
   - 验证物理验证器被调用
   - 验证不合理值被标记或过滤

7. `test_e2e_with_characteristic_curve_validation` - 特性曲线验证集成
   - 计算结果偏离特性曲线
   - 验证特性曲线验证器被调用
   - 验证偏差被记录

**D. 性能基准测试** (1个场景):
8. `test_e2e_performance_benchmark` - 性能基准测试
   - 单设备1小时数据 (3600条)
   - 验证总耗时 < 5秒
   - 验证内存使用 < 500MB
   - 验证吞吐量 > 700条/秒

**测试代码结构** (不实现,仅描述):
```python
import pytest
import time
from datetime import datetime, timedelta
from app.services.calculation.orchestrator import CalculationOrchestrator

@pytest.fixture(scope="module")
def orchestrator():
    """创建编排器实例 (模块级别,复用)"""
    return CalculationOrchestrator(enable_adaptive=True)

@pytest.fixture
def test_time_range_1hour():
    """1小时测试时间范围"""
    start = datetime(2025, 1, 1, 0, 0, 0)
    end = start + timedelta(hours=1)
    return start, end

# A. 完整流程测试
def test_e2e_single_device_single_metric(orchestrator, test_time_range_1hour):
    """测试单设备单指标完整流程"""
    start, end = test_time_range_1hour

    result = orchestrator.calculate_missing_metrics_batch(
        station_id=1,
        device_id=1,
        start_time=start,
        end_time=end,
        metrics=['pump_flow_rate']
    )

    # 验证结果结构
    assert 'metrics_calculated' in result
    assert 'pump_flow_rate' in result['metrics_calculated']

    # 验证数据写入
    assert result['total_points'] > 0
    assert result['valid_points'] > 0

    # 验证数据库中的结果
    # (查询fact_measurements表验证数据已写入)

# B. 循环依赖测试
def test_e2e_circular_dependency_convergence(orchestrator, test_time_range_1hour):
    """测试循环依赖收敛"""
    start, end = test_time_range_1hour

    # 包含循环依赖的指标
    metrics = ['pump_head', 'pump_outlet_pressure', 'pump_inlet_pressure']

    result = orchestrator.calculate_missing_metrics_batch(
        station_id=1,
        device_id=1,
        start_time=start,
        end_time=end,
        metrics=metrics
    )

    # 验证所有指标都计算成功
    assert len(result['metrics_calculated']) == len(metrics)
    assert len(result['metrics_failed']) == 0

    # 验证收敛信息 (从日志或result中获取)
    # assert result['circular_iterations'] <= 10
    # assert result['circular_converged'] == True

# D. 性能基准测试
def test_e2e_performance_benchmark(orchestrator, test_time_range_1hour):
    """测试性能基准"""
    start, end = test_time_range_1hour

    # 记录开始时间和内存
    start_time = time.time()
    import psutil
    process = psutil.Process()
    start_memory = process.memory_info().rss / (1024 * 1024)  # MB

    # 执行计算
    result = orchestrator.calculate_missing_metrics_batch(
        station_id=1,
        device_id=1,
        start_time=start,
        end_time=end,
        metrics=['pump_flow_rate', 'pump_head', 'pump_efficiency']
    )

    # 记录结束时间和内存
    duration = time.time() - start_time
    end_memory = process.memory_info().rss / (1024 * 1024)  # MB
    memory_used = end_memory - start_memory

    # 验证性能指标
    assert duration < 5.0, f"耗时{duration:.2f}秒 > 5秒"
    assert memory_used < 500, f"内存使用{memory_used:.0f}MB > 500MB"

    # 计算吞吐量
    throughput = result['total_points'] / duration
    assert throughput > 700, f"吞吐量{throughput:.0f}条/秒 < 700条/秒"
```

**验证标准**:
- [ ] 测试文件已创建
- [ ] 8个集成测试场景全部实现
- [ ] 测试可执行且通过
- [ ] 性能基准测试通过
- [ ] 端到端流程验证完整

---

#### 步骤4.6: 创建并发测试套件

**测试文件**: `tests/integration/test_concurrent_calculation.py` (新建)

**测试目标**: 验证多设备、多时间段并发计算的正确性和性能

**测试场景细化** (共6个场景):

**A. 并发正确性测试** (3个场景):
1. `test_concurrent_multiple_devices` - 多设备并发计算
   - 使用 `concurrent.futures.ThreadPoolExecutor` 并发计算13个设备
   - 验证结果隔离性 (设备间数据不混淆)
   - 验证所有设备都计算成功

2. `test_concurrent_multiple_time_ranges` - 多时间段并发计算
   - 同一设备的不同时间段并发计算
   - 验证时间段隔离性
   - 验证结果正确性

3. `test_concurrent_mixed_workload` - 混合工作负载
   - 不同设备、不同时间段、不同指标的混合并发
   - 验证无数据竞争
   - 验证无死锁

**B. 并发性能测试** (2个场景):
4. `test_concurrent_speedup` - 并发加速比测试
   - 串行计算13个设备的耗时
   - 并发计算13个设备的耗时
   - 验证加速比 > 3倍 (理想情况下接近CPU核心数)

5. `test_concurrent_resource_usage` - 并发资源使用测试
   - 监控并发计算时的CPU和内存使用
   - 验证CPU使用率 < 80%
   - 验证内存使用 < 2GB

**C. 并发异常处理测试** (1个场景):
6. `test_concurrent_partial_failure` - 部分失败场景
   - 某些设备计算失败
   - 验证不影响其他设备计算
   - 验证失败信息正确记录

**测试代码结构** (不实现,仅描述):
```python
import pytest
import time
from concurrent.futures import ThreadPoolExecutor, as_completed
from datetime import datetime, timedelta
from app.services.calculation.orchestrator import CalculationOrchestrator

@pytest.fixture
def orchestrator():
    """创建编排器实例"""
    return CalculationOrchestrator()

# A. 并发正确性测试
def test_concurrent_multiple_devices(orchestrator):
    """测试多设备并发计算"""
    start = datetime(2025, 1, 1, 0, 0, 0)
    end = start + timedelta(hours=1)
    device_ids = list(range(1, 14))  # 13个设备

    # 并发计算
    with ThreadPoolExecutor(max_workers=4) as executor:
        futures = {
            executor.submit(
                orchestrator.calculate_missing_metrics_batch,
                station_id=1,
                device_id=device_id,
                start_time=start,
                end_time=end,
                metrics=['pump_flow_rate']
            ): device_id
            for device_id in device_ids
        }

        results = {}
        for future in as_completed(futures):
            device_id = futures[future]
            try:
                result = future.result()
                results[device_id] = result
            except Exception as e:
                pytest.fail(f"设备{device_id}计算失败: {e}")

    # 验证所有设备都成功
    assert len(results) == 13
    for device_id, result in results.items():
        assert 'pump_flow_rate' in result['metrics_calculated']

# B. 并发性能测试
def test_concurrent_speedup(orchestrator):
    """测试并发加速比"""
    start = datetime(2025, 1, 1, 0, 0, 0)
    end = start + timedelta(hours=1)
    device_ids = list(range(1, 14))

    # 串行计算
    serial_start = time.time()
    for device_id in device_ids:
        orchestrator.calculate_missing_metrics_batch(
            station_id=1,
            device_id=device_id,
            start_time=start,
            end_time=end,
            metrics=['pump_flow_rate']
        )
    serial_duration = time.time() - serial_start

    # 并发计算
    parallel_start = time.time()
    with ThreadPoolExecutor(max_workers=4) as executor:
        futures = [
            executor.submit(
                orchestrator.calculate_missing_metrics_batch,
                station_id=1,
                device_id=device_id,
                start_time=start,
                end_time=end,
                metrics=['pump_flow_rate']
            )
            for device_id in device_ids
        ]
        for future in as_completed(futures):
            future.result()
    parallel_duration = time.time() - parallel_start

    # 计算加速比
    speedup = serial_duration / parallel_duration

    # 验证加速比 > 3倍
    assert speedup > 3.0, f"加速比{speedup:.2f}倍 < 3倍"
```

**验证标准**:
- [ ] 测试文件已创建
- [ ] 6个并发测试场景全部实现
- [ ] 测试可执行且通过
- [ ] 加速比测试通过
- [ ] 无数据竞争和死锁

---

#### 步骤4.7: 创建关键功能专项测试 (深度测试)

**测试文件**: `tests/specialized/test_circular_dependency_solver.py` (新建)

**测试目标**: 深度测试循环依赖迭代求解算法

**测试场景细化** (共10个场景):

**A. 收敛性测试** (4个场景):
1. `test_circular_solver_simple_convergence` - 简单循环收敛
   - 2个指标的简单循环 (A → B → A)
   - 验证收敛
   - 验证迭代次数 ≤ 5

2. `test_circular_solver_complex_convergence` - 复杂循环收敛
   - 3个指标的复杂循环 (A → B → C → A)
   - 验证收敛
   - 验证迭代次数 ≤ 10

3. `test_circular_solver_convergence_threshold` - 收敛阈值测试
   - 测试不同收敛阈值 (0.001, 0.01, 0.1)
   - 验证阈值越小,迭代次数越多
   - 验证精度与阈值的关系

4. `test_circular_solver_initial_value_sensitivity` - 初值敏感性测试
   - 测试不同初值 (0, 均值, 随机值)
   - 验证收敛到相同结果
   - 验证初值对迭代次数的影响

**B. 精度测试** (2个场景):
5. `test_circular_solver_accuracy` - 精度测试
   - 使用已知解的循环系统
   - 验证计算结果与真实解的误差 < 1%

6. `test_circular_solver_numerical_stability` - 数值稳定性测试
   - 测试极端值场景 (很大、很小的数值)
   - 验证不产生NaN或Inf
   - 验证数值稳定性

**C. 边界条件测试** (2个场景):
7. `test_circular_solver_max_iterations` - 最大迭代次数测试
   - 构造不收敛场景
   - 验证达到最大迭代次数后停止
   - 验证警告日志记录

8. `test_circular_solver_single_metric` - 单指标场景
   - 循环组只有1个指标 (退化情况)
   - 验证正确处理

**D. 性能测试** (2个场景):
9. `test_circular_solver_performance` - 求解性能测试
   - 3600个时间点的循环求解
   - 验证总耗时 < 1秒

10. `test_circular_solver_memory_efficiency` - 内存效率测试
    - 监控求解过程的内存使用
    - 验证内存增长 < 100MB

**测试代码结构** (不实现,仅描述):
```python
import pytest
import numpy as np
from app.services.calculation.orchestrator import CalculationOrchestrator

@pytest.fixture
def orchestrator():
    return CalculationOrchestrator()

# A. 收敛性测试
def test_circular_solver_simple_convergence(orchestrator):
    """测试简单循环收敛"""
    # 构造循环依赖数据
    circular_group = ['metric_a', 'metric_b']
    data = {
        'base_metric': np.array([1.0, 2.0, 3.0])
    }
    timestamps = np.array([0, 1, 2])

    # 求解
    results = orchestrator.solve_circular_dependencies(
        circular_group=circular_group,
        data=data,
        timestamps=timestamps,
        station_id=1,
        device_id=1,
        max_iterations=10,
        convergence_threshold=0.01
    )

    # 验证收敛
    assert 'metric_a' in results
    assert 'metric_b' in results
    assert len(results['metric_a']) == 3

    # 验证无NaN
    assert not np.isnan(results['metric_a']).any()
    assert not np.isnan(results['metric_b']).any()

# B. 精度测试
def test_circular_solver_accuracy(orchestrator):
    """测试精度"""
    # 使用已知解的系统: A = 2*B, B = 0.5*A
    # 真实解: A = B = 0 (或任意相等值)

    circular_group = ['metric_a', 'metric_b']
    data = {}
    timestamps = np.array([0, 1, 2])

    results = orchestrator.solve_circular_dependencies(
        circular_group=circular_group,
        data=data,
        timestamps=timestamps,
        station_id=1,
        device_id=1
    )

    # 验证A和B接近 (相对误差 < 1%)
    relative_error = np.abs(results['metric_a'] - results['metric_b']) / \
                     (np.abs(results['metric_a']) + 1e-10)
    assert relative_error.max() < 0.01
```

**验证标准**:
- [ ] 测试文件已创建
- [ ] 10个专项测试场景全部实现
- [ ] 测试可执行且通过
- [ ] 收敛性验证通过
- [ ] 精度验证通过 (误差 < 1%)
- [ ] 性能验证通过 (< 1秒)

---

**测试文件**: `tests/specialized/test_physics_validator_deep.py` (新建)

**测试目标**: 深度测试物理规律验证器

**测试场景细化** (共12个场景):

**A. 效率验证** (3个场景):
1. `test_physics_validator_efficiency_range` - 效率范围验证 (0-1)
2. `test_physics_validator_efficiency_consistency` - 效率一致性验证 (与功率/流量/扬程的关系)
3. `test_physics_validator_efficiency_curve` - 效率曲线合理性验证

**B. 功率验证** (3个场景):
4. `test_physics_validator_power_flow_head` - 功率-流量-扬程一致性
5. `test_physics_validator_power_torque_speed` - 功率-扭矩-转速一致性
6. `test_physics_validator_power_range` - 功率范围验证

**C. 压力验证** (3个场景):
7. `test_physics_validator_pressure_positive` - 压力正数验证
8. `test_physics_validator_pressure_differential` - 压差验证
9. `test_physics_validator_pressure_rated` - 额定压力验证

**D. 综合验证** (3个场景):
10. `test_physics_validator_integrated` - 综合验证 (多个规则同时验证)
11. `test_physics_validator_strict_mode` - 严格模式验证 (警告视为错误)
12. `test_physics_validator_performance` - 验证性能测试 (3600个点 < 100ms)

**验证标准**:
- [ ] 测试文件已创建
- [ ] 12个专项测试场景全部实现
- [ ] 所有物理规律验证逻辑覆盖
- [ ] 行覆盖率 ≥ 90%

---

**测试文件**: `tests/specialized/test_characteristic_curve_deep.py` (新建)

**测试目标**: 深度测试特性曲线验证器

**测试场景细化** (共10个场景):

**A. 插值测试** (4个场景):
1. `test_curve_interpolation_linear` - 线性插值
2. `test_curve_interpolation_cubic` - 三次样条插值
3. `test_curve_interpolation_boundary` - 边界外推
4. `test_curve_interpolation_accuracy` - 插值精度验证

**B. 验证测试** (3个场景):
5. `test_curve_validation_within_tolerance` - 容差内验证
6. `test_curve_validation_outside_tolerance` - 容差外验证
7. `test_curve_validation_multiple_curves` - 多条曲线验证

**C. 缓存测试** (2个场景):
8. `test_curve_cache_hit` - 缓存命中测试
9. `test_curve_cache_expiration` - 缓存过期测试

**D. 性能测试** (1个场景):
10. `test_curve_interpolation_performance` - 插值性能测试 (1000次 < 100ms)

**验证标准**:
- [ ] 测试文件已创建
- [ ] 10个专项测试场景全部实现
- [ ] 插值精度验证通过
- [ ] 缓存逻辑正确
- [ ] 行覆盖率 ≥ 90%

---

### 4.5 测试执行计划

#### 执行顺序

1. **阶段1: 单元测试** (6小时)
   - 步骤4.1: 泵站级数据获取测试
   - 步骤4.2: 自适应批量管理测试
   - 步骤4.3: 自适应窗口管理测试
   - 步骤4.4: 性能监控器测试

2. **阶段2: 专项测试** (4小时)
   - 步骤4.7: 循环依赖求解器测试
   - 步骤4.7: 物理验证器深度测试
   - 步骤4.7: 特性曲线深度测试

3. **阶段3: 集成测试** (4小时)
   - 步骤4.5: 端到端测试
   - 步骤4.6: 并发测试

4. **阶段4: 性能测试** (2小时)
   - 性能基准测试
   - 并发加速比测试
   - 内存效率测试

#### 测试环境配置

**数据库配置**:
```yaml
# tests/conftest.py
@pytest.fixture(scope="session")
def test_database():
    """测试数据库配置"""
    # 使用测试数据库
    os.environ['DATABASE_URL'] = 'postgresql://test_user:test_pass@localhost:5432/test_db'
    yield
    # 清理
```

**测试数据准备**:
```python
# tests/fixtures/test_data.py
@pytest.fixture
def sample_measurement_data():
    """样本测量数据"""
    return {
        'timestamps': np.arange(3600),
        'pump_flow_rate': np.random.uniform(100, 200, 3600),
        'pump_inlet_pressure': np.random.uniform(0.1, 0.3, 3600),
        # ... 其他指标
    }
```

**环境变量**:
```bash
# .env.test
DATABASE_URL=postgresql://test_user:test_pass@localhost:5432/test_db
LOG_LEVEL=DEBUG
ENABLE_ADAPTIVE=true
```

#### 测试失败处理流程

1. **定位问题**:
   - 查看测试输出和错误信息
   - 查看日志文件 (logs/test_*.log)
   - 使用 `pytest -vv --tb=long` 获取详细堆栈

2. **修复问题**:
   - 修复代码或测试
   - 重新运行失败的测试: `pytest tests/test_xxx.py::test_yyy -v`

3. **重新测试**:
   - 运行所有测试确保无回归
   - 更新测试覆盖率报告

#### 测试报告生成

**覆盖率报告**:
```bash
# 生成HTML覆盖率报告
pytest --cov=app/services/calculation \
       --cov-report=html \
       --cov-report=term-missing \
       --cov-branch \
       tests/

# 查看报告
open htmlcov/index.html
```

**测试报告**:
```bash
# 生成HTML测试报告
pytest --html=reports/test_report.html \
       --self-contained-html \
       tests/

# 查看报告
open reports/test_report.html
```

---

### 4.6 最终检查清单

**测试文件创建**:
- [ ] `tests/test_station_data_loading.py` (15个场景)
- [ ] `tests/test_adaptive_batch.py` (12个场景)
- [ ] `tests/test_adaptive_window.py` (12个场景)
- [ ] `tests/test_performance_monitor.py` (13个场景)
- [ ] `tests/integration/test_missing_metrics_e2e.py` (8个场景)
- [ ] `tests/integration/test_concurrent_calculation.py` (6个场景)
- [ ] `tests/specialized/test_circular_dependency_solver.py` (10个场景)
- [ ] `tests/specialized/test_physics_validator_deep.py` (12个场景)
- [ ] `tests/specialized/test_characteristic_curve_deep.py` (10个场景)

**测试覆盖率**:
- [ ] 行覆盖率 ≥ 80%
- [ ] 分支覆盖率 ≥ 70%
- [ ] 函数覆盖率 = 100%
- [ ] 关键路径覆盖率 = 100%

**测试质量**:
- [ ] 所有测试可执行
- [ ] 所有测试通过
- [ ] 测试命名符合规范
- [ ] 测试数据准备完整
- [ ] 测试清理完整
- [ ] 测试隔离性良好

**性能验证**:
- [ ] 单设备1小时数据计算 < 5秒
- [ ] 并发加速比 > 3倍
- [ ] 内存使用 < 2GB
- [ ] 循环依赖求解 < 1秒

**文档更新**:
- [ ] 测试文档已创建 (tests/README.md)
- [ ] 测试覆盖率报告已生成
- [ ] 测试执行指南已更新

---

## 📝 计划5: 文件拆分计划 (P1)

### 5.1 目标
拆分4个超过600行限制的文件。

### 5.2 前置条件
- ✅ 已读取所有超大文件
- ✅ 已理解文件职责
- ✅ 已确认拆分方案

### 5.3 详细步骤

#### 步骤5.1: 拆分 orchestrator.py (1404行 → 3个文件)

**拆分方案**: 按职责垂直拆分

**目标文件结构**:
```
app/services/calculation/
├── orchestrator.py (300行) - 主编排器
├── data_loader.py (400行) - 数据加载职责
├── calculation_executor.py (450行) - 计算执行职责
└── result_writer.py (250行) - 结果写入职责
```

**拆分步骤**:

1. **创建 data_loader.py** (不实现,仅描述结构)
   - 移动 `load_data()` 方法 (第91-251行)
   - 移动 `load_station_data()` 方法 (第253-378行)
   - 移动 `_load_device_data()` 辅助方法
   - 移动 `_filter_by_running_status()` 辅助方法
   - 移动 `_filter_by_quality_status()` 辅助方法
   - 创建 `DataLoader` 类封装所有数据加载逻辑

2. **创建 calculation_executor.py** (不实现,仅描述结构)
   - 移动 `_calculate_single_metric()` 方法
   - 移动 `_solve_circular_dependencies()` 方法 (第766-906行)
   - 移动 `_iterate_circular_group()` 方法
   - 移动 `_check_convergence()` 方法
   - 创建 `CalculationExecutor` 类封装所有计算逻辑

3. **创建 result_writer.py** (不实现,仅描述结构)
   - 移动 `_write_results()` 方法 (第505-656行)
   - 移动 `_update_metrics_presence()` 方法 (第658-764行)
   - 移动 `_batch_write()` 辅助方法
   - 创建 `ResultWriter` 类封装所有写入逻辑

4. **重构 orchestrator.py** (不实现,仅描述结构)
   - 保留 `CalculationOrchestrator` 类
   - 保留 `calculate_missing_metrics_batch()` 主方法
   - 注入 `DataLoader`、`CalculationExecutor`、`ResultWriter` 依赖
   - 协调各模块的调用

**接口设计** (不实现,仅描述):

```python
# data_loader.py
class DataLoader:
    def load_data(
        self,
        station_id: int,
        device_id: int,
        start_time: datetime,
        end_time: datetime,
        metrics: List[str],
        filter_running: bool = False,
        filter_quality: bool = False
    ) -> Dict[str, np.ndarray]:
        """加载数据"""
        pass

    def load_station_data(
        self,
        station_id: int,
        start_time: datetime,
        end_time: datetime
    ) -> Dict[str, Any]:
        """加载泵站级数据"""
        pass

# calculation_executor.py
class CalculationExecutor:
    def calculate_single_metric(
        self,
        metric_key: str,
        data: Dict[str, np.ndarray],
        params: Dict[str, Any],
        context: Dict[str, Any]
    ) -> np.ndarray:
        """计算单个指标"""
        pass

    def solve_circular_dependencies(
        self,
        circular_group: List[str],
        data: Dict[str, np.ndarray],
        params: Dict[str, Any],
        context: Dict[str, Any]
    ) -> Dict[str, np.ndarray]:
        """求解循环依赖"""
        pass

# result_writer.py
class ResultWriter:
    def write_results(
        self,
        results: Dict[str, np.ndarray],
        station_id: int,
        device_id: int,
        start_time: datetime,
        end_time: datetime
    ) -> int:
        """写入结果"""
        pass

    def update_metrics_presence(
        self,
        station_id: int,
        device_id: int,
        start_time: datetime,
        end_time: datetime,
        computed_metrics: List[str]
    ) -> None:
        """更新metrics_presence表"""
        pass

# orchestrator.py (重构后)
class CalculationOrchestrator:
    def __init__(self):
        self.data_loader = DataLoader()
        self.calculation_executor = CalculationExecutor()
        self.result_writer = ResultWriter()

    def calculate_missing_metrics_batch(
        self,
        station_id: int,
        device_id: int,
        start_time: datetime,
        end_time: datetime
    ) -> Dict[str, Any]:
        """主编排方法"""
        # 1. 加载数据
        data = self.data_loader.load_data(...)

        # 2. 执行计算
        results = self.calculation_executor.calculate_single_metric(...)

        # 3. 写入结果
        self.result_writer.write_results(...)

        return result
```

**验证标准**:
- [ ] 4个文件已创建
- [ ] 每个文件行数 < 600行
- [ ] 接口设计清晰
- [ ] 依赖关系正确
- [ ] 所有测试通过
- [ ] 功能无变化

---

#### 步骤5.2: 拆分 calculators.py (1179行 → 6个文件)

**拆分方案**: 按指标类型拆分

**目标文件结构**:
```
app/services/calculation/calculators/
├── __init__.py (50行) - 注册表和工厂函数
├── flow_calculators.py (250行) - 流量相关计算
├── pressure_calculators.py (200行) - 压力相关计算
├── power_calculators.py (200行) - 功率相关计算
├── motion_calculators.py (200行) - 运动相关计算
└── efficiency_calculators.py (150行) - 效率相关计算
```

**拆分步骤**:

1. **创建 flow_calculators.py**
   - 移动 `pump_flow_rate_method_*` 函数 (6个)
   - 移动 `main_pipeline_flow_rate_*` 函数
   - 移动流量相关的辅助函数

2. **创建 pressure_calculators.py**
   - 移动 `pump_outlet_pressure_method_*` 函数 (4个)
   - 移动 `pump_inlet_pressure_method_*` 函数
   - 移动 `main_pipeline_inlet_pressure_*` 函数
   - 移动 `main_pipeline_outlet_pressure_*` 函数

3. **创建 power_calculators.py**
   - 移动 `pump_active_power_method_*` 函数
   - 移动 `pump_reactive_power_method_*` 函数
   - 移动功率相关的辅助函数

4. **创建 motion_calculators.py**
   - 移动 `pump_speed_method_*` 函数 (3个)
   - 移动 `pump_torque_method_*` 函数 (2个)
   - 移动运动相关的辅助函数

5. **创建 efficiency_calculators.py**
   - 移动 `pump_efficiency_method_*` 函数
   - 移动效率相关的辅助函数

6. **创建 __init__.py**
   - 导入所有计算函数
   - 维护 `CALCULATOR_REGISTRY` 字典
   - 提供 `get_calculator()` 工厂函数

**注册表设计** (不实现,仅描述):

```python
# __init__.py
from .flow_calculators import *
from .pressure_calculators import *
from .power_calculators import *
from .motion_calculators import *
from .efficiency_calculators import *

CALCULATOR_REGISTRY = {
    # 流量计算
    'pump_flow_rate_method_a': pump_flow_rate_method_a,
    'pump_flow_rate_method_b': pump_flow_rate_method_b,
    # ... 其他计算函数
}

def get_calculator(method_id: str):
    """获取计算函数"""
    return CALCULATOR_REGISTRY.get(method_id)
```

**验证标准**:
- [ ] 6个文件已创建
- [ ] 每个文件行数 < 600行
- [ ] 注册表完整
- [ ] 所有计算函数可访问
- [ ] 所有测试通过
- [ ] 功能无变化

---

#### 步骤5.3: 拆分 validator.py (960行 → 2个文件)

**拆分方案**: 按验证类型拆分

**目标文件结构**:
```
app/services/calculation/
├── physics_validator.py (500行) - 物理规律验证
└── range_validator.py (400行) - 数值范围验证
```

**拆分步骤**:

1. **创建 physics_validator.py**
   - 移动 `PhysicsValidator` 类
   - 移动物理规律验证方法
   - 移动效率验证、功率一致性验证、压力验证

2. **创建 range_validator.py**
   - 移动 `RangeValidator` 类
   - 移动数值范围验证方法
   - 移动最大最小值验证、合理性验证

3. **删除原 validator.py**

**接口设计** (不实现,仅描述):

```python
# physics_validator.py
class PhysicsValidator:
    def validate_efficiency(self, data: Dict) -> bool:
        """验证效率是否符合物理规律"""
        pass

    def validate_power_consistency(self, data: Dict) -> bool:
        """验证功率一致性"""
        pass

# range_validator.py
class RangeValidator:
    def validate_range(self, value: float, min_val: float, max_val: float) -> bool:
        """验证数值范围"""
        pass
```

**验证标准**:
- [ ] 2个文件已创建
- [ ] 每个文件行数 < 600行
- [ ] 接口设计清晰
- [ ] 所有测试通过
- [ ] 功能无变化

---

#### 步骤5.4: 拆分 dependency_analyzer.py (782行 → 2个文件)

**拆分方案**: 按功能模块拆分

**目标文件结构**:
```
app/services/calculation/
├── dependency_graph.py (400行) - 依赖图构建和管理
└── topological_sorter.py (350行) - 拓扑排序算法
```

**拆分步骤**:

1. **创建 dependency_graph.py**
   - 移动 `DependencyGraph` 类
   - 移动依赖图构建方法
   - 移动循环依赖检测方法

2. **创建 topological_sorter.py**
   - 移动 `TopologicalSorter` 类
   - 移动拓扑排序算法 (Kahn算法)
   - 移动排序结果处理方法

3. **重构 dependency_analyzer.py**
   - 保留 `DependencyAnalyzer` 类
   - 组合 `DependencyGraph` 和 `TopologicalSorter`

**接口设计** (不实现,仅描述):

```python
# dependency_graph.py
class DependencyGraph:
    def build_graph(self, metrics: List[str]) -> Dict:
        """构建依赖图"""
        pass

    def detect_circular(self) -> List[List[str]]:
        """检测循环依赖"""
        pass

# topological_sorter.py
class TopologicalSorter:
    def sort(self, graph: Dict) -> List[str]:
        """拓扑排序"""
        pass

# dependency_analyzer.py (重构后)
class DependencyAnalyzer:
    def __init__(self):
        self.graph = DependencyGraph()
        self.sorter = TopologicalSorter()

    def analyze(self, metrics: List[str]) -> Dict:
        """分析依赖关系"""
        graph = self.graph.build_graph(metrics)
        order = self.sorter.sort(graph)
        circular = self.graph.detect_circular()
        return {'order': order, 'circular': circular}
```

**验证标准**:
- [ ] 2个文件已创建
- [ ] 每个文件行数 < 600行
- [ ] 接口设计清晰
- [ ] 所有测试通过
- [ ] 功能无变化

---

### 5.4 文件拆分总体检查清单

**代码质量**:
- [ ] 所有新文件行数 < 600行
- [ ] 代码符合编码规范
- [ ] 接口设计清晰
- [ ] 依赖关系正确
- [ ] 无循环依赖

**功能正确性**:
- [ ] 所有功能正常工作
- [ ] 所有测试通过
- [ ] 无功能变化
- [ ] 无性能衰减

**文档更新**:
- [ ] 所有新文件有文件头注释
- [ ] 所有类有类注释
- [ ] 所有函数有函数注释
- [ ] 架构文档已更新

---

## 📊 总体执行顺序 (更新后)

### 执行优先级和工时重新评估

| 优先级 | 任务 | 原工时 | 新工时 | 变化 | 原因 |
|--------|------|--------|--------|------|------|
| P0 | 代码占位符移除 | 0.5h | 0.5h | - | 无变化 |
| P3 | 配置表白名单更新 | 0.5h | 0.5h | - | 无变化 |
| P2 | 种子数据补全 | 2h | 2h | - | 无变化 |
| **P2** | **测试补充** | **8h** | **16h** | **+8h** | **增加集成测试、并发测试、专项测试** |
| P1 | 文件拆分 | 16h | 16h | - | 无变化 |
| **总计** | | **27h** | **35h** | **+8h** | **测试深度增加** |

### 详细执行计划

#### 第1阶段: 快速修复 (P0 + P3, 1小时, 可并行)

**P0: 代码占位符移除** (0.5小时)
- 步骤1.1: 移除 orchestrator.py:1019 的TODO
  - 实现数据验证逻辑 (NaN/Inf检查、范围验证)
  - 添加单元测试
  - 验证日志中文化

- 步骤1.2: 移除 orchestrator.py:1047 的TODO
  - 实现运行台数查询 (从mv_device_running_1s表)
  - 添加单元测试
  - 验证SQL参数化

**P3: 配置表白名单更新** (0.5小时)
- 步骤2.1: 更新 cleanup.py 白名单
  - 添加 metric_calculation_order
  - 添加 pump_characteristic_curves

- 步骤2.2: 创建验证测试
  - 测试白名单表不被清理
  - 测试非白名单表被清理

**并行执行**: P0和P3可同时进行

---

#### 第2阶段: 数据补全 (P2, 2小时, 依赖P0)

**P2: 种子数据补全** (2小时)
- 步骤3.1: 从代码提取方法定义 (0.5小时)
  - 编写Python提取脚本
  - 提取4个指标的方法定义
  - 验证提取数据完整性

- 步骤3.2: 生成SQL INSERT语句 (0.5小时)
  - 生成参数化SQL
  - 验证数据类型匹配
  - 验证外键约束

- 步骤3.3: 执行数据导入 (0.5小时)
  - 创建导入脚本
  - 使用事务执行
  - 记录导入结果

- 步骤3.4: 验证数据完整性 (0.5小时)
  - 查询验证数据
  - 验证字段值正确
  - 验证外键约束满足

**依赖**: 需要P0完成 (验证逻辑完整)

---

#### 第3阶段: 测试补充 (P2, 16小时, 依赖P0)

**阶段3.1: 单元测试** (6小时)
- 步骤4.1: 泵站级数据获取测试 (1.5小时)
  - 15个测试场景
  - 真实数据 + Mock数据

- 步骤4.2: 自适应批量管理测试 (1.5小时)
  - 12个测试场景
  - 包含性能测试

- 步骤4.3: 自适应窗口管理测试 (1.5小时)
  - 12个测试场景
  - 包含EMA平滑测试

- 步骤4.4: 性能监控器测试 (1.5小时)
  - 13个测试场景
  - 包含集成测试

**阶段3.2: 专项测试** (4小时)
- 步骤4.7: 循环依赖求解器测试 (1.5小时)
  - 10个测试场景
  - 收敛性、精度、性能测试

- 步骤4.7: 物理验证器深度测试 (1.5小时)
  - 12个测试场景
  - 所有物理规律覆盖

- 步骤4.7: 特性曲线深度测试 (1小时)
  - 10个测试场景
  - 插值、验证、缓存测试

**阶段3.3: 集成测试** (4小时)
- 步骤4.5: 端到端测试 (2小时)
  - 8个测试场景
  - 完整流程验证
  - 性能基准测试

- 步骤4.6: 并发测试 (2小时)
  - 6个测试场景
  - 并发正确性验证
  - 加速比测试

**阶段3.4: 测试报告** (2小时)
- 生成覆盖率报告
- 生成测试报告
- 验证覆盖率目标
- 更新测试文档

**依赖**: 需要P0完成 (功能完整)

---

#### 第4阶段: 文件拆分 (P1, 16小时, 依赖P0+P2+P3)

**步骤5.1: 拆分 orchestrator.py** (4小时)
- 创建 data_loader.py (1小时)
- 创建 calculation_executor.py (1小时)
- 创建 result_writer.py (1小时)
- 重构 orchestrator.py (1小时)
- 运行所有测试验证

**步骤5.2: 拆分 calculators.py** (6小时)
- 创建 flow_calculators.py (1小时)
- 创建 pressure_calculators.py (1小时)
- 创建 power_calculators.py (1小时)
- 创建 motion_calculators.py (1小时)
- 创建 efficiency_calculators.py (1小时)
- 创建 __init__.py 注册表 (1小时)
- 运行所有测试验证

**步骤5.3: 拆分 validator.py** (3小时)
- 创建 physics_validator.py (1小时)
- 创建 range_validator.py (1小时)
- 运行所有测试验证 (1小时)

**步骤5.4: 拆分 dependency_analyzer.py** (3小时)
- 创建 dependency_graph.py (1小时)
- 创建 topological_sorter.py (1小时)
- 运行所有测试验证 (1小时)

**依赖**: 需要P0、P2、P3全部完成 (稳定基线)

---

### 依赖关系图

```
执行顺序:
┌─────────────────────────────────────────────────────────────┐
│ 第1阶段 (1小时, 可并行)                                      │
├─────────────────────────────────────────────────────────────┤
│ P0: 代码占位符移除 (0.5h) ──┐                               │
│                              ├──> 第2阶段                    │
│ P3: 配置表白名单更新 (0.5h) ─┘                               │
└─────────────────────────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────────────┐
│ 第2阶段 (2小时, 依赖P0)                                      │
├─────────────────────────────────────────────────────────────┤
│ P2: 种子数据补全 (2h)                                        │
│   - 提取方法定义 (0.5h)                                      │
│   - 生成SQL (0.5h)                                           │
│   - 执行导入 (0.5h)                                          │
│   - 验证数据 (0.5h)                                          │
└─────────────────────────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────────────┐
│ 第3阶段 (16小时, 依赖P0)                                     │
├─────────────────────────────────────────────────────────────┤
│ P2: 测试补充 (16h)                                           │
│   - 单元测试 (6h)                                            │
│   - 专项测试 (4h)                                            │
│   - 集成测试 (4h)                                            │
│   - 测试报告 (2h)                                            │
└─────────────────────────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────────────┐
│ 第4阶段 (16小时, 依赖P0+P2+P3)                               │
├─────────────────────────────────────────────────────────────┤
│ P1: 文件拆分 (16h)                                           │
│   - orchestrator.py (4h)                                     │
│   - calculators.py (6h)                                      │
│   - validator.py (3h)                                        │
│   - dependency_analyzer.py (3h)                              │
└─────────────────────────────────────────────────────────────┘

总工时: 35小时
关键路径: P0 → P2 → P2(测试) → P1 (35小时)
并行优化: P0和P3并行 (节省0.5小时)
实际最短工时: 34.5小时
```

### 里程碑

| 里程碑 | 完成标志 | 预计时间 |
|--------|---------|---------|
| M1: 快速修复完成 | P0和P3全部完成 | 第1天 |
| M2: 数据补全完成 | 种子数据导入验证通过 | 第1天 |
| M3: 单元测试完成 | 4个单元测试文件通过 | 第2天 |
| M4: 专项测试完成 | 3个专项测试文件通过 | 第3天 |
| M5: 集成测试完成 | 2个集成测试文件通过 | 第4天 |
| M6: 测试报告完成 | 覆盖率达标 | 第4天 |
| M7: 文件拆分完成 | 所有文件 < 600行 | 第6天 |
| M8: 全部完成 | 所有检查清单通过 | 第6天 |

---

## 🔧 风险评估与应对

### 风险1: 文件拆分破坏现有功能

**风险等级**: 高

**应对措施**:
1. 拆分前运行所有测试,确保基线通过
2. 拆分后立即运行所有测试,确保功能无变化
3. 使用Git分支隔离拆分工作
4. 每拆分一个文件立即验证

### 风险2: 种子数据导入失败

**风险等级**: 中

**应对措施**:
1. 使用事务,失败时自动回滚
2. 导入前备份数据库
3. 验证SQL语句的正确性
4. 分批导入,逐步验证

### 风险3: 测试覆盖不足

**风险等级**: 中

**应对措施**:
1. 使用pytest-cov监控覆盖率
2. 确保覆盖率 ≥ 80%
3. 重点测试边界条件和异常情况

### 风险4: 代码占位符移除引入Bug

**风险等级**: 低

**应对措施**:
1. 详细的单元测试
2. 代码审查
3. 日志记录详细信息

---

## 📝 最终检查清单

### 代码质量检查
- [ ] 所有文件行数 < 600行
- [ ] 无代码占位符 (TODO/FIXME)
- [ ] 所有日志使用中文
- [ ] 所有SQL使用参数化查询
- [ ] 代码符合编码规范

### 功能完整性检查
- [ ] 所有功能正常工作
- [ ] 所有测试通过
- [ ] 测试覆盖率 ≥ 80%
- [ ] 无性能衰减

### 数据完整性检查
- [ ] 种子数据已补全
- [ ] 配置表白名单已更新
- [ ] 数据库表结构完整

### 文档完整性检查
- [ ] 所有新文件有注释
- [ ] 所有函数有docstring
- [ ] 架构文档已更新
- [ ] 变更记录已更新

---

**计划文档完成 - 准备进入执行模式**


