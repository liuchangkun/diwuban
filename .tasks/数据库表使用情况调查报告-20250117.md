# 数据库表使用情况调查报告

**调查日期**: 2025-01-17  
**调查范围**: 8个数据库表的使用情况、废弃状态和建议  
**调查方法**: 代码检索、数据库查询、文档分析

---

## 📊 调查概览

| 表名 | 行数 | 状态 | 废弃风险 | 建议 |
|------|------|------|---------|------|
| dim_metric_metadata | 0 | ⚠️ 空表 | 低 | 保留（已有外键，未来可能使用） |
| device_metric_candidates | 0 | ⚠️ 空表 | 中 | 保留（设计完整，待实施） |
| metric_anomaly_strategy | 0 | ⚠️ 空表 | 中 | 保留（已有种子数据脚本） |
| completion_runs | 6 | ✅ 活跃 | 无 | 保留（正在使用） |
| metrics_presence_per_second_device_legacy | 0 | ❌ 已废弃 | 高 | **可删除** |
| dim_metric_metadata_override | 0 | ⚠️ 空表 | 低 | 保留（已有外键，设计完整） |
| metric_rule_auto_baseline | 0 | ⚠️ 空表 | 低 | 保留（有完整实现） |
| metric_calculation_order | 21 | ✅ 活跃 | 无 | 保留（核心功能） |

---

## 📋 详细分析

### 1. dim_metric_metadata - 指标元数据维度表

#### 表的用途
存储指标的全局物理元数据，包括单位、分辨率、物理边界、饱和阈值等信息，用于质量判定和阈值计算。

#### 表结构
```sql
CREATE TABLE dim_metric_metadata (
  metric_id bigint PRIMARY KEY REFERENCES dim_metric_config(id),
  unit text NULL,                    -- 单位（kW、A、V、Hz等）
  resolution double precision NULL,  -- 分辨率
  phys_min double precision NULL,    -- 物理下界
  phys_max double precision NULL,    -- 物理上界
  saturation_min double precision NULL,
  saturation_max double precision NULL,
  remark text NULL,
  updated_at timestamptz NOT NULL DEFAULT now(),
  updated_by text NULL
);
```

#### 使用位置
- **创建脚本**: `scripts/sql/migrations/017_create_dim_metric_metadata.sql`
- **视图引用**: `scripts/sql/migrations/020_create_v_effective_metric_metadata.sql`
  - 创建了 `v_effective_metric_metadata` 视图，合并全局元数据和覆盖元数据
- **存储过程**: `scripts/sql/migrations/022_alter_sp_refresh_metric_rule_auto_baseline_meta.sql`
  - 存储过程使用元数据进行基线计算
- **Python代码**: `app/services/rules/auto_baseline_b.py`
  - 使用 `v_effective_metric_metadata` 视图读取元数据

#### 使用方式
- **读取**: 通过 `v_effective_metric_metadata` 视图读取（设备>站点>全局优先级）
- **写入**: 手动配置或通过迁移脚本填充

#### 废弃状态
**❌ 未废弃** - 虽然当前为空表，但有以下证据表明该表是活跃设计：
1. ✅ 已有外键约束（`REFERENCES dim_metric_config(id)`）
2. ✅ 有配套视图（`v_effective_metric_metadata`）
3. ✅ 存储过程中有使用
4. ✅ Python代码中有引用

#### 为什么是空的
- 该表需要手动配置或通过数据分析填充
- 当前系统可能依赖默认值或统计值，尚未填充元数据
- 属于"配置优化"阶段的表，非核心功能必需

#### 建议
**保留** - 该表是完整设计的一部分，未来可能需要填充数据以提升质量判定准确性。

---

### 2. device_metric_candidates - 设备指标候选表 [已删除]

**状态**: ✅ 已于 2025-01-17 删除

**删除原因**:
- 该表从未被使用（3个月，48个备份版本，全部为空）
- 已被 `metric_capability_policy` 表 + 动态查询机制替代
- 无外键依赖、无代码引用、无数据丢失风险

**历史信息**:
- **表的用途**: 存储设备可能具有的指标列表，避免为设备计算不支持的指标
- **表结构**: device_id (PK) + metrics (text[])
- **设计意图**: 性能优化，避免无效计算
- **未实施原因**: 功能设计完成但从未填充数据，被其他机制替代

#### 原建议
**保留** - 虽然当前未使用，但设计合理，未来可能实施以优化性能。如果确认不再需要，可在6个月后删除。

---

### 3. metric_anomaly_strategy - 指标异常策略表

#### 表的用途
声明各指标在异常判定中的主策略与关键参数来源，支持层级覆盖（设备>站点>全局）。

#### 表结构
```sql
CREATE TABLE metric_anomaly_strategy (
  metric_id BIGINT NOT NULL,
  station_id BIGINT NOT NULL DEFAULT 0,
  device_id BIGINT NOT NULL DEFAULT 0,
  strategy TEXT NOT NULL,        -- work_condition | physics | residual | hybrid
  bins JSONB NULL,               -- 分桶配置
  model JSONB NULL,              -- 模型与阈值配置
  deps JSONB NULL,               -- 依赖变量清单
  hard_bounds_source TEXT NULL,  -- metadata | rules | none
  enabled BOOLEAN NOT NULL DEFAULT TRUE,
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_by TEXT NULL,
  PRIMARY KEY (metric_id, station_id, device_id)
);
```

#### 使用位置
- **创建脚本**: `scripts/sql/migrations/028_create_metric_anomaly_strategy.sql`
- **种子数据**: `scripts/sql/migrations/029_seed_metric_anomaly_strategy.sql`
  - 包含完整的种子数据插入语句（为所有指标配置全局策略）
- **注释脚本**: `scripts/sql/migrations/030_comment_metric_anomaly_strategy.sql`
- **文档引用**: 多个文档中提到该表的设计和用途

#### 使用方式
- **设计意图**: 
  - 为每个指标配置异常检测策略
  - 支持多种策略：工况判定、物理边界、残差分析、混合模式
  - 支持层级覆盖（设备级>站级>全局）

#### 废弃状态
**⚠️ 未填充** - 该表有完整的设计和种子数据脚本，但未执行：
1. ✅ 表结构完整
2. ✅ 有种子数据脚本（`029_seed_metric_anomaly_strategy.sql`）
3. ✅ 有详细注释
4. ❌ 种子数据未执行（表为空）
5. ❌ 未找到使用该表的Python代码

#### 为什么是空的
- 种子数据脚本存在但未执行
- 可能是新功能，尚未启用
- 异常检测功能可能使用其他机制实现

#### 建议
**保留并填充** - 建议执行种子数据脚本 `029_seed_metric_anomaly_strategy.sql` 以启用该功能。

---

### 4. completion_runs - 完成运行记录表

#### 表的用途
追踪数据补全/识别运行的总体信息与快照，记录每次运行的配置、统计和状态。

#### 表结构
```sql
CREATE TABLE completion_runs (
  run_id bigserial PRIMARY KEY,
  station_id bigint NOT NULL REFERENCES dim_stations(id),
  device_id bigint NOT NULL REFERENCES dim_devices(id),
  start_ts timestamptz NOT NULL,
  end_ts timestamptz NOT NULL,
  code_version text,
  imputation_version text,
  config_snapshot jsonb,
  thresholds_snapshot jsonb,
  rows_read bigint,
  duration_ms integer,
  steps_total integer,
  rerun_policy text CHECK (rerun_policy IN ('append','overwrite','skip')),
  parent_run_id bigint REFERENCES completion_runs(run_id),
  superseded_by_run_id bigint REFERENCES completion_runs(run_id),
  idempotency_key text,
  group_context jsonb,
  status_reason text,
  error_code text,
  created_at timestamptz NOT NULL DEFAULT now()
);
```

#### 使用位置
- **创建脚本**: `scripts/sql/m1/014_create_completion_tracking_tables.sql`
- **Python代码**: `app/services/device_running_job.py`
  - 第165-178行：插入运行记录
  - 第313-318行：更新运行统计
- **查询脚本**: `scripts/dev/inspect_device_running.py`
  - 第50-67行：查询最近的运行记录
- **关联表**: 
  - `completion_steps` - 步骤级详情（ON DELETE CASCADE）
  - `completion_audit` - 审计级统计（ON DELETE CASCADE）
  - `completion_failures` - 失败记录（ON DELETE CASCADE）

#### 使用方式
- **写入**: `device_running_job.py` 在每次运行开始时插入记录，结束时更新统计
- **读取**: 用于查询运行历史、统计分析、问题排查

#### 当前数据
```
run_id | device_id | start_ts            | end_ts              | steps_total | status_reason
-------|-----------|---------------------|---------------------|-------------|---------------
84     | 6         | 2025-05-31 18:00:00 | 2025-05-31 20:00:00 | 1           | ok
83     | 5         | 2025-05-31 18:00:00 | 2025-05-31 20:00:00 | 1           | ok
...
```

#### 废弃状态
**✅ 活跃使用** - 该表正在被积极使用：
1. ✅ 有6条记录
2. ✅ Python代码中有写入逻辑
3. ✅ 有查询和分析代码
4. ✅ 有完整的关联表体系

#### 建议
**保留** - 核心功能表，正在使用中。

---

### 5. metrics_presence_per_second_device_legacy - 每秒设备指标存在性遗留表

#### 表的用途
旧版的指标存在性记录表，已被 `metrics_presence_per_second_device` 替代。

#### 表结构
```sql
CREATE TABLE metrics_presence_per_second_device_legacy (
  station_id bigint NOT NULL,
  device_id bigint NOT NULL,
  ts_second timestamptz NOT NULL,
  available_metrics text[] NOT NULL DEFAULT '{}',
  need_compute_metrics text[] NOT NULL DEFAULT '{}',
  created_at timestamptz NOT NULL DEFAULT now(),
  PRIMARY KEY (station_id, device_id, ts_second)
);
```

#### 使用位置
- **迁移脚本**: `scripts/sql/ts_mpps_migrate.sql`
  - 第55行：将旧表重命名为 `_legacy`
  - 第56行：将新表重命名为正式表名
- **注释脚本**: `scripts/sql/migrations/058_add_remaining_comments.sql`
  - 第676-695行：明确标注为"已废弃"

#### 废弃状态
**✅ 已废弃** - 有明确的废弃证据：
1. ✅ 表名包含 `_legacy` 后缀
2. ✅ 注释明确说明"已废弃"
3. ✅ 有迁移脚本将其替换为新表
4. ✅ 新表 `metrics_presence_per_second_device` 已启用
5. ✅ 表为空（0行）

#### 为什么保留
- 迁移脚本采用"零停机切换"策略，保留旧表作为备份
- 可能用于历史数据查询（但当前为空）

#### 建议
**可安全删除** - 该表已被明确废弃，且为空表，可以安全删除。

**删除步骤**：
```sql
-- 1. 确认新表正常工作
SELECT COUNT(*) FROM metrics_presence_per_second_device;

-- 2. 删除旧表
DROP TABLE IF EXISTS metrics_presence_per_second_device_legacy CASCADE;
```

---

### 6. dim_metric_metadata_override - 指标元数据覆盖维度表

#### 表的用途
为特定站点/设备提供更精细的单位/分辨率/量程与饱和阈值覆盖，支持层级配置。

#### 表结构
```sql
CREATE TABLE dim_metric_metadata_override (
  rule_id bigserial PRIMARY KEY,
  station_id bigint NULL REFERENCES dim_stations(id),
  device_id bigint NULL REFERENCES dim_devices(id),
  metric_id bigint NOT NULL REFERENCES dim_metric_config(id),
  resolution double precision NULL,
  phys_min double precision NULL,
  phys_max double precision NULL,
  saturation_min double precision NULL,
  saturation_max double precision NULL,
  remark text NULL,
  updated_at timestamptz NOT NULL DEFAULT now(),
  updated_by text NULL
);
```

#### 使用位置
- **创建脚本**: `scripts/sql/migrations/018_create_dim_metric_metadata_override.sql`
- **视图引用**: `scripts/sql/migrations/020_create_v_effective_metric_metadata.sql`
  - 在 `v_effective_metric_metadata` 视图中合并覆盖数据
- **外键引用**: `.memory/数据层/数据表清单.md` 记录了外键关系

#### 使用方式
- **设计意图**: 
  - 全局元数据在 `dim_metric_metadata` 中定义
  - 特殊设备/站点的覆盖在此表中定义
  - 通过视图自动合并（设备>站点>全局优先级）

#### 废弃状态
**❌ 未废弃** - 该表是完整设计的一部分：
1. ✅ 有外键约束
2. ✅ 有配套视图
3. ✅ 设计逻辑清晰（层级覆盖）
4. ❌ 当前为空（无覆盖配置）

#### 为什么是空的
- 该表用于特殊情况的覆盖配置
- 当前可能所有设备使用全局默认值
- 属于"精细化配置"功能，非必需

#### 建议
**保留** - 该表是元数据管理体系的重要组成部分，未来可能需要为特殊设备配置覆盖值。

---

### 7. metric_rule_auto_baseline - 指标规则自动基线表

#### 表的用途
从历史有效数据自动计算的基线阈值，包括分位数、MAD、跳变阈值等，用于质量规则生成和阈值推荐。

#### 表结构
```sql
CREATE TABLE metric_rule_auto_baseline (
  baseline_id bigserial PRIMARY KEY,
  station_id bigint NULL,
  device_id bigint NULL,
  metric_id bigint NOT NULL,
  lookback_days int NOT NULL,
  method text NOT NULL,
  -- 分布统计
  p05 double precision,
  p95 double precision,
  median double precision,
  mad double precision,
  -- 跳变/速率建议
  spike_abs double precision,
  roc_abs double precision,
  roc_ratio double precision,
  -- 平台期建议
  flatline_eps double precision,
  flatline_delta double precision,
  -- 审计
  computed_at timestamptz DEFAULT now(),
  version text
);
```

#### 使用位置

**创建和刷新**：
- **创建脚本**: `scripts/sql/migrations/004_create_metric_rule_auto_baseline.sql`
- **存储过程**:
  - `scripts/sql/migrations/009_create_sp_refresh_metric_rule_auto_baseline.sql` - 基于lookback_days
  - `scripts/sql/migrations/013_create_sp_refresh_metric_rule_auto_baseline_win.sql` - 基于时间窗口
- **Python服务**:
  - `app/services/rules/auto_baseline.py::run_auto_baseline()` - 调用存储过程刷新基线
  - `app/services/rules/auto_baseline_b.py::run_auto_baseline_b()` - 使用STL分解的B版本

**数据消费**：
- **质量规则生成**: `app/services/rules/metric_quality_rules.py`
  - 从 `metric_rule_auto_baseline` 读取基线值生成质量规则
- **质量规则B版**: `app/services/rules/metric_quality_rules_b.py`
  - 从 `metric_rule_auto_baseline_shadow` 读取基线值

**外键约束**：
- **验证脚本**: `scripts/sql/validation/P1_add_foreign_keys_quality_rules.sql`
  - 第146-157行：添加外键约束（device_id, station_id）

#### 使用方式

**数据生成流程**：
1. 从 `fact_measurements` 读取历史数据（近N天）
2. 筛选有效数据（quality=0）且稳态数据（phase=1）
3. 计算统计量：分位数（p05, p95, median）、MAD、差分分布
4. 写入 `metric_rule_auto_baseline` 表

**数据使用流程**：
1. `metric_quality_rules` 生成时读取基线值
2. 使用基线值作为质量规则的默认阈值
3. 支持手动调整和覆盖

#### 废弃状态
**❌ 未废弃** - 该表是核心功能的一部分：
1. ✅ 有完整的生成逻辑（存储过程 + Python服务）
2. ✅ 有明确的消费者（质量规则生成）
3. ✅ 有外键约束保护
4. ✅ 有详细的文档和注释
5. ❌ 当前为空（未执行生成流程）

#### 为什么是空的
- 该表需要通过存储过程或Python服务生成
- 生成依赖于 `fact_measurements` 中的历史数据
- 属于"自动化配置"功能，需要手动触发或定期执行

**生成方法**：
```python
# 方法1：调用Python服务
from app.services.rules.auto_baseline import run_auto_baseline
run_auto_baseline(settings, lookback_days=30)

# 方法2：调用存储过程
CALL sp_refresh_metric_rule_auto_baseline(30, NULL, NULL);
```

#### 建议
**保留并生成数据** - 该表是质量管理体系的核心组件，建议：
1. 执行基线生成流程填充数据
2. 设置定期任务自动更新基线
3. 监控基线质量和覆盖率

---

### 8. metric_calculation_order - 指标计算顺序表

#### 表的用途
定义指标之间的依赖关系和计算顺序，支持循环依赖检测，确保指标按正确顺序计算。

#### 表结构
```sql
CREATE TABLE metric_calculation_order (
  metric_key TEXT PRIMARY KEY,
  depends_on TEXT[] NOT NULL DEFAULT '{}',  -- 依赖的指标列表
  priority INTEGER NOT NULL DEFAULT 100,     -- 优先级
  order_index INTEGER NOT NULL,              -- 计算顺序索引
  is_circular BOOLEAN NOT NULL DEFAULT FALSE, -- 是否循环依赖
  circular_group TEXT,                        -- 循环依赖组
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_by TEXT NOT NULL DEFAULT 'system'
);

CREATE INDEX idx_mco_order_index ON metric_calculation_order(order_index);
```

#### 使用位置

**核心实现**：
- **依赖分析器**: `app/services/calculation/dependency_analyzer.py`
  - 第568-636行：`load_calculation_order_from_db()` - 从数据库加载计算顺序
  - 第637-753行：`save_calculation_order()` - 保存计算顺序到数据库
  - 第456-566行：`get_calculation_order()` - 获取计算顺序（优先从缓存/数据库读取）
  - 第149-221行：`topological_sort()` - 拓扑排序算法（Kahn算法）

**管理工具**：
- **Python脚本**: `scripts/python/manage_calculation_order.py`
  - 第26-62行：`view_calculation_order()` - 查看计算顺序
  - 第64-82行：`update_calculation_order()` - 更新计算顺序

**创建和种子数据**：
- **创建脚本**: `scripts/sql/calculation/create_tables.sql`
- **种子数据**: `scripts/sql/adaptive/01_metric_config_related.sql`
  - 第652-663行：插入基础指标的计算顺序

#### 使用方式

**自动生成**：
1. `DependencyAnalyzer` 分析指标依赖关系
2. 使用拓扑排序算法生成计算顺序
3. 自动保存到数据库（`save_calculation_order()`）

**手动管理**：
```python
# 查看计算顺序
python scripts/python/manage_calculation_order.py view

# 更新计算顺序
python scripts/python/manage_calculation_order.py update
```

**运行时使用**：
1. 计算编排器调用 `get_calculation_order()`
2. 优先从缓存读取，缓存未命中则从数据库读取
3. 数据库也没有则动态计算并保存

#### 当前数据
表中有21条记录，包含完整的指标依赖关系：

```
order_index | metric_key                    | depends_on
------------|-------------------------------|----------------------------------
0           | main_pipeline_flow_rate       | []
0           | pump_frequency                | []
0           | pump_active_power             | []
0           | pump_flow_rate                | [main_pipeline_flow_rate, ...]
1           | pump_inlet_pressure           | [pool_liquid_level]
2           | pump_outlet_pressure          | []
3           | pump_speed                    | [pump_frequency]
...
8           | pump_efficiency               | [pump_flow_rate, pump_head, ...]
9           | pump_torque                   | [pump_flow_rate, pump_head, ...]
```

#### 废弃状态
**✅ 活跃使用** - 该表是核心功能：
1. ✅ 有21条记录
2. ✅ 有完整的生成和管理逻辑
3. ✅ 被计算编排器使用
4. ✅ 支持缓存和动态更新
5. ✅ 有管理工具

#### 建议
**保留** - 核心功能表，缺失指标计算的基础设施。

---

## 🎯 总结和建议

### 可以删除的表（1个）

| 表名 | 原因 | 删除风险 |
|------|------|---------|
| metrics_presence_per_second_device_legacy | 已明确废弃，已被新表替代，且为空表 | 低 |

**删除脚本**：
```sql
-- 确认新表正常工作
SELECT COUNT(*) FROM metrics_presence_per_second_device;

-- 删除旧表
DROP TABLE IF EXISTS metrics_presence_per_second_device_legacy CASCADE;
```

---

### 需要填充数据的表（3个）

#### 1. metric_anomaly_strategy
**操作**: 执行种子数据脚本
```bash
psql -f scripts/sql/migrations/029_seed_metric_anomaly_strategy.sql
```

#### 2. metric_rule_auto_baseline
**操作**: 执行基线生成
```python
from app.services.rules.auto_baseline import run_auto_baseline
run_auto_baseline(settings, lookback_days=30)
```

#### 3. device_metric_candidates
**操作**: 评估是否需要实施该功能
- 如果需要性能优化，实施该功能
- 如果不需要，可在6个月后删除

---

### 保留的表（4个）

| 表名 | 状态 | 原因 |
|------|------|------|
| dim_metric_metadata | 空表 | 完整设计，有外键和视图，未来可能使用 |
| dim_metric_metadata_override | 空表 | 元数据管理体系的一部分，支持层级配置 |
| completion_runs | 活跃 | 正在使用，有6条记录 |
| metric_calculation_order | 活跃 | 核心功能，有21条记录 |

---

## 📈 数据填充优先级

1. **P0 - 立即执行**:
   - `metric_rule_auto_baseline` - 质量管理的基础

2. **P1 - 1周内**:
   - `metric_anomaly_strategy` - 异常检测功能

3. **P2 - 评估后决定**:
   - `device_metric_candidates` - 性能优化功能
   - `dim_metric_metadata` - 精细化配置功能
   - `dim_metric_metadata_override` - 特殊设备配置

---

## 📝 附录：表依赖关系

```
dim_metric_config (指标配置)
  ↓
  ├─→ dim_metric_metadata (全局元数据)
  │     ↓
  │   dim_metric_metadata_override (覆盖元数据)
  │     ↓
  │   v_effective_metric_metadata (视图)
  │
  ├─→ metric_calculation_order (计算顺序) ✅ 活跃
  │
  ├─→ metric_anomaly_strategy (异常策略) ⚠️ 待填充
  │
  └─→ metric_rule_auto_baseline (自动基线) ⚠️ 待填充
        ↓
      metric_quality_rules (质量规则)

dim_devices (设备)
  ↓
  ├─→ device_metric_candidates (候选指标) ⚠️ 未实施
  │
  └─→ completion_runs (运行记录) ✅ 活跃
        ↓
      completion_steps (步骤详情)
      completion_audit (审计统计)
      completion_failures (失败记录)

metrics_presence_per_second_device (存在性) ✅ 活跃
metrics_presence_per_second_device_legacy (存在性-旧) ❌ 可删除
```

---

**报告结束**
