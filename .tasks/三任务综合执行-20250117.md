# 三任务综合执行计划

**创建时间**: 2025-01-17
**任务概述**: 按顺序完成三个任务：白名单问题排查、allowed_device_types字段补充、source_hint字段逻辑修改

---

## 任务1：白名单数据库表问题排查与修复

### 目标
彻底解决白名单相关的数据库表问题

### 排查范围
- 代码层：检查所有涉及白名单操作的代码逻辑
- 数据库层：检查白名单表的结构、约束、索引和数据完整性

### 要求
找到根本原因并彻底解决，不是临时修复

---

## 任务2：补充 calculation_method_registry 表的 allowed_device_types 字段

### 目标
填充 `calculation_method_registry.allowed_device_types` 字段的缺失内容

### 方法
- 分析相关代码逻辑，确定该字段应该存储什么类型的数据
- 确定每个计算方法对应的允许设备类型列表
- 将补充逻辑编写成可执行脚本，以便将来数据恢复时使用

### 交付物
- 填充后的数据
- 可重复执行的恢复脚本

---

## 任务3：修改 fact_measurements 表的 source_hint 字段填充逻辑

### 当前行为
指标计算完成后写入数据库时，`fact_measurements.source_hint` 字段统一填写为 `"calculation_orchestrator"`

### 目标行为
改为填写 `"calculation_" + 实际使用的计算方法名称`
- 例如：如果使用的计算方法是 `average`，则填写 `"calculation_average"`
- 例如：如果使用的计算方法是 `sum`，则填写 `"calculation_sum"`

### 要求
修改相关代码逻辑，确保动态拼接计算方法名称

---

## 研究阶段 - 信息收集结果

### 项目规则摘要
（待读取 `.memory/规则层/项目规则.md`）

### 记忆检索结果

#### 已检索的关键信息

**数据库表结构**：
1. `calculation_method_registry` 表
   - 字段：method_id, metric_key, method_name, method_code, priority, dependencies, conditions, formula_ref, accuracy_level, is_enabled, created_at, updated_at, allowed_device_types
   - allowed_device_types 字段：text[] 类型，NOT NULL，默认值 '{}'::text[]
   - 约束：CHECK (allowed_device_types <@ ARRAY['pump','main_pipeline','clear_water_pool','other']::text[])

2. `fact_measurements` 表
   - 字段：id, station_id, device_id, metric_id, ts_raw, ts_bucket, value, source_hint, inserted_at, quality_status, quality_type, quality_meta, quality_codes
   - source_hint 字段：text 类型，可为 NULL

**白名单相关代码**：
- 文件：`app/services/system/cleanup.py`
- 白名单定义在 `preserve_by_schema` 字典中
- 当前白名单包含的计算相关表：
  - calculation_method_registry
  - calculation_parameters
  - calculation_validation_config
  - metric_calculation_order
  - pump_characteristic_curves

**计算方法选择与使用**：
- `MethodSelector` 类负责选择计算方法
- `CalculationOrchestrator` 类负责执行计算并写入结果
- 方法选择返回 `MethodDescriptor` 对象，包含 method_id, method_code 等信息
- 写入时使用固定的 source_hint = "calculation_orchestrator"

**allowed_device_types 补充逻辑**：
- 已存在脚本：`temp/update_allowed_device_types.py` 和 `scripts/sql/tmp/problem6_step3_backfill_allowed_device_types.sql`
- 补充规则：
  - pump_% → '{pump}'::text[]
  - main_pipeline_% → '{main_pipeline}'::text[]
  - pool_% 或 clear_water_pool_% → '{clear_water_pool}'::text[]

### 代码分析

#### 白名单机制分析
（待深入分析）

#### allowed_device_types 使用分析
（待深入分析）

#### source_hint 写入逻辑分析
（待深入分析）

### 关键发现
（待补充）

### 需要澄清的问题
（待补充）

---

## 记忆更新日志

### 研究模式更新
（待补充）

---

## 计划阶段 - 详细实施计划

### 用户确认的任务优先级和方案选择

**任务1（白名单）**：暂不处理
**任务2（allowed_device_types）**：恢复字段内容 + 完善恢复脚本
**任务3（source_hint）**：使用方案3B（区分循环和非循环）

### 规则验证结果

已读取以下规则文档：
- ✅ `.memory/规则层/项目规则.md`
- ✅ `.memory/规则层/编码规范.md`
- ✅ `.memory/规则层/质量标准.md`
- ✅ `.memory/规则层/数据库规范.md`
- ✅ `规则/模式模块/规则-模式3-计划.md`

### 数据库状态验证

**calculation_method_registry 表**：
- 总记录数：22条
- allowed_device_types 字段状态：所有记录均为空数组 `[]`
- 字段类型：`text[] NOT NULL DEFAULT '{}'::text[]`
- 约束：`CHECK (allowed_device_types <@ ARRAY['pump','main_pipeline','clear_water_pool','other']::text[])`

**fact_measurements 表**：
- source_hint 字段类型：`text`（可为 NULL）
- 当前值：统一为 `"calculation_orchestrator"`

### 下一步行动
1. 制定任务2的详细实施计划
2. 制定任务3的详细实施计划
3. 生成检查清单

