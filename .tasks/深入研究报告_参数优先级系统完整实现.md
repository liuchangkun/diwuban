# 深入研究报告：参数优先级系统完整实现

**研究时间**：2025-11-11
**研究对象**：6层参数优先级系统的完整实现，参数加载、合并、覆盖逻辑

---

## 📋 研究目标

分析6层参数优先级系统的完整实现，理解如何为特定设备配置参数，以及参数如何在各组件间传递。

---

## 🔍 6层参数优先级系统

### 参数优先级定义

**代码位置**：`app/services/calculation/orchestrator.py:674-812`

```python
def load_parameters(
    self,
    device_id: int,
    method_id: str,
    station_id: Optional[int] = None
) -> Dict[str, any]:
    """
    加载计算参数（6层参数优先级）
    
    参数来源优先级（从低到高）：
    1. 全局计算参数（calculation_parameters, device_id=NULL, station_id=NULL）
    2. 全局默认额定参数（global_default_rated_params）
    3. 设备额定参数（device_rated_params, device_id=X）
    3.5. 泵站级额定参数（device_rated_params, station_id=X, device_id=NULL）
    4. 泵站级计算参数（calculation_parameters, station_id=X, device_id=NULL）
    5. 设备级计算参数（calculation_parameters, device_id=X）
    
    高优先级参数会覆盖低优先级参数
    """
    params = {}
    param_sources = {}  # 记录参数来源（用于调试）
```

**优先级表**：

| 层级 | 来源 | 表 | 条件 | 优先级 |
|------|------|------|------|--------|
| 1 | 全局计算参数 | calculation_parameters | device_id=NULL, station_id=NULL | 最低 |
| 2 | 全局默认额定参数 | global_default_rated_params | - | 低 |
| 3 | 设备额定参数 | device_rated_params | device_id=X | 中 |
| 3.5 | 泵站级额定参数 | device_rated_params | station_id=X, device_id=NULL | 中高 |
| 4 | 泵站级计算参数 | calculation_parameters | station_id=X, device_id=NULL | 高 |
| 5 | 设备级计算参数 | calculation_parameters | device_id=X | 最高 |

---

## 📊 参数加载实现

### 层级1：全局计算参数

**代码位置**：`orchestrator.py:706-721`

```python
# 层级1：加载全局计算参数（最低优先级）
query = """
    SELECT param_name, param_value, param_value_text, param_type
    FROM calculation_parameters
    WHERE device_id IS NULL
      AND station_id IS NULL
      AND method_id = %s
"""
cur.execute(query, (method_id,))
for row in cur.fetchall():
    param_name, param_value, param_value_text, param_type = row
    # 根据param_type选择使用param_value或param_value_text
    if param_type == 'string' and param_value_text is not None:
        params[param_name] = param_value_text
    else:
        params[param_name] = float(param_value)
    param_sources[param_name] = 'global'
```

**示例数据**：
```sql
-- pump_flow_rate_method_a的全局参数
INSERT INTO calculation_parameters (method_id, param_name, param_value, param_type)
VALUES 
    ('pump_flow_rate_method_a', 'alpha', 1.0, 'numeric'),
    ('pump_flow_rate_method_a', 'beta', 1.0, 'numeric'),
    ('pump_flow_rate_method_a', 'f_thr', 3.0, 'numeric'),
    ('pump_flow_rate_method_a', 'p_thr', 0.5, 'numeric');
```

---

### 层级2：全局默认额定参数

**代码位置**：`orchestrator.py:723-745`

```python
# 层级2：加载全局默认额定参数（如果设备无额定参数）
# 先检查设备是否有额定参数
query = """
    SELECT COUNT(*) FROM device_rated_params WHERE device_id = %s
"""
cur.execute(query, (device_id,))
has_rated_params = cur.fetchone()[0] > 0

if not has_rated_params:
    # 设备无额定参数，使用全局默认值
    query = """
        SELECT param_key, default_value, param_type
        FROM global_default_rated_params
    """
    cur.execute(query)
    for row in cur.fetchall():
        param_key, default_value, param_type = row
        if param_type == 'string':
            params[param_key] = default_value
        else:
            params[param_key] = float(default_value)
        param_sources[param_key] = 'global_default_rated'
```

**示例数据**：
```sql
-- 全局默认额定参数
INSERT INTO global_default_rated_params (param_key, default_value, param_type)
VALUES 
    ('rated_flow', 100.0, 'numeric'),
    ('rated_head', 50.0, 'numeric'),
    ('rated_power', 50.0, 'numeric'),
    ('rated_speed', 1500.0, 'numeric');
```

---

### 层级3：设备额定参数

**代码位置**：`orchestrator.py:747-762`

```python
# 层级3：加载设备额定参数（覆盖全局默认值）
query = """
    SELECT param_key, value_numeric, value_text, param_type
    FROM device_rated_params
    WHERE device_id = %s
"""
cur.execute(query, (device_id,))
for row in cur.fetchall():
    param_key, value_numeric, value_text, param_type = row
    if param_type == 'string' and value_text is not None:
        params[param_key] = value_text
    else:
        params[param_key] = float(value_numeric)
    param_sources[param_key] = 'device_rated'
```

**示例数据**：
```sql
-- 设备3的额定参数
INSERT INTO device_rated_params (device_id, param_key, value_numeric, param_type)
VALUES 
    (3, 'rated_flow', 5.0, 'numeric'),      -- 设备3的额定流量很小
    (3, 'rated_head', 50.0, 'numeric'),
    (3, 'rated_power', 5.0, 'numeric'),     -- 设备3的额定功率很小
    (3, 'rated_speed', 1500.0, 'numeric');
```

---

### 层级3.5：泵站级额定参数

**代码位置**：`orchestrator.py:764-779`

```python
# 层级3.5：加载泵站级额定参数（覆盖设备额定参数）
if station_id:
    query = """
        SELECT param_key, value_numeric, value_text, param_type
        FROM device_rated_params
        WHERE station_id = %s AND device_id IS NULL
    """
    cur.execute(query, (station_id,))
    for row in cur.fetchall():
        param_key, value_numeric, value_text, param_type = row
        if param_type == 'string' and value_text is not None:
            params[param_key] = value_text
        else:
            params[param_key] = float(value_numeric)
        param_sources[param_key] = 'station_rated'
```

---

### 层级4：泵站级计算参数

**代码位置**：`orchestrator.py:781-791`

```python
# 层级4：加载泵站级计算参数（覆盖泵站级额定参数）
if station_id:
    query = """
        SELECT param_name, param_value, param_value_text, param_type
        FROM calculation_parameters
        WHERE station_id = %s AND device_id IS NULL AND method_id = %s
    """
    cur.execute(query, (station_id, method_id))
    for row in cur.fetchall():
        param_name, param_value, param_value_text, param_type = row
        if param_type == 'string' and param_value_text is not None:
            params[param_name] = param_value_text
        else:
            params[param_name] = float(param_value)
        param_sources[param_name] = 'station'
```

---

### 层级5：设备级计算参数

**代码位置**：`orchestrator.py:793-806`

```python
# 层级5：加载设备级计算参数（最高优先级）
query = """
    SELECT param_name, param_value, param_value_text, param_type
    FROM calculation_parameters
    WHERE device_id = %s AND method_id = %s
"""
cur.execute(query, (device_id, method_id))
for row in cur.fetchall():
    param_name, param_value, param_value_text, param_type = row
    if param_type == 'string' and param_value_text is not None:
        params[param_name] = param_value_text
    else:
        params[param_name] = float(param_value)
    param_sources[param_name] = 'device'

logger.debug(
    f"[参数加载] device_id={device_id}, method_id={method_id}, "
    f"params_count={len(params)}, sources={param_sources}"
)

return params
```

**示例数据**：
```sql
-- 设备3的特定参数（降低阈值以适应低负荷运行）
INSERT INTO calculation_parameters (device_id, method_id, param_name, param_value, param_type)
VALUES 
    (3, 'pump_flow_rate_method_a', 'f_thr', 0.5, 'numeric'),  -- 降低频率阈值
    (3, 'pump_flow_rate_method_a', 'p_thr', 0.1, 'numeric');  -- 降低功率阈值
```

---

## 🔥 参数覆盖示例

### 示例：设备3的参数加载过程

**场景**：加载设备3的pump_flow_rate_method_a参数

```python
params = orchestrator.load_parameters(
    device_id=3,
    method_id='pump_flow_rate_method_a',
    station_id=1
)
```

**加载过程**：

```
步骤1：加载全局计算参数
  alpha = 1.0 (来源: global)
  beta = 1.0 (来源: global)
  f_thr = 3.0 (来源: global)
  p_thr = 0.5 (来源: global)

步骤2：加载全局默认额定参数
  （设备3有额定参数，跳过）

步骤3：加载设备额定参数
  rated_flow = 5.0 (来源: device_rated)
  rated_head = 50.0 (来源: device_rated)
  rated_power = 5.0 (来源: device_rated)
  rated_speed = 1500.0 (来源: device_rated)

步骤4：加载泵站级额定参数
  （无泵站级额定参数，跳过）

步骤5：加载泵站级计算参数
  （无泵站级计算参数，跳过）

步骤6：加载设备级计算参数
  f_thr = 0.5 (来源: device) ← 覆盖全局值3.0
  p_thr = 0.1 (来源: device) ← 覆盖全局值0.5

最终参数：
  alpha = 1.0 (来源: global)
  beta = 1.0 (来源: global)
  f_thr = 0.5 (来源: device) ✅ 设备级参数覆盖全局参数
  p_thr = 0.1 (来源: device) ✅ 设备级参数覆盖全局参数
  rated_flow = 5.0 (来源: device_rated)
  rated_head = 50.0 (来源: device_rated)
  rated_power = 5.0 (来源: device_rated)
  rated_speed = 1500.0 (来源: device_rated)
```

---

## 📌 参数传递机制

### 参数在各组件间的传递

**流程**：

```
1. Orchestrator加载参数
   ↓
2. 创建MethodDescriptor（包含params）
   ↓
3. 传递给Calculator
   ↓
4. Calculator使用params执行计算
```

**代码位置**：`orchestrator.py:1885-1902`

```python
# 加载计算参数（6层优先级）
params = self.load_parameters(
    device_id=ctx2.device_id,
    method_id=method_desc.method_id,
    station_id=ctx2.station_id
)

# 将参数设置到 method_desc 中
method_desc_with_params = MethodDescriptor(
    method_id=method_desc.method_id,
    method_code=method_desc.method_code,
    metric_key=method_desc.metric_key,
    priority=method_desc.priority,
    dependencies=method_desc.dependencies,
    conditions=method_desc.conditions,
    params=params,  # ← 设置加载的参数
    validator_hint=method_desc.validator_hint
)

# 调用计算器
calculator_fn = get_calculator_unified(method_desc_with_params.method_id)
values, meta = calculator_fn(ctx2, method_desc_with_params, input_data)
```

---

## 📊 总结

### 6层参数优先级的优势

| 优势 | 描述 |
|------|------|
| 灵活性 | 支持全局、泵站、设备三个层级的参数配置 |
| 可扩展性 | 可以为特定设备配置特殊参数 |
| 降级策略 | 设备无额定参数时使用全局默认值 |
| 调试友好 | 记录每个参数的来源 |

### 如何为设备3配置特殊参数

**步骤1：插入设备级计算参数**
```sql
INSERT INTO calculation_parameters (device_id, method_id, param_name, param_value, param_type)
VALUES 
    (3, 'pump_flow_rate_method_a', 'f_thr', 0.5, 'numeric'),
    (3, 'pump_flow_rate_method_a', 'p_thr', 0.1, 'numeric');
```

**步骤2：验证参数加载**
```python
params = orchestrator.load_parameters(device_id=3, method_id='pump_flow_rate_method_a')
print(params)  # {'f_thr': 0.5, 'p_thr': 0.1, ...}
```

**步骤3：重新计算**
```python
orchestrator.calculate_missing_metrics(
    station_id=1,
    device_id=3,
    start_time='2025-10-23 10:48:00+08',
    end_time='2025-10-23 10:48:10+08',
    metrics=['pump_flow_rate']
)
```

---

**研究完成时间**：2025-11-11
**结论**：6层参数优先级系统提供了灵活的参数配置机制，可以为特定设备配置特殊参数
**关键洞察**：通过设备级计算参数，可以为低负荷运行的设备（如设备3）配置更低的阈值，使其能够正常计算

