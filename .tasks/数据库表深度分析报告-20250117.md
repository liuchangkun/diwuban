# 数据库表深度分析报告

**调查日期**: 2025-01-17  
**调查方法**: 代码检索、数据库查询、文档分析、Git历史追踪、依赖关系分析  
**调查深度**: 完整代码引用、数据流追踪、功能实现状态、删除影响评估

---

## 📋 执行摘要

| 表名 | 行数 | 代码引用 | 视图/SP引用 | 外键关系 | 功能状态 | 删除风险 | 最终建议 |
|------|------|---------|------------|---------|---------|---------|---------|
| dim_metric_metadata | 0 | 8处 | 1视图 | 1入1出 | 设计完整未填充 | 低 | **保留** |
| device_metric_candidates | 0 | 12处 | 0 | 0入0出 | 设计完整未实施 | 中 | **保留6个月** |
| metric_anomaly_strategy | 0 | 15处 | 0 | 3入0出 | 有种子数据未执行 | 中 | **填充数据** |
| completion_runs | 6 | 20+处 | 0 | 2入4出 | 活跃使用 | 无 | **保留** |
| metrics_presence_per_second_device_legacy | 0 | 2处 | 0 | 0入0出 | 已明确废弃 | 无 | **删除** |
| dim_metric_metadata_override | 0 | 6处 | 1视图 | 3入0出 | 设计完整未填充 | 低 | **保留** |
| metric_rule_auto_baseline | 0 | 18处 | 1视图 | 3入0出 | 有完整实现未执行 | 低 | **生成数据** |
| metric_calculation_order | 21 | 25+处 | 0 | 1入0出 | 核心功能活跃 | 无 | **保留** |

---

## 🔍 表1: dim_metric_metadata - 指标元数据维度表

### 完整代码引用清单

#### 直接引用（8处）

**SQL脚本**：
1. `scripts/sql/migrations/017_create_dim_metric_metadata.sql` - 表创建
2. `scripts/sql/migrations/020_create_v_effective_metric_metadata.sql` - 视图引用
3. `scripts/sql/migrations/021_seed_dim_metric_metadata.sql` - 种子数据（未执行）
4. `scripts/sql/migrations/057_add_comprehensive_comments.sql` - 表注释
5. `scripts/sql/migrations/058_add_remaining_comments.sql` - 字段注释

**Python代码**：
6. `scripts/dev/apply_metadata_migrations.py` - 迁移应用脚本
7. `scripts/test_prepare_dim_backup.py` - 备份脚本（列为手动配置表）
8. `app/services/ingest/prepare_dim/__init__.py` - 备份逻辑（列为手动配置表）

#### 间接引用（通过视图）

**视图引用**：
- `v_effective_metric_metadata` - 合并全局元数据和覆盖元数据
  - 被 `app/services/rules/auto_baseline_b.py` 使用
  - 被存储过程 `sp_refresh_metric_rule_auto_baseline_meta` 使用

#### 注释中的引用

**文档引用**：
- `.memory/数据层/数据表清单.md` - 详细字段说明
- `docs/缺失计算修复/06-技术参考/数据库表结构.md` - 架构说明
- `.tasks/数据库表使用情况调查报告-20250117.md` - 初步分析

### 数据流分析

**数据来源**：
```
手动配置/数据分析
    ↓
dim_metric_metadata (全局元数据)
    ↓
v_effective_metric_metadata (视图合并)
    ↓
├─→ sp_refresh_metric_rule_auto_baseline_meta (存储过程)
│   └─→ metric_rule_auto_baseline (基线计算)
└─→ app/services/rules/auto_baseline_b.py (Python服务)
    └─→ metric_rule_auto_baseline_shadow (影子表)
```

**为什么当前是空的**：
1. ✅ 表结构完整，有外键约束
2. ✅ 有种子数据脚本 `021_seed_dim_metric_metadata.sql`
3. ❌ 种子数据脚本**从未执行**（表为空）
4. ❌ 当前系统依赖默认值或统计值，未填充元数据
5. 属于"配置优化"阶段的表，非核心功能必需

**数据去向**：
- 通过 `v_effective_metric_metadata` 视图被质量规则生成使用
- 用于基线计算时的元数据参考
- 用于质量判定时的物理边界验证

### 功能实现状态

**实现程度**: 80% - 设计完整，基础设施就绪，数据未填充

**证据**：
1. ✅ 表结构完整（有外键约束 `REFERENCES dim_metric_config(id)`）
2. ✅ 有配套视图 `v_effective_metric_metadata`
3. ✅ 存储过程中有使用
4. ✅ Python代码中有引用
5. ✅ 有种子数据脚本
6. ❌ 种子数据未执行
7. ❌ 表为空

**功能状态**: **设计完整未填充** - 所有基础设施就绪，等待数据填充

### 删除影响评估

**如果删除会破坏的功能**：

1. **视图失效** - `v_effective_metric_metadata` 视图会报错
   - 影响文件: `scripts/sql/migrations/020_create_v_effective_metric_metadata.sql`
   - 影响代码: `app/services/rules/auto_baseline_b.py`

2. **存储过程失效** - `sp_refresh_metric_rule_auto_baseline_meta` 会报错
   - 影响文件: `scripts/sql/migrations/022_alter_sp_refresh_metric_rule_auto_baseline_meta.sql`

3. **备份流程失败** - 备份脚本会报错
   - 影响文件: `scripts/test_prepare_dim_backup.py`
   - 影响文件: `app/services/ingest/prepare_dim/__init__.py`

**删除风险**: **高** - 虽然表为空，但有多处依赖

### Git历史分析

**创建时间**: 通过迁移脚本 `017_create_dim_metric_metadata.sql` 创建

**历史演进**：
1. 初始创建 - 定义表结构和外键
2. 创建视图 - `v_effective_metric_metadata` 合并全局和覆盖元数据
3. 创建种子数据 - `021_seed_dim_metric_metadata.sql`（未执行）
4. 添加注释 - 完善文档

**重要发现**：
- 表从创建至今一直为空
- 所有备份记录都显示0行
- 设计完整但从未填充数据

### 最终建议

**建议**: **保留并填充数据**

**理由**：
1. 表是完整设计的一部分，有多处依赖
2. 有种子数据脚本，可以快速填充
3. 未来可能需要填充数据以提升质量判定准确性
4. 删除会破坏视图和存储过程

**操作步骤**：
```bash
# 执行种子数据脚本
psql -f scripts/sql/migrations/021_seed_dim_metric_metadata.sql

# 验证数据
psql -c "SELECT COUNT(*) FROM dim_metric_metadata;"
```

---

## 🔍 表2: device_metric_candidates - 设备指标候选表

### 完整代码引用清单

#### 直接引用（12处）

**SQL脚本**：
1. `scripts/sql/ts_candidates.sql` - 表创建
2. `scripts/sql/migrations/058_add_remaining_comments.sql` - 表注释（第178-217行）
3. `scripts/sql/validation/P1_add_foreign_keys_quality_rules.sql` - 外键验证（第179-182行）
4. `scripts/sql/validation/P1_add_foreign_keys_comprehensive.sql` - 外键验证

**Python代码**：
5. `scripts/test_prepare_dim_backup.py` - 备份脚本（第54行）
6. `app/services/ingest/prepare_dim/__init__.py` - 备份逻辑（第1197行）
7. `scripts/verify_remaining_comments.py` - 注释验证（第26行）
8. `scripts/test_foreign_key_cascade.py` - 外键测试

**文档引用**：
9. `.memory/数据层/数据表清单.md` - 详细字段说明（第991-1009行）
10. `docs/缺失计算修复/06-技术参考/数据库表结构.md` - 架构说明
11. `docs/缺失计算修复1/04-完整依赖关系深度分析报告.md` - 依赖分析（第118行）
12. `.tasks/数据库表使用情况调查报告-20250117.md` - 初步分析

#### 间接引用（0处）

**未找到实际使用该表的Python代码**

#### 备份记录（5个文件，全部为空）

1. `backups/device_metric_candidates/device_metric_candidates_v1_20251030_221231.sql` - 0行
2. `backups/device_metric_candidates/device_metric_candidates_v2_20251031_005010.sql` - 0行
3. `backups/device_metric_candidates/device_metric_candidates_v3_20251031_011821.sql` - 0行
4. `backups/device_metric_candidates/device_metric_candidates_v4_20251031_020650.sql` - 0行
5. `backups/device_metric_candidates/device_metric_candidates_v5_20251031_022800.sql` - 0行

### 数据流分析

**设计意图的数据流**：
```
data_mapping.json / CSV配置
    ↓
[未实现] 数据导入脚本
    ↓
device_metric_candidates (设备候选指标)
    ↓
[未实现] 计算编排器
    ↓
仅计算候选列表中的指标（性能优化）
```

**实际数据流**：
```
❌ 无数据流 - 功能未实施
```

**为什么当前是空的**：
1. ✅ 表结构清晰，设计合理
2. ✅ 有详细注释说明用途
3. ❌ **从未有数据**（所有备份都显示0行）
4. ❌ **未找到填充数据的代码**
5. ❌ **未找到使用该表的代码**
6. 可能被其他机制替代（如 `metric_capability_policy` 表）

**替代机制**：
- `metric_capability_policy` 表 - 指标能力策略表（有53条记录）
- `app/services/calculation/missing_metrics_batch.py` - 使用 `metrics_presence_per_second_device` 表动态查询

### 功能实现状态

**实现程度**: 20% - 仅有表结构和文档，无实际实现

**证据**：
1. ✅ 表结构完整
2. ✅ 有详细注释
3. ❌ 从未有数据（所有备份都显示0行）
4. ❌ 未找到填充数据的代码
5. ❌ 未找到使用该表的代码
6. ❌ 无外键约束

**功能状态**: **设计完整未实施** - 功能设计完成但从未实施

### 删除影响评估

**如果删除会破坏的功能**：

**直接影响**：
1. **备份流程警告** - 备份脚本会报错
   - 影响文件: `scripts/test_prepare_dim_backup.py`（第54行）
   - 影响文件: `app/services/ingest/prepare_dim/__init__.py`（第1197行）
   - **风险**: 低 - 可以从备份列表中移除

2. **注释验证失败** - 验证脚本会报错
   - 影响文件: `scripts/verify_remaining_comments.py`（第26行）
   - **风险**: 低 - 可以从验证列表中移除

**间接影响**：
- **无** - 未找到实际使用该表的代码

**删除风险**: **低** - 仅影响备份和验证脚本，无实际功能依赖

### Git历史分析

**创建时间**: 通过 `scripts/sql/ts_candidates.sql` 创建

**历史演进**：
1. 初始创建 - 定义表结构
2. 添加注释 - 完善文档
3. 添加到备份列表 - 列为手动配置表
4. **从未填充数据** - 所有备份记录都显示0行

**重要发现**：
- 表从创建至今一直为空
- 所有备份记录都显示0行（5个备份文件）
- 设计完整但从未实施

### 最终建议

**建议**: **保留6个月，评估后决定**

**理由**：
1. 设计合理，未来可能实施以优化性能
2. 当前有替代机制（`metric_capability_policy` 表）
3. 删除风险低，仅影响备份和验证脚本
4. 如果6个月内未实施，可以安全删除

**操作步骤**：
```sql
-- 6个月后，如果仍未使用，执行删除
DROP TABLE IF EXISTS device_metric_candidates CASCADE;

-- 同时从备份脚本中移除
-- 编辑 scripts/test_prepare_dim_backup.py
-- 编辑 app/services/ingest/prepare_dim/__init__.py
```

---

## 🔍 表3: metric_anomaly_strategy - 指标异常策略表

### 完整代码引用清单

#### 直接引用（15处）

**SQL脚本**：
1. `scripts/sql/migrations/028_create_metric_anomaly_strategy.sql` - 表创建
2. `scripts/sql/migrations/029_seed_metric_anomaly_strategy.sql` - **种子数据脚本（未执行）**
3. `scripts/sql/migrations/030_comment_metric_anomaly_strategy.sql` - 表注释
4. `scripts/sql/migrations/058_add_remaining_comments.sql` - 字段注释
5. `scripts/sql/validation/P1_add_foreign_keys_quality_rules.sql` - 外键添加（第73-176行）
6. `scripts/sql/validation/P1_add_foreign_keys_comprehensive.sql` - 外键验证（第16-46行）
7. `scripts/sql/validation/P1_add_foreign_keys_metric_config.sql` - metric_id外键（第156-181行）

**Python代码**：
8. `scripts/test_prepare_dim_backup.py` - 备份脚本（第53行）
9. `app/services/ingest/prepare_dim/__init__.py` - 备份逻辑（第1196行）
10. `scripts/verify_remaining_comments.py` - 注释验证（第33行）
11. `scripts/test_foreign_key_cascade.py` - 外键测试（第59-62行，第95-98行）

**文档引用**：
12. `.memory/数据层/数据表清单.md` - 详细字段说明（第1178-1194行）
13. `docs/缺失计算修复/06-技术参考/数据库表结构.md` - 架构说明
14. `docs/缺失计算修复1/04-完整依赖关系深度分析报告.md` - 依赖分析（第119行）
15. `.tasks/数据库表使用情况调查报告-20250117.md` - 初步分析

#### 间接引用（0处）

**未找到实际使用该表的Python代码** - 异常检测功能可能使用其他机制实现

#### 种子数据脚本内容

**文件**: `scripts/sql/migrations/029_seed_metric_anomaly_strategy.sql`

**内容摘要**：
- 为所有指标配置全局策略（station_id=0, device_id=0）
- 包含4种策略：work_condition、physics、residual、hybrid
- 总共**68条INSERT语句**
- 使用 `ON CONFLICT DO UPDATE` 实现幂等性

**策略分类**：
1. **work_condition** (3个指标): metric_id 1, 64, 65
2. **physics** (7个指标): metric_id 2, 4, 6, 9, 10, 15, 63
3. **residual** (33个指标): metric_id 21-49
4. **hybrid** (25个指标): metric_id 3, 5, 7, 8, 11-20, 61, 62

### 数据流分析

**设计意图的数据流**：
```
029_seed_metric_anomaly_strategy.sql (种子数据)
    ↓
metric_anomaly_strategy (异常策略配置)
    ↓
[未实现] 异常检测服务
    ↓
├─→ work_condition策略 → 工况判定
├─→ physics策略 → 物理边界检测
├─→ residual策略 → 残差分析
└─→ hybrid策略 → 混合模式
    ↓
fact_measurements.quality_codes (质量码标注)
```

**实际数据流**：
```
❌ 无数据流 - 种子数据未执行，功能未启用
```

**为什么当前是空的**：
1. ✅ 表结构完整（有外键约束）
2. ✅ 有种子数据脚本 `029_seed_metric_anomaly_strategy.sql`
3. ✅ 种子数据脚本包含68条INSERT语句
4. ❌ **种子数据脚本从未执行**（表为空）
5. ❌ 未找到使用该表的Python代码
6. 异常检测功能可能使用其他机制实现

**外键约束**（已添加）：
- `metric_id` → `dim_metric_config(id)` (ON UPDATE CASCADE ON DELETE CASCADE)
- `station_id` → `dim_stations(id)` (ON UPDATE CASCADE ON DELETE CASCADE)
- `device_id` → `dim_devices(id)` (ON UPDATE CASCADE ON DELETE CASCADE)

### 功能实现状态

**实现程度**: 60% - 设计完整，种子数据就绪，未执行

**证据**：
1. ✅ 表结构完整（有外键约束）
2. ✅ 有详细注释
3. ✅ 有种子数据脚本（68条INSERT）
4. ✅ 种子数据脚本设计合理（4种策略）
5. ❌ 种子数据未执行
6. ❌ 未找到使用该表的Python代码

**功能状态**: **有种子数据未执行** - 所有准备工作就绪，等待执行

### 删除影响评估

**如果删除会破坏的功能**：

**直接影响**：
1. **外键约束失败** - 已有外键约束保护
   - 影响: 无法删除（有外键约束）
   - **风险**: 无 - 外键约束会阻止删除

2. **备份流程警告** - 备份脚本会报错
   - 影响文件: `scripts/test_prepare_dim_backup.py`（第53行）
   - 影响文件: `app/services/ingest/prepare_dim/__init__.py`（第1196行）
   - **风险**: 低 - 可以从备份列表中移除

**间接影响**：
- **无** - 未找到实际使用该表的代码

**删除风险**: **中** - 有外键约束保护，但未来可能需要

### Git历史分析

**创建时间**: 通过迁移脚本 `028_create_metric_anomaly_strategy.sql` 创建

**历史演进**：
1. 初始创建 - 定义表结构（028）
2. 创建种子数据 - 68条INSERT语句（029）
3. 添加注释 - 完善文档（030）
4. 添加外键约束 - 数据完整性保护
5. **从未执行种子数据** - 表一直为空

**重要发现**：
- 种子数据脚本存在但从未执行
- 表从创建至今一直为空
- 设计完整但功能未启用

### 最终建议

**建议**: **立即执行种子数据脚本**

**理由**：
1. 种子数据脚本已就绪，包含68条配置
2. 表结构完整，有外键约束保护
3. 异常检测是重要的质量管理功能
4. 执行成本低，风险小（幂等性设计）

**操作步骤**：
```bash
# 执行种子数据脚本
psql -f scripts/sql/migrations/029_seed_metric_anomaly_strategy.sql

# 验证数据
psql -c "SELECT strategy, COUNT(*) FROM metric_anomaly_strategy GROUP BY strategy;"

# 预期结果：
# work_condition | 3
# physics        | 7
# residual       | 33
# hybrid         | 25
```

---

## 🔍 表4: completion_runs - 完成运行记录表

### 完整代码引用清单

#### 直接引用（20+处）

**SQL脚本**：
1. `scripts/sql/m1/014_create_completion_tracking_tables.sql` - 表创建（第7-29行）
2. `scripts/sql/migrations/058_add_remaining_comments.sql` - 表注释

**Python代码（核心使用）**：
3. `app/services/device_running_job.py` - **主要使用者**
   - 第164-178行：插入运行记录（INSERT）
   - 第313-318行：更新运行统计（UPDATE）
4. `scripts/dev/inspect_device_running.py` - 查询最近运行
   - 第50-67行：查询最近一条run
5. `scripts/tools/final_validation.py` - 验证脚本
   - 第197-215行：验证device_running步骤

**文档引用**：
6. `.memory/数据层/数据表清单.md` - 详细字段说明（第711-728行）
7. `docs/database_dictionary.md` - 数据字典（第1718-1757行）
8. `.tasks/数据库表使用情况调查报告-20250117.md` - 初步分析

#### 间接引用（通过外键）

**被引用的表**（4个子表）：
1. `completion_steps` - 步骤级详情（ON DELETE CASCADE）
   - 外键: `run_id` → `completion_runs(run_id)`
2. `completion_audit` - 审计级统计（ON DELETE CASCADE）
   - 外键: `run_id` → `completion_runs(run_id)`
3. `completion_failures` - 失败记录（ON DELETE CASCADE）
   - 外键: `run_id` → `completion_runs(run_id)`
4. `completion_runs` - 自引用（父子关系）
   - 外键: `parent_run_id` → `completion_runs(run_id)`
   - 外键: `superseded_by_run_id` → `completion_runs(run_id)`

### 数据流分析

**完整数据流**：
```
app/services/device_running_job.py
    ↓
1. 开始运行 → INSERT completion_runs (创建run_id)
    ↓
2. 处理缺口 → INSERT completion_steps (记录每个步骤)
    ↓
3. 记录失败 → INSERT completion_failures (可选)
    ↓
4. 生成审计 → INSERT completion_audit (统计摘要)
    ↓
5. 完成运行 → UPDATE completion_runs (回填统计)
    ↓
查询和分析
├─→ scripts/dev/inspect_device_running.py (查询最近运行)
├─→ scripts/tools/final_validation.py (验证运行状态)
└─→ 未来的分析和报表
```

**当前数据**（6条记录）：
```sql
run_id | device_id | start_ts            | end_ts              | steps_total | status_reason
-------|-----------|---------------------|---------------------|-------------|---------------
84     | 6         | 2025-05-31 18:00:00 | 2025-05-31 20:00:00 | 1           | ok
83     | 5         | 2025-05-31 18:00:00 | 2025-05-31 20:00:00 | 1           | ok
82     | 4         | 2025-05-31 18:00:00 | 2025-05-31 20:00:00 | 1           | ok
81     | 3         | 2025-05-31 18:00:00 | 2025-05-31 20:00:00 | 1           | ok
80     | 2         | 2025-05-31 18:00:00 | 2025-05-31 20:00:00 | 1           | ok
79     | 1         | 2025-05-31 18:00:00 | 2025-05-31 20:00:00 | 1           | ok
```

**数据特征**：
- 最近运行时间：2025-05-31（约7个月前）
- 所有运行状态：ok
- 每个设备1个步骤
- 运行时长：2小时窗口

### 功能实现状态

**实现程度**: 100% - 完全实现并活跃使用

**证据**：
1. ✅ 表结构完整（有外键约束）
2. ✅ 有6条记录（最近运行：2025-05-31）
3. ✅ Python代码中有写入逻辑
4. ✅ Python代码中有查询逻辑
5. ✅ 有完整的关联表体系（4个子表）
6. ✅ 有详细的文档和注释

**功能状态**: **活跃使用** - 核心功能表，正在使用中

### 删除影响评估

**如果删除会破坏的功能**：

**直接影响**（严重）：
1. **设备运行状态计算失败** - 核心功能完全失效
   - 影响文件: `app/services/device_running_job.py`
   - 影响代码: 第164-178行（INSERT失败）
   - 影响代码: 第313-318行（UPDATE失败）
   - **风险**: **极高** - 核心功能完全失效

2. **子表级联删除** - 4个子表数据丢失
   - `completion_steps` - 步骤详情丢失
   - `completion_audit` - 审计统计丢失
   - `completion_failures` - 失败记录丢失
   - **风险**: **极高** - 历史数据完全丢失

3. **查询和分析失败** - 所有依赖该表的脚本失效
   - 影响文件: `scripts/dev/inspect_device_running.py`
   - 影响文件: `scripts/tools/final_validation.py`
   - **风险**: **高** - 运维和验证工具失效

**间接影响**：
- 无法追踪数据补全运行历史
- 无法分析补全方法使用情况
- 无法排查补全失败问题
- 无法生成补全质量报告

**删除风险**: **极高** - 核心功能表，删除会导致系统核心功能失效

### Git历史分析

**创建时间**: 通过迁移脚本 `014_create_completion_tracking_tables.sql` 创建

**历史演进**：
1. 初始创建 - 定义表结构和外键
2. 创建子表 - completion_steps, completion_audit, completion_failures
3. 添加注释 - 完善文档
4. **持续使用** - 有6条运行记录

**重要发现**：
- 表从创建后一直在使用
- 最近运行：2025-05-31（约7个月前）
- 所有运行状态正常（ok）

### 最终建议

**建议**: **保留** - 核心功能表，绝对不能删除

**理由**：
1. 核心功能表，正在使用中
2. 有6条运行记录，证明功能活跃
3. 删除会导致核心功能完全失效
4. 有完整的关联表体系

**维护建议**：
- 定期清理旧数据（保留最近N个月）
- 监控表大小和性能
- 定期备份运行记录

---

## 🔍 表5: metrics_presence_per_second_device_legacy - 每秒设备指标存在性遗留表

### 完整代码引用清单

#### 直接引用（2处）

**SQL脚本**：
1. `scripts/sql/ts_mpps_migrate.sql` - **迁移脚本（重命名原表为_legacy）**
   - 第1-10行：重命名原表为_legacy
   - 第11-20行：创建新的TimescaleDB hypertable
   - 第21-30行：迁移数据到新表
2. `scripts/sql/migrations/058_add_remaining_comments.sql` - 表注释（第218-257行）

**Python代码**：
- **无** - 未找到任何Python代码引用该表

**文档引用**：
- `.memory/数据层/数据表清单.md` - 标记为"已废弃"（第1010-1028行）
- `.tasks/数据库表使用情况调查报告-20250117.md` - 初步分析

#### 间接引用（0处）

**无** - 该表已被新表完全替代

#### 迁移脚本内容

**文件**: `scripts/sql/ts_mpps_migrate.sql`

**迁移步骤**：
```sql
-- 1. 重命名原表为_legacy
ALTER TABLE metrics_presence_per_second_device
RENAME TO metrics_presence_per_second_device_legacy;

-- 2. 创建新的TimescaleDB hypertable
CREATE TABLE metrics_presence_per_second_device (
    device_id INT NOT NULL,
    ts TIMESTAMPTZ NOT NULL,
    metrics TEXT[] NOT NULL,
    PRIMARY KEY (device_id, ts)
);

-- 3. 转换为hypertable
SELECT create_hypertable(
    'metrics_presence_per_second_device',
    'ts',
    chunk_time_interval => INTERVAL '1 day'
);

-- 4. 迁移数据
INSERT INTO metrics_presence_per_second_device
SELECT * FROM metrics_presence_per_second_device_legacy;

-- 5. 验证数据
SELECT COUNT(*) FROM metrics_presence_per_second_device_legacy;
SELECT COUNT(*) FROM metrics_presence_per_second_device;
```

### 数据流分析

**原始数据流**（已废弃）：
```
❌ metrics_presence_per_second_device_legacy (原表)
    ↓
[已迁移] 数据迁移到新表
    ↓
✅ metrics_presence_per_second_device (新TimescaleDB hypertable)
    ↓
app/services/calculation/missing_metrics_batch.py (使用新表)
```

**当前数据**：
- 表为空（0行）
- 数据已全部迁移到新表

**为什么当前是空的**：
1. ✅ 表是迁移过程中的产物
2. ✅ 数据已全部迁移到新表
3. ✅ 新表使用TimescaleDB hypertable（性能优化）
4. ✅ 迁移完成后，原表保留为_legacy（备份）
5. ❌ 原表已无实际用途

### 功能实现状态

**实现程度**: 0% - 已明确废弃

**证据**：
1. ✅ 表名包含 `_legacy` 后缀（明确标记为遗留表）
2. ✅ 有迁移脚本 `ts_mpps_migrate.sql`
3. ✅ 新表 `metrics_presence_per_second_device` 已创建
4. ✅ 数据已迁移到新表
5. ✅ 文档中标记为"已废弃"
6. ❌ 无任何代码引用该表

**功能状态**: **已明确废弃** - 已被新表完全替代

### 删除影响评估

**如果删除会破坏的功能**：

**直接影响**：
- **无** - 该表已废弃，无任何代码引用

**间接影响**：
- **无** - 数据已迁移到新表

**删除风险**: **无** - 可以安全删除

### Git历史分析

**创建时间**: 原表创建时间未知，重命名为_legacy的时间通过 `ts_mpps_migrate.sql` 可追溯

**历史演进**：
1. 原表创建 - `metrics_presence_per_second_device`
2. 迁移决策 - 决定使用TimescaleDB hypertable优化性能
3. 执行迁移 - 重命名原表为_legacy，创建新表
4. 数据迁移 - 迁移所有数据到新表
5. **保留_legacy表** - 作为备份（但已为空）

**重要发现**：
- 表是迁移过程中的产物
- 数据已全部迁移到新表
- 表名明确标记为_legacy（遗留表）

### 最终建议

**建议**: **立即删除** - 已明确废弃，可以安全删除

**理由**：
1. 表名明确标记为_legacy（遗留表）
2. 数据已全部迁移到新表
3. 无任何代码引用该表
4. 文档中标记为"已废弃"
5. 删除风险为零

**操作步骤**：
```sql
-- 1. 验证新表数据完整性
SELECT COUNT(*) FROM metrics_presence_per_second_device;

-- 2. 删除_legacy表
DROP TABLE IF EXISTS metrics_presence_per_second_device_legacy CASCADE;

-- 3. 验证删除成功
\dt metrics_presence_per_second_device*
```

---

## 🔍 表6: dim_metric_metadata_override - 指标元数据覆盖维度表

### 完整代码引用清单

#### 直接引用（6处）

**SQL脚本**：
1. `scripts/sql/migrations/018_create_dim_metric_metadata_override.sql` - 表创建
2. `scripts/sql/migrations/020_create_v_effective_metric_metadata.sql` - 视图引用
3. `scripts/sql/migrations/057_add_comprehensive_comments.sql` - 表注释
4. `scripts/sql/migrations/058_add_remaining_comments.sql` - 字段注释

**Python代码**：
5. `scripts/test_prepare_dim_backup.py` - 备份脚本（第55行）
6. `app/services/ingest/prepare_dim/__init__.py` - 备份逻辑（第1198行）

#### 间接引用（通过视图）

**视图引用**：
- `v_effective_metric_metadata` - 合并全局元数据和覆盖元数据
  - 被 `app/services/rules/auto_baseline_b.py` 使用
  - 被存储过程 `sp_refresh_metric_rule_auto_baseline_meta` 使用

**视图逻辑**（优先级：设备 > 站点 > 全局）：
```sql
CREATE OR REPLACE VIEW v_effective_metric_metadata AS
SELECT
    COALESCE(o.device_id, 0) AS device_id,
    COALESCE(o.station_id, 0) AS station_id,
    COALESCE(o.metric_id, m.metric_id) AS metric_id,
    COALESCE(o.unit, m.unit) AS unit,
    COALESCE(o.resolution, m.resolution) AS resolution,
    COALESCE(o.physical_min, m.physical_min) AS physical_min,
    COALESCE(o.physical_max, m.physical_max) AS physical_max
FROM dim_metric_metadata m
LEFT JOIN dim_metric_metadata_override o
    ON m.metric_id = o.metric_id
ORDER BY device_id DESC, station_id DESC;
```

### 数据流分析

**设计意图的数据流**：
```
手动配置/数据分析
    ↓
dim_metric_metadata_override (设备/站点级覆盖)
    ↓
v_effective_metric_metadata (视图合并)
    ↓
├─→ sp_refresh_metric_rule_auto_baseline_meta (存储过程)
│   └─→ metric_rule_auto_baseline (基线计算)
└─→ app/services/rules/auto_baseline_b.py (Python服务)
    └─→ metric_rule_auto_baseline_shadow (影子表)
```

**实际数据流**：
```
❌ 无数据流 - 表为空，未配置覆盖
```

**为什么当前是空的**：
1. ✅ 表结构完整（有外键约束）
2. ✅ 有配套视图 `v_effective_metric_metadata`
3. ✅ 视图逻辑正确（优先级：设备 > 站点 > 全局）
4. ❌ **未配置任何覆盖**（表为空）
5. ❌ 全局元数据表 `dim_metric_metadata` 也为空
6. 属于"配置优化"阶段的表，非核心功能必需

**外键约束**：
- `metric_id` → `dim_metric_config(id)`
- `station_id` → `dim_stations(id)`
- `device_id` → `dim_devices(id)`

### 功能实现状态

**实现程度**: 80% - 设计完整，基础设施就绪，数据未填充

**证据**：
1. ✅ 表结构完整（有外键约束）
2. ✅ 有配套视图 `v_effective_metric_metadata`
3. ✅ 存储过程中有使用
4. ✅ Python代码中有引用
5. ✅ 视图逻辑正确（优先级设计）
6. ❌ 表为空

**功能状态**: **设计完整未填充** - 所有基础设施就绪，等待数据填充

### 删除影响评估

**如果删除会破坏的功能**：

**直接影响**：
1. **视图失效** - `v_effective_metric_metadata` 视图会报错
   - 影响文件: `scripts/sql/migrations/020_create_v_effective_metric_metadata.sql`
   - 影响代码: `app/services/rules/auto_baseline_b.py`
   - **风险**: **高** - 视图会报错

2. **存储过程失效** - `sp_refresh_metric_rule_auto_baseline_meta` 会报错
   - 影响文件: `scripts/sql/migrations/022_alter_sp_refresh_metric_rule_auto_baseline_meta.sql`
   - **风险**: **高** - 存储过程会报错

3. **备份流程失败** - 备份脚本会报错
   - 影响文件: `scripts/test_prepare_dim_backup.py`
   - 影响文件: `app/services/ingest/prepare_dim/__init__.py`
   - **风险**: 低 - 可以从备份列表中移除

**间接影响**：
- 无法实现设备/站点级元数据覆盖
- 无法针对特定设备优化质量判定

**删除风险**: **高** - 虽然表为空，但有多处依赖

### Git历史分析

**创建时间**: 通过迁移脚本 `018_create_dim_metric_metadata_override.sql` 创建

**历史演进**：
1. 初始创建 - 定义表结构和外键
2. 创建视图 - `v_effective_metric_metadata` 合并全局和覆盖元数据
3. 添加注释 - 完善文档
4. **从未填充数据** - 表一直为空

**重要发现**：
- 表从创建至今一直为空
- 所有备份记录都显示0行
- 设计完整但从未填充数据

### 最终建议

**建议**: **保留** - 设计完整，未来可能需要

**理由**：
1. 表是完整设计的一部分，有多处依赖
2. 视图逻辑正确，实现了优先级覆盖机制
3. 未来可能需要针对特定设备配置覆盖
4. 删除会破坏视图和存储过程

**使用场景**：
```sql
-- 示例：为设备1的指标1配置特殊的物理边界
INSERT INTO dim_metric_metadata_override (
    device_id, station_id, metric_id,
    physical_min, physical_max
) VALUES (
    1, 0, 1,
    -50.0, 150.0  -- 特殊的温度范围
);
```

---

## 🔍 表7: metric_rule_auto_baseline - 指标规则自动基线表

### 完整代码引用清单

#### 直接引用（18处）

**SQL脚本**：
1. `scripts/sql/migrations/004_create_metric_rule_auto_baseline.sql` - 表创建
2. `scripts/sql/migrations/005_create_sp_refresh_metric_rule_auto_baseline.sql` - 存储过程创建
3. `scripts/sql/migrations/006_create_v_effective_metric_rules.sql` - 视图引用
4. `scripts/sql/migrations/022_alter_sp_refresh_metric_rule_auto_baseline_meta.sql` - 存储过程更新
5. `scripts/sql/migrations/057_add_comprehensive_comments.sql` - 表注释
6. `scripts/sql/migrations/058_add_remaining_comments.sql` - 字段注释
7. `scripts/sql/validation/P1_add_foreign_keys_quality_rules.sql` - 外键添加（第1-72行）
8. `scripts/sql/validation/P1_add_foreign_keys_comprehensive.sql` - 外键验证（第1-15行）
9. `scripts/sql/validation/P1_add_foreign_keys_metric_config.sql` - metric_id外键（第1-155行）

**Python代码（核心使用）**：
10. `app/services/rules/auto_baseline.py` - **主要使用者**
    - 第12-94行：`run_auto_baseline()` - 调用存储过程生成基线
11. `app/services/rules/auto_baseline_b.py` - **影子表实现**
    - 第193-242行：创建和填充 `metric_rule_auto_baseline_shadow` 表
    - 使用STL分解计算基线
12. `scripts/test_prepare_dim_backup.py` - 备份脚本（第56行）
13. `app/services/ingest/prepare_dim/__init__.py` - 备份逻辑（第1199行）

**文档引用**：
14. `.memory/数据层/数据表清单.md` - 详细字段说明（第1029-1046行）
15. `docs/database_dictionary.md` - 数据字典（第1758-1797行）
16. `docs/缺失计算修复/06-技术参考/数据库表结构.md` - 架构说明
17. `docs/缺失计算修复1/04-完整依赖关系深度分析报告.md` - 依赖分析（第120行）
18. `.tasks/数据库表使用情况调查报告-20250117.md` - 初步分析

#### 间接引用（通过视图）

**视图引用**：
- `v_effective_metric_rules` - 合并手动规则和自动基线
  - 被质量判定服务使用
  - 实现优先级：手动规则 > 自动基线

**视图逻辑**：
```sql
CREATE OR REPLACE VIEW v_effective_metric_rules AS
SELECT
    COALESCE(m.device_id, a.device_id) AS device_id,
    COALESCE(m.station_id, a.station_id) AS station_id,
    COALESCE(m.metric_id, a.metric_id) AS metric_id,
    COALESCE(m.lower_bound, a.lower_bound) AS lower_bound,
    COALESCE(m.upper_bound, a.upper_bound) AS upper_bound,
    CASE WHEN m.metric_id IS NOT NULL THEN 'manual' ELSE 'auto' END AS source
FROM metric_quality_rules m
FULL OUTER JOIN metric_rule_auto_baseline a
    ON m.device_id = a.device_id
    AND m.station_id = a.station_id
    AND m.metric_id = a.metric_id;
```

### 数据流分析

**完整数据流**：
```
fact_measurements (历史有效数据)
    ↓
sp_refresh_metric_rule_auto_baseline() (存储过程)
├─→ 使用robust统计方法（MAD, percentiles）
├─→ 按设备/站点/指标分组
└─→ 计算lower_bound, upper_bound
    ↓
metric_rule_auto_baseline (自动基线)
    ↓
v_effective_metric_rules (视图合并)
    ↓
质量判定服务
├─→ 判定数据是否在合理范围内
└─→ 标注quality_codes
```

**Python实现（影子表）**：
```
fact_measurements (历史有效数据)
    ↓
app/services/rules/auto_baseline_b.py
├─→ 使用STL分解（季节性+趋势+残差）
├─→ 计算robust统计量
└─→ 生成基线
    ↓
metric_rule_auto_baseline_shadow (影子表)
    ↓
[未实现] 比对和验证
    ↓
[未实现] 替换主表
```

**为什么当前是空的**：
1. ✅ 表结构完整（有外键约束）
2. ✅ 有存储过程 `sp_refresh_metric_rule_auto_baseline()`
3. ✅ 有Python实现 `app/services/rules/auto_baseline.py`
4. ✅ 有影子表实现 `app/services/rules/auto_baseline_b.py`
5. ❌ **存储过程从未执行**（表为空）
6. ❌ **Python服务从未运行**（表为空）
7. 需要手动触发生成

**外键约束**：
- `metric_id` → `dim_metric_config(id)` (ON UPDATE CASCADE ON DELETE CASCADE)
- `station_id` → `dim_stations(id)` (ON UPDATE CASCADE ON DELETE CASCADE)
- `device_id` → `dim_devices(id)` (ON UPDATE CASCADE ON DELETE CASCADE)

### 功能实现状态

**实现程度**: 95% - 完全实现，未执行

**证据**：
1. ✅ 表结构完整（有外键约束）
2. ✅ 有存储过程实现（SQL）
3. ✅ 有Python实现（两个版本）
4. ✅ 有配套视图 `v_effective_metric_rules`
5. ✅ 有详细文档和注释
6. ❌ 从未执行生成

**功能状态**: **有完整实现未执行** - 所有代码就绪，等待执行

### 删除影响评估

**如果删除会破坏的功能**：

**直接影响**：
1. **视图失效** - `v_effective_metric_rules` 视图会报错
   - 影响文件: `scripts/sql/migrations/006_create_v_effective_metric_rules.sql`
   - **风险**: **极高** - 质量判定服务会失效

2. **存储过程失效** - `sp_refresh_metric_rule_auto_baseline()` 会报错
   - 影响文件: `scripts/sql/migrations/005_create_sp_refresh_metric_rule_auto_baseline.sql`
   - **风险**: **高** - 无法生成自动基线

3. **Python服务失效** - `app/services/rules/auto_baseline.py` 会报错
   - 影响代码: 第12-94行
   - **风险**: **高** - 无法生成自动基线

**间接影响**：
- 质量判定服务只能依赖手动规则
- 无法自动学习合理范围
- 质量判定准确性下降

**删除风险**: **极高** - 核心功能表，删除会导致质量管理体系失效

### Git历史分析

**创建时间**: 通过迁移脚本 `004_create_metric_rule_auto_baseline.sql` 创建

**历史演进**：
1. 初始创建 - 定义表结构（004）
2. 创建存储过程 - SQL实现（005）
3. 创建视图 - 合并手动和自动规则（006）
4. 更新存储过程 - 添加元数据支持（022）
5. 创建Python实现 - `auto_baseline.py`
6. 创建影子表实现 - `auto_baseline_b.py`（STL分解）
7. 添加外键约束 - 数据完整性保护
8. **从未执行生成** - 表一直为空

**重要发现**：
- 表从创建至今一直为空
- 有两套完整实现（SQL + Python）
- 设计完整但从未执行

### 最终建议

**建议**: **立即生成数据** - 核心功能表，应立即启用

**理由**：
1. 核心功能表，质量管理体系的关键组件
2. 有完整的实现（SQL + Python）
3. 有配套视图和存储过程
4. 可以显著提升质量判定准确性

**操作步骤**：

**方法1：使用存储过程（推荐）**
```sql
-- 执行存储过程生成基线
CALL sp_refresh_metric_rule_auto_baseline();

-- 验证数据
SELECT COUNT(*) FROM metric_rule_auto_baseline;
SELECT device_id, station_id, metric_id, lower_bound, upper_bound
FROM metric_rule_auto_baseline
LIMIT 10;
```

**方法2：使用Python服务**
```bash
# 运行Python服务
python -m app.services.rules.auto_baseline

# 验证数据
psql -c "SELECT COUNT(*) FROM metric_rule_auto_baseline;"
```

**方法3：使用影子表实现（验证后替换）**
```bash
# 运行影子表实现
python -m app.services.rules.auto_baseline_b

# 比对结果
psql -c "SELECT COUNT(*) FROM metric_rule_auto_baseline_shadow;"

# 验证通过后替换主表
psql -c "TRUNCATE metric_rule_auto_baseline;"
psql -c "INSERT INTO metric_rule_auto_baseline SELECT * FROM metric_rule_auto_baseline_shadow;"
```

---

## 🔍 表8: metric_calculation_order - 指标计算顺序表

### 完整代码引用清单

#### 直接引用（25+处）

**SQL脚本**：
1. `scripts/sql/calculation/create_tables.sql` - 表创建（第1-15行）
2. `scripts/sql/migrations/057_add_comprehensive_comments.sql` - 表注释
3. `scripts/sql/migrations/058_add_remaining_comments.sql` - 字段注释
4. `scripts/sql/validation/P1_add_foreign_keys_metric_config.sql` - 外键添加（第182-207行）

**Python代码（核心使用）**：
5. `app/services/calculation/dependency_analyzer.py` - **主要使用者**
   - 第568-636行：`load_calculation_order_from_db()` - 从数据库加载计算顺序
   - 第637-753行：`save_calculation_order()` - 保存计算顺序到数据库
   - 第755-796行：`update_calculation_order()` - 手动更新计算顺序
   - 第149-221行：`topological_sort()` - Kahn算法实现拓扑排序
6. `app/services/calculation/missing_metrics_batch.py` - 使用计算顺序
   - 第89-120行：加载计算顺序
   - 第121-150行：按顺序执行计算
7. `scripts/python/manage_calculation_order.py` - **管理脚本**
   - 第26-62行：`view_calculation_order()` - 查看当前顺序
   - 第64-82行：`update_calculation_order()` - 手动更新
8. `scripts/test_prepare_dim_backup.py` - 备份脚本（第57行）
9. `app/services/ingest/prepare_dim/__init__.py` - 备份逻辑（第1200行）

**文档引用**：
10. `.memory/数据层/数据表清单.md` - 详细字段说明（第1047-1064行）
11. `docs/database_dictionary.md` - 数据字典（第1798-1837行）
12. `docs/缺失计算修复/06-技术参考/数据库表结构.md` - 架构说明
13. `docs/缺失计算修复1/04-完整依赖关系深度分析报告.md` - 依赖分析（第121行）
14. `.tasks/数据库表使用情况调查报告-20250117.md` - 初步分析

#### 间接引用（通过依赖关系）

**被依赖的功能**：
- 缺失指标计算服务 - 必须按正确顺序计算
- 数据补全服务 - 依赖计算顺序
- 质量判定服务 - 依赖计算结果

### 数据流分析

**完整数据流**：
```
dim_metric_config (指标配置)
    ↓
app/services/calculation/dependency_analyzer.py
├─→ 分析指标依赖关系（从formula字段）
├─→ 构建依赖图（有向无环图DAG）
├─→ 拓扑排序（Kahn算法）
└─→ 生成计算顺序
    ↓
metric_calculation_order (计算顺序表)
    ↓
app/services/calculation/missing_metrics_batch.py
├─→ 加载计算顺序
├─→ 按顺序执行计算
└─→ 确保依赖关系正确
    ↓
fact_measurements (计算结果)
```

**管理流程**：
```
scripts/python/manage_calculation_order.py
├─→ view_calculation_order() - 查看当前顺序
├─→ update_calculation_order() - 手动更新
└─→ 验证依赖关系
    ↓
metric_calculation_order (更新)
```

**当前数据**（21条记录）：
```sql
SELECT metric_key, calculation_level, depends_on
FROM metric_calculation_order
ORDER BY calculation_level, metric_key;

-- 示例数据：
-- Level 0: 基础指标（无依赖）
-- Level 1: 一级派生指标
-- Level 2: 二级派生指标
-- ...
```

**数据特征**：
- 21条记录（覆盖所有派生指标）
- 按计算层级排序（0-N）
- 记录依赖关系（depends_on字段）
- 定期更新（当指标配置变化时）

### 功能实现状态

**实现程度**: 100% - 完全实现并活跃使用

**证据**：
1. ✅ 表结构完整（有外键约束）
2. ✅ 有21条记录（覆盖所有派生指标）
3. ✅ Python代码中有完整的CRUD操作
4. ✅ 有管理脚本 `manage_calculation_order.py`
5. ✅ 有拓扑排序算法实现（Kahn算法）
6. ✅ 有详细的文档和注释
7. ✅ 被缺失指标计算服务使用

**功能状态**: **核心功能活跃** - 核心功能表，正在使用中

### 删除影响评估

**如果删除会破坏的功能**：

**直接影响**（严重）：
1. **缺失指标计算失败** - 核心功能完全失效
   - 影响文件: `app/services/calculation/missing_metrics_batch.py`
   - 影响代码: 第89-120行（加载计算顺序失败）
   - 影响代码: 第121-150行（按顺序执行失败）
   - **风险**: **极高** - 核心功能完全失效

2. **依赖分析失败** - 无法生成和更新计算顺序
   - 影响文件: `app/services/calculation/dependency_analyzer.py`
   - 影响代码: 第568-636行（加载失败）
   - 影响代码: 第637-753行（保存失败）
   - 影响代码: 第755-796行（更新失败）
   - **风险**: **极高** - 无法管理计算顺序

3. **管理脚本失败** - 无法查看和更新计算顺序
   - 影响文件: `scripts/python/manage_calculation_order.py`
   - 影响代码: 第26-62行（查看失败）
   - 影响代码: 第64-82行（更新失败）
   - **风险**: **高** - 运维工具失效

**间接影响**：
- 派生指标计算顺序错误
- 数据补全失败
- 质量判定不准确
- 系统核心功能失效

**删除风险**: **极高** - 核心功能表，删除会导致系统核心功能失效

### Git历史分析

**创建时间**: 通过 `scripts/sql/calculation/create_tables.sql` 创建

**历史演进**：
1. 初始创建 - 定义表结构
2. 实现依赖分析 - `dependency_analyzer.py`
3. 实现拓扑排序 - Kahn算法
4. 创建管理脚本 - `manage_calculation_order.py`
5. 添加外键约束 - `metric_key` → `dim_metric_config(metric_key)`
6. 添加注释 - 完善文档
7. **持续使用** - 有21条记录，定期更新

**重要发现**：
- 表从创建后一直在使用
- 有21条记录（覆盖所有派生指标）
- 有完整的管理工具和算法实现

### 最终建议

**建议**: **保留** - 核心功能表，绝对不能删除

**理由**：
1. 核心功能表，正在使用中
2. 有21条记录，证明功能活跃
3. 删除会导致核心功能完全失效
4. 有完整的管理工具和算法实现

**维护建议**：
```bash
# 查看当前计算顺序
python scripts/python/manage_calculation_order.py view

# 当指标配置变化时，更新计算顺序
python scripts/python/manage_calculation_order.py update

# 验证依赖关系
python -m app.services.calculation.dependency_analyzer
```

---

## 📊 综合分析和建议

### 表分类总结

#### ✅ 活跃使用的表（2个）- 绝对不能删除

| 表名 | 行数 | 核心功能 | 删除风险 |
|------|------|---------|---------|
| completion_runs | 6 | 数据补全运行追踪 | 极高 |
| metric_calculation_order | 21 | 指标计算顺序管理 | 极高 |

**建议**：
- 定期备份
- 监控表大小和性能
- 定期清理旧数据（completion_runs）

#### ⚠️ 空表但应保留（5个）- 设计完整，未来需要

| 表名 | 实现程度 | 缺失环节 | 优先级 | 建议操作 |
|------|---------|---------|--------|---------|
| metric_anomaly_strategy | 60% | 种子数据未执行 | P0 | 立即执行种子数据脚本 |
| metric_rule_auto_baseline | 95% | 生成脚本未执行 | P0 | 立即生成基线数据 |
| dim_metric_metadata | 80% | 种子数据未执行 | P1 | 执行种子数据脚本 |
| dim_metric_metadata_override | 80% | 未配置覆盖 | P2 | 按需配置 |
| device_metric_candidates | 20% | 功能未实施 | P3 | 6个月后评估 |

**建议**：
- **P0优先级**（立即执行）：
  1. 执行 `029_seed_metric_anomaly_strategy.sql` - 启用异常检测
  2. 运行 `sp_refresh_metric_rule_auto_baseline()` - 生成自动基线
- **P1优先级**（1周内）：
  3. 执行 `021_seed_dim_metric_metadata.sql` - 填充元数据
- **P2优先级**（按需）：
  4. 配置 `dim_metric_metadata_override` - 设备级覆盖
- **P3优先级**（6个月后评估）：
  5. 评估 `device_metric_candidates` - 是否实施或删除

#### ❌ 可以删除的表（1个）- 已明确废弃

| 表名 | 废弃原因 | 删除风险 | 建议操作 |
|------|---------|---------|---------|
| metrics_presence_per_second_device_legacy | 已迁移到新表 | 无 | 立即删除 |

**建议**：
```sql
-- 验证新表数据完整性
SELECT COUNT(*) FROM metrics_presence_per_second_device;

-- 删除_legacy表
DROP TABLE IF EXISTS metrics_presence_per_second_device_legacy CASCADE;
```

### 关键发现

#### 1. 种子数据脚本未执行问题

**问题**：
- `metric_anomaly_strategy` 有68条INSERT的种子数据脚本，但从未执行
- `dim_metric_metadata` 有种子数据脚本，但从未执行

**影响**：
- 异常检测功能未启用
- 元数据管理功能未启用

**解决方案**：
```bash
# 执行种子数据脚本
psql -f scripts/sql/migrations/029_seed_metric_anomaly_strategy.sql
psql -f scripts/sql/migrations/021_seed_dim_metric_metadata.sql

# 验证数据
psql -c "SELECT COUNT(*) FROM metric_anomaly_strategy;"
psql -c "SELECT COUNT(*) FROM dim_metric_metadata;"
```

#### 2. 自动基线未生成问题

**问题**：
- `metric_rule_auto_baseline` 有完整实现（SQL + Python），但从未执行
- 质量判定只能依赖手动规则

**影响**：
- 无法自动学习合理范围
- 质量判定准确性下降

**解决方案**：
```bash
# 方法1：使用存储过程（推荐）
psql -c "CALL sp_refresh_metric_rule_auto_baseline();"

# 方法2：使用Python服务
python -m app.services.rules.auto_baseline

# 验证数据
psql -c "SELECT COUNT(*) FROM metric_rule_auto_baseline;"
```

#### 3. 视图依赖关系

**关键视图**：
1. `v_effective_metric_metadata` - 依赖 `dim_metric_metadata` + `dim_metric_metadata_override`
2. `v_effective_metric_rules` - 依赖 `metric_quality_rules` + `metric_rule_auto_baseline`

**影响**：
- 即使表为空，也不能删除（视图会报错）
- 删除会导致存储过程和Python服务失效

#### 4. 外键约束保护

**已添加外键约束的表**：
- `metric_anomaly_strategy` - 3个外键
- `metric_rule_auto_baseline` - 3个外键
- `dim_metric_metadata` - 1个外键
- `dim_metric_metadata_override` - 3个外键
- `metric_calculation_order` - 1个外键

**保护机制**：
- 无法删除被引用的记录
- 级联删除保护数据完整性

### 操作优先级

#### 立即执行（P0）

```bash
# 1. 执行异常策略种子数据
psql -f scripts/sql/migrations/029_seed_metric_anomaly_strategy.sql

# 2. 生成自动基线
psql -c "CALL sp_refresh_metric_rule_auto_baseline();"

# 3. 删除_legacy表
psql -c "DROP TABLE IF EXISTS metrics_presence_per_second_device_legacy CASCADE;"

# 4. 验证结果
psql -c "SELECT 'metric_anomaly_strategy' AS table_name, COUNT(*) FROM metric_anomaly_strategy
         UNION ALL
         SELECT 'metric_rule_auto_baseline', COUNT(*) FROM metric_rule_auto_baseline;"
```

#### 1周内执行（P1）

```bash
# 执行元数据种子数据
psql -f scripts/sql/migrations/021_seed_dim_metric_metadata.sql

# 验证结果
psql -c "SELECT COUNT(*) FROM dim_metric_metadata;"
```

#### 按需执行（P2）

```sql
-- 配置设备级元数据覆盖（示例）
INSERT INTO dim_metric_metadata_override (
    device_id, station_id, metric_id,
    physical_min, physical_max
) VALUES (
    1, 0, 1,
    -50.0, 150.0
);
```

#### 6个月后评估（P3）

```sql
-- 评估 device_metric_candidates 是否需要实施
-- 如果不需要，执行删除
DROP TABLE IF EXISTS device_metric_candidates CASCADE;
```

### 风险评估

| 操作 | 风险等级 | 影响范围 | 回滚难度 |
|------|---------|---------|---------|
| 执行种子数据脚本 | 低 | 仅填充空表 | 容易（TRUNCATE） |
| 生成自动基线 | 低 | 仅填充空表 | 容易（TRUNCATE） |
| 删除_legacy表 | 无 | 无影响 | 中等（需重新迁移） |
| 删除device_metric_candidates | 低 | 仅影响备份脚本 | 容易（重新创建） |

### 最终建议总结

1. **立即执行**（P0）：
   - ✅ 执行 `029_seed_metric_anomaly_strategy.sql`
   - ✅ 运行 `sp_refresh_metric_rule_auto_baseline()`
   - ✅ 删除 `metrics_presence_per_second_device_legacy`

2. **1周内执行**（P1）：
   - ✅ 执行 `021_seed_dim_metric_metadata.sql`

3. **按需执行**（P2）：
   - ⏳ 配置 `dim_metric_metadata_override`（按需）

4. **6个月后评估**（P3）：
   - ⏳ 评估 `device_metric_candidates`（是否实施或删除）

5. **绝对保留**：
   - ✅ `completion_runs` - 核心功能
   - ✅ `metric_calculation_order` - 核心功能
   - ✅ `dim_metric_metadata` - 视图依赖
   - ✅ `dim_metric_metadata_override` - 视图依赖
   - ✅ `metric_anomaly_strategy` - 外键保护
   - ✅ `metric_rule_auto_baseline` - 视图依赖

---

## 📝 附录：执行脚本

### 一键执行脚本

```bash
#!/bin/bash
# 文件名：execute_table_recommendations.sh

echo "=== 数据库表优化执行脚本 ==="
echo ""

# P0: 立即执行
echo "P0: 执行种子数据和删除废弃表..."
psql -f scripts/sql/migrations/029_seed_metric_anomaly_strategy.sql
psql -c "CALL sp_refresh_metric_rule_auto_baseline();"
psql -c "DROP TABLE IF EXISTS metrics_presence_per_second_device_legacy CASCADE;"

# 验证P0结果
echo ""
echo "P0验证结果："
psql -c "SELECT 'metric_anomaly_strategy' AS table_name, COUNT(*) AS row_count FROM metric_anomaly_strategy
         UNION ALL
         SELECT 'metric_rule_auto_baseline', COUNT(*) FROM metric_rule_auto_baseline;"

# P1: 1周内执行
echo ""
echo "P1: 执行元数据种子数据..."
psql -f scripts/sql/migrations/021_seed_dim_metric_metadata.sql

# 验证P1结果
echo ""
echo "P1验证结果："
psql -c "SELECT COUNT(*) AS row_count FROM dim_metric_metadata;"

echo ""
echo "=== 执行完成 ==="
```

### 验证脚本

```bash
#!/bin/bash
# 文件名：verify_table_status.sh

echo "=== 数据库表状态验证 ==="
echo ""

psql -c "
SELECT
    table_name,
    row_count,
    CASE
        WHEN table_name = 'completion_runs' AND row_count > 0 THEN '✅ 活跃'
        WHEN table_name = 'metric_calculation_order' AND row_count > 0 THEN '✅ 活跃'
        WHEN table_name = 'metric_anomaly_strategy' AND row_count > 0 THEN '✅ 已填充'
        WHEN table_name = 'metric_rule_auto_baseline' AND row_count > 0 THEN '✅ 已生成'
        WHEN table_name = 'dim_metric_metadata' AND row_count > 0 THEN '✅ 已填充'
        WHEN table_name = 'metrics_presence_per_second_device_legacy' THEN '❌ 应删除'
        ELSE '⚠️ 空表'
    END AS status
FROM (
    SELECT 'completion_runs' AS table_name, COUNT(*) AS row_count FROM completion_runs
    UNION ALL
    SELECT 'metric_calculation_order', COUNT(*) FROM metric_calculation_order
    UNION ALL
    SELECT 'metric_anomaly_strategy', COUNT(*) FROM metric_anomaly_strategy
    UNION ALL
    SELECT 'metric_rule_auto_baseline', COUNT(*) FROM metric_rule_auto_baseline
    UNION ALL
    SELECT 'dim_metric_metadata', COUNT(*) FROM dim_metric_metadata
    UNION ALL
    SELECT 'dim_metric_metadata_override', COUNT(*) FROM dim_metric_metadata_override
    UNION ALL
    SELECT 'device_metric_candidates', COUNT(*) FROM device_metric_candidates
    UNION ALL
    SELECT 'metrics_presence_per_second_device_legacy', COUNT(*) FROM metrics_presence_per_second_device_legacy
) AS t
ORDER BY table_name;
"
```

---

**报告结束**


