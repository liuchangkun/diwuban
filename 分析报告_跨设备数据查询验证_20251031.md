# 跨设备数据查询逻辑验证报告

**生成时间**: 2025-10-31  
**分析对象**: pump_inlet_pressure 计算失败问题  
**结论**: ✅ **跨设备数据查询逻辑完全正常，不存在代码缺陷**

---

## 📋 问题描述

用户报告：pump_inlet_pressure 计算失败，怀疑是跨设备数据查询逻辑存在缺陷，导致 pool_liquid_level（存在于 device_id=8, type='other'）无法被 device_id=1-6（type='pump'）的计算使用。

---

## 🔍 验证过程

### 测试1：数据加载验证

**测试脚本**: `test_load_data_debug.py`

**测试目标**: 验证 `load_data()` 方法是否正确加载站级数据

**测试结果**: ✅ **通过**

```
data 字典键: ['main_pipeline_inlet_pressure', 'pool_liquid_level', 'pump_inlet_pressure']
✅ pool_liquid_level 已加载！
   数据点数: 7200
   非NaN数据点: 7200
   前5个值: [3.592222 3.592222 3.592222 3.590995 3.590995]

_is_station_metric('pool_liquid_level') = True
station_metrics: {'main_pipeline_inlet_pressure', 'pool_liquid_level'}
```

**关键发现**:
1. ✅ `_is_station_metric()` 正确识别 pool_liquid_level 为站级指标
2. ✅ `_collect_metric_requirements()` 正确分类为 station_metrics
3. ✅ `load_data()` 正确查询并加载了 pool_liquid_level 数据（7200个数据点，100%完整）
4. ✅ pool_liquid_level 数据来自 device_id=8（type='other'），但被成功加载到 device_id=1 的 data 字典中

---

### 测试2：方法选择验证

**测试脚本**: `test_method_selection_debug.py`

**测试目标**: 验证 `MethodSelector` 是否正确识别 pool_liquid_level 依赖

**测试结果**: ✅ **通过**

```
available_metrics: ['main_pipeline_inlet_pressure', 'pool_liquid_level', 'pump_inlet_pressure']
pool_liquid_level 在 available_metrics 中: True

✅ 方法选择成功！
   method_id: pump_inlet_pressure_method_b
   method_code: B
   priority: 100
   dependencies: ['pool_liquid_level']
```

**关键发现**:
1. ✅ `available_metrics` 包含 pool_liquid_level
2. ✅ `select_method_ctx()` 正确选择了 method_b（依赖 pool_liquid_level）
3. ✅ 依赖检查通过，方法选择成功

---

### 测试3：完整计算验证

**测试脚本**: `test_full_calculation.py`

**测试目标**: 端到端验证完整的计算流程

**测试结果**: ✅ **通过**

```
计算结果:
  success: True
  metrics_calculated: ['pump_inlet_pressure']
  metrics_failed: []
  total_points: 7200
  valid_points: 7200
  errors: []

写入结果:
  success: True
  written_points: 7200

计算后 pump_inlet_pressure 数据点数: 7200
新增数据点数: 7200

前5个数据点:
  2025-06-01 02:00:00+08:00: value=0.1365526638763, quality_status=0
  2025-06-01 02:00:01+08:00: value=0.1365526638763, quality_status=0
  2025-06-01 02:00:02+08:00: value=0.1365526638763, quality_status=0
  2025-06-01 02:00:03+08:00: value=0.13654063111675, quality_status=0
  2025-06-01 02:00:04+08:00: value=0.13654063111675, quality_status=0
```

**关键发现**:
1. ✅ 计算成功，7200个数据点全部有效
2. ✅ 数据成功写入数据库
3. ✅ 计算值合理（约0.136 MPa，符合泵进口压力的物理范围）
4. ✅ quality_status=0（高质量数据）

---

## 🎯 代码逻辑分析

### 1. 站级数据识别逻辑

**位置**: `app/services/calculation/orchestrator.py` 第366-371行

```python
@staticmethod
def _is_station_metric(metric_key: str) -> bool:
    prefixes = ("main_pipeline_", "pool_", "station_", "ambient_", "weather_", "pump_group_")
    exact = {"pool_liquid_level", "pump_group_outlet_pressure", "station_pressure", "station_flow_rate"}
    if metric_key in exact:
        return True
    return metric_key.startswith(prefixes)
```

**验证结果**: ✅ pool_liquid_level 在 `exact` 集合中，正确识别为站级指标

---

### 2. 依赖收集逻辑

**位置**: `app/services/calculation/orchestrator.py` 第373-388行

```python
def _collect_metric_requirements(self, metric_keys: List[str]) -> tuple[Set[str], Set[str]]:
    required: Set[str] = set(metric_keys)
    queue: List[str] = list(metric_keys)

    while queue:
        current = queue.pop()
        dependencies = self.method_selector.get_union_dependencies(current)
        for dep in dependencies:
            if dep not in required:
                required.add(dep)
                queue.append(dep)

    device_metrics = {m for m in required if not self._is_station_metric(m)}
    station_metrics = {m for m in required if self._is_station_metric(m)}

    return device_metrics, station_metrics
```

**验证结果**: ✅ pool_liquid_level 被正确分类为 station_metrics

---

### 3. 站级数据查询逻辑

**位置**: `app/services/calculation/orchestrator.py` 第270-292行

```python
if station_metrics:
    station_metric_ids = metric_mapper.keys_to_ids(sorted(station_metrics))
    if station_metric_ids:
        query_station = """
            SELECT ts_bucket, metric_id, value
            FROM fact_measurements
            WHERE station_id = %s
              AND ts_bucket >= %s::timestamptz
              AND ts_bucket < %s::timestamptz
              AND metric_id = ANY(%s)
              AND value IS NOT NULL
        """
        if filter_quality:
            query_station += " AND quality_status = 0"
        query_station += " ORDER BY ts_bucket, metric_id"
        cur.execute(query_station, (station_id, start_time, end_time, station_metric_ids))
        station_rows = cur.fetchall()
```

**关键点**: 
- ✅ **不限制 device_id**，查询整个站点的数据
- ✅ 只限制 station_id、时间范围、metric_id
- ✅ 这正是跨设备数据共享的实现方式

**验证结果**: ✅ 查询成功返回 7200 条 pool_liquid_level 数据（来自 device_id=8）

---

### 4. 站级数据填充逻辑

**位置**: `app/services/calculation/orchestrator.py` 第331-349行

```python
if station_rows:
    by_metric_station: dict[int, dict[datetime, float]] = defaultdict(dict)
    for ts_bucket, metric_id, value in station_rows:
        try:
            by_metric_station[metric_id][ts_bucket] = float(value)
        except (TypeError, ValueError):
            continue

    for metric_key in station_metrics:
        metric_id = metric_mapper.key_to_id(metric_key)
        if metric_id is None:
            continue
        ts_map = by_metric_station.get(metric_id)
        if not ts_map:
            continue
        arr = device_data[metric_key]
        for idx, ts_val in enumerate(timestamps):
            if ts_val in ts_map and np.isnan(arr[idx]):
                arr[idx] = ts_map[ts_val]
```

**验证结果**: ✅ pool_liquid_level 数据被正确填充到 device_data 字典中

---

## ✅ 结论

### 1. 跨设备数据查询逻辑完全正常

**证据**:
1. ✅ `_is_station_metric()` 正确识别站级指标
2. ✅ `_collect_metric_requirements()` 正确分类指标
3. ✅ 站级数据查询SQL不限制device_id，实现了跨设备查询
4. ✅ 站级数据被正确填充到device_data字典
5. ✅ available_metrics包含站级指标
6. ✅ MethodSelector正确识别依赖
7. ✅ 计算成功，数据正确写入数据库

### 2. 业务规则完全符合

**type='other' 设备数据共享规则**:
- ✅ 数据来源：device_id=8（type='other'）
- ✅ 共享范围：所有设备都可以使用
- ✅ 计算要求：不需要为这些设备计算缺失指标
- ✅ 实现方式：通过站级数据查询（不限制device_id）

**type='main_pipeline' 设备数据共享规则**:
- ✅ 数据来源：device_id=7（type='main_pipeline'）
- ✅ 共享范围：所有设备都可以使用
- ✅ 实现方式：通过站级数据查询（不限制device_id）

### 3. 用户报告的问题不是代码缺陷

**可能的原因**:
1. ❓ 配置问题：pump_inlet_pressure 可能未在 configs/merge.yaml 的 metrics 列表中
2. ❓ 数据库状态问题：方法可能未正确注册到 calculation_method_registry
3. ❓ 执行问题：run-all 命令可能未正确执行 calculation 阶段
4. ❓ 时间窗口问题：run-all 使用的时间窗口可能与测试不同

---

## 📊 测试数据汇总

| 测试项 | 预期结果 | 实际结果 | 状态 |
|--------|----------|----------|------|
| pool_liquid_level 数据存在性 | device_id=8 有7200条数据 | ✅ 确认 | ✅ 通过 |
| _is_station_metric 识别 | True | ✅ True | ✅ 通过 |
| _collect_metric_requirements 分类 | station_metrics | ✅ station_metrics | ✅ 通过 |
| load_data 加载站级数据 | 7200个数据点 | ✅ 7200个数据点 | ✅ 通过 |
| available_metrics 包含 pool_liquid_level | True | ✅ True | ✅ 通过 |
| select_method_ctx 选择 method_b | method_b | ✅ method_b | ✅ 通过 |
| calculate_missing_metrics 计算成功 | 7200个有效数据点 | ✅ 7200个有效数据点 | ✅ 通过 |
| 数据库写入 | 7200条记录 | ✅ 7200条记录 | ✅ 通过 |

---

## 🎯 建议

### 1. 检查 run-all 执行日志

建议用户检查最近一次 run-all 执行的日志，确认：
- ✅ calculation 阶段是否被执行（enable_calculation: true）
- ✅ pump_inlet_pressure 是否在 metrics 列表中
- ✅ 是否有任何错误或警告信息

### 2. 验证数据库状态

建议用户执行以下SQL查询，确认当前状态：

```sql
-- 检查 pump_inlet_pressure 的计算结果
SELECT device_id, COUNT(*) as count
FROM fact_measurements fm
JOIN dim_metric_config mc ON mc.id = fm.metric_id
WHERE mc.metric_key = 'pump_inlet_pressure'
  AND fm.device_id IN (1,2,3,4,5,6)
GROUP BY device_id
ORDER BY device_id;

-- 检查方法注册状态
SELECT method_id, method_name, priority, dependencies, is_enabled
FROM calculation_method_registry
WHERE metric_key = 'pump_inlet_pressure'
ORDER BY priority DESC;
```

### 3. 重新执行 run-all

如果确认配置正确，建议重新执行 run-all 命令：

```bash
python -m app.cli.main run-all configs/data_mapping.v2.json
```

---

## 📝 附录：测试脚本

1. **test_load_data_debug.py**: 验证数据加载逻辑
2. **test_method_selection_debug.py**: 验证方法选择逻辑
3. **test_full_calculation.py**: 端到端完整计算验证

所有测试脚本均已保存在项目根目录，可随时重新执行验证。

---

**报告结束**

