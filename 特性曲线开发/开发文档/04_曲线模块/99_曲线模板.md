# 特性曲线拟合系统 - 曲线模块模板

**版本**: v1.0
**创建日期**: 2025-12-03
**最后更新**: 2025-12-04
**状态**: 模板文档

---

## 📋 使用说明

> **本文档是新增曲线模块的标准模板**。新增曲线时，复制此文件并按照以下结构填写。

---

## 1. 曲线概述

### 1.1 基本信息

| 属性 | 值 |
|------|-----|
| **曲线类型** | `[曲线类型代码]` |
| **物理意义** | [物理意义描述] |
| **单调性** | [递增/递减/单峰] |
| **推荐方法** | `[推荐方法ID]` |
| **文件路径** | `app/services/characteristic_curves/curves/[xxx]_curve.py` |

### 1.2 物理特性

| 特性 | 描述 |
|------|------|
| **单调性** | [单调性描述] |
| **凸性** | [凸性描述] |
| **零流量点** | [零流量点特性] |
| **最大流量点** | [最大流量点特性] |

### 1.3 数学模型

**模型公式**：
```
[数学公式]
```

**约束条件**：
- [约束1]
- [约束2]

---

## 2. 子模块设计

### 2.1 模块结构

```
[XXX]Curve
├── [XXX]DataExtractor   # 数据提取器
├── [XXX]Preprocessor    # 预处理器
├── [XXX]Constraints     # 物理约束
├── [XXX]Normalizer      # 归一化器
├── [XXX]MethodSelector  # 方法选择器
├── [XXX]CurveFitter     # 拟合器
└── [XXX]Validator       # 验证器
```

### 2.2 DataExtractor 数据提取器

| 属性 | 说明 |
|------|------|
| **职责** | 从 fact_measurements 提取数据 |
| **必需指标** | `[指标1]`, `[指标2]` |

```python
class [XXX]DataExtractor:
    REQUIRED_METRICS = ['[指标1]', '[指标2]']

    def extract(
        self,
        device_id: int,
        start_time: datetime,
        end_time: datetime,
        min_points: int = 100
    ) -> pd.DataFrame:
        """提取数据，返回 DataFrame[ts_bucket, X, Y]"""
        pass
```

### 2.3 Constraints 物理约束

| 约束类型 | 条件 | 说明 |
|---------|------|------|
| **[约束1]** | [条件] | [说明] |
| **[约束2]** | [条件] | [说明] |

### 2.4 Validator 验证器

| 验证项 | 标准 | 说明 |
|--------|------|------|
| **精度** | R² > [阈值] | 拟合优度 |
| **误差** | RMSE < [阈值] | 均方根误差 |
| **[验证项]** | [标准] | [说明] |

---

## 3. 数据流详细

### 3.1 阶段流程

> **注意**：以下为简化的阶段描述。完整的13阶段定义请参见 [02_数据流定义.md](../02_架构设计/02_数据流定义.md)

```
阶段1: 数据提取
├── 输入: device_id, fit_window
├── 数据源: fact_measurements
└── 输出: DataFrame[ts_bucket, X, Y]
    ↓
阶段2-5: [中间处理阶段]（场景识别、预处理、稳态识别等）
    ↓
阶段6: 拟合
├── 输入: 标准化数据, 约束参数
├── 方法: [推荐方法]
└── 输出: FitResult
    ↓
阶段7: 验证
└── 输出: ValidationResult
```

---

## 4. 使用示例

```python
from app.services.characteristic_curves.curves import [XXX]Curve

# 创建曲线实例
curve = [XXX]Curve(...)

# 执行拟合
result = curve.fit(device_id=12345, ...)
```

---

**相关文档**：
- [00_曲线基类.md](./00_曲线基类.md) - 曲线基类设计

---

**文档结束**

