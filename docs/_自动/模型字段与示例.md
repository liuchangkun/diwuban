# 模型字段与示例（自动生成｜含注释）

> 说明：解析 app/schemas 下的 Pydantic BaseModel，列出字段类型、默认值与描述；并生成示例 JSON。


## 模块：app\schemas\__init__.py

## 模块：app\schemas\base.py

## 模块：app\schemas\curve.py

## 模块：app\schemas\data.py
### 模型：TimeSeriesResponse
> 摘要：统一的时间序列响应模型 - data: 列表，元素为字典（前端按扁平结构使用）
- 字段：
  - `data` <List[Dict[str, Any]]>：数据列表
  - `metadata` <Optional[Dict[str, Any]]>：元数据
  - `total_count` <int>：总数
  - `query_time_ms` <float>：查询耗时（毫秒） —— 作用（推断）：时间戳/时间字段
  - `has_more` <bool>：是否还有更多 —— 作用（推断）：布尔标志
- 示例：
  ```json
  {"data": [], "metadata": {}, "total_count": 0, "query_time_ms": 0.0, "has_more": false}
  ```


## 模块：app\schemas\device.py

## 模块：app\schemas\optimization.py

## 模块：app\schemas\station.py

---

> 生成器：generate_project_wiki.py｜时间：2025-09-25 08:36
