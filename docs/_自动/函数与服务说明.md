# 函数与服务说明（自动生成｜含注释）

> 说明：扫描 app/services 下的函数与类方法，展示签名、docstring 摘要、参数、返回、日志、数据库、外部交互（HTTP/FS/子进程/环境变量/其他SDK）。


<a id="svc.app.services.admin.clear_db.py"></a>
## 模块：app\services\admin\clear_db.py
<a id="svc.app.services.admin.clear_db.py.truncate_public_schema"></a>
### 函数：truncate_public_schema()
> 摘要：清空 public 架构下所有表数据（TRUNCATE + RESTART IDENTITY + CASCADE）。
> 功能说明（推断）：数据库清空/维护工具（高风险操作）
- 参数：
  - settings <Settings>
- 返回：<None>
- 日志输出：无
- 数据库操作：有（commit、cursor、execute）
- HTTP 调用：无
- 文件系统：无
- 子进程：无
- 环境变量：无
- 其他SDK：无


<a id="svc.app.services.admin.db_ping.py"></a>
## 模块：app\services\admin\db_ping.py
<a id="svc.app.services.admin.db_ping.py.run_db_ping"></a>
### 函数：run_db_ping()
> 摘要：（未提供 docstring）
> 功能说明（推断）：执行一项端到端/批处理任务
- 参数：
  - settings
  - verbose <bool>
- 返回：<Dict[str, Any]>
- 日志输出：无
- 数据库操作：有（cursor、execute、fetchone）
- HTTP 调用：无
- 文件系统：无
- 子进程：无
- 环境变量：无
- 其他SDK：无


<a id="svc.app.services.base.py"></a>
## 模块：app\services\base.py

<a id="svc.app.services.curve_fitting.py"></a>
## 模块：app\services\curve_fitting.py

<a id="svc.app.services.data_completion.py"></a>
## 模块：app\services\data_completion.py

<a id="svc.app.services.data_import.py"></a>
## 模块：app\services\data_import.py

<a id="svc.app.services.device_running_job.py"></a>
## 模块：app\services\device_running_job.py
<a id="svc.app.services.device_running_job.py.json_dumps"></a>
### 函数：json_dumps()
> 摘要：（未提供 docstring）
> 功能说明（推断）：设备运行/停机区段计算与落库
- 参数：
  - obj <Any>
- 返回：<str>
- 日志输出：无
- 数据库操作：无
- HTTP 调用：无
- 文件系统：无
- 子进程：无
- 环境变量：无
- 其他SDK：无

<a id="svc.app.services.device_running_job.py.main"></a>
### 函数：main()
> 摘要：（未提供 docstring）
> 功能说明（推断）：设备运行/停机区段计算与落库
- 参数：
  - argv <Optional[List[str]]>
- 返回：<None>
- 日志输出：无
- 数据库操作：无
- HTTP 调用：无
- 文件系统：无
- 子进程：无
- 环境变量：无
- 其他SDK：无

<a id="svc.app.services.device_running_job.py.DeviceRunningJob"></a>
### 类：DeviceRunningJob
> 摘要：设备运行状态计算与落库作业。
<a id="svc.app.services.device_running_job.py.DeviceRunningJob.resolve_devices"></a>
#### 方法：resolve_devices()
> 摘要：（未提供 docstring）
> 功能说明（推断）：设备运行/停机区段计算与落库
- 返回：<List[int]>
- 日志输出：无
- 数据库操作：有（cursor）
- HTTP 调用：无
- 文件系统：无
- 子进程：无
- 环境变量：无
- 其他SDK：无

<a id="svc.app.services.device_running_job.py.DeviceRunningJob.resolve_window"></a>
#### 方法：resolve_window()
> 摘要：（未提供 docstring）
> 功能说明（推断）：设备运行/停机区段计算与落库
- 参数：
  - device_id <int>
  - start_ts <Optional[datetime]>
  - end_ts <Optional[datetime]>
- 返回：<Optional[Tuple[datetime, datetime]]>
- 日志输出：无
- 数据库操作：有（cursor）
- HTTP 调用：无
- 文件系统：无
- 子进程：无
- 环境变量：无
- 其他SDK：无

<a id="svc.app.services.device_running_job.py.DeviceRunningJob.run_for_device"></a>
#### 方法：run_for_device()
> 摘要：（未提供 docstring）
> 功能说明（推断）：设备运行/停机区段计算与落库
- 参数：
  - device_id <int>
  - start_ts <Optional[datetime]>
  - end_ts <Optional[datetime]>
- 返回：<None>
- 日志输出：无
- 数据库操作：有（cursor、execute、commit、fetchone）
- HTTP 调用：无
- 文件系统：无
- 子进程：无
- 环境变量：无
- 其他SDK：无

<a id="svc.app.services.device_running_job.py.DeviceRunningJob.run"></a>
#### 方法：run()
> 摘要：（未提供 docstring）
> 功能说明（推断）：设备运行/停机区段计算与落库
- 参数：
  - device_ids <Optional[List[int]]>
  - start_ts <Optional[datetime]>
  - end_ts <Optional[datetime]>
- 返回：<None>
- 日志输出：无
- 数据库操作：无
- HTTP 调用：无
- 文件系统：无
- 子进程：无
- 环境变量：无
- 其他SDK：无


<a id="svc.app.services.ingest.backpressure.py"></a>
## 模块：app\services\ingest\backpressure.py
<a id="svc.app.services.ingest.backpressure.py.BackpressureController"></a>
### 类：BackpressureController
<a id="svc.app.services.ingest.backpressure.py.BackpressureController.decide"></a>
#### 方法：decide()
> 摘要：根据 p95 与失败率决定是否收缩批大小/并发，或恢复。
- 参数：
  - p95_ms <int>
  - fail_rate <float>
- 返回：<dict>
- 日志输出：无
- 数据库操作：无
- HTTP 调用：无
- 文件系统：无
- 子进程：无
- 环境变量：无
- 其他SDK：无


<a id="svc.app.services.ingest.check_mapping.py"></a>
## 模块：app\services\ingest\check_mapping.py
<a id="svc.app.services.ingest.check_mapping.py.check_mapping_paths"></a>
### 函数：check_mapping_paths()
> 摘要：只读一致性检查：
- 参数：
  - settings <Settings>
  - mapping_path <Path>
- 返回：<Dict[str, Any]>
- 日志输出：无
- 数据库操作：无
- HTTP 调用：有（session.get）
- 文件系统：有（read_text、exists、replace；路径：\）
- 子进程：无
- 环境变量：无
- 其他SDK：无


<a id="svc.app.services.ingest.check_mapping_cli.py"></a>
## 模块：app\services\ingest\check_mapping_cli.py
<a id="svc.app.services.ingest.check_mapping_cli.py.run_check_mapping_command"></a>
### 函数：run_check_mapping_command()
> 摘要：（未提供 docstring）
> 功能说明（推断）：执行一项端到端/批处理任务
- 参数：
  - settings
  - mapping_path <Path>
  - out <Path | None>
  - show_all <bool>
- 返回：<Dict[str, Any]>
- 日志输出：无
- 数据库操作：无
- HTTP 调用：无
- 文件系统：有（mkdir、write_text）
- 子进程：无
- 环境变量：无
- 其他SDK：无


<a id="svc.app.services.ingest.copy_workers.py"></a>
## 模块：app\services\ingest\copy_workers.py
<a id="svc.app.services.ingest.copy_workers.py.copy_from_mapping"></a>
### 函数：copy_from_mapping()
> 摘要：从 mapping.json 并发导入 CSV 到 staging。
> 功能说明（推断）：从映射并行导入 CSV 到 staging
- 参数：
  - settings <Settings>
  - mapping_path <Path>
  - run_id <str | None>
- 返回：<CopyStats>
- 日志输出：无
- 数据库操作：无
- HTTP 调用：有（session.get）
- 文件系统：有（exists）
- 子进程：无
- 环境变量：无
- 其他SDK：无


<a id="svc.app.services.ingest.create_staging.py"></a>
## 模块：app\services\ingest\create_staging.py
<a id="svc.app.services.ingest.create_staging.py.create_staging"></a>
### 函数：create_staging()
> 摘要：创建 staging_raw/staging_rejects（若不存在）。幂等。
> 功能说明（推断）：创建/刷新装载中间表（幂等）
- 参数：
  - settings <Settings>
- 返回：<None>
- 日志输出：无
- 数据库操作：无
- HTTP 调用：无
- 文件系统：无
- 子进程：无
- 环境变量：无
- 其他SDK：无


<a id="svc.app.services.ingest.merge_cli.py"></a>
## 模块：app\services\ingest\merge_cli.py
<a id="svc.app.services.ingest.merge_cli.py.run_merge_fact_command"></a>
### 函数：run_merge_fact_command()
> 摘要：（未提供 docstring）
> 功能说明（推断）：窗口内去重/对齐并合并入事实表
- 参数：
  - settings
  - window_start <str>
  - window_end <str>
- 返回：<Dict[str, Any]>
- 日志输出：无
- 数据库操作：无
- HTTP 调用：无
- 文件系统：有（replace；路径：Z）
- 子进程：无
- 环境变量：无
- 其他SDK：无


<a id="svc.app.services.ingest.merge_service.py"></a>
## 模块：app\services\ingest\merge_service.py
<a id="svc.app.services.ingest.merge_service.py.merge_window"></a>
### 函数：merge_window()
> 摘要：执行一个 UTC 窗口的集合式合并。
> 功能说明（推断）：窗口内去重/对齐并合并入事实表
- 参数：
  - settings <Settings>
  - window_start_utc <datetime>
  - window_end_utc <datetime>
  - device_id <int | None>
- 返回：<MergeStats>
- 日志输出：无
- 数据库操作：无
- HTTP 调用：有（session.get）
- 文件系统：有（replace）
- 子进程：无
- 环境变量：无
- 其他SDK：无


<a id="svc.app.services.ingest.run_all.py"></a>
## 模块：app\services\ingest\run_all.py
<a id="svc.app.services.ingest.run_all.py.run_all"></a>
### 函数：run_all()
> 摘要：执行端到端导入与合并（无任何日志副作用）。
> 功能说明（推断）：执行一项端到端/批处理任务
- 参数：
  - settings <"Settings">
  - mapping <Path>
  - window_start_utc <datetime>
  - window_end_utc <datetime>
  - run_dir <Path | None>
  - use_staging_time_range <bool>
- 返回：<None>
- 日志输出：无
- 数据库操作：无
- HTTP 调用：无
- 文件系统：无
- 子进程：无
- 环境变量：无
- 其他SDK：无


<a id="svc.app.services.ingest.source_hint.py"></a>
## 模块：app\services\ingest\source_hint.py
<a id="svc.app.services.ingest.source_hint.py.make_source_hint"></a>
### 函数：make_source_hint()
> 摘要：生成 source_hint v2：data/<rel>|batch=<run_id>|ver=2。
- 参数：
  - settings <Settings>
  - base_dir <Path>
  - csv_path <Path>
- 返回：<str>
- 日志输出：无
- 数据库操作：无
- HTTP 调用：无
- 文件系统：有（replace；路径：\）
- 子进程：无
- 环境变量：无
- 其他SDK：无


<a id="svc.app.services.optimization.py"></a>
## 模块：app\services\optimization.py

<a id="svc.app.services.pump_group_curve_fitting.py"></a>
## 模块：app\services\pump_group_curve_fitting.py

<a id="svc.app.services.quality.full_pass.py"></a>
## 模块：app\services\quality\full_pass.py
<a id="svc.app.services.quality.full_pass.py.run_quality_full_pass"></a>
### 函数：run_quality_full_pass()
> 摘要：（未提供 docstring）
> 功能说明（推断）：执行一项端到端/批处理任务
- 参数：
  - settings
  - start <str>
  - end <str>
  - lookback_days <int>
  - station_id <Optional[int]>
  - device_id <Optional[int]>
- 返回：<Dict[str, Any]>
- 日志输出：无
- 数据库操作：无
- HTTP 调用：无
- 文件系统：无
- 子进程：无
- 环境变量：无
- 其他SDK：无


<a id="svc.app.services.quality.mark_window.py"></a>
## 模块：app\services\quality\mark_window.py
<a id="svc.app.services.quality.mark_window.py.mark_quality_window"></a>
### 函数：mark_quality_window()
> 摘要：质量打标（窗口级）。
> 功能说明（推断）：对窗口数据进行质量打标，产出命中与样例
- 参数：
  - settings
  - start <str>
  - end <str>
  - station_id <Optional[int]>
  - device_id <Optional[int]>
  - codes <Optional[list[int]]>
  - diag_level <str | None>
  - run_id <str | None>
- 返回：<Dict[str, Any]>
- 日志输出：有（getLogger）
- 数据库操作：有（cursor、execute、commit、rollback、fetchall、fetchone；表：public.quality_profile_log、public.quality_diagnosis_log）
- HTTP 调用：无
- 文件系统：有（replace；路径：Z）
- 子进程：无
- 环境变量：无
- 其他SDK：无


<a id="svc.app.services.reporting.code_dist.py"></a>
## 模块：app\services\reporting\code_dist.py
<a id="svc.app.services.reporting.code_dist.py.export_window_code_distribution"></a>
### 函数：export_window_code_distribution()
> 摘要：按窗口导出质量码分布（JSON + CSV）。
> 功能说明（推断）：质量码分布报表导出（JSON/CSV）
- 参数：
  - settings <Settings>
  - start_iso <str>
  - end_iso <str>
  - out_dir <Path | None>
- 返回：<Dict[str, Any]>
- 日志输出：无
- 数据库操作：有（cursor、execute、fetchall；表：public.fact_measurements、public.quality_code_dict）
- HTTP 调用：无
- 文件系统：有（mkdir、write_text、open、replace；路径：w、Z）
- 子进程：无
- 环境变量：无
- 其他SDK：无

<a id="svc.app.services.reporting.code_dist.py.export_recent_code_distribution"></a>
### 函数：export_recent_code_distribution()
> 摘要：基于 quality_profile_log 的近 24h / 7d 质量码命中分布（JSON）。
> 功能说明（推断）：质量码分布报表导出（JSON/CSV）
- 参数：
  - settings <Settings>
  - hours_24 <bool>
  - days_7 <bool>
  - out_dir <Path | None>
- 返回：<Dict[str, Any]>
- 日志输出：无
- 数据库操作：有（cursor、execute、fetchall；表：public.quality_profile_log）
- HTTP 调用：无
- 文件系统：有（mkdir、write_text）
- 子进程：无
- 环境变量：无
- 其他SDK：无


<a id="svc.app.services.reporting.data_quality.py"></a>
## 模块：app\services\reporting\data_quality.py
<a id="svc.app.services.reporting.data_quality.py.generate_report"></a>
### 函数：generate_report()
> 摘要：生成数据质量报表。
> 功能说明（推断）：数据质量报表生成
- 参数：
  - settings <Settings>
  - start_utc <datetime>
  - end_utc <datetime>
  - run_dir <Path | None>
  - expected_interval_seconds <int>
  - top_k <int>
  - group_by <str>
- 返回：<Dict[str, Any]>
- 日志输出：无
- 数据库操作：有（cursor、execute、fetchall）
- HTTP 调用：有（session.get）
- 文件系统：有（exists、mkdir、write_text、open、read_text；路径：r）
- 子进程：无
- 环境变量：无
- 其他SDK：无


<a id="svc.app.services.reporting.data_report_cli.py"></a>
## 模块：app\services\reporting\data_report_cli.py
<a id="svc.app.services.reporting.data_report_cli.py.run_data_report_command"></a>
### 函数：run_data_report_command()
> 摘要：（未提供 docstring）
> 功能说明（推断）：导出统计/质量/覆盖率等报表
- 参数：
  - settings
  - window_start <str>
  - window_end <str>
  - expected_interval <int>
  - top_k <int>
  - group_by <str>
  - run_dir <Optional[str]>
- 返回：<Dict[str, Any]>
- 日志输出：无
- 数据库操作：无
- HTTP 调用：无
- 文件系统：有（replace；路径：Z）
- 子进程：无
- 环境变量：无
- 其他SDK：无


<a id="svc.app.services.reporting.presence_cli.py"></a>
## 模块：app\services\reporting\presence_cli.py
<a id="svc.app.services.reporting.presence_cli.py.run_presence_compute_command"></a>
### 函数：run_presence_compute_command()
> 摘要：（未提供 docstring）
> 功能说明（推断）：presence 计算/写入（分片+并发）
- 参数：
  - settings
  - station_name <Optional[str]>
  - station_id <Optional[int]>
  - device_id <Optional[int]>
  - start <Optional[str]>
  - end <Optional[str]>
  - batch_days <int>
  - rolling_days <int>
  - dry_run <bool>
- 返回：<Dict[str, Any]>
- 日志输出：无
- 数据库操作：无
- HTTP 调用：无
- 文件系统：无
- 子进程：无
- 环境变量：无
- 其他SDK：无


<a id="svc.app.services.reporting.presence_writer.py"></a>
## 模块：app\services\reporting\presence_writer.py
<a id="svc.app.services.reporting.presence_writer.py.iter_day_slices"></a>
### 函数：iter_day_slices()
> 摘要：（未提供 docstring）
> 功能说明（推断）：presence 计算/写入（分片+并发）
- 参数：
  - start <datetime>
  - end <datetime>
  - days <int>
- 返回：<Iterable[tuple[datetime, datetime]]>
- 日志输出：无
- 数据库操作：无
- HTTP 调用：无
- 文件系统：无
- 子进程：无
- 环境变量：无
- 其他SDK：无

<a id="svc.app.services.reporting.presence_writer.py.iter_minute_slices"></a>
### 函数：iter_minute_slices()
> 摘要：（未提供 docstring）
> 功能说明（推断）：presence 计算/写入（分片+并发）
- 参数：
  - start <datetime>
  - end <datetime>
  - minutes <int>
- 返回：<Iterable[tuple[datetime, datetime]]>
- 日志输出：无
- 数据库操作：无
- HTTP 调用：无
- 文件系统：无
- 子进程：无
- 环境变量：无
- 其他SDK：无

<a id="svc.app.services.reporting.presence_writer.py.detect_full_or_incremental"></a>
### 函数：detect_full_or_incremental()
> 摘要：返回 [start,end) UTC 窗口。
> 功能说明（推断）：presence 计算/写入（分片+并发）
- 参数：
  - conn <psycopg.Connection>
- 返回：<tuple[datetime, datetime]>
- 日志输出：无
- 数据库操作：有（cursor、execute、fetchone；表：public.fact_measurements、public.metrics_presence_per_second_device）
- HTTP 调用：无
- 文件系统：有（replace）
- 子进程：无
- 环境变量：无
- 其他SDK：无

<a id="svc.app.services.reporting.presence_writer.py.ensure_partitions"></a>
### 函数：ensure_partitions()
> 摘要：确保指定时间戳对应的分区存在；若已存在则不重复创建。
> 功能说明（推断）：presence 计算/写入（分片+并发）
- 参数：
  - conn <psycopg.Connection>
  - ts <datetime>
- 返回：<None>
- 日志输出：无
- 数据库操作：有（cursor、execute、fetchone；表：timescaledb_information.hypertables）
- HTTP 调用：无
- 文件系统：无
- 子进程：无
- 环境变量：无
- 其他SDK：无

<a id="svc.app.services.reporting.presence_writer.py.list_station_devices"></a>
### 函数：list_station_devices()
> 摘要：获取站点下的设备名称列表（不依赖 active 字段，保持兼容）。
> 功能说明（推断）：presence 计算/写入（分片+并发）
- 参数：
  - conn <psycopg.Connection>
  - station_name <str>
- 返回：<list[str]>
- 日志输出：无
- 数据库操作：有（cursor、execute、fetchall；表：public.dim_devices、public.dim_stations）
- HTTP 调用：无
- 文件系统：无
- 子进程：无
- 环境变量：无
- 其他SDK：无

<a id="svc.app.services.reporting.presence_writer.py.upsert_window"></a>
### 函数：upsert_window()
> 摘要：执行一个窗口 UPSERT，返回影响行数。
> 功能说明（推断）：presence 计算/写入（分片+并发）
- 参数：
  - conn <psycopg.Connection>
  - start <datetime>
  - end <datetime>
  - station_id <Optional[int]>
  - device_id <Optional[int]>
- 返回：<int>
- 日志输出：无
- 数据库操作：有（cursor、execute）
- HTTP 调用：无
- 文件系统：有（read_text）
- 子进程：无
- 环境变量：无
- 其他SDK：无

<a id="svc.app.services.reporting.presence_writer.py.run_presence_compute"></a>
### 函数：run_presence_compute()
> 摘要：对 presence 表进行批量 UPSERT（自适应切片 + 受控并发）。
> 功能说明（推断）：presence 计算/写入（分片+并发）
- 参数：
  - station_name <Optional[str]>
  - station_id <Optional[int]>
  - device_id <Optional[int]>
  - start <Optional[datetime]>
  - end <Optional[datetime]>
  - batch_days <int>
  - rolling_days <int>
  - minutes_per_batch <int>
  - device_concurrency <int>
- 返回：<dict>
- 日志输出：无
- 数据库操作：有（cursor、execute、fetchone、commit、fetchall；表：public.dim_devices、public.dim_stations）
- HTTP 调用：有（session.get）
- 文件系统：有（replace）
- 子进程：无
- 环境变量：无
- 其他SDK：无


<a id="svc.app.services.reporting.rules_diff_report.py"></a>
## 模块：app\services\reporting\rules_diff_report.py
<a id="svc.app.services.reporting.rules_diff_report.py.run_rules_diff_report"></a>
### 函数：run_rules_diff_report()
> 摘要：生成 A 表与 B 影子表的差异报告：
> 功能说明（推断）：导出统计/质量/覆盖率等报表
- 参数：
  - settings
  - method <str>
  - version <str>
  - out_dir <Optional[str]>
  - top_k <int>
- 返回：<Dict[str, Any]>
- 日志输出：无
- 数据库操作：有（cursor、execute、fetchall、fetchone；表：public.metric_rule_auto_baseline_shadow、public.metric_rule_auto_baseline、public.metric_quality_rules_shadow、public.metric_quality_rules）
- HTTP 调用：无
- 文件系统：有（makedirs、open）
- 子进程：无
- 环境变量：无
- 其他SDK：无


<a id="svc.app.services.rules.auto_baseline.py"></a>
## 模块：app\services\rules\auto_baseline.py
<a id="svc.app.services.rules.auto_baseline.py.run_auto_baseline"></a>
### 函数：run_auto_baseline()
> 摘要：调用数据库过程刷新自动基线（仅库端过程）。
> 功能说明（推断）：自动基线刷新（库端过程/影子方案）
- 参数：
  - settings
  - lookback_days <int>
  - station_id <Optional[int]>
  - device_id <Optional[int]>
- 返回：<Dict[str, Any]>
- 日志输出：无
- 数据库操作：有（cursor、execute）
- HTTP 调用：无
- 文件系统：无
- 子进程：无
- 环境变量：无
- 其他SDK：无


<a id="svc.app.services.rules.auto_baseline_b.py"></a>
## 模块：app\services\rules\auto_baseline_b.py
<a id="svc.app.services.rules.auto_baseline_b.py.run_auto_baseline_b"></a>
### 函数：run_auto_baseline_b()
> 摘要：方案B：基于 STL 的自动基线（影子输出），在 Residual 上做稳健估计。
> 功能说明（推断）：自动基线刷新（库端过程/影子方案）
- 参数：
  - settings
  - lookback_days <int>
  - station_id <Optional[int]>
  - device_id <Optional[int]>
  - method <str>
  - version <str>
- 返回：<Dict[str, Any]>
- 日志输出：无
- 数据库操作：有（cursor、execute、fetchone、fetchall；表：public.dim_metric_config、public.fact_measurements、public.metric_rule_auto_baseline、public.v_effective_metric_metadata、public.metric_rule_auto_baseline_shadow、SET）
- HTTP 调用：无
- 文件系统：无
- 子进程：无
- 环境变量：无
- 其他SDK：无


<a id="svc.app.services.rules.metric_quality_rules.py"></a>
## 模块：app\services\rules\metric_quality_rules.py
<a id="svc.app.services.rules.metric_quality_rules.py.compute_metric_quality_rules"></a>
### 函数：compute_metric_quality_rules()
> 摘要：基于自动基线(metric_rule_auto_baseline)填充/补全 metric_quality_rules：
> 功能说明（推断）：质量规则生成/影子生成
- 参数：
  - settings
  - station_id <Optional[int]>
  - device_id <Optional[int]>
- 返回：<Dict[str, Any]>
- 日志输出：无
- 数据库操作：有（cursor、execute、commit、fetchall；表：public.metric_rule_auto_baseline、base、base.station_id、base.device_id、base.metric_id、missing、public.metric_quality_rules）
- HTTP 调用：无
- 文件系统：无
- 子进程：无
- 环境变量：无
- 其他SDK：无


<a id="svc.app.services.rules.metric_quality_rules_b.py"></a>
## 模块：app\services\rules\metric_quality_rules_b.py
<a id="svc.app.services.rules.metric_quality_rules_b.py.compute_metric_quality_rules_shadow"></a>
### 函数：compute_metric_quality_rules_shadow()
> 摘要：方案B影子：从 metric_rule_auto_baseline_shadow 生成质量规则影子
> 功能说明（推断）：质量规则生成/影子生成
- 参数：
  - settings
  - station_id <Optional[int]>
  - device_id <Optional[int]>
  - method <str>
  - version <str>
- 返回：<Dict[str, Any]>
- 日志输出：无
- 数据库操作：有（cursor、execute、fetchall；表：public.metric_rule_auto_baseline_shadow、public.metric_quality_rules_shadow、SET）
- HTTP 调用：无
- 文件系统：无
- 子进程：无
- 环境变量：无
- 其他SDK：无


<a id="svc.app.services.rules.running_thresholds.py"></a>
## 模块：app\services\rules\running_thresholds.py
<a id="svc.app.services.rules.running_thresholds.py.run_running_thresholds"></a>
### 函数：run_running_thresholds()
> 摘要：高性能刷新 device_running_thresholds（仅库端过程），返回执行摘要。
> 功能说明（推断）：设备运行阈值刷新/影子计算
- 参数：
  - settings
  - start <Optional[str]>
  - end <Optional[str]>
  - station_id <Optional[int]>
  - device_id <Optional[int]>
  - ensure_rows <bool>
  - method <str>
- 返回：<Dict[str, Any]>
- 日志输出：无
- 数据库操作：有（cursor、commit、execute、fetchone、fetchall、rollback；表：public.device_running_thresholds、public.fact_measurements、public.dim_metric_config、ids、base_pf、agg）
- HTTP 调用：无
- 文件系统：有（replace、exists、read_text；路径：Z）
- 子进程：无
- 环境变量：无
- 其他SDK：无


<a id="svc.app.services.rules.seed_from_data.py"></a>
## 模块：app\services\rules\seed_from_data.py
<a id="svc.app.services.rules.seed_from_data.py.seed_from_existing_data"></a>
### 函数：seed_from_existing_data()
> 摘要：单次窗口内的全集合计算（不分批）：
> 功能说明（推断）：质量阈值/规则的刷新或影子计算
- 参数：
  - settings
  - start <Optional[datetime]>
  - end <Optional[datetime]>
  - lookback_days <int>
  - station_id <Optional[int]>
  - device_id <Optional[int]>
- 返回：<Dict[str, Any]>
- 日志输出：无
- 数据库操作：有（cursor、execute、fetchone；表：public.metric_rule_auto_baseline、public.metric_quality_rules、public.device_running_thresholds、public.quality_code_dict、public.fact_measurements）
- HTTP 调用：无
- 文件系统：有（replace）
- 子进程：无
- 环境变量：无
- 其他SDK：无

<a id="svc.app.services.rules.seed_from_data.py.batch_seed_from_existing_data"></a>
### 函数：batch_seed_from_existing_data()
> 摘要：批量计算（支持快速模式）：
> 功能说明（推断）：质量阈值/规则的刷新或影子计算
- 参数：
  - settings
  - start <Optional[datetime]>
  - end <Optional[datetime]>
  - lookback_days <int>
  - station_id <Optional[int]>
  - device_ids <Optional[Iterable[int]]>
  - max_retries <int>
  - work_mem <Optional[str]>
  - log_prefix <str>
  - fast_mode <bool>
- 返回：<Dict[str, Any]>
- 日志输出：有（getLogger）
- 数据库操作：有（commit、cursor、execute、fetchone、fetchall、rollback；表：public.fact_measurements、public.metric_rule_auto_baseline、public.metric_quality_rules、public.device_running_thresholds、public.quality_code_dict、b.station_id、b.device_id、b.metric_id、public.dim_metric_config、base、agg、ids）
- HTTP 调用：无
- 文件系统：有（replace）
- 子进程：无
- 环境变量：无
- 其他SDK：无


<a id="svc.app.services.run_all.orchestrator.py"></a>
## 模块：app\services\run_all\orchestrator.py
<a id="svc.app.services.run_all.orchestrator.py.run_all"></a>
### 函数：run_all()
> 摘要：通过配置文件开关控制每个子流程：
> 功能说明（推断）：端到端流程编排与调度
- 参数：
  - settings
  - mapping <str>
  - use_staging_time_range <bool>
  - window_start <Optional[str]>
  - window_end <Optional[str]>
  - summary_json <Optional[str]>
  - with_device_running <bool>
  - with_presence <bool>
  - with_quality_mark <bool>
  - with_device_phase <bool>
  - device_id <int | None>
  - quality_codes <list[int] | None>
  - quality_diag_level <str | None>
  - quality_parallel <int | str>
- 返回：<Dict[str, Any]>
- 日志输出：无
- 数据库操作：有（commit、cursor、execute、fetchall、fetchone；表：public.quality_profile_log、public.fact_measurements、public.mv_presence_1s_any、public.v_effective_metric_rules、public.metric_rule_auto_baseline）
- HTTP 调用：有（session.get）
- 文件系统：有（mkdir、write_text、exists、replace、read_text；路径：Z）
- 子进程：无
- 环境变量：无
- 其他SDK：无


<a id="svc.app.services.run_all.refresh_all.py"></a>
## 模块：app\services\run_all\refresh_all.py
<a id="svc.app.services.run_all.refresh_all.py.run_refresh_all"></a>
### 函数：run_refresh_all()
> 摘要：（未提供 docstring）
> 功能说明（推断）：执行一项端到端/批处理任务
- 参数：
  - settings
  - plan <RefreshAllPlan>
- 返回：<Dict[str, Any]>
- 日志输出：无
- 数据库操作：无
- HTTP 调用：有（session.get）
- 文件系统：有（replace；路径：Z）
- 子进程：无
- 环境变量：无
- 其他SDK：无


<a id="svc.app.services.system.cleanup.py"></a>
## 模块：app\services\system\cleanup.py
<a id="svc.app.services.system.cleanup.py.backup_logs_directory"></a>
### 函数：backup_logs_directory()
> 摘要：备份logs目录
> 功能说明（推断）：系统清理：日志与数据库清理辅助
- 参数：
  - logs_dir <Path>
  - backup_count <int>
- 返回：<None>
- 日志输出：无
- 数据库操作：无
- HTTP 调用：无
- 文件系统：有（exists）
- 子进程：无
- 环境变量：无
- 其他SDK：无

<a id="svc.app.services.system.cleanup.py.cleanup_old_backups"></a>
### 函数：cleanup_old_backups()
> 摘要：清理旧的备份目录
> 功能说明（推断）：系统清理：日志与数据库清理辅助
- 参数：
  - parent_dir <Path>
  - prefix <str>
  - keep_count <int>
- 返回：<None>
- 日志输出：无
- 数据库操作：无
- HTTP 调用：无
- 文件系统：有（exists、iterdir、rmtree）
- 子进程：无
- 环境变量：无
- 其他SDK：无

<a id="svc.app.services.system.cleanup.py.clear_logs_directory"></a>
### 函数：clear_logs_directory()
> 摘要：清理logs目录
> 功能说明（推断）：系统清理：日志与数据库清理辅助
- 参数：
  - logs_dir <Path>
  - backup_count <int>
- 返回：<None>
- 日志输出：无
- 数据库操作：无
- HTTP 调用：无
- 文件系统：有（exists、rmtree）
- 子进程：无
- 环境变量：无
- 其他SDK：无

<a id="svc.app.services.system.cleanup.py.clear_database"></a>
### 函数：clear_database()
> 摘要：清理数据库
> 功能说明（推断）：系统清理：日志与数据库清理辅助
- 参数：
  - settings <Settings>
- 返回：<None>
- 日志输出：无
- 数据库操作：有（cursor、execute、commit、fetchall）
- HTTP 调用：有（session.get）
- 文件系统：有（exists、read_text）
- 子进程：无
- 环境变量：无
- 其他SDK：无

<a id="svc.app.services.system.cleanup.py.get_system_status"></a>
### 函数：get_system_status()
> 摘要：获取系统状态信息
> 功能说明（推断）：系统清理：日志与数据库清理辅助
- 参数：
  - settings <Settings>
- 返回：<dict>
- 日志输出：无
- 数据库操作：有（cursor、execute、fetchone）
- HTTP 调用：无
- 文件系统：有（exists）
- 子进程：无
- 环境变量：无
- 其他SDK：无


---

> 生成器：generate_project_wiki.py｜时间：2025-09-25 08:36
