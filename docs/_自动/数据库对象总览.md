# 数据库对象总览（自动生成｜含注释）

> 说明：来源 generated/db_objects.json（优先展示 comment 的语义说明）。


<a id="db.api"></a>
## schema: api
<a id="db.monitoring"></a>
## schema: monitoring
<a id="db.monitoring.ab_test_results"></a>
### 表：ab_test_results
> 作用（推断）：monitoring 架构下的对象
- run_id (bigint)：（未写注释） —— 作用（推断）：运行ID（批次/任务标识）
- phase (text)：（未写注释）
- rows_count (bigint)：（未写注释）
- duration_ms (double precision)：（未写注释）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.monitoring.ab_test_runs"></a>
### 表：ab_test_runs
> 作用（推断）：monitoring 架构下的对象
- id (bigint)：（未写注释） —— 作用（推断）：主键ID
- run_time (timestamp with time zone)：（未写注释）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- start_ts (timestamp with time zone)：（未写注释） —— 作用（推断）：窗口起始时间（timestamptz）
- end_ts (timestamp with time zone)：（未写注释） —— 作用（推断）：窗口结束时间（timestamptz）
- metric_ids (ARRAY)：（未写注释）

<a id="db.monitoring.pgss_snapshot"></a>
### 表：pgss_snapshot
> 作用（推断）：monitoring 架构下的对象
- snapshot_time (timestamp with time zone)：（未写注释）
- dbname (text)：（未写注释）
- userid (oid)：（未写注释）
- queryid (bigint)：（未写注释）
- calls (bigint)：（未写注释）
- rows (bigint)：（未写注释）
- blk_read_time (double precision)：（未写注释）
- blk_write_time (double precision)：（未写注释）
- temp_blks_read (bigint)：（未写注释）
- temp_blks_written (bigint)：（未写注释）

<a id="db.public"></a>
## schema: public
<a id="db.public.completion_audit"></a>
### 表：completion_audit
> 作用（推断）：补全/追踪审计相关：运行、步骤、失败、变更等记录
> 来源注释：追踪-审计级
- id (bigint)：审计记录ID —— 作用（推断）：主键ID
- run_id (bigint)：运行ID（唯一） —— 作用（推断）：运行ID（批次/任务标识）
- coverage_before (numeric)：补全前覆盖率
- coverage_after (numeric)：补全后覆盖率
- missing_before (bigint)：补全前缺失点数
- missing_after (bigint)：补全后缺失点数
- max_gap_before_sec (integer)：补全前最大缺口（秒）
- max_gap_after_sec (integer)：补全后最大缺口（秒）
- count_ffill (integer)：前向填充次数
- count_mean (integer)：均值填充次数
- count_reg (integer)：回归填充次数
- count_curve (integer)：曲线填充次数
- count_skipped_long_gap (integer)：跳过的长缺口次数
- startup_drop_ratio (numeric)：启停落点剔除比例
- rejects_counts (jsonb)：拒绝计数（jsonb，按类别）
- thresholds_snapshot_ref (text)：阈值快照引用（文本）
- group_consistency (jsonb)：组一致性审计（jsonb）
- audit_time (timestamp with time zone)：审计时间

<a id="db.public.completion_failures"></a>
### 表：completion_failures
> 作用（推断）：补全/追踪审计相关：运行、步骤、失败、变更等记录
> 来源注释：追踪-失败清单
- id (bigint)：失败记录ID —— 作用（推断）：主键ID
- run_id (bigint)：运行ID —— 作用（推断）：运行ID（批次/任务标识）
- object_key (jsonb)：对象键（jsonb：station_id/device_id/metric_id）
- gap_id (bigint)：缺口段ID —— 作用（推断）：缺口段ID/关联ID
- reason_code (text)：失败原因代码
- evidence_uri (text)：证据URI
- suggested_action (text)：建议动作
- created_at (timestamp with time zone)：创建时间 —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.completion_runs"></a>
### 表：completion_runs
> 作用（推断）：补全/追踪审计相关：运行、步骤、失败、变更等记录
> 来源注释：追踪-运行级
- run_id (bigint)：运行ID —— 作用（推断）：运行ID（批次/任务标识）
- station_id (bigint)：泵站ID —— 作用（推断）：站点外键
- device_id (bigint)：设备ID —— 作用（推断）：设备外键
- start_ts (timestamp with time zone)：运行时间窗起（timestamptz） —— 作用（推断）：窗口起始时间（timestamptz）
- end_ts (timestamp with time zone)：运行时间窗止（timestamptz） —— 作用（推断）：窗口结束时间（timestamptz）
- code_version (text)：代码版本标识（如 git 哈希）
- imputation_version (text)：补全过程版本/算法版本
- config_snapshot (jsonb)：配置快照（jsonb）
- thresholds_snapshot (jsonb)：阈值快照（jsonb）
- rows_read (bigint)：读取的事实点数量
- duration_ms (integer)：运行耗时（毫秒）
- steps_total (integer)：步骤数量
- rerun_policy (text)：复跑策略：append/overwrite/skip
- parent_run_id (bigint)：父运行ID（复跑来源）
- superseded_by_run_id (bigint)：被替代运行ID（被后续运行覆盖）
- idempotency_key (text)：幂等键（避免重复写入）
- group_context (jsonb)：泵组上下文（jsonb）
- status_reason (text)：状态原因摘要
- error_code (text)：错误代码摘要
- created_at (timestamp with time zone)：创建时间 —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.completion_steps"></a>
### 表：completion_steps
> 作用（推断）：补全/追踪审计相关：运行、步骤、失败、变更等记录
> 来源注释：追踪-步骤级
- id (bigint)：步骤记录ID —— 作用（推断）：主键ID
- run_id (bigint)：所属运行ID —— 作用（推断）：运行ID（批次/任务标识）
- device_id (bigint)：设备ID —— 作用（推断）：设备外键
- metric_id (bigint)：指标ID —— 作用（推断）：指标外键
- gap_id (bigint)：缺口段ID —— 作用（推断）：缺口段ID/关联ID
- gap_start_ts (timestamp with time zone)：缺口开始时间
- gap_end_ts (timestamp with time zone)：缺口结束时间
- gap_len_sec (integer)：缺口时长（秒）
- gap_class (text)：缺口分类（short/medium/long/in_startstop）
- in_startstop_window (boolean)：是否位于启停窗口内
- methods_tried (jsonb)：尝试的方法列表（jsonb）
- fallback_path (text)：回退路径
- confidence_reason (ARRAY)：置信理由（数组）
- residual_stats (jsonb)：残差统计（jsonb，可选）
- step_duration_ms (integer)：步骤耗时（毫秒）
- rows_considered (bigint)：考虑的点数量
- data_source (text)：数据来源（original/derived）
- created_at (timestamp with time zone)：创建时间 —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.device_metric_candidates"></a>
### 表：device_metric_candidates
> 作用（推断）：业务核心对象表/视图
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- metrics (ARRAY)：（未写注释）

<a id="db.public.device_rated_params"></a>
### 表：device_rated_params
> 作用（推断）：业务核心对象表/视图
> 来源注释：设备额定参数（可选）
- id (bigint)：ID（PK） —— 作用（推断）：主键ID
- device_id (bigint)：设备ID —— 作用（推断）：设备外键
- param_key (text)：参数键（如 rated_power）
- value_numeric (numeric)：数值（numeric） —— 作用（推断）：数值（numeric）
- value_text (text)：文本值
- unit (text)：单位 —— 作用（推断）：单位
- source (text)：来源
- effective_from (timestamp with time zone)：生效起
- effective_to (timestamp with time zone)：生效止
- created_at (timestamp with time zone)：创建时间 —— 作用（推断）：创建时间（timestamptz）
- updated_at (timestamp with time zone)：更新时间 —— 作用（推断）：更新时间（timestamptz）

<a id="db.public.device_running_thresholds"></a>
### 表：device_running_thresholds
> 作用（推断）：设备运行判定阈值集中配置（电流/功率/频率）
> 来源注释：设备运行判定阈值（集中配置）
- device_id (bigint)：设备ID（PK） —— 作用（推断）：设备外键
- enable_i (boolean)：是否启用电流判定（取三相最大）
- enable_p (boolean)：是否启用功率判定
- enable_f (boolean)：是否启用频率判定
- i_on (double precision)：电流开启阈值
- i_off (double precision)：电流关闭阈值
- p_on (double precision)：功率开启阈值
- p_off (double precision)：功率关闭阈值
- f_on (double precision)：频率开启阈值
- f_off (double precision)：频率关闭阈值
- grace_hold_secs (integer)：缺报延续秒数
- min_run_secs (integer)：最小运行段长度（用于窗口聚合）
- min_stop_secs (integer)：最小停机段长度（用于窗口聚合）
- smoothing_secs (integer)：平滑窗口秒数（可选）
- updated_at (timestamp with time zone)：更新时间 —— 作用（推断）：更新时间（timestamptz）
- updated_by (text)：更新人/来源

<a id="db.public.dim_devices"></a>
### 表：dim_devices
> 作用（推断）：设备维表：设备元数据（站点关联、类型、名称等）
> 来源注释：设备维表
- id (bigint)：设备ID（PK） —— 作用（推断）：主键ID
- station_id (bigint)：泵站ID —— 作用（推断）：站点外键
- name (text)：设备名称（站内唯一）
- type (text)：设备类型（pump 等）
- pump_type (text)：泵类型（variable_frequency/soft_start 等）
- extra (jsonb)：额外信息（jsonb）
- created_at (timestamp with time zone)：创建时间 —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.dim_mapping_items"></a>
### 表：dim_mapping_items
> 作用（推断）：导入映射快照：源到内部维度映射记录（一致性校验）
> 来源注释：映射快照（导入时使用）
- id (bigint)：ID（PK） —— 作用（推断）：主键ID
- mapping_hash (text)：映射哈希（用于快照一致性） —— 作用（推断）：映射快照哈希（一致性）
- station_name (text)：站点名（源） —— 作用（推断）：站点名称（源）
- device_name (text)：设备名（源） —— 作用（推断）：设备名称（源）
- metric_key (text)：指标键（源）
- source_hint (text)：来源提示 —— 作用（推断）：数据来源线索（如文件路径/批次/版本）
- created_at (timestamp with time zone)：创建时间 —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.dim_metric_config"></a>
### 表：dim_metric_config
> 作用（推断）：指标配置维表：指标键、单位、展示、取值范围
> 来源注释：指标配置
- id (bigint)：指标ID（PK） —— 作用（推断）：主键ID
- metric_key (text)：指标键（唯一，如 pump_frequency）
- unit (text)：单位（如 Hz、kW、A） —— 作用（推断）：单位
- unit_display (text)：单位显示 —— 作用（推断）：单位显示
- decimals_policy (text)：小数策略（as_is 等）
- fixed_decimals (smallint)：固定小数位（可选）
- value_type (text)：值类型（number 等）
- valid_min (numeric)：有效最小值
- valid_max (numeric)：有效最大值
- created_at (timestamp with time zone)：创建时间 —— 作用（推断）：创建时间（timestamptz）
- updated_at (timestamp with time zone)：更新时间 —— 作用（推断）：更新时间（timestamptz）

<a id="db.public.dim_stations"></a>
### 表：dim_stations
> 作用（推断）：泵站维表：站点元数据（名称、时区、属性）
> 来源注释：泵站维表
- id (bigint)：泵站ID（PK） —— 作用（推断）：主键ID
- name (text)：泵站名称
- extra (jsonb)：额外信息（jsonb，建议包含 tz）
- created_at (timestamp with time zone)：创建时间 —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.fact_measurements"></a>
### 表：fact_measurements
> 作用（推断）：事实明细表：时间序列度量主数据（按站/设备/指标/时刻）
- id (bigint)：ID（PK） —— 作用（推断）：主键ID
- station_id (bigint)：泵站ID —— 作用（推断）：站点外键
- device_id (bigint)：设备ID —— 作用（推断）：设备外键
- metric_id (bigint)：指标ID —— 作用（推断）：指标外键
- ts_raw (timestamp with time zone)：原始时间（建议 UTC） —— 作用（推断）：原始时间戳（timestamptz，建议UTC）
- ts_bucket (timestamp with time zone)：整秒对齐时间 —— 作用（推断）：桶/分箱时间戳（按粒度聚合）
- value (numeric)：数值 —— 作用（推断）：数值（numeric/float）
- source_hint (text)：来源提示 —— 作用（推断）：数据来源线索（如文件路径/批次/版本）
- inserted_at (timestamp with time zone)：插入时间 —— 作用（推断）：插入时间（timestamptz）

<a id="db.public.metric_capability_policy"></a>
### 表：metric_capability_policy
> 作用（推断）：指标可采能力策略：记录各指标可采性与是否需要计算
> 来源注释：指标获取/计算能力策略表：记录每个 metric_key 的获取情况(不能/可能/可以)与是否需要计算(需要/不需要)
- metric_key (text)：指标键；与 dim_metric_config.metric_key 一一对应
- acquisition_status (text)：获取情况：不能/可能/可以；不能=无法直接采集，可能=部分站点可采，
可以=肯定可采
- compute_flag (text)：是否计算补充：需要=必须通过其他指标计算获得；不需要=不可计算或不需要计算
- updated_at (timestamp with time zone)：策略最后更新时间 —— 作用（推断）：更新时间（timestamptz）
- updated_by (text)：最后更新人（可填 sys/脚本名）

<a id="db.public.metrics_presence_per_second_device"></a>
### 表：metrics_presence_per_second_device
> 作用（推断）：指标在场性/可用性统计（按设备/时间片统计可用指标）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.metrics_presence_per_second_device_legacy"></a>
### 表：metrics_presence_per_second_device_legacy
> 作用（推断）：指标在场性/可用性统计（按设备/时间片统计可用指标）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202507"></a>
### 表：mpps_w_202507
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202507_0"></a>
### 表：mpps_w_202507_0
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202507_1"></a>
### 表：mpps_w_202507_1
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202507_10"></a>
### 表：mpps_w_202507_10
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202507_11"></a>
### 表：mpps_w_202507_11
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202507_12"></a>
### 表：mpps_w_202507_12
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202507_13"></a>
### 表：mpps_w_202507_13
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202507_14"></a>
### 表：mpps_w_202507_14
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202507_15"></a>
### 表：mpps_w_202507_15
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202507_2"></a>
### 表：mpps_w_202507_2
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202507_3"></a>
### 表：mpps_w_202507_3
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202507_4"></a>
### 表：mpps_w_202507_4
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202507_5"></a>
### 表：mpps_w_202507_5
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202507_6"></a>
### 表：mpps_w_202507_6
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202507_7"></a>
### 表：mpps_w_202507_7
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202507_8"></a>
### 表：mpps_w_202507_8
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202507_9"></a>
### 表：mpps_w_202507_9
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202508"></a>
### 表：mpps_w_202508
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202508_0"></a>
### 表：mpps_w_202508_0
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202508_1"></a>
### 表：mpps_w_202508_1
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202508_10"></a>
### 表：mpps_w_202508_10
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202508_11"></a>
### 表：mpps_w_202508_11
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202508_12"></a>
### 表：mpps_w_202508_12
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202508_13"></a>
### 表：mpps_w_202508_13
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202508_14"></a>
### 表：mpps_w_202508_14
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202508_15"></a>
### 表：mpps_w_202508_15
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202508_2"></a>
### 表：mpps_w_202508_2
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202508_3"></a>
### 表：mpps_w_202508_3
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202508_4"></a>
### 表：mpps_w_202508_4
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202508_5"></a>
### 表：mpps_w_202508_5
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202508_6"></a>
### 表：mpps_w_202508_6
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202508_7"></a>
### 表：mpps_w_202508_7
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202508_8"></a>
### 表：mpps_w_202508_8
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202508_9"></a>
### 表：mpps_w_202508_9
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202509"></a>
### 表：mpps_w_202509
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202509_0"></a>
### 表：mpps_w_202509_0
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202509_1"></a>
### 表：mpps_w_202509_1
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202509_10"></a>
### 表：mpps_w_202509_10
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202509_11"></a>
### 表：mpps_w_202509_11
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202509_12"></a>
### 表：mpps_w_202509_12
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202509_13"></a>
### 表：mpps_w_202509_13
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202509_14"></a>
### 表：mpps_w_202509_14
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202509_15"></a>
### 表：mpps_w_202509_15
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202509_2"></a>
### 表：mpps_w_202509_2
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202509_3"></a>
### 表：mpps_w_202509_3
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202509_4"></a>
### 表：mpps_w_202509_4
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202509_5"></a>
### 表：mpps_w_202509_5
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202509_6"></a>
### 表：mpps_w_202509_6
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202509_7"></a>
### 表：mpps_w_202509_7
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202509_8"></a>
### 表：mpps_w_202509_8
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202509_9"></a>
### 表：mpps_w_202509_9
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202510"></a>
### 表：mpps_w_202510
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202510_0"></a>
### 表：mpps_w_202510_0
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202510_1"></a>
### 表：mpps_w_202510_1
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202510_10"></a>
### 表：mpps_w_202510_10
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202510_11"></a>
### 表：mpps_w_202510_11
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202510_12"></a>
### 表：mpps_w_202510_12
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202510_13"></a>
### 表：mpps_w_202510_13
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202510_14"></a>
### 表：mpps_w_202510_14
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202510_15"></a>
### 表：mpps_w_202510_15
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202510_2"></a>
### 表：mpps_w_202510_2
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202510_3"></a>
### 表：mpps_w_202510_3
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202510_4"></a>
### 表：mpps_w_202510_4
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202510_5"></a>
### 表：mpps_w_202510_5
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202510_6"></a>
### 表：mpps_w_202510_6
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202510_7"></a>
### 表：mpps_w_202510_7
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202510_8"></a>
### 表：mpps_w_202510_8
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202510_9"></a>
### 表：mpps_w_202510_9
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202511"></a>
### 表：mpps_w_202511
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202511_0"></a>
### 表：mpps_w_202511_0
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202511_1"></a>
### 表：mpps_w_202511_1
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202511_10"></a>
### 表：mpps_w_202511_10
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202511_11"></a>
### 表：mpps_w_202511_11
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202511_12"></a>
### 表：mpps_w_202511_12
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202511_13"></a>
### 表：mpps_w_202511_13
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202511_14"></a>
### 表：mpps_w_202511_14
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202511_15"></a>
### 表：mpps_w_202511_15
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202511_2"></a>
### 表：mpps_w_202511_2
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202511_3"></a>
### 表：mpps_w_202511_3
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202511_4"></a>
### 表：mpps_w_202511_4
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202511_5"></a>
### 表：mpps_w_202511_5
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202511_6"></a>
### 表：mpps_w_202511_6
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202511_7"></a>
### 表：mpps_w_202511_7
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202511_8"></a>
### 表：mpps_w_202511_8
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202511_9"></a>
### 表：mpps_w_202511_9
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202512"></a>
### 表：mpps_w_202512
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202512_0"></a>
### 表：mpps_w_202512_0
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202512_1"></a>
### 表：mpps_w_202512_1
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202512_10"></a>
### 表：mpps_w_202512_10
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202512_11"></a>
### 表：mpps_w_202512_11
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202512_12"></a>
### 表：mpps_w_202512_12
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202512_13"></a>
### 表：mpps_w_202512_13
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202512_14"></a>
### 表：mpps_w_202512_14
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202512_15"></a>
### 表：mpps_w_202512_15
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202512_2"></a>
### 表：mpps_w_202512_2
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202512_3"></a>
### 表：mpps_w_202512_3
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202512_4"></a>
### 表：mpps_w_202512_4
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202512_5"></a>
### 表：mpps_w_202512_5
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202512_6"></a>
### 表：mpps_w_202512_6
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202512_7"></a>
### 表：mpps_w_202512_7
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202512_8"></a>
### 表：mpps_w_202512_8
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202512_9"></a>
### 表：mpps_w_202512_9
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202513"></a>
### 表：mpps_w_202513
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202513_0"></a>
### 表：mpps_w_202513_0
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202513_1"></a>
### 表：mpps_w_202513_1
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202513_10"></a>
### 表：mpps_w_202513_10
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202513_11"></a>
### 表：mpps_w_202513_11
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202513_12"></a>
### 表：mpps_w_202513_12
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202513_13"></a>
### 表：mpps_w_202513_13
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202513_14"></a>
### 表：mpps_w_202513_14
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202513_15"></a>
### 表：mpps_w_202513_15
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202513_2"></a>
### 表：mpps_w_202513_2
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202513_3"></a>
### 表：mpps_w_202513_3
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202513_4"></a>
### 表：mpps_w_202513_4
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202513_5"></a>
### 表：mpps_w_202513_5
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202513_6"></a>
### 表：mpps_w_202513_6
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202513_7"></a>
### 表：mpps_w_202513_7
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202513_8"></a>
### 表：mpps_w_202513_8
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202513_9"></a>
### 表：mpps_w_202513_9
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202514"></a>
### 表：mpps_w_202514
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202514_0"></a>
### 表：mpps_w_202514_0
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202514_1"></a>
### 表：mpps_w_202514_1
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202514_10"></a>
### 表：mpps_w_202514_10
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202514_11"></a>
### 表：mpps_w_202514_11
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202514_12"></a>
### 表：mpps_w_202514_12
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202514_13"></a>
### 表：mpps_w_202514_13
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202514_14"></a>
### 表：mpps_w_202514_14
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202514_15"></a>
### 表：mpps_w_202514_15
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202514_2"></a>
### 表：mpps_w_202514_2
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202514_3"></a>
### 表：mpps_w_202514_3
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202514_4"></a>
### 表：mpps_w_202514_4
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202514_5"></a>
### 表：mpps_w_202514_5
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202514_6"></a>
### 表：mpps_w_202514_6
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202514_7"></a>
### 表：mpps_w_202514_7
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202514_8"></a>
### 表：mpps_w_202514_8
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202514_9"></a>
### 表：mpps_w_202514_9
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202515"></a>
### 表：mpps_w_202515
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202515_0"></a>
### 表：mpps_w_202515_0
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202515_1"></a>
### 表：mpps_w_202515_1
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202515_10"></a>
### 表：mpps_w_202515_10
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202515_11"></a>
### 表：mpps_w_202515_11
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202515_12"></a>
### 表：mpps_w_202515_12
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202515_13"></a>
### 表：mpps_w_202515_13
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202515_14"></a>
### 表：mpps_w_202515_14
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202515_15"></a>
### 表：mpps_w_202515_15
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202515_2"></a>
### 表：mpps_w_202515_2
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202515_3"></a>
### 表：mpps_w_202515_3
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202515_4"></a>
### 表：mpps_w_202515_4
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202515_5"></a>
### 表：mpps_w_202515_5
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202515_6"></a>
### 表：mpps_w_202515_6
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202515_7"></a>
### 表：mpps_w_202515_7
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202515_8"></a>
### 表：mpps_w_202515_8
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202515_9"></a>
### 表：mpps_w_202515_9
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202516"></a>
### 表：mpps_w_202516
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202516_0"></a>
### 表：mpps_w_202516_0
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202516_1"></a>
### 表：mpps_w_202516_1
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202516_10"></a>
### 表：mpps_w_202516_10
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202516_11"></a>
### 表：mpps_w_202516_11
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202516_12"></a>
### 表：mpps_w_202516_12
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202516_13"></a>
### 表：mpps_w_202516_13
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202516_14"></a>
### 表：mpps_w_202516_14
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202516_15"></a>
### 表：mpps_w_202516_15
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202516_2"></a>
### 表：mpps_w_202516_2
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202516_3"></a>
### 表：mpps_w_202516_3
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202516_4"></a>
### 表：mpps_w_202516_4
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202516_5"></a>
### 表：mpps_w_202516_5
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202516_6"></a>
### 表：mpps_w_202516_6
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202516_7"></a>
### 表：mpps_w_202516_7
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202516_8"></a>
### 表：mpps_w_202516_8
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202516_9"></a>
### 表：mpps_w_202516_9
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202537"></a>
### 表：mpps_w_202537
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202537_0"></a>
### 表：mpps_w_202537_0
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202537_1"></a>
### 表：mpps_w_202537_1
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202537_10"></a>
### 表：mpps_w_202537_10
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202537_11"></a>
### 表：mpps_w_202537_11
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202537_12"></a>
### 表：mpps_w_202537_12
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202537_13"></a>
### 表：mpps_w_202537_13
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202537_14"></a>
### 表：mpps_w_202537_14
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202537_15"></a>
### 表：mpps_w_202537_15
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202537_2"></a>
### 表：mpps_w_202537_2
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202537_3"></a>
### 表：mpps_w_202537_3
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202537_4"></a>
### 表：mpps_w_202537_4
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202537_5"></a>
### 表：mpps_w_202537_5
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202537_6"></a>
### 表：mpps_w_202537_6
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202537_7"></a>
### 表：mpps_w_202537_7
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202537_8"></a>
### 表：mpps_w_202537_8
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.mpps_w_202537_9"></a>
### 表：mpps_w_202537_9
> 作用（推断）：presence 按周期分片宽表（分区/分表，秒级聚合）
- station_id (bigint)：（未写注释） —— 作用（推断）：站点外键
- device_id (bigint)：（未写注释） —— 作用（推断）：设备外键
- ts_second (timestamp with time zone)：（未写注释） —— 作用（推断）：秒级时间戳（timestamptz）
- available_metrics (ARRAY)：（未写注释） —— 作用（推断）：可用指标列表（ARRAY）
- need_compute_metrics (ARRAY)：（未写注释） —— 作用（推断）：需要计算得到的指标列表（ARRAY）
- created_at (timestamp with time zone)：（未写注释） —— 作用（推断）：创建时间（timestamptz）

<a id="db.public.staging_raw"></a>
### 表：staging_raw
> 作用（推断）：装载中间表：导入阶段原始数据（UNLOGGED，清洗/合并前）
> 来源注释：导入阶段原始数据（UNLOGGED，临时用）
- station_name (text)：（未写注释） —— 作用（推断）：站点名称（源）
- device_name (text)：（未写注释） —— 作用（推断）：设备名称（源）
- metric_key (text)：（未写注释）
- TagName (text)：（未写注释）
- DataTime (text)：（未写注释）
- DataValue (text)：（未写注释）
- source_hint (text)：（未写注释） —— 作用（推断）：数据来源线索（如文件路径/批次/版本）
- loaded_at (timestamp with time zone)：（未写注释）

<a id="db.public.staging_rejects"></a>
### 表：staging_rejects
> 作用（推断）：装载中间表：导入阶段拒绝/错误记录（UNLOGGED）
> 来源注释：导入阶段拒绝/错误记录（UNLOGGED，临时用）
- station_name (text)：（未写注释） —— 作用（推断）：站点名称（源）
- device_name (text)：（未写注释） —— 作用（推断）：设备名称（源）
- metric_key (text)：（未写注释）
- TagName (text)：（未写注释）
- DataTime (text)：（未写注释）
- DataValue (text)：（未写注释）
- source_hint (text)：（未写注释） —— 作用（推断）：数据来源线索（如文件路径/批次/版本）
- error_msg (text)：（未写注释）
- rejected_at (timestamp with time zone)：（未写注释）

<a id="db.reporting"></a>
## schema: reporting
<a id="db.timescaledb_experimental"></a>
## schema: timescaledb_experimental
---

> 生成器：generate_project_wiki.py｜时间：2025-09-25 08:36
