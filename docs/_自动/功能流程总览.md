# 功能流程总览（自动生成｜含注释）

> 说明：依据 services/run_all/orchestrator.py，提炼关键步骤并解释 Why/What/Impact/Rollback。


## 总体流程
1) prepare_dim：准备维度表（站/设备/指标），确保后续合并维度可用
2) create_staging：创建/刷新 staging 结构，承接映射后的原始数据
3) ingest_copy：按映射复制数据并统计 files/rows（审计用途）
4) merge_fact：在窗口内合并数据到事实表 fact_measurements（统一时区处理）
5) device_running（可选）：计算设备运行状态与审计
6) quality_mark（可选）：质量打标，输出规则命中/资源消耗等摘要与样例
7) presence（可选）：按窗口计算存在性汇总

## 我们为什么这样做（Why）
- 将采集→暂存→合并→质量→存在性的链路串联，保障每步均可审计与回放
- 配置开关来自 configs/merge.yaml，可按任务裁剪开销

## 做了什么（What）
- 对窗口进行统一解析：输入缺 tz 时按系统默认 tz 解释，输出统一为本地时区 +08 样式

- 在质量打标前自动刷新相关物化视图，提高判定时效

- 并行质量打标支持 auto 并行度估算，输出 rule_summary 与样例（anomaly.case）


## 影响范围（Impact）
- 数据写入：staging_*、fact_measurements、presence/quality 相关表

- 资源占用：并行度/窗口大小影响 CPU/内存/扫描行数


## 如何验证（Evidence）
- 查看 logs/activity.log、logs/anomaly.log 中的 window_start/window_end/rule_summary

- 检查 reports/ 下导出的质量分布文件与 run_all_summary.json


## 如需回滚（Rollback）
- 减小窗口或关闭可选步骤（merge.yaml: run_all.*=false），避免写入

- 对事实表的修复：可通过补写/覆盖策略复跑；必要时使用清理脚本


---

> 生成器：generate_project_wiki.py｜时间：2025-09-25 08:36
