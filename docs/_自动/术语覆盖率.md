# 术语覆盖率（自动生成）

> 说明：统计可识别对象（API 路由、数据库表、服务函数、Pydantic 模型）的术语命中情况，辅助补充术语词典。

## API 路由
- 对象数：10，命中：10（100.0%）

## 数据库表
- 对象数：207，命中：207（100.0%）

## 服务函数/方法
- 对象数：47，命中：0（0.0%）
- 未命中（建议补充到术语词典）：
  - truncate_public_schema()
  - run_db_ping()
  - json_dumps()
  - main()
  - DeviceRunningJob.resolve_devices()
  - DeviceRunningJob.resolve_window()
  - DeviceRunningJob.run_for_device()
  - DeviceRunningJob.run()
  - BackpressureController.decide()
  - check_mapping_paths()
  - run_check_mapping_command()
  - copy_from_mapping()
  - create_staging()
  - run_merge_fact_command()
  - merge_window()
  - run_all()
  - make_source_hint()
  - run_quality_full_pass()
  - mark_quality_window()
  - export_window_code_distribution()

## Pydantic 模型
- 对象数：1，命中：0（0.0%）
- 未命中（建议补充到术语词典）：
  - TimeSeriesResponse

---

> 生成器：generate_project_wiki.py｜时间：2025-09-25 08:36
