# 配置说明

## 背景与目标
- 统一 YAML 配置源，优先级：CLI > ENV > YAML > 默认。

## 配置项与快照（自动块）
<!-- AUTO-WIKI:BEGIN:CONFIGS -->

# 配置项总览（自动生成｜含注释）

> 说明：汇总 configs/*.yaml 的关键项，用于快速理解可调参数；具体以 YAML 为准。


## database.yaml
- host：（详见 YAML 上方注释）
- port：（详见 YAML 上方注释）
- dbname：（详见 YAML 上方注释）
- user：（详见 YAML 上方注释）
- password：（详见 YAML 上方注释）
- dsn_read：（详见 YAML 上方注释）
- dsn_write：（详见 YAML 上方注释）
- staging_unlogged：（详见 YAML 上方注释）
- pool：（详见 YAML 上方注释）
- min_size：最小连接数：保持活跃的最少连接数量
- max_size：最大连接数：并发连接上限，影响系统负载
- max_inactive_connection_lifetime：非活跃连接生存时间（秒）
- timeouts：（详见 YAML 上方注释）
- connect_timeout_ms：连接超时：建立连接的最大等待时间
- statement_timeout_ms：语句超时：单个SQL执行的最大时间
- query_timeout_ms：查询超时：复杂查询的最大执行时间
- connection_acquire_timeout_ms：获取连接超时：从连接池获取连接的最大等待时间
- connection_validation_timeout_ms：连接验证超时：验证连接健康状态的最大时间
- pool_shutdown_timeout_ms：连接池关闭超时：关闭连接池的最大等待时间
- retry：（详见 YAML 上方注释）
- max_retries：最大重试次数：连接失败时的重试上限
- retry_delay_ms：重试延迟：每次重试间的等待时间
- backoff_multiplier：退避倍数：指数退避的倍数因子

## device_running.yaml
- slice_granularity：week|day：切片粒度（建议 week，匹配事实表周分区）
- max_device_concurrency：设备级并发（当前实现串行，预留扩展位）
- update_only_when_changed：仅值变化更新（ON CONFLICT DO UPDATE ... WHERE value IS DISTINCT FROM EXCLUDED.value）
- force_recompute：强制重算（预留开关）
- audit_granularity：追踪层审计聚合粒度（day|week），当前写入 steps.residual_stats 汇总

## error_handling.yaml
- retry：（详见 YAML 上方注释）
- default：（详见 YAML 上方注释）
- max_retries：最大重试次数
- base_delay：基础延迟时间（秒）
- max_delay：最大延迟时间（秒）
- backoff_multiplier：退避倍数
- jitter：是否启用抖动
- strategy：重试策略：fixed, linear, exponential, fibonacci
- network_errors：（详见 YAML 上方注释）
- max_retries：网络错误允许更多重试
- base_delay：较短的基础延迟
- max_delay：较短的最大延迟
- backoff_multiplier：较小的退避倍数
- jitter：（详见 YAML 上方注释）
- strategy：（详见 YAML 上方注释）
- database_connection_errors：（详见 YAML 上方注释）
- max_retries：数据库连接错误允许更多重试
- base_delay：标准基础延迟
- max_delay：中等最大延迟
- backoff_multiplier：标准退避倍数
- jitter：（详见 YAML 上方注释）
- strategy：（详见 YAML 上方注释）
- high_priority_errors：（详见 YAML 上方注释）
- max_retries：较少重试次数
- base_delay：很短的基础延迟
- max_delay：很短的最大延迟
- backoff_multiplier：很小的退避倍数
- jitter：不使用抖动，确保快速响应
- strategy：（详见 YAML 上方注释）
- degradable_errors：（详见 YAML 上方注释）
- max_retries：较少重试次数
- base_delay：较长的基础延迟
- max_delay：中等最大延迟
- backoff_multiplier：较小的退避倍数
- jitter：（详见 YAML 上方注释）
- strategy：（详见 YAML 上方注释）
- resource_exhausted_errors：（详见 YAML 上方注释）
- max_retries：中等重试次数
- base_delay：较长的基础延迟，等待资源释放
- max_delay：较长的最大延迟
- backoff_multiplier：标准退避倍数
- jitter：（详见 YAML 上方注释）
- strategy：（详见 YAML 上方注释）
- circuit_breaker：（详见 YAML 上方注释）
- default：（详见 YAML 上方注释）
- failure_threshold：失败阈值
- recovery_timeout：恢复超时时间（秒）
- half_open_max_calls：半开状态最大调用次数
- half_open_success_threshold：半开状态成功阈值
- database_operations：（详见 YAML 上方注释）
- failure_threshold：数据库操作失败阈值
- recovery_timeout：较短的恢复超时
- half_open_max_calls：（详见 YAML 上方注释）
- half_open_success_threshold：（详见 YAML 上方注释）
- database_connection_creation：（详见 YAML 上方注释）
- failure_threshold：连接创建失败阈值较低
- recovery_timeout：较长的恢复超时
- half_open_max_calls：较少的半开测试调用
- half_open_success_threshold：较低的成功阈值
- api_calls：（详见 YAML 上方注释）
- failure_threshold：API调用失败阈值
- recovery_timeout：中等恢复超时
- half_open_max_calls：（详见 YAML 上方注释）
- half_open_success_threshold：（详见 YAML 上方注释）
- file_operations：（详见 YAML 上方注释）
- failure_threshold：文件操作失败阈值
- recovery_timeout：较短的恢复超时
- half_open_max_calls：（详见 YAML 上方注释）
- half_open_success_threshold：（详见 YAML 上方注释）
- network_operations：（详见 YAML 上方注释）
- failure_threshold：网络操作失败阈值较低
- recovery_timeout：较长的恢复超时
- half_open_max_calls：（详见 YAML 上方注释）
- half_open_success_threshold：（详见 YAML 上方注释）
- error_analysis：（详见 YAML 上方注释）
- recording：（详见 YAML 上方注释）
- max_records：最大错误记录数
- analysis_window：分析时间窗口（秒）
- cleanup_interval：清理间隔（秒）
- auto_cleanup：是否自动清理过期记录
- pattern_detection：（详见 YAML 上方注释）
- min_frequency：最小频率阈值
- time_window：模式检测时间窗口（秒）
- similarity_threshold：相似度阈值
- enable_auto_detection：是否启用自动模式检测
- trend_analysis：（详见 YAML 上方注释）
- analysis_interval：趋势分析间隔（秒）
- trend_window：趋势分析时间窗口（秒）
- rate_threshold：错误率阈值（每分钟）
- enable_prediction：是否启用故障预测
- reporting：（详见 YAML 上方注释）
- auto_generate：是否自动生成报告
- report_interval：报告生成间隔（秒）
- max_reports：最大报告数量
- include_patterns：是否包含错误模式
- include_trends：是否包含趋势分析
- include_suggestions：是否包含恢复建议
- monitoring：（详见 YAML 上方注释）
- decorator：（详见 YAML 上方注释）
- enable_context_capture：是否捕获上下文信息
- max_context_size：最大上下文大小（字符）
- capture_args：是否捕获函数参数
- capture_return：是否捕获返回值（可能包含敏感信息）
- exclude_sensitive_keys：排除的敏感键名
- performance：（详见 YAML 上方注释）
- enable_timing：是否启用执行时间监控
- slow_threshold：慢操作阈值（秒）
- memory_monitoring：是否启用内存监控（可能影响性能）
- alerting：（详见 YAML 上方注释）
- enable_alerts：是否启用告警（需要外部告警系统）
- error_rate_threshold：错误率告警阈值
- consecutive_failures_threshold：连续失败告警阈值
- circuit_breaker_open_alert：断路器开启时是否告警
- validation：（详见 YAML 上方注释）
- retry_validation：（详见 YAML 上方注释）
- max_retries_limit：最大重试次数上限
- max_delay_limit：最大延迟时间上限（秒）
- min_base_delay：最小基础延迟（秒）
- valid_strategies：有效的重试策略
- circuit_breaker_validation：（详见 YAML 上方注释）
- max_failure_threshold：最大失败阈值
- max_recovery_timeout：最大恢复超时（秒）
- min_recovery_timeout：最小恢复超时（秒）
- max_half_open_calls：最大半开调用次数
- analysis_validation：（详见 YAML 上方注释）
- max_records_limit：最大记录数上限
- max_analysis_window：最大分析窗口（秒，24小时）
- min_analysis_window：最小分析窗口（秒）
- max_context_size_limit：最大上下文大小上限
- initialization：（详见 YAML 上方注释）
- startup：（详见 YAML 上方注释）
- validate_config：启动时是否验证配置
- initialize_components：是否初始化所有组件
- fail_on_invalid_config：配置无效时是否失败
- hot_reload：（详见 YAML 上方注释）
- enable_hot_reload：是否启用配置热更新
- watch_config_files：是否监控配置文件变化
- reload_interval：重新加载间隔（秒）
- backup_on_reload：重新加载时是否备份当前配置

## ingest.yaml
- mode：auto|direct|staging（默认 auto）
- workers：并发线程/任务数
- commit_interval：提交批大小（行）
- p95_window：计算 P95 的滑动窗口批次数
- csv：（详见 YAML 上方注释）
- delimiter：分隔符：CSV字段分隔字符
- encoding：编码格式：文件字符编码
- quote_char：引用字符：包围含特殊字符的字段
- escape_char：转义字符：转义特殊字符
- allow_bom：允许BOM：处理UTF-8 BOM标记
- batch：（详见 YAML 上方注释）
- size：批次大小：每批处理的记录数
- max_memory_mb：最大内存：单批次最大内存使用量（MB）
- parallel_batches：并行批次：同时处理的批次数量
- error_handling：（详见 YAML 上方注释）
- max_errors_per_file：单文件最大错误数：超过后跳过文件
- error_threshold_percent：错误阈值百分比：超过后终止处理
- continue_on_error：遇错继续：是否在错误后继续处理
- performance：（详见 YAML 上方注释）
- read_buffer_size：读缓冲区大小（字节）
- write_buffer_size：写缓冲区大小（字节）
- connection_pool_size：连接池大小：并发数据库连接数
- backpressure：（详见 YAML 上方注释）
- thresholds：（详见 YAML 上方注释）
- p95_ms：P95 批耗时阈值（ms）
- fail_rate：错误率阈值（0~1）
- min_batch：最小批大小（行）
- min_workers：最小并发数
- default_paths：（详见 YAML 上方注释）
- mapping_file：迁移自 config/，统一到 configs/
- dim_metric_config：后续若迁移到 configs/，需同步更新此路径
- default_window：（详见 YAML 上方注释）
- process_all_data：（详见 YAML 上方注释）
- start_utc：（详见 YAML 上方注释）
- end_utc：（详见 YAML 上方注释）

## logging.yaml
- logging：日志系统根配置
- timezone：日志时间戳时区
- time_format：时间格式（含毫秒与时区）
- level：根 logger 级别（影响默认阈值）
- format：输出格式：pipe|json|keyvalue（当前实现 pipe 管道分隔）
- localize：是否将 extra_json 的键中文化
- sql_slow_ms：慢SQL阈值（毫秒；>=该值标记为慢SQL）
- sql：（详见 YAML 上方注释）
- explain_on_slow：慢SQL时尝试采集 EXPLAIN 摘要（仅 SELECT）
- explain_timeout_ms：EXPLAIN 最长等待时间
- explain_max_length：EXPLAIN 文本最大长度（超出截断）
- capture：采集策略（自动参数/返回值脱敏与截断）
- params：是否采集函数入参（方案A自动采集）
- return：是否采集函数返回值
- max_field_length：字符串最大长度；超出则截断并追加...
- mask_keys：需要脱敏的键（不区分大小写）
- activity：过程日志增强
- step_slow_ms：步骤慢阈值（>0 则启用）
- capture_result_summary：对返回结果做摘要（shape/len/统计/哈希）
- summary_max_length：摘要文本最大长度
- jobs：（详见 YAML 上方注释）
- retry_log：作业重试日志控制
- enabled：（详见 YAML 上方注释）
- include_policy：是否输出策略细节（backoff/jitter 等）
- threshold_ms：超过该值才输出详细重试日志
- dependencies：（详见 YAML 上方注释）
- http_out：（详见 YAML 上方注释）
- enabled：是否记录出站HTTP日志
- mask_headers：（详见 YAML 上方注释）
- cache：（详见 YAML 上方注释）
- enabled：是否记录缓存日志
- slow_ms：缓存操作慢阈值（仅在慢或未命中时输出）
- database：（详见 YAML 上方注释）
- tx：（详见 YAML 上方注释）
- enabled：是否记录事务边界（BEGIN/COMMIT/ROLLBACK）
- pool_log：（详见 YAML 上方注释）
- enabled：是否记录连接池关键事件
- acquire_slow_ms：获取连接慢阈值
- create_slow_ms：创建连接慢阈值
- health_check_slow_ms：健康检查慢阈值
- include_thread：（详见 YAML 上方注释）
- include_stack：（详见 YAML 上方注释）
- async：（详见 YAML 上方注释）
- enabled：（详见 YAML 上方注释）
- queue_size：（详见 YAML 上方注释）
- batch_size：（详见 YAML 上方注释）
- flush_interval_ms：（详见 YAML 上方注释）
- backpressure：block|drop_debug|drop_oldest
- force_sync_levels：（详见 YAML 上方注释）
- hot_reload：（详见 YAML 上方注释）
- enabled：（详见 YAML 上方注释）
- backend：none|watchfiles
- outputs：（详见 YAML 上方注释）
- - name：输出名称（可自定义多个 console/file 输出）
- type：输出类型：console=控制台，file=文件
- enabled：是否启用该输出
- level：该输出拦截的最小日志级别
- color：控制台彩色是否启用（为避免重定向ANSI与UTF-16问题，默认关闭）
- colors：级别→颜色映射（值可用颜色名或ANSI码；优先使用此映射，否则用内置默认）
- DEBUG：灰（或使用 "\u001b[90m"）
- INFO：绿
- WARNING：黄
- ERROR：红
- CRITICAL：亮红
- - name：（详见 YAML 上方注释）
- type：（详见 YAML 上方注释）
- enabled：（详见 YAML 上方注释）
- level：（详见 YAML 上方注释）
- path：（详见 YAML 上方注释）
- rotate：（详见 YAML 上方注释）
- type：（详见 YAML 上方注释）
- when：（详见 YAML 上方注释）
- interval：（详见 YAML 上方注释）
- backup_count：（详见 YAML 上方注释）
- compress：（详见 YAML 上方注释）
- - name：（详见 YAML 上方注释）
- type：（详见 YAML 上方注释）
- enabled：（详见 YAML 上方注释）
- level：（详见 YAML 上方注释）
- path：（详见 YAML 上方注释）
- rotate：（详见 YAML 上方注释）
- type：（详见 YAML 上方注释）
- max_bytes：（详见 YAML 上方注释）
- backup_count：（详见 YAML 上方注释）
- - name：（详见 YAML 上方注释）
- type：（详见 YAML 上方注释）
- enabled：（详见 YAML 上方注释）
- level：（详见 YAML 上方注释）
- path：（详见 YAML 上方注释）
- rotate：（详见 YAML 上方注释）
- type：（详见 YAML 上方注释）
- when：（详见 YAML 上方注释）
- interval：（详见 YAML 上方注释）
- backup_count：（详见 YAML 上方注释）
- route：（详见 YAML 上方注释）
- only_loggers：（详见 YAML 上方注释）
- - name：（详见 YAML 上方注释）
- type：（详见 YAML 上方注释）
- enabled：（详见 YAML 上方注释）
- level：（详见 YAML 上方注释）
- path：（详见 YAML 上方注释）
- rotate：（详见 YAML 上方注释）
- type：（详见 YAML 上方注释）
- when：（详见 YAML 上方注释）
- interval：（详见 YAML 上方注释）
- backup_count：（详见 YAML 上方注释）
- compress：（详见 YAML 上方注释）
- route：（详见 YAML 上方注释）
- only_loggers：移除 sql，避免污染异常诊断文件
- - name：（详见 YAML 上方注释）
- type：（详见 YAML 上方注释）
- enabled：（详见 YAML 上方注释）
- level：（详见 YAML 上方注释）
- path：（详见 YAML 上方注释）
- rotate：（详见 YAML 上方注释）
- type：（详见 YAML 上方注释）
- when：（详见 YAML 上方注释）
- interval：（详见 YAML 上方注释）
- backup_count：（详见 YAML 上方注释）
- route：（详见 YAML 上方注释）
- only_loggers：（详见 YAML 上方注释）
- fields：（详见 YAML 上方注释）
- include：（详见 YAML 上方注释）
- localize_map：（详见 YAML 上方注释）
- type：（详见 YAML 上方注释）
- event：（详见 YAML 上方注释）
- params：（详见 YAML 上方注释）
- duration_ms：（详见 YAML 上方注释）
- resource_delta：（详见 YAML 上方注释）
- return：（详见 YAML 上方注释）
- sql：（详见 YAML 上方注释）
- rows：（详见 YAML 上方注释）
- error：（详见 YAML 上方注释）
- action：（详见 YAML 上方注释）
- status：（详见 YAML 上方注释）
- detail：（详见 YAML 上方注释）
- request_id：（详见 YAML 上方注释）
- trace_id：（详见 YAML 上方注释）
- user_id：（详见 YAML 上方注释）
- tenant：（详见 YAML 上方注释）
- slow：（详见 YAML 上方注释）
- result：（详见 YAML 上方注释）
- threadName：（详见 YAML 上方注释）
- window：（详见 YAML 上方注释）
- start：（详见 YAML 上方注释）
- end：（详见 YAML 上方注释）
- device_id：（详见 YAML 上方注释）
- run_id：（详见 YAML 上方注释）
- dedup_ratio：（详见 YAML 上方注释）
- sql_cost_ms：（详见 YAML 上方注释）
- rows_merged：（详见 YAML 上方注释）
- rows_deduped：（详见 YAML 上方注释）
- rows_input：（详见 YAML 上方注释）
- affected_rows：（详见 YAML 上方注释）
- stats：（详见 YAML 上方注释）
- flags：（详见 YAML 上方注释）
- codes：（详见 YAML 上方注释）
- diag_level：（详见 YAML 上方注释）
- pool_id：（详见 YAML 上方注释）
- use_count：（详见 YAML 上方注释）
- wait_ms：（详见 YAML 上方注释）
- total：（详见 YAML 上方注释）
- idle：（详见 YAML 上方注释）
- active：（详见 YAML 上方注释）
- age_ms：（详见 YAML 上方注释）
- idle_ms：（详见 YAML 上方注释）
- duration：（详见 YAML 上方注释）
- dsn_preview：（详见 YAML 上方注释）
- deps：（详见 YAML 上方注释）
- psutil：（详见 YAML 上方注释）
- colorlog：（详见 YAML 上方注释）
- concurrent_log_handler：（详见 YAML 上方注释）
- orjson：（详见 YAML 上方注释）
- palette：颜色名称→ANSI 映射（PipeFormatter 不再硬编码）
- grey：（详见 YAML 上方注释）
- gray：（详见 YAML 上方注释）
- green：（详见 YAML 上方注释）
- yellow：（详见 YAML 上方注释）
- red：（详见 YAML 上方注释）
- bright_red：（详见 YAML 上方注释）
- blue：（详见 YAML 上方注释）
- cyan：（详见 YAML 上方注释）
- magenta：（详见 YAML 上方注释）

## merge.yaml
- window：（详见 YAML 上方注释）
- size：窗口跨度（相对 now 或由 end 推导）。示例："30m"|"24h"|"7d"
- start：绝对开始时间（ISO8601，例如 "2025-09-01T00:00:00+08:00"）；null 表示由 size 与 end 推导
- end：绝对结束时间（ISO8601）；null 表示由调度器/CLI 决定或用 now；与 start 共同定义 [start,end)
- tz：（详见 YAML 上方注释）
- default_station_tz：默认站点时区（若元数据缺失则使用）；建议与 system.yaml 保持一致
- allow_missing_tz：当元数据缺失时是否允许继续；为 false 则报错/拒绝
- missing_tz_policy：缺失时区处理：default=使用 default_station_tz；utc=使用 UTC；reject=拒绝
- segmented：（详见 YAML 上方注释）
- enabled：是否按粒度对大窗分段执行，以降低内存/锁持有时间
- granularity：分段粒度：15m|30m|1h|2h|6h|12h|24h；建议能被 window.size 整除
- run_all：（详见 YAML 上方注释）
- prepare_dim：维表/元数据准备（站、设备、指标等），一般首次或增量运行需要
- create_staging：创建/刷新临时分区或中间表（便于高效合并）
- ingest_copy：原始数据装载/复制（从外部源或落盘区到数据库）
- merge_fact：将去重/对齐后的秒级数据合并到事实表
- device_running：识别设备运行/停机段（供后续规则使用，可按需关闭）
- device_running_cfg：（详见 YAML 上方注释）
- slice：day|week
- max_device_concurrency：设备并发
- update_only_when_changed：（详见 YAML 上方注释）
- force_recompute：（详见 YAML 上方注释）
- audit_granularity：day|week
- reset_logs：（详见 YAML 上方注释）
- reset_db：（详见 YAML 上方注释）
- reset_db_schemas：（详见 YAML 上方注释）
- presence：统计“秒级在场”（presence）指标与相关报表
- quality_mark：（详见 YAML 上方注释）
- anomaly_diagnosis：（详见 YAML 上方注释）
- level：诊断输出详略等级（由诊断模块支持的等级决定；常用 brief）
- quality_mark_codes_enabled：（详见 YAML 上方注释）

## system.yaml
- directories：（详见 YAML 上方注释）
- data：数据文件目录
- logs：日志文件目录
- configs：配置文件目录
- temp：临时文件目录
- backup：备份文件目录
- timezone：（详见 YAML 上方注释）
- default：默认时区
- storage：数据库存储时区
- display：显示时区
- system：（详见 YAML 上方注释）
- encoding：默认文件编码
- locale：系统语言环境
- max_workers：系统级别默认最大工作线程数

## web.yaml
- server：（详见 YAML 上方注释）
- host：服务器绑定地址
- port：服务器端口
- reload：开发模式下是否启用热重载
- workers：生产环境下的工作进程数
- api：（详见 YAML 上方注释）
- title：（详见 YAML 上方注释）
- description：（详见 YAML 上方注释）
- version：（详见 YAML 上方注释）
- docs_url：Swagger文档路径
- redoc_url：ReDoc文档路径
- minimal_window_minutes：测量接口最小默认时间窗口（分钟），用于控制默认查询范围
- app：（详见 YAML 上方注释）
- debug：调试模式
- log_requests：是否记录HTTP请求日志
- cors_enabled：是否启用CORS
- cors_origins：允许的跨域来源
- performance：（详见 YAML 上方注释）
- request_timeout：请求超时时间（秒）
- max_request_size：最大请求大小（16MB）
- keepalive_timeout：Keep-Alive超时时间（秒）

---

> 生成器：generate_project_wiki.py｜时间：2025-09-25 08:36
<!-- AUTO-WIKI:END:CONFIGS -->

## 敏感信息与脱敏
- 生产环境隐藏口令；日志禁止输出敏感字段。

## 环境差异
- 本地/测试/生产在连接、日志、调度上存在差异，需标注。

## 常见误区
- 代码中拼接 DSN/SQL；应改为读取配置+参数化。

## 变更记录
- 初始化骨架。

