# 方案8：参数历史记录和增量优化修复方案

**方案编号**：方案8
**创建日期**：2025-10-26
**最后更新**：2025-10-26
**版本**：v2.0
**优先级**：P0（最高优先级）
**预计工作量**：3-5天

**关联问题**（5个）：
- 问题1.4：参数优化器从未启用 ⭐⭐⭐
- 问题1.5：P矩阵每次都重新初始化 ⭐⭐⭐
- 问题1.6：P矩阵从未保存到数据库 ⭐⭐⭐
- 问题1.7：optimization_history表设计缺陷 ⭐⭐
- 问题1.8：calculation_parameters表设计缺陷 ⭐⭐

**修复目标**：
1. ✅ 扩展数据库表结构（添加P矩阵存储字段）
2. ✅ 修改optimize_parameters()方法（加载P矩阵）
3. ✅ 修改_save_optimization_result()方法（保存P矩阵）
4. ✅ 启用参数优化器（enable_optimization=True）
5. ✅ 实现增量优化（基于历史P矩阵）
6. ✅ 验证增量优化效果

---

## 1. 问题分析

### 1.1 核心问题

**用户需求**："优化参数每次优化不应该从0开始，需要基于历史记录进行增量优化"

**当前状态**：
- ❌ 每次优化都重新初始化协方差矩阵 P = np.eye(n) * 1000
- ❌ 协方差矩阵P从未保存到数据库
- ❌ 下次优化时无法加载上次的P矩阵
- ❌ 完全违背了RLS算法的增量学习特性

### 1.2 RLS算法原理

递归最小二乘法(RLS)是一种**在线学习算法**，核心特点：

**更新公式**：
```
K(k) = P(k-1) * φ(k) / [λ + φ(k)ᵀ * P(k-1) * φ(k)]  # 增益矩阵
θ(k) = θ(k-1) + K(k) * [y(k) - φ(k)ᵀ * θ(k-1)]      # 参数更新
P(k) = [P(k-1) - K(k) * φ(k)ᵀ * P(k-1)] / λ          # 协方差更新
```

**关键点**：
- P(k) **必须**基于 P(k-1) 更新
- P矩阵包含参数估计的不确定性信息
- 随着数据增多，P矩阵元素逐渐减小，参数估计越来越准确
- **如果每次都重置P，就失去了递归特性**

### 1.3 根本原因

1. **代码缺陷**：
   - `optimize_parameters()` 第129行：每次都重新初始化P
   - `_save_optimization_result()` 第652-760行：从未保存P矩阵
   - `load_parameters()` 第682-720行：从未加载P矩阵

2. **表结构缺陷**：
   - `optimization_history` 表缺少 `method_id` 字段
   - `optimization_history` 表缺少 `covariance_matrix` 字段
   - `calculation_parameters` 表缺少 `covariance_matrix` 字段

3. **功能未启用**：
   - 参数优化器在生产环境中默认禁用（`enable_optimization=False`）
   - 所有优化相关功能从未使用

---

## 2. 修复目标

### 2.1 核心目标

1. **实现增量优化**：
   - 每次优化基于上次的P矩阵
   - 参数估计的不确定性逐渐降低
   - 优化效果逐步提升

2. **完善历史记录**：
   - 保存每次优化的完整信息（θ, P, metrics）
   - 支持查询历史优化记录
   - 支持回滚到历史版本

3. **启用优化功能**：
   - 在测试环境验证功能
   - 在生产环境逐步启用
   - 建立监控和回滚机制

### 2.2 成功标准

1. **功能验证**：
   - ✅ 第2次优化使用第1次的P矩阵
   - ✅ P矩阵的trace逐渐减小（不确定性降低）
   - ✅ 参数估计的confidence逐渐提高
   - ✅ 优化效果（RMSE, R²）逐步改善

2. **数据验证**：
   - ✅ optimization_history 表有记录
   - ✅ calculation_parameters.covariance_matrix 字段有数据
   - ✅ 可以查询历史优化记录
   - ✅ 可以回滚到历史版本

---

## 3. 实施步骤

### 步骤1：扩展数据库表结构

#### 3.1.1 扩展 optimization_history 表

**目标**：添加 method_id 和 covariance_matrix 字段

**SQL脚本**：
```sql
-- 文件：migrations/add_optimization_history_fields.sql

-- 步骤1：添加 method_id 字段
ALTER TABLE optimization_history
ADD COLUMN method_id TEXT;

-- 步骤2：添加 covariance_matrix 字段（存储P矩阵）
ALTER TABLE optimization_history
ADD COLUMN covariance_matrix JSONB;

-- 步骤3：添加 rls_state 字段（存储RLS算法的其他状态）
ALTER TABLE optimization_history
ADD COLUMN rls_state JSONB;

-- 步骤4：为 method_id 添加索引
CREATE INDEX idx_oh_method_id ON optimization_history(method_id);

-- 步骤5：添加复合索引（常用查询组合）
CREATE INDEX idx_oh_device_method_time 
ON optimization_history(device_id, method_id, created_at DESC);

-- 步骤6：添加唯一约束（防止重复记录）
-- 注意：同一时刻同一设备同一方法只能有一条优化记录
ALTER TABLE optimization_history
ADD CONSTRAINT uq_oh_device_method_time 
UNIQUE (station_id, device_id, metric_key, method_id, created_at);

-- 步骤7：添加注释
COMMENT ON COLUMN optimization_history.method_id IS '计算方法ID（如 FLOW_COEF_V1）';
COMMENT ON COLUMN optimization_history.covariance_matrix IS 'RLS协方差矩阵P（JSONB格式，存储为二维数组）';
COMMENT ON COLUMN optimization_history.rls_state IS 'RLS算法的其他状态（如遗忘因子、迭代次数等）';
```

**验证SQL**：
```sql
-- 验证字段已添加
SELECT 
    column_name,
    data_type,
    is_nullable
FROM information_schema.columns
WHERE table_name = 'optimization_history'
    AND column_name IN ('method_id', 'covariance_matrix', 'rls_state')
ORDER BY ordinal_position;

-- 验证索引已创建
SELECT indexname, indexdef
FROM pg_indexes
WHERE tablename = 'optimization_history'
    AND indexname IN ('idx_oh_method_id', 'idx_oh_device_method_time');

-- 验证唯一约束已添加
SELECT constraint_name, constraint_type
FROM information_schema.table_constraints
WHERE table_name = 'optimization_history'
    AND constraint_name = 'uq_oh_device_method_time';
```

#### 3.1.2 扩展 calculation_parameters 表

**目标**：添加 covariance_matrix 字段

**SQL脚本**：
```sql
-- 文件：migrations/add_calculation_parameters_covariance.sql

-- 步骤1：添加 covariance_matrix 字段
ALTER TABLE calculation_parameters
ADD COLUMN covariance_matrix JSONB;

-- 步骤2：添加 rls_iterations 字段（记录RLS迭代次数）
ALTER TABLE calculation_parameters
ADD COLUMN rls_iterations INTEGER DEFAULT 0;

-- 步骤3：添加 last_p_trace 字段（记录P矩阵的迹，用于监控）
ALTER TABLE calculation_parameters
ADD COLUMN last_p_trace NUMERIC;

-- 步骤4：添加注释
COMMENT ON COLUMN calculation_parameters.covariance_matrix IS '当前的RLS协方差矩阵P（JSONB格式）';
COMMENT ON COLUMN calculation_parameters.rls_iterations IS 'RLS算法的累计迭代次数';
COMMENT ON COLUMN calculation_parameters.last_p_trace IS 'P矩阵的迹（trace），用于监控参数估计的不确定性';
```

**验证SQL**：
```sql
-- 验证字段已添加
SELECT 
    column_name,
    data_type,
    is_nullable,
    column_default
FROM information_schema.columns
WHERE table_name = 'calculation_parameters'
    AND column_name IN ('covariance_matrix', 'rls_iterations', 'last_p_trace')
ORDER BY ordinal_position;
```

---

### 步骤2：修改 parameter_optimizer.py

#### 3.2.1 修改 optimize_parameters() 方法

**目标**：加载上次优化的P矩阵，实现增量优化

**代码修改**：
```python
# 文件：app/services/calculation/parameter_optimizer.py
# 位置：第56-239行

def optimize_parameters(
    self,
    method_id: str,
    measured_data: Dict[str, np.ndarray],
    calculated_data: Dict[str, np.ndarray],
    current_params: Dict[str, float],
    param_bounds: Optional[Dict[str, Tuple[float, float]]] = None,
    device_id: Optional[int] = None,
    station_id: Optional[int] = None
) -> Tuple[Dict[str, float], Dict[str, Any]]:
    """优化参数"""
    
    logger.info("[流程-开始] [参数优化]", ...)
    
    # 验证输入数据
    self._validate_input_data(measured_data, calculated_data)
    
    # 构建观测矩阵和输出向量
    X, y = self._build_observation_matrix(measured_data, calculated_data, method_id)
    
    if X is None or y is None:
        raise DataValidationError("无法构建观测矩阵")
    
    # ========== 修改开始 ==========
    # 加载上次优化的协方差矩阵P（如果存在）
    P_previous, rls_iterations_previous = self._load_covariance_matrix(
        method_id=method_id,
        device_id=device_id,
        station_id=station_id,
        param_names=sorted(current_params.keys())
    )
    
    # 初始化协方差矩阵
    n_params = len(current_params)
    if P_previous is not None:
        # 使用上次优化的P矩阵（增量优化）
        P = P_previous
        logger.info(
            "[流程-阶段] [加载历史P矩阵]",
            extra={
                "extra_data": {
                    "method_id": method_id,
                    "device_id": device_id,
                    "P_trace": float(np.trace(P)),
                    "P_condition": float(np.linalg.cond(P)),
                    "previous_iterations": rls_iterations_previous,
                    "optimization_mode": "incremental"
                }
            }
        )
    else:
        # 首次优化，初始化P矩阵
        P = np.eye(n_params) * 1000
        rls_iterations_previous = 0
        logger.info(
            "[流程-阶段] [初始化P矩阵]",
            extra={
                "extra_data": {
                    "method_id": method_id,
                    "device_id": device_id,
                    "P_trace": float(np.trace(P)),
                    "optimization_mode": "first_time"
                }
            }
        )
    # ========== 修改结束 ==========
    
    # 将参数转换为向量
    param_names = sorted(current_params.keys())
    theta = np.array([current_params[name] for name in param_names])
    
    # 应用RLS算法
    logger.info("[流程-阶段] [开始RLS优化]", ...)
    theta_new, P_new, residuals = self.apply_rls(X, y, theta, P)
    
    # ========== 修改开始 ==========
    # 计算累计迭代次数
    rls_iterations_new = rls_iterations_previous + X.shape[0]
    # ========== 修改结束 ==========
    
    logger.info(
        "[流程-阶段] [RLS优化完成]",
        extra={
            "extra_data": {
                "optimized_params": theta_new.tolist(),
                "param_changes": {...},
                "residual_stats": {...},
                "covariance_stats": {
                    "trace": float(np.trace(P_new)),
                    "condition_number": float(np.linalg.cond(P_new)),
                    # ========== 修改开始 ==========
                    "trace_reduction": float(np.trace(P) - np.trace(P_new)) if P_previous is not None else None,
                    "total_iterations": rls_iterations_new
                    # ========== 修改结束 ==========
                }
            }
        }
    )
    
    # 应用参数约束
    if param_bounds:
        theta_new = self._apply_constraints(theta_new, param_names, param_bounds)
    
    # 验证参数合理性
    if not self._validate_parameters(theta_new, param_names, param_bounds):
        return current_params, {"success": False, "reason": "参数验证失败"}
    
    # 构建新参数字典
    new_params = {name: float(theta_new[i]) for i, name in enumerate(param_names)}
    
    # 计算优化效果指标
    metrics = self._calculate_metrics(y, X @ theta_new, residuals, P_new)
    
    # 计算置信度
    confidence = self._calculate_confidence(residuals, P_new)
    metrics['confidence'] = confidence
    
    # ========== 修改开始 ==========
    # 保存优化结果（包含P矩阵）
    self._save_optimization_result(
        method_id=method_id,
        old_params=current_params,
        new_params=new_params,
        metrics=metrics,
        device_id=device_id,
        station_id=station_id,
        P_matrix=P_new,  # 新增参数
        rls_iterations=rls_iterations_new  # 新增参数
    )
    # ========== 修改结束 ==========
    
    logger.info("[流程-完成] [参数优化完成]", ...)
    
    return new_params, metrics
```

#### 3.2.2 添加 _load_covariance_matrix() 方法

**目标**：从数据库加载上次优化的P矩阵

**代码实现**：
```python
# 文件：app/services/calculation/parameter_optimizer.py
# 位置：新增方法（在 _save_optimization_result 之前）

def _load_covariance_matrix(
    self,
    method_id: str,
    device_id: Optional[int],
    station_id: Optional[int],
    param_names: List[str]
) -> Tuple[Optional[np.ndarray], int]:
    """
    加载上次优化的协方差矩阵P
    
    Args:
        method_id: 计算方法ID
        device_id: 设备ID（可选）
        station_id: 站点ID（可选）
        param_names: 参数名称列表（已排序）
    
    Returns:
        (P矩阵, RLS迭代次数)，如果没有历史记录则返回 (None, 0)
    """
    try:
        with get_connection() as conn:
            with conn.cursor() as cur:
                # 查询最新的优化记录
                query = """
                    SELECT 
                        covariance_matrix,
                        rls_state
                    FROM optimization_history
                    WHERE method_id = %s
                      AND (device_id IS NOT DISTINCT FROM %s)
                      AND (station_id IS NOT DISTINCT FROM %s)
                    ORDER BY created_at DESC
                    LIMIT 1
                """
                cur.execute(query, (method_id, device_id, station_id))
                row = cur.fetchone()
                
                if row and row[0] is not None:
                    # 解析P矩阵（从JSONB转换为numpy数组）
                    P_list = row[0]  # JSONB自动解析为Python list
                    P = np.array(P_list, dtype=float)
                    
                    # 验证P矩阵的维度
                    n_params = len(param_names)
                    if P.shape != (n_params, n_params):
                        logger.warning(
                            f"P矩阵维度不匹配: 期望 ({n_params}, {n_params}), 实际 {P.shape}",
                            extra={
                                "method_id": method_id,
                                "device_id": device_id,
                                "expected_shape": (n_params, n_params),
                                "actual_shape": P.shape
                            }
                        )
                        return None, 0
                    
                    # 解析RLS状态
                    rls_iterations = 0
                    if row[1] is not None:
                        rls_state = row[1]
                        rls_iterations = rls_state.get('iterations', 0)
                    
                    logger.info(
                        f"成功加载历史P矩阵",
                        extra={
                            "method_id": method_id,
                            "device_id": device_id,
                            "P_trace": float(np.trace(P)),
                            "P_condition": float(np.linalg.cond(P)),
                            "rls_iterations": rls_iterations
                        }
                    )
                    
                    return P, rls_iterations
                else:
                    logger.info(
                        f"未找到历史P矩阵，将使用初始值",
                        extra={"method_id": method_id, "device_id": device_id}
                    )
                    return None, 0
                    
    except Exception as e:
        logger.error(f"加载P矩阵失败: {e}", exc_info=True)
        return None, 0
```

---

### 步骤3：修改 _save_optimization_result() 方法

**目标**：保存P矩阵到数据库，并插入 optimization_history 表

**代码修改**：
```python
# 文件：app/services/calculation/parameter_optimizer.py
# 位置：第652-760行（完全重写）

def _save_optimization_result(
    self,
    method_id: str,
    old_params: Dict[str, float],
    new_params: Dict[str, float],
    metrics: Dict[str, Any],
    device_id: Optional[int] = None,
    station_id: Optional[int] = None,
    P_matrix: Optional[np.ndarray] = None,  # 新增参数
    rls_iterations: int = 0  # 新增参数
) -> None:
    """保存优化结果到数据库"""
    try:
        with get_connection() as conn:
            with conn.cursor() as cur:
                import json

                # ========== 新增：保存到 optimization_history 表 ==========
                if P_matrix is not None:
                    # 将P矩阵转换为JSONB格式（二维数组）
                    P_list = P_matrix.tolist()

                    # 构建RLS状态
                    rls_state = {
                        'iterations': rls_iterations,
                        'forgetting_factor': self.forgetting_factor,
                        'timestamp': datetime.now().isoformat()
                    }

                    # 插入 optimization_history 表
                    # 注意：需要从 metrics 中获取 metric_key
                    # 这里假设 method_id 包含 metric_key 信息，或者需要从外部传入
                    # 为了简化，我们从 method_id 推断 metric_key
                    # 例如：FLOW_COEF_V1 -> pump_flow_rate
                    metric_key = self._infer_metric_key_from_method_id(method_id)

                    insert_query = """
                        INSERT INTO optimization_history (
                            station_id,
                            device_id,
                            metric_key,
                            method_id,
                            optimization_type,
                            params_before,
                            params_after,
                            improvement_score,
                            data_window_start,
                            data_window_end,
                            data_points_count,
                            covariance_matrix,
                            rls_state,
                            created_at,
                            created_by
                        ) VALUES (
                            %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, NOW(), 'optimizer'
                        )
                    """

                    cur.execute(
                        insert_query,
                        (
                            station_id,
                            device_id,
                            metric_key,
                            method_id,
                            'RLS',  # optimization_type
                            json.dumps(old_params),  # params_before
                            json.dumps(new_params),  # params_after
                            metrics.get('r2'),  # improvement_score（使用R²作为改进分数）
                            datetime.now() - timedelta(hours=1),  # data_window_start（示例）
                            datetime.now(),  # data_window_end
                            metrics.get('n_samples', 0),  # data_points_count
                            json.dumps(P_list),  # covariance_matrix
                            json.dumps(rls_state)  # rls_state
                        )
                    )

                    logger.info(
                        f"已保存优化历史到 optimization_history 表",
                        extra={
                            "method_id": method_id,
                            "device_id": device_id,
                            "metric_key": metric_key,
                            "P_trace": float(np.trace(P_matrix))
                        }
                    )
                # ========== 新增结束 ==========

                # 构建优化历史记录（用于JSONB字段）
                history_entry = {
                    'timestamp': datetime.now().isoformat(),
                    'old_params': old_params,
                    'new_params': new_params,
                    'metrics': metrics
                }
                history_json = json.dumps(history_entry)

                # 更新每个参数
                for param_name, new_value in new_params.items():
                    # 构建单个参数的历史记录
                    param_history_entry = {
                        'timestamp': datetime.now().isoformat(),
                        'old_value': old_params.get(param_name),
                        'new_value': new_value,
                        'metrics': metrics
                    }
                    param_history_json = json.dumps(param_history_entry)

                    # ========== 修改：同时更新 covariance_matrix 字段 ==========
                    update_query = """
                        UPDATE calculation_parameters
                        SET
                            param_value = %s,
                            confidence_score = %s,
                            last_optimized_at = NOW(),
                            optimization_count = COALESCE(optimization_count, 0) + 1,
                            optimization_history = (
                                CASE
                                    WHEN jsonb_array_length(COALESCE(optimization_history, '[]'::jsonb)) >= 10
                                    THEN (
                                        (SELECT jsonb_agg(elem)
                                         FROM (
                                             SELECT elem
                                             FROM jsonb_array_elements(COALESCE(optimization_history, '[]'::jsonb)) elem
                                             OFFSET 1
                                         ) sub)
                                        || %s::jsonb
                                    )
                                    ELSE COALESCE(optimization_history, '[]'::jsonb) || %s::jsonb
                                END
                            ),
                            covariance_matrix = %s,
                            rls_iterations = %s,
                            last_p_trace = %s,
                            updated_at = NOW(),
                            updated_by = 'optimizer'
                        WHERE method_id = %s
                          AND param_name = %s
                          AND (device_id IS NOT DISTINCT FROM %s)
                          AND (station_id IS NOT DISTINCT FROM %s)
                    """

                    # 准备P矩阵的JSONB（所有参数共享同一个P矩阵）
                    P_json = json.dumps(P_matrix.tolist()) if P_matrix is not None else None
                    P_trace = float(np.trace(P_matrix)) if P_matrix is not None else None

                    cur.execute(
                        update_query,
                        (
                            new_value,
                            metrics.get('confidence', 0.5),
                            param_history_json,
                            param_history_json,
                            P_json,  # 新增
                            rls_iterations,  # 新增
                            P_trace,  # 新增
                            method_id,
                            param_name,
                            device_id,
                            station_id
                        )
                    )
                    # ========== 修改结束 ==========

                    if cur.rowcount > 0:
                        logger.info(
                            f"参数已更新: {param_name} = {new_value}",
                            extra={
                                "method_id": method_id,
                                "param_name": param_name,
                                "old_value": old_params.get(param_name),
                                "new_value": new_value,
                                "confidence": metrics.get('confidence', 0.5),
                                "P_trace": P_trace
                            }
                        )
                    else:
                        logger.warning(
                            f"参数更新失败，记录不存在: {param_name}",
                            extra={
                                "method_id": method_id,
                                "param_name": param_name,
                                "device_id": device_id,
                                "station_id": station_id
                            }
                        )

                conn.commit()

    except Exception as e:
        logger.error(f"保存优化结果失败: {e}", exc_info=True)
        raise DatabaseError(f"保存优化结果失败: {e}") from e


def _infer_metric_key_from_method_id(self, method_id: str) -> str:
    """从 method_id 推断 metric_key"""
    # 简单的映射规则（可以从数据库查询）
    method_to_metric = {
        'FLOW_COEF_V1': 'pump_flow_rate',
        'FLOW_SIMPLE_V1': 'pump_flow_rate',
        'HEAD_COEF_V1': 'pump_head',
        'HEAD_SIMPLE_V1': 'pump_head',
        'EFF_SIMPLE_V1': 'pump_efficiency',
        'PIN_COEF_V1': 'pump_input_power',
        # ... 其他映射
    }
    return method_to_metric.get(method_id, 'unknown')
```

---

## 4. 测试用例

### 4.1 单元测试

#### 测试1：验证P矩阵的保存和加载

```python
# 文件：tests/unit/services/calculation/test_parameter_optimizer_incremental.py

import pytest
import numpy as np
from app.services.calculation.parameter_optimizer import ParameterOptimizer

class TestIncrementalOptimization:
    """增量优化测试"""

    def test_save_and_load_covariance_matrix(self):
        """测试P矩阵的保存和加载"""
        optimizer = ParameterOptimizer(forgetting_factor=0.98)

        # 准备测试数据
        method_id = 'FLOW_COEF_V1'
        device_id = 1
        station_id = 1
        param_names = ['a0', 'a1', 'a2']

        # 创建一个测试P矩阵
        P_original = np.array([
            [100.0, 10.0, 5.0],
            [10.0, 50.0, 2.0],
            [5.0, 2.0, 30.0]
        ])

        # 保存P矩阵（通过 _save_optimization_result）
        old_params = {'a0': 1.0, 'a1': 2.0, 'a2': 3.0}
        new_params = {'a0': 1.1, 'a1': 2.1, 'a2': 3.1}
        metrics = {'rmse': 0.5, 'r2': 0.95, 'confidence': 0.9}

        optimizer._save_optimization_result(
            method_id=method_id,
            old_params=old_params,
            new_params=new_params,
            metrics=metrics,
            device_id=device_id,
            station_id=station_id,
            P_matrix=P_original,
            rls_iterations=100
        )

        # 加载P矩阵
        P_loaded, iterations = optimizer._load_covariance_matrix(
            method_id=method_id,
            device_id=device_id,
            station_id=station_id,
            param_names=param_names
        )

        # 验证
        assert P_loaded is not None, "P矩阵应该被成功加载"
        assert iterations == 100, "RLS迭代次数应该是100"
        np.testing.assert_array_almost_equal(P_loaded, P_original, decimal=6)

    def test_incremental_optimization_reduces_p_trace(self):
        """测试增量优化会降低P矩阵的迹"""
        optimizer = ParameterOptimizer(forgetting_factor=0.98)

        # 准备测试数据
        method_id = 'FLOW_COEF_V1'
        device_id = 1
        station_id = 1

        # 第1次优化
        measured_data_1 = {'flow': np.array([1.0, 2.0, 3.0])}
        calculated_data_1 = {'flow': np.array([1.1, 2.1, 3.1])}
        current_params_1 = {'a0': 1.0, 'a1': 2.0}

        new_params_1, metrics_1 = optimizer.optimize_parameters(
            method_id=method_id,
            measured_data=measured_data_1,
            calculated_data=calculated_data_1,
            current_params=current_params_1,
            device_id=device_id,
            station_id=station_id
        )

        # 获取第1次优化后的P矩阵的迹
        P_1, _ = optimizer._load_covariance_matrix(
            method_id=method_id,
            device_id=device_id,
            station_id=station_id,
            param_names=['a0', 'a1']
        )
        trace_1 = np.trace(P_1)

        # 第2次优化（使用第1次的P矩阵）
        measured_data_2 = {'flow': np.array([4.0, 5.0, 6.0])}
        calculated_data_2 = {'flow': np.array([4.1, 5.1, 6.1])}

        new_params_2, metrics_2 = optimizer.optimize_parameters(
            method_id=method_id,
            measured_data=measured_data_2,
            calculated_data=calculated_data_2,
            current_params=new_params_1,  # 使用第1次的结果
            device_id=device_id,
            station_id=station_id
        )

        # 获取第2次优化后的P矩阵的迹
        P_2, _ = optimizer._load_covariance_matrix(
            method_id=method_id,
            device_id=device_id,
            station_id=station_id,
            param_names=['a0', 'a1']
        )
        trace_2 = np.trace(P_2)

        # 验证：第2次的trace应该小于第1次（不确定性降低）
        assert trace_2 < trace_1, f"增量优化应该降低P矩阵的迹: {trace_2} < {trace_1}"
```

---

## 5. 风险评估

### 5.1 技术风险

| 风险ID | 风险描述 | 概率 | 影响 | 缓解措施 |
|-------|---------|------|------|---------|
| R1 | P矩阵过大导致数据库性能问题 | 中 | 中 | 使用JSONB压缩，定期清理旧记录 |
| R2 | P矩阵数值不稳定（条件数过大） | 中 | 高 | 添加条件数监控，超过阈值时重置P |
| R3 | 参数优化导致计算结果不稳定 | 低 | 高 | 先在测试环境验证，逐步启用 |
| R4 | 历史记录表增长过快 | 低 | 中 | 设置保留策略（如保留最近100次） |

### 5.2 业务风险

| 风险ID | 风险描述 | 概率 | 影响 | 缓解措施 |
|-------|---------|------|------|---------|
| B1 | 优化后的参数不如默认参数 | 中 | 中 | 添加回滚功能，监控优化效果 |
| B2 | 用户不理解增量优化的原理 | 高 | 低 | 编写用户文档，提供可视化监控 |
| B3 | 生产环境启用后出现问题 | 低 | 高 | 灰度发布，先在单个设备试运行 |

---

## 6. 回滚方案

### 6.1 数据库回滚

```sql
-- 回滚步骤1：删除新增的字段
ALTER TABLE optimization_history
DROP COLUMN IF EXISTS method_id,
DROP COLUMN IF EXISTS covariance_matrix,
DROP COLUMN IF EXISTS rls_state;

ALTER TABLE calculation_parameters
DROP COLUMN IF EXISTS covariance_matrix,
DROP COLUMN IF EXISTS rls_iterations,
DROP COLUMN IF EXISTS last_p_trace;

-- 回滚步骤2：删除新增的索引
DROP INDEX IF EXISTS idx_oh_method_id;
DROP INDEX IF EXISTS idx_oh_device_method_time;

-- 回滚步骤3：删除唯一约束
ALTER TABLE optimization_history
DROP CONSTRAINT IF EXISTS uq_oh_device_method_time;
```

### 6.2 代码回滚

1. 恢复 `optimize_parameters()` 方法到原始版本（重新初始化P）
2. 恢复 `_save_optimization_result()` 方法到原始版本（不保存P）
3. 删除 `_load_covariance_matrix()` 方法

---

## 7. 监控指标

### 7.1 关键指标

1. **P矩阵的迹（trace）**：
   - 监控：每次优化后记录
   - 预期：逐渐减小
   - 告警：突然增大或超过初始值

2. **参数置信度（confidence）**：
   - 监控：每次优化后记录
   - 预期：逐渐提高
   - 告警：低于0.5或突然下降

3. **优化效果（R²）**：
   - 监控：每次优化后记录
   - 预期：逐渐提高
   - 告警：低于0.8或突然下降

4. **RLS迭代次数**：
   - 监控：累计迭代次数
   - 预期：持续增长
   - 告警：长时间不增长（说明优化未运行）

---

**方案结束**

