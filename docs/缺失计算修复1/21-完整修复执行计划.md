# 完整修复执行计划

## 📋 计划概述

**目标**：确保运行中的水泵（设备179, 181）的所有缺失指标都能成功计算

**发现的问题**：
1. **P0级问题**：method_selector.py 的 check_conditions 方法逻辑错误，导致所有包含计算参数的方法都被拒绝
2. **P1级问题**：pump_inlet_pressure 没有计算方法注册到数据库

**修复优先级**：
- 优先修复 P0 问题（影响 pump_speed, pump_torque, main_pipeline_inlet_pressure 等多个指标）
- 然后修复 P1 问题（影响 pump_inlet_pressure 及其下游指标）

---

## 🔧 修复步骤

### 步骤1：修复 check_conditions 方法（P0）

**文件**：`app/services/calculation/method_selector.py`

**修改位置**：lines 192-244

**修改内容**：
在 line 198 之后添加：
```python
# 跳过数值型计算参数（这些参数应该从 calculation_parameters 表读取，不是条件）
if isinstance(value, (int, float)) and key not in ['min_running_pumps', 'max_running_pumps']:
    continue
```

**完整修改后的代码**：
```python
def check_conditions(
    self,
    conditions: Dict[str, Any],
    context: Dict[str, Any]
) -> bool:
    """
    检查条件是否满足

    Args:
        conditions: 方法所需的条件字典
        context: 当前上下文字典

    Returns:
        True 如果所有条件都满足，否则 False
    """
    if not conditions:
        return True

    for key, value in conditions.items():
        # 跳过描述性字段
        if key in ['description', 'note', 'comment']:
            continue
        
        # ✅ 新增：跳过数值型计算参数
        # 这些参数应该从 calculation_parameters 表读取，不是条件
        if isinstance(value, (int, float)) and key not in ['min_running_pumps', 'max_running_pumps']:
            continue

        # 处理特殊的布尔条件（如 requires_cumulative_flow, requires_training）
        if key.startswith('requires_') or key.startswith('has_'):
            # 这些条件需要在context中明确设置为True
            if not context.get(key, False):
                return False
            continue

        # 处理 min_running_pumps 条件
        if key == 'min_running_pumps':
            running_count = context.get('running_count', 0)
            if running_count < value:
                return False
            continue

        # 处理 max_running_pumps 条件
        if key == 'max_running_pumps':
            running_count = context.get('running_count', 0)
            if running_count > value:
                return False
            continue

        # 处理其他条件
        if key not in context:
            return False

        context_value = context[key]

        # 处理不同类型的条件
        if isinstance(value, dict):
            # 复杂条件（如 {'exact': 1}, {'min': 2}, {'max': 10}）
            if 'exact' in value:
                if context_value != value['exact']:
                    return False
            if 'min' in value:
                if context_value < value['min']:
                    return False
            if 'max' in value:
                if context_value > value['max']:
                    return False
        else:
            # 简单条件（直接比较）
            if context_value != value:
                return False

    return True
```

**验证方法**：
1. 使用 diagnostics 工具检查语法
2. 重启服务
3. 运行计算任务，查看日志中 pump_speed, pump_torque, main_pipeline_inlet_pressure 是否成功计算

**预期结果**：
- ✅ pump_speed 成功计算（方法A，从 pump_frequency）
- ✅ pump_torque 成功计算（方法B，从 pump_active_power + pump_frequency）
- ✅ main_pipeline_inlet_pressure 成功计算（方法B，从 pool_liquid_level）

---

### 步骤2：注册 pump_inlet_pressure 计算方法（P1）

**文件**：
1. `scripts/sql/calculation/fix_1.1_step1_register_methods.sql`
2. `scripts/sql/calculation/fix_1.1_step2_update_metric_order.sql`

**修改内容**：

**修改 fix_1.1_step1_register_methods.sql**：
调整方法优先级以避免循环依赖：
```sql
-- 方法B：从 pool_liquid_level 推导（优先级最高）
priority = 100  -- 改为最高优先级

-- 方法A：从 main_pipeline_inlet_pressure 推导（备选）
priority = 90   -- 降低优先级
```

**修改 fix_1.1_step2_update_metric_order.sql**：
```sql
-- 不需要手动设置 depends_on！
-- 系统会自动调用 get_dependencies() 生成
-- 因为方法B优先级最高，depends_on 会自动变成 ['pool_liquid_level']
```

**执行步骤**：
1. 修改 SQL 脚本
2. 执行 `fix_1.1_step1_register_methods.sql`
3. 执行 `fix_1.1_step2_update_metric_order.sql`
4. 验证数据库中的记录

**验证方法**：
```sql
-- 验证方法注册
SELECT method_code, priority, dependencies 
FROM calculation_method_registry 
WHERE metric_key = 'pump_inlet_pressure'
ORDER BY priority DESC;

-- 验证计算顺序
SELECT metric_key, depends_on, order_index 
FROM metric_calculation_order 
WHERE metric_key = 'pump_inlet_pressure';
```

**预期结果**：
- ✅ pump_inlet_pressure 有2个方法注册
- ✅ 方法B优先级100，依赖 pool_liquid_level
- ✅ 方法A优先级90，依赖 main_pipeline_inlet_pressure
- ✅ metric_calculation_order 中 depends_on = ['pool_liquid_level']

---

### 步骤3：运行完整计算并验证

**执行命令**：
```python
from app.services.calculation.missing_metrics_batch import MissingMetricsBatchCalculator

calculator = MissingMetricsBatchCalculator()
result = calculator.calculate_missing_metrics_for_station(
    station_id=23,
    start_time='2025-05-31T18:00:00+00:00',
    end_time='2025-05-31T20:00:00+00:00',
    write_to_db=True
)
```

**验证方法**：
```sql
-- 验证设备179的所有指标数据
SELECT 
    dmc.metric_key,
    COUNT(*) as record_count,
    MIN(fm.value) as min_value,
    MAX(fm.value) as max_value,
    AVG(fm.value) as avg_value
FROM fact_measurements fm
JOIN dim_metric_config dmc ON fm.metric_id = dmc.id
WHERE fm.station_id = 23
  AND fm.device_id = '179'
  AND fm.ts_bucket >= '2025-05-31T18:00:00+00:00'::timestamptz
  AND fm.ts_bucket < '2025-05-31T20:00:00+00:00'::timestamptz
  AND dmc.metric_key IN (
    'pump_inlet_pressure',
    'pump_head',
    'pump_speed',
    'pump_torque',
    'pump_efficiency',
    'pump_cumulative_flow',
    'main_pipeline_inlet_pressure',
    'main_pipeline_outlet_pressure'
  )
GROUP BY dmc.metric_key
ORDER BY dmc.metric_key;
```

**预期结果**：
| 指标 | 记录数 | 预期来源 |
|------|--------|----------|
| pump_inlet_pressure | 7200 | 方法B（pool_liquid_level） |
| pump_head | 7200 | 方法A/B（pump_outlet_pressure - pump_inlet_pressure） |
| pump_speed | 7200 | 方法A（pump_frequency） |
| pump_torque | 7200 | 方法B（pump_active_power + pump_frequency） |
| pump_efficiency | 3999 | 方法A（pump_flow_rate + pump_head + pump_active_power） |
| pump_cumulative_flow | 3999 | 方法A（pump_flow_rate 积分） |
| main_pipeline_inlet_pressure | 7200 | 方法B（pool_liquid_level） |
| main_pipeline_outlet_pressure | 7200 | 方法A（pump_head + main_pipeline_inlet_pressure） |

---

## 📊 修复效果预测

### 修复前（当前状态）

**设备179**：
- ✅ pump_flow_rate：3999条
- ✅ pump_outlet_pressure：7200条
- ❌ pump_inlet_pressure：0条
- ❌ pump_head：0条
- ❌ pump_speed：0条
- ❌ pump_torque：0条
- ❌ pump_efficiency：0条
- ✅ pump_cumulative_flow：3999条（已修复）
- ❌ main_pipeline_inlet_pressure：0条
- ❌ main_pipeline_outlet_pressure：0条

**成功率**：3/10 = 30%

### 修复后（预期）

**设备179**：
- ✅ pump_flow_rate：3999条
- ✅ pump_outlet_pressure：7200条
- ✅ pump_inlet_pressure：7200条
- ✅ pump_head：7200条
- ✅ pump_speed：7200条
- ✅ pump_torque：7200条
- ✅ pump_efficiency：3999条
- ✅ pump_cumulative_flow：3999条
- ✅ main_pipeline_inlet_pressure：7200条
- ✅ main_pipeline_outlet_pressure：7200条

**成功率**：10/10 = 100%

---

## ⚠️ 风险评估

### 风险1：check_conditions 修改影响其他指标

**风险等级**：低

**原因**：
- 修改只是跳过数值型参数，不影响真正的条件检查
- 真正的条件（min_running_pumps, requires_xxx 等）仍然会被检查

**缓解措施**：
- 在测试环境先验证
- 查看日志确认没有其他指标受影响

### 风险2：pump_inlet_pressure 循环依赖

**风险等级**：低

**原因**：
- 已调整优先级，方法B（pool_liquid_level）优先级最高
- 系统有循环依赖处理机制

**缓解措施**：
- 验证 metric_calculation_order 中的 depends_on
- 查看日志确认没有循环依赖警告

### 风险3：数据质量问题

**风险等级**：低

**原因**：
- 所有依赖数据都已验证可用
- 数据质量正常（pump_frequency: 49.14-49.22 Hz, pool_liquid_level: 3.56-3.59 m）

**缓解措施**：
- 计算后验证数据范围合理性
- 对比传感器数据（如果有）

---

## 📝 执行检查清单

### 步骤1：修复 check_conditions

- [ ] 修改 `app/services/calculation/method_selector.py`
- [ ] 使用 diagnostics 检查语法
- [ ] 重启服务
- [ ] 运行测试计算
- [ ] 查看日志验证 pump_speed, pump_torque, main_pipeline_inlet_pressure 成功计算

### 步骤2：注册 pump_inlet_pressure

- [ ] 修改 `fix_1.1_step1_register_methods.sql`（调整优先级）
- [ ] 修改 `fix_1.1_step2_update_metric_order.sql`（删除手动设置的 depends_on）
- [ ] 执行 SQL 脚本
- [ ] 验证数据库记录

### 步骤3：完整验证

- [ ] 运行完整计算任务
- [ ] 查询数据库验证所有指标数据
- [ ] 检查日志确认没有错误
- [ ] 验证数据范围合理性

---

**制定人员**: AI Agent  
**制定时间**: 2025-10-27  
**文档版本**: v1.0

