# 外键约束和数据管理机制审查报告

## 📋 文档信息

**分析日期**：2025-10-27  
**分析范围**：19个配置和历史数据表的外键约束状态和数据管理机制  
**文档版本**：v1.0  
**状态**：✅ 审查完成

---

## 🔍 审查范围

### 审查的19个表

1. optimization_history
2. calculation_parameters
3. calculation_validation_config
4. device_rated_params
5. device_running_thresholds
6. device_running_thresholds_shadow
7. dim_device_capabilities
8. dim_metric_metadata_override
9. metric_anomaly_strategy
10. metric_quality_rules
11. metric_quality_rules_shadow
12. metric_rule_auto_baseline
13. metric_rule_auto_baseline_shadow
14. pump_characteristic_curves
15. device_metric_candidates
16. calculation_method_registry
17. metric_calculation_order
18. metric_capability_policy
19. dim_metric_metadata

---

## 📊 核心发现

### 关键统计

| 维度 | 数量 | 说明 |
|------|------|------|
| **总表数** | 19个 | 审查范围内的所有表 |
| **配置数据表** | 16个 | 影响程序如何运行的参数和规则 |
| **历史数据表** | 1个 | 记录程序运行结果的数据 |
| **Shadow表** | 2个 | 影子输出表（用于对比和验证） |
| **已有完整外键的表** | 10个 | 所有ID列都有外键约束 |
| **部分缺少外键的表** | 6个 | 某些ID列缺少外键约束 |
| **完全无外键的表** | 1个 | 历史数据表（合理） |
| **已纳入备份机制的表** | 17个 | 在 prepare_dim 中被备份 |
| **未纳入备份机制的表** | 2个 | 需要补充 |

---

## 📋 完整表分类清单

### 分类1：配置数据表（已有完整外键）- 10个

| 表名 | 包含的ID列 | 外键状态 | 记录数 | 备份状态 | 说明 |
|------|-----------|---------|--------|---------|------|
| **calculation_parameters** | station_id, device_id, metric_key | ✅ 全部有外键CASCADE | 38 | ✅ 已备份 | 计算参数表 |
| **device_rated_params** | device_id | ✅ 有外键CASCADE | 46 | ✅ 已备份 | 设备额定参数表 |
| **device_running_thresholds_shadow** | device_id | ✅ 有外键CASCADE | 0 | ✅ 已备份 | 运行阈值影子表 |
| **dim_device_capabilities** | device_id | ✅ 有外键CASCADE | 5 | ✅ 已备份 | 设备能力表 |
| **dim_metric_metadata_override** | station_id, device_id, metric_id | ✅ 全部有外键CASCADE | 0 | ✅ 已备份 | 指标元数据覆盖表 |
| **metric_quality_rules_shadow** | station_id, device_id, metric_id | ✅ 全部有外键CASCADE | 54 | ✅ 已备份 | 质量规则影子表 |
| **metric_rule_auto_baseline_shadow** | station_id, device_id, metric_id | ✅ 全部有外键CASCADE | 54 | ✅ 已备份 | 自动基线影子表 |
| **pump_characteristic_curves** | device_id | ✅ 有外键CASCADE | 0 | ✅ 已备份 | 泵特性曲线表 |
| **calculation_method_registry** | metric_key | ✅ 有外键CASCADE | 28 | ✅ 已备份 | 计算方法注册表 |
| **metric_calculation_order** | metric_key | ✅ 有外键CASCADE | 21 | ✅ 已备份 | 指标计算顺序表 |
| **dim_metric_metadata** | metric_id | ✅ 有外键CASCADE | 0 | ❌ 未备份 | 指标元数据表 |

**小计**：10个表，外键约束完整，无风险

---

### 分类2：配置数据表（部分缺少外键）- 6个

| 表名 | 包含的ID列 | 外键状态 | 记录数 | 优先级 | 备份状态 | 风险说明 |
|------|-----------|---------|--------|--------|---------|---------|
| **calculation_validation_config** | station_id✅, device_id✅, metric_key❌ | 部分缺失 | 50 | **P0** | ✅ 已备份 | **metric_key无外键** |
| **device_running_thresholds** | device_id❌ | 缺失 | 8 | **P0** | ✅ 已备份 | **device_id无外键** |
| **metric_anomaly_strategy** | station_id❌, device_id❌, metric_id❌ | 全部缺失 | 0 | **P1** | ❌ 未备份 | **所有ID列无外键** |
| **metric_quality_rules** | station_id❌, device_id❌, metric_id❌ | 全部缺失 | 0 | **P1** | ✅ 已备份 | **所有ID列无外键** |
| **metric_rule_auto_baseline** | station_id❌, device_id❌, metric_id❌ | 全部缺失 | 0 | **P1** | ✅ 已备份 | **所有ID列无外键** |
| **metric_capability_policy** | metric_key❌ | 缺失 | 53 | **P1** | ✅ 已备份 | **metric_key无外键** |
| **device_metric_candidates** | device_id❌ | 缺失 | 0 | **P2** | ❌ 未备份 | **device_id无外键** |

**小计**：6个表，部分或全部缺少外键约束，需要修复

---

### 分类3：历史数据表（不需要外键）- 1个

| 表名 | 包含的ID列 | 外键状态 | 记录数 | 备份状态 | 说明 |
|------|-----------|---------|--------|---------|------|
| **optimization_history** | station_id✅, device_id✅, metric_key❌ | 部分有外键 | 2 | ❌ 未备份 | 优化历史记录表（历史数据） |

**分类依据**：
- **表用途**：记录参数优化的历史记录（params_before, params_after, improvement_score）
- **代码使用证据**：
  - `app/services/calculation/parameter_optimizer.py::_load_covariance_matrix()` - 加载历史优化记录
  - `app/services/calculation/parameter_optimizer.py::_save_optimization_result()` - 保存优化结果
- **为什么不需要外键**：
  - ✅ 历史数据应保留历史ID，即使设备/指标ID变化
  - ✅ 用于追溯历史优化过程，不影响程序运行逻辑
  - ✅ 已有 device_id 和 station_id 的外键（CASCADE），但 metric_key 不需要外键

**小计**：1个表，历史数据表，不需要为 metric_key 添加外键

---

### 分类4：Shadow表（已有完整外键）- 2个

| 表名 | 包含的ID列 | 外键状态 | 记录数 | 备份状态 | 说明 |
|------|-----------|---------|--------|---------|------|
| **metric_quality_rules_shadow** | station_id, device_id, metric_id | ✅ 全部有外键CASCADE | 54 | ✅ 已备份 | 影子输出：方案B质量规则参数 |
| **metric_rule_auto_baseline_shadow** | station_id, device_id, metric_id | ✅ 全部有外键CASCADE | 54 | ✅ 已备份 | 影子输出：方案B自动基线 |

**小计**：2个表，外键约束完整，无风险

---

## 🚨 需要立即处理的问题（P0）

### 1. calculation_validation_config - 计算结果验证配置表

**当前状态**：
- ✅ station_id → dim_stations.id（CASCADE）
- ✅ device_id → dim_devices.id（CASCADE）
- ❌ metric_key → dim_metric_config.metric_key（**无外键**）

**风险等级**：**极高**

**业务影响**：
- ❌ 如果 metric_key 变化，程序无法验证计算结果
- ❌ 错误的计算结果可能被接受
- ❌ 数据质量无法保证

**代码使用证据**：
- `app/services/calculation/validator.py::Validator._load_validation_config()` - 加载验证配置

**备份状态**：✅ 已纳入 prepare_dim 备份机制

---

### 2. device_running_thresholds - 设备运行阈值表

**当前状态**：
- ❌ device_id → dim_devices.id（**无外键**）

**风险等级**：**极高**

**业务影响**：
- ❌ 如果 device_id 变化，程序无法判断设备运行状态
- ❌ 运行状态判定失效
- ❌ 设备运行分析失效

**代码使用证据**：
- `app/services/rules/running_thresholds.py::run_running_thresholds()` - 刷新运行阈值
- `app/adapters/db/device_running_sql.py::list_default_device_ids()` - 查询有阈值配置的设备

**备份状态**：✅ 已纳入 prepare_dim 备份机制

---

## ⚠️ 需要1个月内处理的问题（P1）

### 1. metric_anomaly_strategy - 异常判定策略表

**当前状态**：
- ❌ station_id → dim_stations.id（**无外键**）
- ❌ device_id → dim_devices.id（**无外键**）
- ❌ metric_id → dim_metric_config.id（**无外键**）

**风险等级**：高

**业务影响**：
- ❌ 如果ID变化，程序无法应用正确的异常检测策略
- ❌ 异常检测失效
- ❌ 异常数据可能被忽略

**备份状态**：❌ **未纳入 prepare_dim 备份机制**

---

### 2. metric_quality_rules - 度量质量规则阈值表

**当前状态**：
- ❌ station_id → dim_stations.id（**无外键**）
- ❌ device_id → dim_devices.id（**无外键**）
- ❌ metric_id → dim_metric_config.id（**无外键**）

**风险等级**：高

**业务影响**：
- ❌ 如果ID变化，程序无法应用正确的质量规则
- ❌ 数据质量评价失效
- ❌ 异常数据可能被接受

**代码使用证据**：
- `app/services/rules/metric_quality_rules.py::compute_metric_quality_rules()` - 基于自动基线填充质量规则

**备份状态**：✅ 已纳入 prepare_dim 备份机制

---

### 3. metric_rule_auto_baseline - 自动基线表

**当前状态**：
- ❌ station_id → dim_stations.id（**无外键**）
- ❌ device_id → dim_devices.id（**无外键**）
- ❌ metric_id → dim_metric_config.id（**无外键**）

**风险等级**：高

**业务影响**：
- ❌ 如果ID变化，程序无法生成正确的自动基线
- ❌ 质量规则补全失效
- ❌ 阈值建议不准确

**代码使用证据**：
- `app/services/rules/auto_baseline.py::run_auto_baseline()` - 刷新自动基线

**备份状态**：✅ 已纳入 prepare_dim 备份机制

---

### 4. metric_capability_policy - 指标获取/计算能力策略表

**当前状态**：
- ❌ metric_key → dim_metric_config.metric_key（**无外键**）

**风险等级**：高

**业务影响**：
- ❌ 如果 metric_key 变化，程序无法判断指标是否需要计算
- ❌ 可能尝试计算无法计算的指标
- ❌ 可能忽略需要计算的指标

**备份状态**：✅ 已纳入 prepare_dim 备份机制

---

## 📦 数据管理机制审查

### prepare_dim 备份机制分析

**阶段1（在 merge-fact 前执行）**：
1. **备份17个表**（无条件执行）
2. **清空非备份表**
3. **重建维度表**（dim_stations, dim_devices, dim_metric_config）
4. **恢复手动配置表**（6个A类表）
5. **重建基础配置表**（4个B类表）

**阶段2（在 merge-fact 后执行）**：
1. **清空规则表**（6个D类表）
2. **生成规则表**（调用6个规则生成函数）
3. **生成映射表**（dim_mapping_items）

---

### 备份表清单（17个）

**A类：手动配置表（6个）**：
1. dim_device_capabilities ✅
2. dim_metric_metadata_override ✅
3. pump_characteristic_curves ✅
4. quality_code_dict ✅
5. calculation_validation_config ✅
6. metric_capability_policy ✅

**B类：配置表（4个）**：
1. calculation_parameters ✅
2. device_rated_params ✅
3. calculation_method_registry ✅
4. metric_calculation_order ✅

**C类：维度表（1个）**：
1. dim_metric_config ✅

**D类：规则表（6个）**：
1. metric_rule_auto_baseline ✅
2. metric_rule_auto_baseline_shadow ✅
3. metric_quality_rules ✅
4. metric_quality_rules_shadow ✅
5. device_running_thresholds ✅
6. device_running_thresholds_shadow ✅

---

### 遗漏的表（2个）

| 表名 | 分类 | 是否需要备份 | 建议操作 |
|------|------|-------------|---------|
| **metric_anomaly_strategy** | 配置数据表 | ✅ 需要 | 添加到A类手动配置表 |
| **device_metric_candidates** | 配置数据表 | ⚠️ 可选 | 可添加到A类手动配置表 |
| **dim_metric_metadata** | 配置数据表 | ✅ 需要 | 添加到A类手动配置表 |

**说明**：
- **metric_anomaly_strategy**：异常判定策略表，影响异常检测逻辑，应该备份
- **device_metric_candidates**：设备指标候选表，当前记录数为0，可能是自动生成的，可选备份
- **dim_metric_metadata**：指标元数据表，当前记录数为0，但应该备份以防未来使用

---

## 📝 外键需求分析

### 需要添加外键的表（6个）

| 表名 | 缺少的外键 | 目标表 | 目标列 | 优先级 | 记录数 | 风险等级 |
|------|-----------|--------|--------|--------|--------|---------|
| **calculation_validation_config** | metric_key | dim_metric_config | metric_key | **P0** | 50 | **极高** |
| **device_running_thresholds** | device_id | dim_devices | id | **P0** | 8 | **极高** |
| **metric_anomaly_strategy** | station_id, device_id, metric_id | dim_stations, dim_devices, dim_metric_config | id, id, id | **P1** | 0 | 高 |
| **metric_quality_rules** | station_id, device_id, metric_id | dim_stations, dim_devices, dim_metric_config | id, id, id | **P1** | 0 | 高 |
| **metric_rule_auto_baseline** | station_id, device_id, metric_id | dim_stations, dim_devices, dim_metric_config | id, id, id | **P1** | 0 | 高 |
| **metric_capability_policy** | metric_key | dim_metric_config | metric_key | **P1** | 53 | 高 |
| **device_metric_candidates** | device_id | dim_devices | id | **P2** | 0 | 中 |

### 不需要添加外键的表（1个）

**optimization_history**：
- **原因**：历史数据表，保留历史ID是合理的
- **说明**：已有 device_id 和 station_id 的外键，但 metric_key 不需要外键

---

## 🎯 优先级建议

### P0优先级（立即执行）- 2个表

1. **calculation_validation_config**：为 metric_key 添加外键约束
2. **device_running_thresholds**：为 device_id 添加外键约束

### P1优先级（1个月内）- 4个表

1. **metric_anomaly_strategy**：为 station_id, device_id, metric_id 添加外键约束
2. **metric_quality_rules**：为 station_id, device_id, metric_id 添加外键约束
3. **metric_rule_auto_baseline**：为 station_id, device_id, metric_id 添加外键约束
4. **metric_capability_policy**：为 metric_key 添加外键约束

### P2优先级（长期规划）- 1个表

1. **device_metric_candidates**：为 device_id 添加外键约束

---

## 📊 完整统计

### 表分类统计

| 分类 | 数量 | 需要外键 | 已有外键 | 缺少外键 | 优先级 |
|------|------|---------|---------|---------|--------|
| **配置数据表** | 16个 | 16个 | 10个 | 6个 | P0: 2个, P1: 4个, P2: 0个 |
| **历史数据表** | 1个 | 0个 | 0个 | 0个 | 无需操作 |
| **Shadow表** | 2个 | 2个 | 2个 | 0个 | 无风险 |

### 外键约束统计

| 维度 | 数量 | 百分比 |
|------|------|--------|
| **需要外键的配置表** | 16个 | 100% |
| **已有完整外键的配置表** | 10个 | 62.5% |
| **缺少外键的配置表** | 6个 | 37.5% |
| **P0优先级** | 2个 | 12.5% |
| **P1优先级** | 4个 | 25% |
| **P2优先级** | 0个 | 0% |

### 备份机制统计

| 维度 | 数量 | 百分比 |
|------|------|--------|
| **需要备份的配置表** | 16个 | 100% |
| **已纳入备份的配置表** | 14个 | 87.5% |
| **未纳入备份的配置表** | 2个 | 12.5% |

---

---

## 📋 详细对比分析表

### 完整对比清单（19个表）

| # | 表名 | 分类 | station_id外键 | device_id外键 | metric_key外键 | metric_id外键 | 备份状态 | 优先级 | 需要操作 |
|---|------|------|---------------|--------------|--------------|-------------|---------|--------|---------|
| 1 | optimization_history | 历史数据 | ✅ CASCADE | ✅ CASCADE | ❌ 不需要 | - | ❌ 未备份 | - | 无需操作（历史数据） |
| 2 | calculation_parameters | 配置数据 | ✅ CASCADE | ✅ CASCADE | ✅ CASCADE | - | ✅ 已备份 | - | 无需操作 |
| 3 | calculation_validation_config | 配置数据 | ✅ CASCADE | ✅ CASCADE | ❌ **缺失** | - | ✅ 已备份 | **P0** | **添加metric_key外键** |
| 4 | device_rated_params | 配置数据 | - | ✅ CASCADE | - | - | ✅ 已备份 | - | 无需操作 |
| 5 | device_running_thresholds | 配置数据 | - | ❌ **缺失** | - | - | ✅ 已备份 | **P0** | **添加device_id外键** |
| 6 | device_running_thresholds_shadow | Shadow表 | - | ✅ CASCADE | - | - | ✅ 已备份 | - | 无需操作 |
| 7 | dim_device_capabilities | 配置数据 | - | ✅ CASCADE | - | - | ✅ 已备份 | - | 无需操作 |
| 8 | dim_metric_metadata_override | 配置数据 | ✅ CASCADE | ✅ CASCADE | - | ✅ CASCADE | ✅ 已备份 | - | 无需操作 |
| 9 | metric_anomaly_strategy | 配置数据 | ❌ **缺失** | ❌ **缺失** | - | ❌ **缺失** | ❌ **未备份** | **P1** | **添加3个外键 + 添加到备份** |
| 10 | metric_quality_rules | 配置数据 | ❌ **缺失** | ❌ **缺失** | - | ❌ **缺失** | ✅ 已备份 | **P1** | **添加3个外键** |
| 11 | metric_quality_rules_shadow | Shadow表 | ✅ CASCADE | ✅ CASCADE | - | ✅ CASCADE | ✅ 已备份 | - | 无需操作 |
| 12 | metric_rule_auto_baseline | 配置数据 | ❌ **缺失** | ❌ **缺失** | - | ❌ **缺失** | ✅ 已备份 | **P1** | **添加3个外键** |
| 13 | metric_rule_auto_baseline_shadow | Shadow表 | ✅ CASCADE | ✅ CASCADE | - | ✅ CASCADE | ✅ 已备份 | - | 无需操作 |
| 14 | pump_characteristic_curves | 配置数据 | - | ✅ CASCADE | - | - | ✅ 已备份 | - | 无需操作 |
| 15 | device_metric_candidates | 配置数据 | - | ❌ **缺失** | - | - | ❌ **未备份** | **P2** | **添加device_id外键 + 添加到备份** |
| 16 | calculation_method_registry | 配置数据 | - | - | ✅ CASCADE | - | ✅ 已备份 | - | 无需操作 |
| 17 | metric_calculation_order | 配置数据 | - | - | ✅ CASCADE | - | ✅ 已备份 | - | 无需操作 |
| 18 | metric_capability_policy | 配置数据 | - | - | ❌ **缺失** | - | ✅ 已备份 | **P1** | **添加metric_key外键** |
| 19 | dim_metric_metadata | 配置数据 | - | - | - | ✅ CASCADE | ❌ **未备份** | - | **添加到备份** |

---

## 🔧 配置更新建议

### 更新 prepare_dim 备份机制

**当前备份表清单（17个）**：
```python
tables_to_backup = [
    # A类：手动配置表（6个）
    "dim_device_capabilities",
    "dim_metric_metadata_override",
    "pump_characteristic_curves",
    "quality_code_dict",
    "calculation_validation_config",
    "metric_capability_policy",
    # B类：配置表（4个）
    "calculation_parameters",
    "device_rated_params",
    "calculation_method_registry",
    "metric_calculation_order",
    # C类：维度表（1个）
    "dim_metric_config",
    # D类：规则表（6个）
    "metric_rule_auto_baseline",
    "metric_rule_auto_baseline_shadow",
    "metric_quality_rules",
    "metric_quality_rules_shadow",
    "device_running_thresholds",
    "device_running_thresholds_shadow",
]
```

**建议更新后的备份表清单（20个）**：
```python
tables_to_backup = [
    # A类：手动配置表（9个）← 增加3个
    "dim_device_capabilities",
    "dim_metric_metadata_override",
    "dim_metric_metadata",  # ← 新增
    "pump_characteristic_curves",
    "quality_code_dict",
    "calculation_validation_config",
    "metric_capability_policy",
    "metric_anomaly_strategy",  # ← 新增
    "device_metric_candidates",  # ← 新增
    # B类：配置表（4个）
    "calculation_parameters",
    "device_rated_params",
    "calculation_method_registry",
    "metric_calculation_order",
    # C类：维度表（1个）
    "dim_metric_config",
    # D类：规则表（6个）
    "metric_rule_auto_baseline",
    "metric_rule_auto_baseline_shadow",
    "metric_quality_rules",
    "metric_quality_rules_shadow",
    "device_running_thresholds",
    "device_running_thresholds_shadow",
]
```

**修改文件**：`app/services/ingest/prepare_dim/__init__.py`

**修改位置**：
- 第1176-1198行：`tables_to_backup` 列表定义
- 第282-304行：`backup_tables` 集合定义（用于日志记录）

---

## 📝 执行计划

### 阶段1：立即执行（P0）

**时间**：立即
**任务**：

1. ✅ 执行 P0 优先级SQL脚本
   - 为 calculation_validation_config.metric_key 添加外键约束
   - 为 device_running_thresholds.device_id 添加外键约束

2. ✅ 验证外键约束是否正常工作

3. ✅ 测试 ID 更新时的级联行为

---

### 阶段2：1个月内执行（P1）

**时间**：1个月内
**任务**：

1. ✅ 执行 P1 优先级SQL脚本
   - 为 metric_anomaly_strategy 添加3个外键约束
   - 为 metric_quality_rules 添加3个外键约束
   - 为 metric_rule_auto_baseline 添加3个外键约束
   - 为 metric_capability_policy.metric_key 添加外键约束

2. ✅ 更新 prepare_dim 备份机制
   - 添加 metric_anomaly_strategy 到备份表清单
   - 添加 device_metric_candidates 到备份表清单
   - 添加 dim_metric_metadata 到备份表清单

3. ✅ 验证备份和恢复机制是否正常工作

---

### 阶段3：监控和验证

**时间**：执行后1周
**任务**：

1. ✅ 监控系统运行情况
2. ✅ 检查是否有外键约束冲突
3. ✅ 验证数据完整性
4. ✅ 收集用户反馈

---

**文档版本**：v1.0
**分析日期**：2025-10-27
**状态**：✅ 审查完成
**下一步**：
1. 创建P0优先级SQL脚本（2个表）
2. 创建P1优先级SQL脚本（4个表）
3. 更新 prepare_dim 备份机制（添加3个遗漏的表）

