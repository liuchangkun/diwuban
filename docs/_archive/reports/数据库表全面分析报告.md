# 数据库表全面分析与分类报告

> **生成时间**: 2025-10-18  
> **数据库**: PostgreSQL + TimescaleDB  
> **项目**: 泵站监控系统  
> **分析范围**: 所有业务表（public schema）

---

## 📊 执行摘要

### 数据库当前状态
- **总表数**: 33个核心业务表（不含分区子表和临时表）
- **有数据的表**: 8个
- **空表**: 25个
- **需要立即处理的问题**: 
  - ❌ **calculation_method_registry**: 0行（预期11行）- 计算方法注册表为空
  - ❌ **calculation_parameters**: 0行（预期>0行）- 计算参数表为空
  - ❌ **device_rated_params**: 0行（预期8行）- 设备额定参数表为空
  - ❌ **所有规则表为空**: 需要通过 prepare-dim stage2 重新生成

---

## 第一部分：表的完整清单与分类

### 1. 维度表（Dimension Tables）

#### 1.1 dim_stations（泵站维度表）
- **用途**: 存储泵站基本信息
- **表类型**: 维度表
- **当前行数**: 1
- **预期行数**: 1-2（根据 data_mapping.v2.json）
- **状态**: ✅ 正常
- **数据填充方式**: 
  - **来源**: `configs/data_mapping.v2.json`
  - **填充时机**: prepare-dim stage1（在 merge-fact 前）
  - **填充函数**: `app/services/ingest/prepare_dim/__init__.py::_upsert_station()`
  - **SQL脚本**: 无（通过Python代码UPSERT）
- **字段结构**:
  - `id` (BIGSERIAL): 主键
  - `name` (TEXT): 泵站名称
  - `location` (TEXT): 位置
  - `created_at`, `updated_at` (TIMESTAMPTZ): 审计字段

#### 1.2 dim_devices（设备维度表）
- **用途**: 存储设备基本信息（泵、总管等）
- **表类型**: 维度表
- **当前行数**: 8
- **预期行数**: 8-13（根据 data_mapping.v2.json）
- **状态**: ✅ 正常
- **数据填充方式**:
  - **来源**: `configs/data_mapping.v2.json`
  - **填充时机**: prepare-dim stage1
  - **填充函数**: `app/services/ingest/prepare_dim/__init__.py::_upsert_device()`
  - **外键依赖**: `station_id` → `dim_stations.id`
- **字段结构**:
  - `id` (BIGSERIAL): 主键
  - `station_id` (BIGINT): 外键 → dim_stations.id
  - `name` (TEXT): 设备名称
  - `type` (TEXT): 设备类型（pump/main_pipeline）
  - `is_active` (BOOLEAN): 是否激活

#### 1.3 dim_metric_config（指标配置维度表）
- **用途**: 存储所有指标的配置信息（metric_key、单位、取值范围等）
- **表类型**: 维度表
- **当前行数**: 54
- **预期行数**: 50-60
- **状态**: ✅ 正常
- **数据填充方式**:
  - **来源**: `scripts/sql/dim_metric_config.sql`
  - **填充时机**: prepare-dim stage1
  - **填充函数**: `app/services/ingest/prepare_dim/__init__.py::_reload_metric_config_from_sql()`
  - **SQL脚本**: `scripts/sql/dim_metric_config.sql`（完整的INSERT语句）
- **字段结构**:
  - `id` (BIGSERIAL): 主键
  - `metric_key` (TEXT): 指标键（唯一）
  - `unit_display` (TEXT): 显示单位
  - `value_min`, `value_max` (NUMERIC): 取值范围
  - `description` (TEXT): 描述

---

### 2. 事实表（Fact Tables）

#### 2.1 fact_measurements（时序事实表）
- **用途**: 存储所有设备的秒级对齐测量数据
- **表类型**: 事实表（TimescaleDB Hypertable）
- **当前行数**: 0
- **预期行数**: 取决于数据导入
- **状态**: ⚠️ 空表（等待数据导入）
- **数据填充方式**:
  - **来源**: staging_raw → merge-fact 流程
  - **填充时机**: run-all 流程的 merge-fact 阶段
  - **填充函数**: `app/services/ingest/merge_service.py::merge_window()`
  - **分区策略**: 
    - 时间分区：`ts_bucket`（1天chunk）
    - 空间分区：`device_id`（8个分区）
- **字段结构**:
  - `id` (BIGSERIAL): 主键
  - `station_id`, `device_id`, `metric_id` (BIGINT): 外键
  - `ts_raw` (TIMESTAMPTZ): 原始时间戳
  - `ts_bucket` (TIMESTAMPTZ): 对齐后的秒级时间戳
  - `value` (NUMERIC): 测量值
  - `quality_status` (SMALLINT): 质量状态码
  - `source_hint` (TEXT): 数据来源提示
  - `inserted_at` (TIMESTAMPTZ): 插入时间
- **重要性**: ⭐⭐⭐⭐⭐ 核心数据表，不应清空

---

### 3. 配置表（Configuration Tables）

#### 3.1 calculation_method_registry（计算方法注册表）
- **用途**: 记录每个指标的所有计算方法（方法A、B、C等）
- **表类型**: 配置表
- **当前行数**: 0 ❌
- **预期行数**: 11-22（根据 `docs/计算原理和公式.md`）
- **状态**: ❌ 异常（应有数据但为空）
- **数据填充方式**:
  - **来源**: `scripts/sql/calculation/init_methods.sql`
  - **填充时机**: prepare-dim stage1（重建配置表阶段）
  - **填充函数**: `app/services/ingest/prepare_dim/__init__.py::_rebuild_config_tables()`
  - **SQL脚本**: `scripts/sql/calculation/init_methods.sql`
- **字段结构**:
  - `method_id` (TEXT): 主键（如：pump_flow_rate_method_a）
  - `metric_key` (TEXT): 外键 → dim_metric_config.metric_key
  - `method_name` (TEXT): 方法名称
  - `method_code` (TEXT): 方法代码（A/B/C）
  - `priority` (INTEGER): 优先级（越大越优先）
  - `dependencies` (TEXT[]): 依赖的指标列表
  - `conditions` (JSONB): 使用条件
  - `formula_ref` (TEXT): 公式文档引用
  - `accuracy_level` (TEXT): 精度等级（high/medium/low）
  - `is_enabled` (BOOLEAN): 是否启用
  - `allowed_device_types` (TEXT[]): 允许的设备类型
- **恢复方式**: 通过 prepare-dim stage1 自动重建

#### 3.2 calculation_parameters（计算参数表）
- **用途**: 存储每个设备每个指标每个方法的参数（如alpha、beta、f_thr等）
- **表类型**: 配置表
- **当前行数**: 0 ❌
- **预期行数**: >0（全局默认参数）
- **状态**: ❌ 异常
- **数据填充方式**:
  - **来源**: `scripts/sql/calculation/init_params.sql`
  - **填充时机**: prepare-dim stage1
  - **填充函数**: `app/services/ingest/prepare_dim/__init__.py::_rebuild_config_tables()`
  - **SQL脚本**: `scripts/sql/calculation/init_params.sql`
- **字段结构**:
  - `id` (BIGSERIAL): 主键
  - `station_id`, `device_id` (BIGINT): 外键（NULL表示全局默认）
  - `metric_key` (TEXT): 外键 → dim_metric_config.metric_key
  - `method_id` (TEXT): 外键 → calculation_method_registry.method_id
  - `param_name` (TEXT): 参数名（如：alpha, beta, f_thr）
  - `param_value` (NUMERIC): 参数值
  - `param_type` (TEXT): 参数类型（float/int/bool）
  - `is_optimizable` (BOOLEAN): 是否可优化
  - `optimization_history` (JSONB): 优化历史记录
- **恢复方式**: 通过 prepare-dim stage1 自动重建

#### 3.3 calculation_validation_config（计算结果验证配置表）
- **用途**: 存储计算结果验证的配置参数
- **表类型**: 配置表
- **当前行数**: 50
- **预期行数**: 50
- **状态**: ✅ 正常
- **数据填充方式**:
  - **来源**: `scripts/sql/validation/init_validation_config.sql`
  - **填充时机**: 手动执行SQL脚本
  - **SQL脚本**: `scripts/sql/validation/init_validation_config.sql`
- **恢复方式**: 需要备份恢复（手动配置表）

#### 3.4 device_rated_params（设备额定参数表）
- **用途**: 存储每个设备的额定参数（额定功率、额定电流、额定频率等）
- **表类型**: 配置表
- **当前行数**: 0 ❌
- **预期行数**: 8-13（每个设备多个参数）
- **状态**: ❌ 异常
- **数据填充方式**:
  - **来源**: `scripts/migrations/20250829_seed_device_rated_params.sql`
  - **填充时机**: prepare-dim stage1
  - **填充函数**: `app/services/ingest/prepare_dim/__init__.py::_rebuild_config_tables()`
  - **SQL脚本**: `scripts/migrations/20250829_seed_device_rated_params.sql`
- **字段结构**:
  - `id` (BIGSERIAL): 主键
  - `device_id` (BIGINT): 外键 → dim_devices.id
  - `param_key` (TEXT): 参数键（如：rated_power、rated_current）
  - `value_numeric` (NUMERIC): 数值
  - `value_text` (TEXT): 文本值
  - `unit` (TEXT): 单位
  - `source` (TEXT): 来源
- **恢复方式**: 通过 prepare-dim stage1 自动重建

#### 3.5 metric_calculation_order（指标计算顺序表）
- **用途**: 记录指标的依赖关系和计算顺序
- **表类型**: 配置表
- **当前行数**: 0 ❌
- **预期行数**: >0
- **状态**: ❌ 异常
- **数据填充方式**:
  - **来源**: 通过 DependencyAnalyzer 分析生成
  - **填充时机**: prepare-dim stage1
  - **填充函数**: `app/services/ingest/prepare_dim/__init__.py::_rebuild_config_tables()`
  - **生成逻辑**: `app/services/calculation/dependency_analyzer.py::DependencyAnalyzer.save_to_database()`
- **恢复方式**: 通过 prepare-dim stage1 自动重建

#### 3.6 metric_capability_policy（指标获取/计算能力策略表）
- **用途**: 记录每个 metric_key 的获取情况（不能/可能/可以）与是否需要计算
- **表类型**: 配置表
- **当前行数**: 53
- **预期行数**: 50-60
- **状态**: ✅ 正常
- **数据填充方式**: 手动配置或通过脚本生成
- **恢复方式**: 需要备份恢复

---

### 4. 规则表（Rule Tables）

#### 4.1 metric_rule_auto_baseline（自动基线表）
- **用途**: 从历史有效数据自动计算的基线阈值（分位数、MAD、跳变阈值等）
- **表类型**: 规则表（自动计算）
- **当前行数**: 0 ❌
- **预期行数**: >0（每个设备×指标一行）
- **状态**: ⚠️ 待计算
- **数据填充方式**:
  - **来源**: fact_measurements（近30天有效且稳态数据）
  - **填充时机**: prepare-dim stage2（在 merge-fact 后）
  - **填充函数**: `app/services/rules/auto_baseline.py::run_auto_baseline()`
  - **存储过程**: `public.sp_refresh_metric_rule_auto_baseline()`
  - **算法**: 稳健分位数估计（p05, p95, median, MAD）
- **字段结构**:
  - `baseline_id` (BIGSERIAL): 主键
  - `station_id`, `device_id`, `metric_id` (BIGINT): 外键
  - `lookback_days` (INT): 回溯天数
  - `method` (TEXT): 方法名称
  - `p05`, `p95`, `median`, `mad` (DOUBLE PRECISION): 分布统计
  - `spike_abs`, `roc_abs`, `roc_ratio` (DOUBLE PRECISION): 跳变/速率建议
  - `flatline_eps`, `flatline_delta` (DOUBLE PRECISION): 平台期建议
  - `computed_at` (TIMESTAMPTZ): 计算时间
  - `version` (TEXT): 版本
- **恢复方式**: 通过 prepare-dim stage2 自动重新计算

#### 4.2 metric_rule_auto_baseline_shadow（自动基线影子表）
- **用途**: 方案B（STL分解）的自动基线输出，不影响正式表
- **表类型**: 规则表（影子表）
- **当前行数**: 0
- **预期行数**: >0
- **状态**: ⚠️ 待计算
- **数据填充方式**:
  - **来源**: fact_measurements（STL分解后的残差）
  - **填充时机**: prepare-dim stage2
  - **填充函数**: `app/services/rules/auto_baseline_b.py::run_auto_baseline_b()`
  - **算法**: STL分解 + 稳健估计
- **恢复方式**: 通过 prepare-dim stage2 自动重新计算

#### 4.3 metric_quality_rules（度量质量规则表）
- **用途**: 存储质量规则阈值（越界、跳变、速率、平台期等）
- **表类型**: 规则表（手工覆盖 + 自动补全）
- **当前行数**: 0 ❌
- **预期行数**: >0
- **状态**: ⚠️ 待计算
- **数据填充方式**:
  - **来源**: metric_rule_auto_baseline（自动基线）
  - **填充时机**: prepare-dim stage2
  - **填充函数**: `app/services/rules/metric_quality_rules.py::compute_metric_quality_rules()`
  - **逻辑**: 基于自动基线填充/补全（不覆盖人工配置）
- **字段结构**:
  - `rule_id` (BIGSERIAL): 主键
  - `station_id`, `device_id`, `metric_id` (BIGINT): 外键
  - `value_min`, `value_max` (DOUBLE PRECISION): 越界阈值
  - `spike_abs`, `roc_abs`, `roc_ratio` (DOUBLE PRECISION): 跳变/速率阈值
  - `flatline_eps`, `flatline_delta` (DOUBLE PRECISION): 平台期阈值
  - `remark` (TEXT): 备注
- **恢复方式**: 通过 prepare-dim stage2 自动重新计算

#### 4.4 metric_quality_rules_shadow（质量规则影子表）
- **用途**: 方案B的质量规则参数输出
- **表类型**: 规则表（影子表）
- **当前行数**: 0
- **预期行数**: >0
- **状态**: ⚠️ 待计算
- **数据填充方式**:
  - **来源**: metric_rule_auto_baseline_shadow
  - **填充时机**: prepare-dim stage2
  - **填充函数**: `app/services/rules/metric_quality_rules_b.py::compute_metric_quality_rules_b()`
- **恢复方式**: 通过 prepare-dim stage2 自动重新计算

#### 4.5 device_running_thresholds（设备运行阈值表）
- **用途**: 存储设备启停判定的阈值（功率、电流、频率的on/off阈值）
- **表类型**: 规则表（自动计算 + 手工覆盖）
- **当前行数**: 0 ❌
- **预期行数**: 8-12（每个设备一行）
- **状态**: ⚠️ 待计算
- **数据填充方式**:
  - **来源**: fact_measurements（功率因数、电流等历史数据）
  - **填充时机**: prepare-dim stage2
  - **填充函数**: `app/services/rules/running_thresholds.py::compute_device_running_thresholds()`
  - **算法**: 分位数估计（p20, p80）
- **字段结构**:
  - `device_id` (BIGINT): 主键
  - `enable_i`, `enable_p`, `enable_f` (BOOLEAN): 启用标志
  - `i_on`, `i_off` (DOUBLE PRECISION): 电流阈值
  - `p_on`, `p_off` (DOUBLE PRECISION): 功率阈值
  - `f_on`, `f_off` (DOUBLE PRECISION): 频率阈值
  - `pf_min`, `pf_max` (DOUBLE PRECISION): 功率因数范围
  - `grace_hold_secs`, `min_run_secs`, `min_stop_secs` (INTEGER): 时间参数
  - `smoothing_secs` (INTEGER): 平滑窗口
- **恢复方式**: 通过 prepare-dim stage2 自动重新计算

#### 4.6 device_running_thresholds_shadow（运行阈值影子表）
- **用途**: 方案B（GMM等）计算的PF阈值输出
- **表类型**: 规则表（影子表）
- **当前行数**: 0
- **预期行数**: >0
- **状态**: ⚠️ 待计算
- **数据填充方式**:
  - **来源**: fact_measurements（GMM双峰分布拟合）
  - **填充时机**: prepare-dim stage2
  - **填充函数**: `app/services/rules/running_thresholds_b.py::compute_device_running_thresholds_b()`
- **恢复方式**: 通过 prepare-dim stage2 自动重新计算

---

### 5. 手动配置表（Manual Configuration Tables）

#### 5.1 dim_device_capabilities（设备能力表）
- **用途**: 记录设备能力（是否变频、频率范围、额定功率/电流等）
- **表类型**: 手动配置表
- **当前行数**: 0
- **预期行数**: 8-12
- **状态**: ⚠️ 需要人工填写
- **数据填充方式**: 人工填写
- **字段结构**:
  - `device_id` (BIGINT): 主键
  - `vfd_enabled` (BOOLEAN): 是否变频
  - `freq_min`, `freq_max` (DOUBLE PRECISION): 频率范围
  - `rated_power_kw`, `rated_current_a` (DOUBLE PRECISION): 额定功率/电流
  - `remark` (TEXT): 备注
- **恢复方式**: 需要备份恢复（prepare-dim stage1 会自动备份和恢复）

#### 5.2 dim_metric_metadata_override（指标元数据覆盖表）
- **用途**: 按站点/设备粒度覆盖全局指标元数据
- **表类型**: 手动配置表
- **当前行数**: 0
- **预期行数**: 0-10
- **状态**: ✅ 正常（可选配置）
- **数据填充方式**: 人工填写
- **恢复方式**: 需要备份恢复（prepare-dim stage1 会自动备份和恢复）

#### 5.3 pump_characteristic_curves（水泵特性曲线表）
- **用途**: 存储水泵特性曲线数据（Q-H、Q-P、Q-η曲线）
- **表类型**: 手动配置表
- **当前行数**: 71
- **预期行数**: 50-100
- **状态**: ✅ 正常
- **数据填充方式**: 人工填写或导入
- **恢复方式**: 需要备份恢复

#### 5.4 quality_code_dict（质量代码字典表）
- **用途**: 质量代码字典（101-751等），用于质量标注的统一编码与中文释义
- **表类型**: 手动配置表
- **当前行数**: 34
- **预期行数**: 30-50
- **状态**: ✅ 正常
- **数据填充方式**: 手动配置或SQL脚本
- **恢复方式**: 需要备份恢复

---

### 6. 物化视图（Materialized Views）

#### 6.1 mv_device_running_1s（设备运行状态物化视图）
- **用途**: 按设备逐秒运行状态的物化结果（0=停机，1=运行）
- **表类型**: 物化视图
- **当前行数**: 43,200
- **预期行数**: 取决于时间窗口
- **状态**: ✅ 正常
- **数据填充方式**: 自动刷新（基于 fact_measurements 和 device_running_thresholds）
- **刷新时机**: 定时刷新或手动刷新

#### 6.2 metrics_presence_per_second_device（设备-秒级指标可用性表）
- **用途**: 记录每个设备每秒有哪些可用指标、哪些需要计算的指标
- **表类型**: 物化视图（分区表）
- **当前行数**: 50,400
- **预期行数**: 取决于时间窗口
- **状态**: ✅ 正常
- **数据填充方式**: 自动刷新

---

### 7. 审计/日志表（Audit/Log Tables）

#### 7.1 completion_runs（缺失指标计算运行记录表）
- **用途**: 记录每次缺失指标计算的运行信息
- **表类型**: 审计表
- **当前行数**: 0
- **预期行数**: 随运行累积
- **状态**: ✅ 正常（空表）
- **数据填充方式**: 缺失指标计算流程自动写入

#### 7.2 completion_steps（缺失指标计算步骤记录表）
- **用途**: 记录每次计算的详细步骤
- **表类型**: 审计表
- **当前行数**: 0
- **预期行数**: 随运行累积
- **状态**: ✅ 正常（空表）

#### 7.3 calculation_failures_log（计算失败日志表）
- **用途**: 记录所有计算和验证失败的情况
- **表类型**: 日志表
- **当前行数**: 未查询
- **预期行数**: 随运行累积
- **状态**: ✅ 正常

---

## 第二部分：澄清 prepare_dim 的工作流程

### prepare_dim_stage1（在 merge-fact 之前执行）

**触发条件**: `configs/merge.yaml` 中 `prepare_dim: true`

**执行步骤**:

1. **备份手动配置表**（2个表）
   - `dim_device_capabilities`（设备能力表）
   - `dim_metric_metadata_override`（指标元数据覆盖表）
   - **备份方式**: 使用 station_name/device_name/metric_key 进行名称匹配
   - **函数**: `_backup_manual_tables(cur) -> Dict[str, list]`

2. **清空依赖表**（14个表，按依赖顺序）
   - 第1层：`fact_measurements`（所有历史数据）
   - 第2层：`completion_runs`, `completion_steps`（审计数据）
   - 第3层：`device_rated_params`, `device_running_thresholds_shadow`
   - 第4层：`dim_device_capabilities`, `dim_metric_metadata_override`（已备份）
   - 第5层：`metric_quality_rules_shadow`, `metric_rule_auto_baseline_shadow`
   - 第6层：`dim_devices`（设备维度表）
   - 第7层：`calculation_parameters`（只清空全局默认参数）
   - 第8层：`dim_stations`（站点维度表）
   - 第9层：`metric_calculation_order`, `dim_mapping_items`
   - 第10层：`dim_metric_config`（指标配置维度表）
   - **函数**: `_clear_dependent_tables(cur) -> int`

3. **重建维度表**（3个表）
   - 从 `configs/data_mapping.v2.json` 重新生成：
     - `dim_stations`（泵站维度表）
     - `dim_devices`（设备维度表）
   - 从 `scripts/sql/dim_metric_config.sql` 重新生成：
     - `dim_metric_config`（指标配置维度表）
   - **函数**: `_upsert_station()`, `_upsert_device()`, `_reload_metric_config_from_sql()`

4. **恢复手动配置表**（2个表）
   - 使用备份数据恢复：
     - `dim_device_capabilities`
     - `dim_metric_metadata_override`
   - **恢复方式**: 使用 UPSERT（ON CONFLICT DO UPDATE）
   - **函数**: `_restore_manual_tables(cur, backup_data) -> None`

5. **重建基础配置表**（4个表）
   - `calculation_method_registry`（计算方法注册表）
     - SQL脚本：`scripts/sql/calculation/init_methods.sql`
   - `calculation_parameters`（全局默认参数）
     - SQL脚本：`scripts/sql/calculation/init_params.sql`
   - `device_rated_params`（设备额定参数）
     - SQL脚本：`scripts/migrations/20250829_seed_device_rated_params.sql`
   - `metric_calculation_order`（指标计算顺序）
     - 生成逻辑：`DependencyAnalyzer.save_to_database()`
   - **函数**: `_rebuild_config_tables(settings, cur) -> None`

### prepare_dim_stage2（在 merge-fact 之后执行）

**触发条件**: `configs/merge.yaml` 中 `prepare_dim_stage2: true`

**执行步骤**:

1. **清空规则表**（6个表）
   - `metric_rule_auto_baseline`
   - `metric_rule_auto_baseline_shadow`
   - `metric_quality_rules`
   - `metric_quality_rules_shadow`
   - `device_running_thresholds`
   - `device_running_thresholds_shadow`
   - **函数**: `_clear_rule_tables(cur) -> int`

2. **生成规则表**（6个函数，按顺序执行）
   - **函数1**: `run_auto_baseline(settings, lookback_days=30)`
     - 生成表：`metric_rule_auto_baseline`
     - 数据来源：`fact_measurements`（近30天有效且稳态数据）
     - 算法：稳健分位数估计
   - **函数2**: `run_auto_baseline_b(settings, lookback_days=30)`
     - 生成表：`metric_rule_auto_baseline_shadow`
     - 数据来源：`fact_measurements`（STL分解）
     - 算法：STL分解 + 稳健估计
   - **函数3**: `compute_metric_quality_rules(settings)`
     - 生成表：`metric_quality_rules`
     - 数据来源：`metric_rule_auto_baseline`
     - 逻辑：基于自动基线填充/补全
   - **函数4**: `compute_metric_quality_rules_b(settings)`
     - 生成表：`metric_quality_rules_shadow`
     - 数据来源：`metric_rule_auto_baseline_shadow`
   - **函数5**: `compute_device_running_thresholds(settings)`
     - 生成表：`device_running_thresholds`
     - 数据来源：`fact_measurements`（功率因数、电流等）
     - 算法：分位数估计
   - **函数6**: `compute_device_running_thresholds_b(settings)`
     - 生成表：`device_running_thresholds_shadow`
     - 数据来源：`fact_measurements`（GMM双峰分布）
   - **函数**: `_generate_rule_tables(settings) -> Dict[str, Any]`

3. **生成映射表**（可选）
   - `dim_mapping_items`（映射表）
   - **状态**: TODO（当前未实现）

---

## 第三部分：当前数据库状态分析

### 核心业务表状态汇总

| 表名 | 用途 | 表类型 | 填充方式 | 数据来源 | 填充时机 | 当前行数 | 预期行数 | 状态 |
|------|------|--------|---------|---------|---------|---------|---------|------|
| **dim_stations** | 泵站维度表 | 维度表 | JSON导入 | data_mapping.v2.json | prepare-dim stage1 | 1 | 1-2 | ✅ 正常 |
| **dim_devices** | 设备维度表 | 维度表 | JSON导入 | data_mapping.v2.json | prepare-dim stage1 | 8 | 8-13 | ✅ 正常 |
| **dim_metric_config** | 指标配置维度表 | 维度表 | SQL脚本 | dim_metric_config.sql | prepare-dim stage1 | 54 | 50-60 | ✅ 正常 |
| **fact_measurements** | 时序事实表 | 事实表 | merge-fact | staging_raw | merge-fact | 0 | 取决于导入 | ⚠️ 空表 |
| **calculation_method_registry** | 计算方法注册表 | 配置表 | SQL脚本 | init_methods.sql | prepare-dim stage1 | 0 | 11-22 | ❌ 异常 |
| **calculation_parameters** | 计算参数表 | 配置表 | SQL脚本 | init_params.sql | prepare-dim stage1 | 0 | >0 | ❌ 异常 |
| **calculation_validation_config** | 验证配置表 | 配置表 | SQL脚本 | init_validation_config.sql | 手动执行 | 50 | 50 | ✅ 正常 |
| **device_rated_params** | 设备额定参数表 | 配置表 | SQL脚本 | seed_device_rated_params.sql | prepare-dim stage1 | 0 | 8-13 | ❌ 异常 |
| **metric_calculation_order** | 指标计算顺序表 | 配置表 | Python生成 | DependencyAnalyzer | prepare-dim stage1 | 0 | >0 | ❌ 异常 |
| **metric_capability_policy** | 指标能力策略表 | 配置表 | 手动配置 | 手动 | 手动 | 53 | 50-60 | ✅ 正常 |
| **metric_rule_auto_baseline** | 自动基线表 | 规则表 | Python计算 | fact_measurements | prepare-dim stage2 | 0 | >0 | ⚠️ 待计算 |
| **metric_rule_auto_baseline_shadow** | 自动基线影子表 | 规则表 | Python计算 | fact_measurements | prepare-dim stage2 | 0 | >0 | ⚠️ 待计算 |
| **metric_quality_rules** | 质量规则表 | 规则表 | Python计算 | auto_baseline | prepare-dim stage2 | 0 | >0 | ⚠️ 待计算 |
| **metric_quality_rules_shadow** | 质量规则影子表 | 规则表 | Python计算 | auto_baseline_shadow | prepare-dim stage2 | 0 | >0 | ⚠️ 待计算 |
| **device_running_thresholds** | 运行阈值表 | 规则表 | Python计算 | fact_measurements | prepare-dim stage2 | 0 | 8-12 | ⚠️ 待计算 |
| **device_running_thresholds_shadow** | 运行阈值影子表 | 规则表 | Python计算 | fact_measurements | prepare-dim stage2 | 0 | >0 | ⚠️ 待计算 |
| **dim_device_capabilities** | 设备能力表 | 手动配置表 | 人工填写 | 手动 | 手动 | 0 | 8-12 | ⚠️ 需填写 |
| **dim_metric_metadata_override** | 指标元数据覆盖表 | 手动配置表 | 人工填写 | 手动 | 手动 | 0 | 0-10 | ✅ 正常 |
| **pump_characteristic_curves** | 水泵特性曲线表 | 手动配置表 | 人工填写 | 手动 | 手动 | 71 | 50-100 | ✅ 正常 |
| **quality_code_dict** | 质量代码字典表 | 手动配置表 | SQL脚本 | 手动 | 手动 | 34 | 30-50 | ✅ 正常 |
| **mv_device_running_1s** | 设备运行状态视图 | 物化视图 | 自动刷新 | fact_measurements | 定时刷新 | 43,200 | 取决于窗口 | ✅ 正常 |
| **metrics_presence_per_second_device** | 指标可用性表 | 物化视图 | 自动刷新 | fact_measurements | 定时刷新 | 50,400 | 取决于窗口 | ✅ 正常 |
| **completion_runs** | 计算运行记录表 | 审计表 | 自动写入 | 计算流程 | 运行时 | 0 | 随运行累积 | ✅ 正常 |
| **completion_steps** | 计算步骤记录表 | 审计表 | 自动写入 | 计算流程 | 运行时 | 0 | 随运行累积 | ✅ 正常 |

---

## 第四部分：表的分类汇总

### A. 手动配置表（需要备份恢复）

**特征**: 数据由人工填写或通过配置文件导入，不能通过计算重新生成

**表清单**:
1. `dim_device_capabilities`（设备能力表）- 人工填写
2. `dim_metric_metadata_override`（指标元数据覆盖表）- 人工填写
3. `pump_characteristic_curves`（水泵特性曲线表）- 人工填写
4. `quality_code_dict`（质量代码字典表）- 手动配置
5. `calculation_validation_config`（验证配置表）- SQL脚本
6. `metric_capability_policy`（指标能力策略表）- 手动配置

**备份策略**:
- prepare-dim stage1 会自动备份和恢复前2个表
- 其他表需要手动备份（SQL导出或数据导出）

### B. 自动计算表（需要重新计算恢复）

**特征**: 数据通过历史数据计算生成，可以重新计算

**表清单**:
1. `metric_rule_auto_baseline`（自动基线表）
2. `metric_rule_auto_baseline_shadow`（自动基线影子表）
3. `metric_quality_rules`（质量规则表）
4. `metric_quality_rules_shadow`（质量规则影子表）
5. `device_running_thresholds`（运行阈值表）
6. `device_running_thresholds_shadow`（运行阈值影子表）

**恢复策略**:
- 通过 prepare-dim stage2 自动重新计算
- 依赖 fact_measurements 表的历史数据

### C. 维度表（通过配置文件重建）

**特征**: 从 configs/data_mapping.v2.json 文件读取并重建

**表清单**:
1. `dim_stations`（泵站维度表）
2. `dim_devices`（设备维度表）
3. `dim_metric_config`（指标配置维度表）- 从SQL脚本重建

**恢复策略**:
- 通过 prepare-dim stage1 自动重建

### D. 事实表（不清空，只追加）

**特征**: 存储原始测量数据，不应该被清空

**表清单**:
1. `fact_measurements`（时序事实表）

**重要性**: ⭐⭐⭐⭐⭐ 核心数据表，不应清空

### E. 临时表/日志表（可以清空）

**特征**: 临时数据或日志数据，清空不影响业务

**表清单**:
1. `staging_raw`（导入暂存原始数据）
2. `staging_rejects`（导入拒收记录）
3. `completion_runs`（计算运行记录）
4. `completion_steps`（计算步骤记录）
5. `completion_audit`（计算审计记录）
6. `completion_failures`（计算失败记录）
7. `calculation_failures_log`（计算失败日志）
8. `calculation_performance_metrics`（计算性能指标）
9. `quality_diagnosis_log`（质量诊断日志）
10. `quality_profile_log`（质量性能剖析日志）

---

## 第五部分：问题诊断与解决方案

### 当前问题清单

#### 问题1: 配置表为空 ❌

**受影响的表**:
- `calculation_method_registry`（0行，预期11-22行）
- `calculation_parameters`（0行，预期>0行）
- `device_rated_params`（0行，预期8-13行）
- `metric_calculation_order`（0行，预期>0行）

**原因分析**:
- prepare-dim stage1 未正确执行
- 或者 SQL 脚本执行失败

**解决方案**:
```bash
# 方案1：重新执行 prepare-dim stage1
python -m app.cli.main prepare-dim configs/data_mapping.v2.json --stage 1

# 方案2：手动执行 SQL 脚本
psql -U postgres -d your_database -f scripts/sql/calculation/init_methods.sql
psql -U postgres -d your_database -f scripts/sql/calculation/init_params.sql
psql -U postgres -d your_database -f scripts/migrations/20250829_seed_device_rated_params.sql
```

#### 问题2: 规则表为空 ⚠️

**受影响的表**:
- 所有6个规则表（auto_baseline、quality_rules、running_thresholds及其影子表）

**原因分析**:
- prepare-dim stage2 未执行
- 或者 fact_measurements 表为空（无历史数据）

**解决方案**:
```bash
# 前提：fact_measurements 表必须有数据
# 方案1：通过 run-all 流程自动执行
# 在 configs/merge.yaml 中设置 prepare_dim_stage2: true

# 方案2：单独执行 prepare-dim stage2
python -m app.cli.main prepare-dim configs/data_mapping.v2.json --stage 2
```

#### 问题3: dim_device_capabilities 表为空 ⚠️

**原因分析**:
- 这是手动配置表，需要人工填写

**解决方案**:
```sql
-- 示例：为设备1填写能力信息
INSERT INTO dim_device_capabilities (
    device_id, vfd_enabled, freq_min, freq_max, rated_power_kw, rated_current_a, remark
) VALUES (
    1, true, 0, 50, 100, 200, '变频泵'
) ON CONFLICT (device_id) DO UPDATE SET
    vfd_enabled = EXCLUDED.vfd_enabled,
    freq_min = EXCLUDED.freq_min,
    freq_max = EXCLUDED.freq_max,
    rated_power_kw = EXCLUDED.rated_power_kw,
    rated_current_a = EXCLUDED.rated_current_a,
    remark = EXCLUDED.remark;
```

---

## 第六部分：完整的恢复流程

### 场景1：从零开始初始化数据库

```bash
# 步骤1：创建数据库表结构
psql -U postgres -d your_database -f scripts/create_db_and_tables.sql

# 步骤2：执行 prepare-dim stage1（重建维度表和配置表）
python -m app.cli.main prepare-dim configs/data_mapping.v2.json --stage 1

# 步骤3：手动填写 dim_device_capabilities 表
# （通过SQL或管理界面）

# 步骤4：导入数据到 fact_measurements
python -m app.cli.main run-all configs/data_mapping.v2.json --use-staging-time-range

# 步骤5：执行 prepare-dim stage2（生成规则表）
python -m app.cli.main prepare-dim configs/data_mapping.v2.json --stage 2
```

### 场景2：prepare_dim: true 时的完整流程

```yaml
# configs/merge.yaml
run_all:
  prepare_dim: true         # 阶段1：重建维度表（在 merge-fact 前）
  create_staging: true
  ingest_copy: true
  merge_fact: true
  prepare_dim_stage2: true  # 阶段2：生成规则表（在 merge-fact 后）
```

**执行流程**:
1. prepare-dim stage1 → 备份手动配置表 → 清空依赖表 → 重建维度表 → 恢复手动配置表 → 重建配置表
2. create-staging → 创建暂存表
3. ingest-copy → 导入数据到暂存表
4. merge-fact → 合并数据到 fact_measurements
5. prepare-dim stage2 → 清空规则表 → 生成规则表

---

## 附录：表依赖关系图

```
dim_stations (泵站维度表)
  ↓
dim_devices (设备维度表)
  ↓
  ├─→ dim_device_capabilities (设备能力表) [手动配置]
  ├─→ device_rated_params (设备额定参数表) [SQL脚本]
  ├─→ device_running_thresholds (运行阈值表) [自动计算]
  └─→ device_running_thresholds_shadow (运行阈值影子表) [自动计算]

dim_metric_config (指标配置维度表)
  ↓
  ├─→ calculation_method_registry (计算方法注册表) [SQL脚本]
  │     ↓
  │   calculation_parameters (计算参数表) [SQL脚本]
  │
  ├─→ metric_calculation_order (指标计算顺序表) [Python生成]
  ├─→ dim_metric_metadata (指标元数据表) [SQL脚本]
  └─→ dim_metric_metadata_override (指标元数据覆盖表) [手动配置]

fact_measurements (时序事实表)
  ↓
  ├─→ metric_rule_auto_baseline (自动基线表) [自动计算]
  │     ↓
  │   metric_quality_rules (质量规则表) [自动计算]
  │
  ├─→ metric_rule_auto_baseline_shadow (自动基线影子表) [自动计算]
  │     ↓
  │   metric_quality_rules_shadow (质量规则影子表) [自动计算]
  │
  ├─→ device_running_thresholds (运行阈值表) [自动计算]
  └─→ device_running_thresholds_shadow (运行阈值影子表) [自动计算]
```

---

## 总结

### 关键发现

1. **配置表问题**: 4个配置表为空（calculation_method_registry、calculation_parameters、device_rated_params、metric_calculation_order），需要通过 prepare-dim stage1 重建

2. **规则表问题**: 6个规则表为空，需要通过 prepare-dim stage2 重新计算（前提是 fact_measurements 有数据）

3. **手动配置表**: dim_device_capabilities 需要人工填写

4. **数据恢复策略**:
   - **备份恢复**: 手动配置表（6个）
   - **重新计算**: 规则表（6个）
   - **配置文件重建**: 维度表（3个）
   - **SQL脚本重建**: 配置表（4个）

### 建议操作

1. **立即执行**: `python -m app.cli.main prepare-dim configs/data_mapping.v2.json --stage 1`
2. **验证结果**: 检查4个配置表是否有数据
3. **填写手动配置**: 填写 dim_device_capabilities 表
4. **导入数据**: 执行 run-all 流程导入数据到 fact_measurements
5. **生成规则**: `python -m app.cli.main prepare-dim configs/data_mapping.v2.json --stage 2`

---

**报告结束**

