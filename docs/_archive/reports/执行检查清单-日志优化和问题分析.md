# 执行检查清单 - 日志优化和问题分析

> **创建时间**：2025-10-20
> **模式**：计划模式（RIPER-5协议）
> **版本**：v1.0

---

## 📋 检查清单概述

本文档为日志系统优化和问题分析任务的详细执行检查清单，按照RIPER-5协议要求，每个检查项必须在执行前验证代码存在性，执行后验证结果正确性。

---

## ✅ 阶段1：规则表为空问题修复（方案A+B+C）

### 检查项1.1：修改 auto_baseline_b.py - 复用现有时间窗口查询逻辑

**执行前验证**：
- [ ] 使用 `view` 工具查看 `app/services/rules/auto_baseline_b.py` 第34-35行
- [ ] 确认当前代码为：
  ```python
  end_ts = _dt.utcnow()
  start_ts = end_ts - _td(days=int(max(1, lookback_days)))
  ```
- [ ] 使用 `view` 工具查看 `app/services/rules/running_thresholds_b.py` 第80-93行
- [ ] 确认现有的自动窗口查询逻辑（参考实现）

**执行操作**：
- [ ] 使用 `str-replace-editor` 工具修改第34-35行
- [ ] **复用** `running_thresholds_b.py` 的自动窗口查询逻辑（不要重复造轮子）
- [ ] 添加窗口限制逻辑（不超过 lookback_days）
- [ ] 添加无数据时的处理逻辑

**执行后验证**：
- [ ] 使用 `view` 工具查看修改后的代码
- [ ] 确认新代码与 `running_thresholds_b.py` 的逻辑一致
- [ ] 确认新代码包含窗口限制逻辑
- [ ] 确认新代码包含无数据处理逻辑

**预期结果**：
- 代码成功修改，复用现有逻辑
- 代码格式正确，无语法错误
- 与 `running_thresholds_b.py` 保持一致

---

### 检查项1.2：修改 auto_baseline.py - 检查并使用新存储过程

**执行前验证**：
- [ ] 使用 `view` 工具查看 `app/services/rules/auto_baseline.py` 第31-34行
- [ ] 确认当前代码调用 `sp_refresh_metric_rule_auto_baseline`
- [ ] 使用 `query_PostgreSQL` 工具验证存储过程 `sp_refresh_metric_rule_auto_baseline_win` 是否存在：
  ```sql
  SELECT proname FROM pg_proc WHERE proname = 'sp_refresh_metric_rule_auto_baseline_win';
  ```

**执行操作**：
- [ ] 使用 `str-replace-editor` 工具修改第31-34行
- [ ] 添加存储过程检查逻辑（运行时检查）
- [ ] **如果新存储过程存在**：查询实际窗口并调用新存储过程
- [ ] **如果新存储过程不存在**：保持原有逻辑（调用旧存储过程）

**执行后验证**：
- [ ] 使用 `view` 工具查看修改后的代码
- [ ] 确认新代码包含存储过程检查逻辑
- [ ] 确认新代码包含两种调用路径（新/旧存储过程）
- [ ] 确认新代码包含实际窗口查询逻辑

**预期结果**：
- 代码成功修改，支持两种存储过程
- 运行时自动选择可用的存储过程
- 向后兼容，不影响现有环境

---

### 检查项1.3：降低 auto_baseline_b.py 的最小数据量要求

**执行前验证**：
- [ ] 使用 `view` 工具查看 `app/services/rules/auto_baseline_b.py` 第154行
- [ ] 确认当前代码为：`if df.index.size < 120:`

**执行操作**：
- [ ] 使用 `str-replace-editor` 工具修改第154行
- [ ] 将 `120` 改为 `30`
- [ ] 添加警告日志（数据点 < 30 跳过，30-120 警告）

**执行后验证**：
- [ ] 使用 `view` 工具查看修改后的代码
- [ ] 确认阈值已改为 `30`
- [ ] 确认添加了警告日志

**预期结果**：
- 最小数据量要求降低到30个数据点
- 添加了适当的警告日志

---

### 检查项1.4：降低 running_thresholds_b.py 的最小数据量要求

**执行前验证**：
- [ ] 使用 `view` 工具查看 `app/services/rules/running_thresholds_b.py` 第18行
- [ ] 确认当前代码为：`if x.size < 200:`

**执行操作**：
- [ ] 使用 `str-replace-editor` 工具修改第18行
- [ ] 将 `200` 改为 `50`
- [ ] 添加警告日志（数据点 < 50 跳过，50-200 警告）

**执行后验证**：
- [ ] 使用 `view` 工具查看修改后的代码
- [ ] 确认阈值已改为 `50`
- [ ] 确认添加了警告日志

**预期结果**：
- 最小数据量要求降低到50个数据点
- 添加了适当的警告日志

---

### 检查项1.5-1.10：在6个规则生成函数中添加/完善详细日志

**涉及文件**：
1. `app/services/rules/auto_baseline_b.py` - **无现有日志，需全部添加**
2. `app/services/rules/auto_baseline.py` - **有 log_db_function，需完善/替换**
3. `app/services/rules/running_thresholds_b.py` - **无现有日志，需全部添加**
4. `app/services/rules/running_thresholds.py` - **无现有日志，需全部添加**
5. `app/services/rules/metric_quality_rules.py` - **有 log_db_function，需完善/替换**
6. `app/services/rules/metric_quality_rules_b.py` - **无现有日志，需全部添加**

**每个文件的执行前验证**：
- [ ] 使用 `view` 工具查看整个文件
- [ ] 确认函数签名和主要逻辑
- [ ] **搜索现有日志**：使用 `search_query_regex` 搜索 `_act\.(info|warning|error|debug)|log_db_function`
- [ ] 确认哪些日志需要完善，哪些需要替换，哪些需要添加

**每个文件的执行操作**：
- [ ] **第一步**：在文件开头添加日志模块导入（如果没有）：
  ```python
  from app.core.logging.setup import get_logger
  _act = get_logger(__name__)
  ```
- [ ] **第二步**：在函数开始处添加 `[规则生成-开始]` 日志
- [ ] **第三步**：在数据加载后添加 `[规则生成-数据加载]` 日志
- [ ] **第四步**：在数据过滤后添加 `[规则生成-过滤]` 日志
- [ ] **第五步**：在每个设备/指标计算时添加 `[规则生成-计算]` 日志
- [ ] **第六步**：在数据插入后添加 `[规则生成-插入]` 日志
- [ ] **第七步**：在跳过计算时添加 `[规则生成-跳过]` 日志
- [ ] **第八步**：在函数结束处添加 `[规则生成-完成]` 日志
- [ ] **第九步**：**替换** `log_db_function` 为新的日志格式（如果存在）
- [ ] **第十步**：**删除** 冗余的日志输出

**每个文件的执行后验证**：
- [ ] 使用 `view` 工具查看修改后的代码
- [ ] 确认所有7类日志都已添加/完善
- [ ] 确认日志格式符合规范（使用 `extra` 参数传递结构化数据）
- [ ] 确认日志内容包含所有必要信息
- [ ] **确认没有重复的日志输出**
- [ ] **确认旧的 log_db_function 已被替换**

**预期结果**：
- 所有6个函数都有完整的日志输出
- 日志格式统一，内容完整
- 旧的日志已被替换，无重复输出

---

### 检查项1.11：清理所有规则生成函数中的冗余代码

**涉及文件**：
- 所有6个规则生成函数

**执行前验证**：
- [ ] 使用 `view` 工具查看每个文件的完整内容
- [ ] 搜索未使用的导入：使用 IDE 的诊断功能
- [ ] 搜索注释掉的代码：使用 `search_query_regex` 搜索 `^[ ]*#.*`
- [ ] 搜索空的异常处理：使用 `search_query_regex` 搜索 `except.*:\s*pass`

**执行操作**：
- [ ] **删除未使用的导入**：
  - 检查所有 `import` 语句
  - 删除未使用的模块（如 `time` 如果只用于 log_db_function）
- [ ] **删除注释掉的代码**：
  - 删除所有 `# ...` 注释掉的代码块
  - 保留有用的注释说明
- [ ] **删除冗余的异常处理**：
  - 删除空的 `try-except-pass` 块
  - 合并重复的异常处理逻辑
- [ ] **删除未使用的变量**：
  - 删除定义但未使用的变量
- [ ] **删除重复的查询逻辑**：
  - 确保时间窗口查询只执行一次
  - 确保 metric_id 查询只执行一次

**执行后验证**：
- [ ] 使用 `diagnostics` 工具检查是否有未使用的导入
- [ ] 使用 `view` 工具确认没有注释掉的代码
- [ ] 使用 `view` 工具确认没有空的异常处理
- [ ] 确认代码更简洁，无冗余

**预期结果**：
- 所有冗余代码已删除
- 代码更简洁、可读性更高
- 无未使用的导入、变量、异常处理

---

## ✅ 阶段2：日志系统优化（方案A+B+C）

### 检查项2.1：补充 localize_map 中的缺失字段

**执行前验证**：
- [ ] 使用 `view` 工具查看 `configs/logging.yaml` 第134-178行
- [ ] 确认当前 `localize_map` 的内容
- [ ] 确认缺失的字段列表（17个字段）

**执行操作**：
- [ ] 使用 `str-replace-editor` 工具修改 `localize_map` 部分
- [ ] 添加17个缺失字段的中文翻译：
  - function: 函数
  - thread: 线程
  - thread_id: 线程ID
  - thread_name: 线程名称
  - version: 版本
  - method: 方法
  - station_id: 站点ID
  - config: 配置
  - lookback_days: 回溯天数
  - start: 开始时间
  - end: 结束时间
  - window: 窗口
  - filters: 过滤条件
  - samples: 样本
  - remark: 备注
  - computed_at: 计算时间
  - priority: 优先级

**执行后验证**：
- [ ] 使用 `view` 工具查看修改后的 `localize_map`
- [ ] 确认所有17个字段都已添加
- [ ] 确认YAML格式正确，无语法错误

**预期结果**：
- `localize_map` 包含所有必要的字段翻译
- YAML格式正确

---

### 检查项2.2：修改 device_running_job.py - 添加详细日志

**执行前验证**：
- [ ] 使用 `view` 工具查看 `app/services/device_running_job.py` 第136-352行
- [ ] 确认 `run_for_device()` 方法的主要逻辑
- [ ] 确认当前日志输出位置

**执行操作**：
- [ ] 在方法开始处添加 `[计算设备运行状态-开始]` 日志
- [ ] 在数据加载后添加 `[计算设备运行状态-加载数据]` 日志
- [ ] 在每个时间点计算时添加 `[计算设备运行状态-计算]` 日志（包含电流、功率、频率、阈值、运行状态、健康度分数）
- [ ] 在方法结束处添加 `[计算设备运行状态-完成]` 日志

**执行后验证**：
- [ ] 使用 `view` 工具查看修改后的代码
- [ ] 确认所有4类日志都已添加
- [ ] 确认日志格式符合规范
- [ ] 确认日志内容包含所有必要信息（设备ID、时间点、电流、功率、频率、阈值、运行状态、健康度分数）

**预期结果**：
- `run_for_device()` 方法添加了详细的日志输出
- 日志格式统一，内容完整

---

### 检查项2.3：修改 presence_writer.py - 添加详细日志

**执行前验证**：
- [ ] 使用 `view` 工具查看 `app/services/reporting/presence_writer.py` 第244-263行
- [ ] 确认 `run_presence_compute()` 函数的主要逻辑
- [ ] 确认当前日志输出位置

**执行操作**：
- [ ] 在函数开始处添加 `[统计指标存在性-开始]` 日志
- [ ] 在扫描指标后添加 `[统计指标存在性-扫描]` 日志（包含可用指标列表、需要计算的指标列表）
- [ ] 在每个指标计算时添加 `[统计指标存在性-计算]` 日志
- [ ] 在函数结束处添加 `[统计指标存在性-完成]` 日志

**执行后验证**：
- [ ] 使用 `view` 工具查看修改后的代码
- [ ] 确认所有4类日志都已添加
- [ ] 确认日志格式符合规范
- [ ] 确认日志内容包含所有必要信息

**预期结果**：
- `run_presence_compute()` 函数添加了详细的日志输出
- 日志格式统一，内容完整

---

## ✅ 阶段3：备份恢复失败修复（方案A）

### 检查项3.1：修复 backup.py 的 SQL 生成逻辑

**执行前验证**：
- [ ] 使用 `codebase-retrieval` 工具查找 `BackupManager` 类和 `_generate_sql_backup` 方法
- [ ] 使用 `view` 工具查看 `_generate_sql_backup()` 方法的完整实现
- [ ] 确认当前代码如何处理时间戳字段
- [ ] 确认当前代码如何处理字符串字段

**执行操作**：
- [ ] 使用 `str-replace-editor` 工具修改 `_generate_sql_backup()` 方法
- [ ] 为时间戳类型添加引号：`'2025-10-20 14:22:49.056537+08'`
- [ ] 为字符串类型添加引号并转义单引号
- [ ] 为数值类型不添加引号
- [ ] 为布尔类型转换为 `true`/`false`

**执行后验证**：
- [ ] 使用 `view` 工具查看修改后的代码
- [ ] 确认所有类型都有正确的处理逻辑
- [ ] 确认时间戳类型添加了引号
- [ ] 确认字符串类型添加了引号并转义单引号

**预期结果**：
- `_generate_sql_backup()` 方法正确处理所有数据类型
- 生成的SQL语法正确

---

### 检查项3.2：测试备份功能

**执行前验证**：
- [ ] 确认备份功能已修复
- [ ] 确认数据库中有测试数据

**执行操作**：
- [ ] 使用 `launch-process` 工具执行 `python -m app.cli.main prepare-dim stage=1`
- [ ] 等待命令执行完成
- [ ] 使用 `view` 工具查看生成的备份文件（`backups/dim_metric_config/dim_metric_config_v1_*.sql`）

**执行后验证**：
- [ ] 确认备份文件已生成
- [ ] 确认备份SQL语法正确（时间戳有引号）
- [ ] 确认备份SQL包含所有数据

**预期结果**：
- 备份文件生成成功
- 备份SQL语法正确

---

### 检查项3.3：测试恢复功能

**执行前验证**：
- [ ] 确认备份文件存在且语法正确
- [ ] 确认数据库中有测试数据

**执行操作**：
- [ ] 使用 `query_PostgreSQL` 工具查询表的当前行数
- [ ] 使用 `launch-process` 工具执行恢复命令（如果有）
- [ ] 或者手动执行备份SQL（使用 `psql` 命令）

**执行后验证**：
- [ ] 使用 `query_PostgreSQL` 工具查询表的恢复后行数
- [ ] 确认行数与备份前一致
- [ ] 确认数据内容与备份前一致

**预期结果**：
- 恢复功能正常工作
- 数据完整性验证通过

---

## ✅ 阶段4：全面验证和测试

### 检查项4.1：运行 run-all 命令验证规则表生成

**执行前验证**：
- [ ] 确认所有代码修改已完成
- [ ] 确认数据库中有测试数据

**执行操作**：
- [ ] 使用 `launch-process` 工具执行 `python -m app.cli.main run-all configs/data_mapping.v2.json`
- [ ] 等待命令执行完成（预计耗时 ~60秒）
- [ ] 使用 `read-process` 工具读取命令输出

**执行后验证**：
- [ ] 确认命令执行成功（exit code = 0）
- [ ] 使用 `query_PostgreSQL` 工具查询规则表行数：
  ```sql
  SELECT 
      'metric_rule_auto_baseline' as table_name, COUNT(*) as row_count FROM metric_rule_auto_baseline
  UNION ALL
  SELECT 'metric_rule_auto_baseline_shadow', COUNT(*) FROM metric_rule_auto_baseline_shadow
  UNION ALL
  SELECT 'metric_quality_rules', COUNT(*) FROM metric_quality_rules
  UNION ALL
  SELECT 'metric_quality_rules_shadow', COUNT(*) FROM metric_quality_rules_shadow
  UNION ALL
  SELECT 'device_running_thresholds', COUNT(*) FROM device_running_thresholds
  UNION ALL
  SELECT 'device_running_thresholds_shadow', COUNT(*) FROM device_running_thresholds_shadow;
  ```
- [ ] 确认所有规则表都有数据（行数 > 0）

**预期结果**：
- run-all 命令执行成功
- 所有规则表都有数据

---

### 检查项4.2：验证日志格式

**执行前验证**：
- [ ] 确认 run-all 命令已执行完成

**执行操作**：
- [ ] 使用 `view` 工具查看 `logs/app.log` 文件
- [ ] 搜索 `[规则生成-开始]`、`[规则生成-数据加载]`、`[规则生成-计算]` 等标签
- [ ] 搜索 `[计算设备运行状态-开始]`、`[统计指标存在性-开始]` 等标签

**执行后验证**：
- [ ] 确认所有关键操作都有操作标签
- [ ] 确认所有字段名都已中文化
- [ ] 确认日志格式统一（使用 `extra` 参数传递结构化数据）
- [ ] 确认日志内容完整（包含所有必要信息）

**预期结果**：
- 日志格式统一，符合规范
- 所有字段名都已中文化

---

### 检查项4.3：创建测试报告

**执行前验证**：
- [ ] 确认所有验证都已完成

**执行操作**：
- [ ] 使用 `save-file` 工具创建 `测试报告-日志优化和问题修复.md`
- [ ] 包含以下内容：
  - 测试概述
  - 测试环境
  - 测试结果（每个检查项的结果）
  - 数据验证（规则表行数、日志格式）
  - 发现的问题（如果有）
  - 建议和改进

**执行后验证**：
- [ ] 确认测试报告已创建
- [ ] 确认测试报告内容完整
- [ ] 确认测试报告格式正确

**预期结果**：
- 测试报告创建成功
- 测试报告内容完整

---

## 📊 执行进度跟踪

### 阶段1：规则表为空问题修复（12项）
- [ ] 检查项1.1：修改 auto_baseline_b.py - 复用现有时间窗口查询逻辑
- [ ] 检查项1.2：修改 auto_baseline.py - 检查并使用新存储过程
- [ ] 检查项1.3：降低 auto_baseline_b.py 的最小数据量要求
- [ ] 检查项1.4：降低 running_thresholds_b.py 的最小数据量要求
- [ ] 检查项1.5：在 auto_baseline_b.py 中添加详细日志（无现有日志）
- [ ] 检查项1.6：在 auto_baseline.py 中完善详细日志（替换 log_db_function）
- [ ] 检查项1.7：在 running_thresholds_b.py 中添加详细日志（无现有日志）
- [ ] 检查项1.8：在 running_thresholds.py 中添加详细日志（无现有日志）
- [ ] 检查项1.9：在 metric_quality_rules.py 中完善详细日志（替换 log_db_function）
- [ ] 检查项1.10：在 metric_quality_rules_b.py 中添加详细日志（无现有日志）
- [ ] 检查项1.11：清理所有规则生成函数中的冗余代码

### 阶段2：日志系统优化（3项）
- [ ] 检查项2.1：补充 localize_map 中的缺失字段
- [ ] 检查项2.2：修改 device_running_job.py - 添加详细日志
- [ ] 检查项2.3：修改 presence_writer.py - 添加详细日志

### 阶段3：备份恢复失败修复（3项）
- [ ] 检查项3.1：修复 backup.py 的 SQL 生成逻辑
- [ ] 检查项3.2：测试备份功能
- [ ] 检查项3.3：测试恢复功能

### 阶段4：全面验证和测试（3项）
- [ ] 检查项4.1：运行 run-all 命令验证规则表生成
- [ ] 检查项4.2：验证日志格式
- [ ] 检查项4.3：创建测试报告

**总计**：21个检查项（修订后）

---

**文档结束**

