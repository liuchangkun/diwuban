# 数据表清单

> 最后更新：2025-09-30
> 更新者：AI（已填充）

## 核心事实表

### fact_measurements
**用途**：核心事实表，存储时序测量数据
**表类型**：TimescaleDB Hypertable
**Schema**：public
**大小**：176 kB
**数据量**：约806,400行

#### 字段详细列表

| 字段名 | 数据类型 | 可空 | 默认值 | 约束 | 功能说明 |
|--------|---------|------|--------|------|---------|
| id | bigint | NO | - | - | 主键ID（非自增） |
| station_id | bigint | NO | - | 外键 → dim_stations.id | 泵站ID |
| device_id | bigint | NO | - | 外键 → dim_devices.id | 设备ID |
| metric_id | bigint | NO | - | 外键 → dim_metric_config.id | 指标ID |
| ts_raw | timestamptz | NO | - | - | 原始时间戳（未对齐） |
| ts_bucket | timestamptz | NO | - | - | 时间桶（对齐后的时间戳，用于分区和查询） |
| value | numeric | NO | - | - | 测量值（高精度数值） |
| source_hint | text | YES | NULL | - | 数据来源提示（如：csv_import、api、calculated） |
| inserted_at | timestamptz | YES | now() | - | 数据插入时间 |
| quality_status | smallint | NO | 0 | - | 质量状态码（0=正常，1=有问题） |
| quality_type | text | YES | NULL | - | 质量类型（如：outlier、gap、invalid） |
| quality_meta | jsonb | YES | NULL | - | 质量元数据（JSON格式，存储详细质量信息） |
| quality_codes | integer[] | YES | '{}'::integer[] | - | 质量码数组（如：[101, 111]表示物理越界+异常跳变） |

#### 索引列表

| 索引名 | 索引类型 | 索引字段 | 说明 |
|--------|---------|---------|------|
| fact_measurements_new_pkey | UNIQUE BTREE | (station_id, device_id, metric_id, ts_bucket) | 主键索引，确保唯一性 |
| ux_fact_sdm_tb | UNIQUE BTREE | (station_id, device_id, metric_id, ts_bucket) | 唯一索引（备用） |
| fact_measurements_new_ts_bucket_idx | BRIN | (ts_bucket) | 时间范围索引，适合时序数据 |
| idx_fm_brin_ts | BRIN | (ts_bucket) | 时间范围索引（pages_per_range=128） |
| fact_measurements_new_device_id_ts_bucket_idx | BTREE | (device_id, ts_bucket) | 按设备查询时序数据 |
| fact_measurements_new_station_id_ts_bucket_idx | BTREE | (station_id, ts_bucket DESC) | 按泵站查询时序数据（降序） |
| fact_measurements_new_station_id_ts_bucket_device_id_metric_idx | BTREE | (station_id, ts_bucket, device_id, metric_id) INCLUDE (value) | 复合索引，包含value列 |
| idx_fm_s_d_m_t | BTREE | (station_id, device_id, metric_id, ts_bucket) | 完整复合索引 |
| ix_fact_sdm_tb | BTREE | (station_id, device_id, metric_id, ts_bucket) INCLUDE (value) | 复合索引，包含value列 |
| idx_fact_measurements_quality_codes_gin | GIN | (quality_codes) | 质量码数组索引，支持数组查询 |

#### 外键关系

- `station_id` → `dim_stations.id`
- `device_id` → `dim_devices.id`
- `metric_id` → `dim_metric_config.id`

#### 分区策略

- **分区类型**：TimescaleDB Hypertable自动分区
- **分区键**：ts_bucket（时间字段）
- **分区粒度**：自动管理
- **空间分区**：按 device_id 进行空间分区

#### 使用示例

```sql
-- 查询单个设备的时序数据
SELECT ts_bucket, value
FROM fact_measurements
WHERE device_id = 1
  AND metric_id = 10
  AND ts_bucket BETWEEN '2025-01-01' AND '2025-01-31'
ORDER BY ts_bucket;

-- 查询有质量问题的数据
SELECT *
FROM fact_measurements
WHERE quality_status = 1
  AND 101 = ANY(quality_codes);  -- 物理越界

-- 插入数据（应使用 api.upsert_measurement）
SELECT api.upsert_measurement(
    p_station_id := 1,
    p_device_id := 1,
    p_metric_id := 10,
    p_ts_raw := '2025-01-01 10:00:00+00',
    p_value := 123.45,
    p_source_hint := 'csv_import'
);
```

---

## 维度表

### dim_stations
**用途**：泵站维度表，存储泵站基础信息
**大小**：48 kB
**主键**：id (bigint, 自增)
**字段**：
- id (bigint) - 主键
- name (text, 非空) - 泵站名称
- extra (jsonb) - 扩展信息（建议包含时区）
- created_at (timestamptz) - 创建时间

**实际数据**：2个泵站
- 二期供水泵房（7个设备）
- 二期取水泵房（6个设备）

**关联关系**：被 dim_devices.station_id 引用

---

### dim_devices
**用途**：设备维度表，存储设备元数据
**大小**：48 kB
**主键**：id (bigint, 自增)
**字段**：
- id (bigint) - 主键
- station_id (bigint) - 泵站ID，外键关联 dim_stations.id
- name (text) - 设备名称
- type (text) - 设备类型（pump/main_pipeline）
- pump_type (text) - 泵类型（variable_frequency/soft_start）
- extra (jsonb) - 扩展信息
- created_at (timestamptz) - 创建时间

**实际数据**：13个设备
- 变频泵：8台
- 软启动泵：3台
- 总管：2个

**关联关系**：
- 外键关联 dim_stations.id
- 被 fact_measurements.device_id 引用

---

### dim_metric_config
**用途**：指标配置维度表，定义所有指标的元数据
**大小**：48 kB
**主键**：id (bigint, 自增)
**唯一键**：metric_key (text)
**字段**：
- id (bigint) - 主键
- metric_key (text, 唯一) - 指标键名
- unit (text) - 单位
- unit_display (text) - 显示单位
- decimals_policy (text) - 小数策略，默认 as_is
- fixed_decimals (smallint) - 固定小数位数
- value_type (text) - 值类型（bool/int/number）
- valid_min (numeric) - 有效最小值
- valid_max (numeric) - 有效最大值
- created_at (timestamptz) - 创建时间
- updated_at (timestamptz) - 更新时间

**关联关系**：被 fact_measurements.metric_id 引用

---

## 质量管理表

### quality_code_dict
**用途**：质量码字典表
**大小**：32 kB
**说明**：定义所有质量码的含义和分类

### quality_diagnosis_log
**用途**：质量诊断日志表
**大小**：128 kB
**说明**：记录质量检查的诊断信息

### quality_eval_by_device_metric
**用途**：按设备和指标的质量评估表
**大小**：8192 bytes
**说明**：存储质量评估结果

### quality_profile_log
**用途**：质量剖面日志表
**大小**：200 kB
**说明**：记录质量剖面分析结果

---

## 规则配置表

### metric_quality_rules
**用途**：指标质量规则表
**大小**：48 kB
**说明**：定义各指标的质量检查规则

### metric_quality_rules_shadow
**用途**：质量规则影子表
**大小**：16 kB
**说明**：用于规则实验和对比

### metric_rule_auto_baseline
**用途**：自动基线规则表
**大小**：56 kB
**说明**：定义自动基线计算规则

### metric_rule_auto_baseline_shadow
**用途**：自动基线规则影子表
**大小**：16 kB
**说明**：用于基线算法实验

### device_running_thresholds
**用途**：设备运行阈值表
**大小**：32 kB
**说明**：定义设备运行状态判定阈值

### device_running_thresholds_shadow
**用途**：运行阈值影子表
**大小**：16 kB
**说明**：用于阈值实验

---

## 设备能力表

### dim_device_capabilities
**用途**：设备能力维度表
**大小**：16 kB
**说明**：定义设备支持的功能和能力

### device_metric_candidates
**用途**：设备指标候选表
**大小**：16 kB
**说明**：定义设备可能产生的指标

### device_rated_params
**用途**：设备额定参数表
**大小**：32 kB
**说明**：存储设备的额定参数（额定功率、流量等）

### metric_capability_policy
**用途**：指标能力策略表
**大小**：32 kB
**说明**：定义指标的计算和验证策略

### metric_anomaly_strategy
**用途**：指标异常策略表
**大小**：16 kB
**说明**：定义异常检测和处理策略

---

## 物化视图表

### mv_device_running_1s
**用途**：设备运行状态物化视图（1秒粒度）
**大小**：15 MB
**说明**：预计算的设备运行状态数据

### mv_metric_60s_stats
**用途**：指标60秒统计物化视图
**大小**：177 MB
**说明**：预计算的60秒聚合统计数据

### mv_presence_1s
**用途**：指标存在性物化视图（1秒粒度）
**大小**：183 MB
**说明**：记录每秒每个指标的存在性

---

## 分区表（按周分区）

### metrics_presence_per_second_device
**用途**：按设备的秒级指标存在性表
**大小**：136 kB
**分区策略**：按周分区 + 哈希分区（16个分区）
**分区表命名**：mpps_w_YYYYWW_N（如 mpps_w_202507_0）
**分区数量**：约160个分区表（10周 × 16哈希分区）

---

## 临时表

### staging_raw
**用途**：数据导入临时表
**大小**：16 kB
**说明**：CSV导入时的暂存表

### staging_rejects
**用途**：数据导入拒绝表
**大小**：16 kB
**说明**：存储导入失败的数据

---

## 数据完成任务表

### completion_runs
**用途**：数据完成任务运行表
**大小**：112 kB
**说明**：记录数据完成任务的执行情况

### completion_steps
**用途**：数据完成步骤表
**大小**：80 kB
**说明**：记录任务的详细步骤

### completion_audit
**用途**：数据完成审计表
**大小**：64 kB
**说明**：审计数据完成过程

### completion_failures
**用途**：数据完成失败表
**大小**：32 kB
**说明**：记录失败的完成任务

---

## 映射表

### dim_mapping_items
**用途**：映射项维度表
**大小**：16 kB
**说明**：存储各种映射关系

### dim_metric_metadata
**用途**：指标元数据表
**大小**：16 kB
**说明**：存储指标的额外元数据

### dim_metric_metadata_override
**用途**：指标元数据覆盖表
**大小**：16 kB
**说明**：允许覆盖默认元数据

---

## 表统计总结

- **总表数**：约230个（包括分区表）
- **核心表**：4个（fact_measurements + 3个维度表）
- **质量管理表**：4个
- **规则配置表**：6个
- **物化视图**：3个（总大小约375 MB）
- **分区表**：约160个（按周+哈希分区）
- **临时表**：2个
- **其他辅助表**：约50个
