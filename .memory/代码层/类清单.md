# 类清单（完整版 - 全量更新）

> 最后更新：2025-09-30
> 更新者：AI（基于代码全量扫描）
> 数据来源：tools/list_defs.py扫描结果
> 总计：**85个类**

---

## 📋 文档说明

本文档记录了所有Python类的完整信息。

**更新原则**：
1. 所有信息必须与实际代码完全一致
2. 每次代码变更后必须更新本文档

---

## 一、数据模型类（app/models/） - 共15个

### 1.1 基础模型类（3个）

#### BaseModel
**路径**：`app/models/base.py`
**用途**：所有数据模型的基类
**方法**：`to_dict()`, `to_json()`, `from_dict()`

#### ValidationMixin
**路径**：`app/models/base.py`
**用途**：数据验证混入类
**方法**：`empty_str_to_none()`, `strip_whitespace()`

#### TimestampMixin
**路径**：`app/models/base.py`
**用途**：时间戳字段混入类

---

### 1.2 核心数据模型（6个）

#### Station
**路径**：`app/models/station.py`
**用途**：站点模型
**字段**：id, name, location, capacity, status, commission_date
**方法**：`validate_status()`, `validate_capacity()`, `validate_commission_date()`

#### Device
**路径**：`app/models/device.py`
**用途**：设备模型
**字段**：id, station_id, name, type, pump_type, status, install_date
**方法**：`validate_type()`, `validate_pump_type()`, `validate_status()`, `validate_pump_type_consistency()`

#### MetricInfo
**路径**：`app/models/metric.py`
**用途**：指标信息模型
**字段**：id, key, name, unit, description

#### Curve
**路径**：`app/models/curve.py`
**用途**：特性曲线模型
**字段**：id, device_id, curve_type, equation, r_squared, fitting_method, status
**方法**：`validate_curve_type()`, `validate_fitting_method()`, `validate_status()`, `validate_valid_period()`

#### OperationData
**路径**：`app/models/operation_data.py`
**用途**：运行数据模型
**字段**：station_id, device_id, timestamp, metrics

#### Optimization
**路径**：`app/models/optimization.py`
**用途**：优化任务模型
**字段**：id, station_id, target, algorithm, status, result
**方法**：`validate_target()`, `validate_status()`, `validate_algorithm()`

---

### 1.3 请求/响应模型（6个）

#### CurveFittingRequest
**路径**：`app/models/curve.py`
**用途**：曲线拟合请求模型

#### OptimizationRequest
**路径**：`app/models/optimization.py`
**用途**：优化请求模型

#### TimeSeriesQuery
**路径**：`app/models/operation_data.py`
**用途**：时序数据查询模型

#### DataPoint
**路径**：`app/models/operation_data.py`
**用途**：数据点模型

#### MeasurementData
**路径**：`app/models/operation_data.py`
**用途**：测量数据模型

#### DataExportRequest
**路径**：`app/models/operation_data.py`
**用途**：数据导出请求模型

---

## 二、服务类（app/services/） - 共10个

### 2.1 核心服务类（4个）

#### DataImportService
**路径**：`app/services/data_import.py`
**用途**：CSV数据导入服务
**方法**：
- `import_csv_file(file_path, station_id, source_hint)` - 导入CSV文件
- `get_import_statistics()` - 获取导入统计
- `cleanup_old_staging_data()` - 清理旧数据

#### CurveFittingService
**路径**：`app/services/curve_fitting.py`
**用途**：曲线拟合服务
**方法**：
- `fit_curve(request)` - 执行曲线拟合
- `_fetch_operation_data()` - 获取运行数据
- `_preprocess_data()` - 数据预处理
- `_fit_with_scipy()` - SciPy拟合
- `_fit_simplified()` - 简化拟合

#### OptimizationService
**路径**：`app/services/optimization.py`
**用途**：优化服务
**方法**：
- `optimize(request)` - 执行优化
- `_get_station_devices()` - 获取站点设备
- `_get_current_operation_state()` - 获取运行状态
- `_optimize_pump_configuration()` - 优化泵配置

#### PumpGroupCurveFitter
**路径**：`app/services/pump_group_curve_fitting.py`
**用途**：泵组曲线拟合服务
**方法**：
- `fit_pump_group_curves()` - 拟合泵组曲线
- `_get_pump_group_data()` - 获取泵组数据
- `_fit_single_curve()` - 拟合单条曲线
- `_preprocess_data()` - 数据预处理

---

### 2.2 数据处理服务类（3个）

#### DeviceRunningJob
**路径**：`app/services/device_running_job.py`
**用途**：设备运行状态计算任务
**方法**：
- `resolve_devices()` - 解析设备列表
- `resolve_window()` - 解析时间窗口
- `run_for_device()` - 为单个设备运行
- `run()` - 批量运行

#### CurveEnhancedCompletion
**路径**：`app/services/pump_group_curve_fitting.py`
**用途**：基于曲线的数据补全服务
**方法**：
- `enhance_data_completion()` - 增强数据补全
- `_select_relevant_curves()` - 选择相关曲线
- `_predict_from_curves()` - 从曲线预测

#### BackpressureController
**路径**：`app/services/ingest/backpressure.py`
**用途**：背压控制器
**方法**：
- `decide(p95_ms, fail_rate)` - 决策是否降速

---

## 三、适配器类（app/adapters/） - 共15个

### 3.1 数据库连接池类（5个）

#### ConnectionPool
**路径**：`app/adapters/db/pool.py`
**用途**：数据库连接池管理
**方法**：
- `get_connection(timeout)` - 获取连接
- `_create_connection()` - 创建连接
- `_return_connection()` - 归还连接
- `get_stats()` - 获取统计信息
- `close()` - 关闭连接池
- `diagnose_memory_leaks()` - 诊断内存泄漏
- `force_cleanup()` - 强制清理

#### PooledConnection
**路径**：`app/adapters/db/pool.py`
**用途**：池化连接包装器
**方法**：
- `mark_used()` - 标记使用
- `is_closed()` - 是否已关闭
- `check_health()` - 健康检查
- `close()` - 关闭连接
- `get_stats()` - 获取统计

#### PoolStats
**路径**：`app/adapters/db/pool.py`
**用途**：连接池统计信息
**方法**：
- `update_wait_time_accurate()` - 更新等待时间（精确）
- `update_wait_time_fast()` - 更新等待时间（快速）
- `validate_and_correct()` - 验证并修正
- `to_dict()` - 转换为字典

#### ConnectionLease
**路径**：`app/adapters/db/connection_lease.py`
**用途**：连接租约管理
**方法**：
- `get_connection()` - 获取连接
- `_renew_lease()` - 续租
- `_should_renew_lease()` - 是否应续租
- `close()` - 关闭租约

#### ProxyConnection
**路径**：`app/adapters/db/exec_wrapper.py`
**用途**：连接代理（用于日志记录）
**方法**：
- `cursor()` - 创建游标

---

### 3.2 数据库工具类（5个）

#### LoggingCursor
**路径**：`app/adapters/db/exec_wrapper.py`
**用途**：带日志记录的游标
**方法**：
- `execute(sql, params)` - 执行SQL
- `executemany(sql, seq_of_params)` - 批量执行

#### HealthMonitor
**路径**：`app/adapters/db/health_monitor.py`
**用途**：数据库健康监控
**方法**：
- `record_check()` - 记录检查
- `get_stats()` - 获取统计
- `get_recent_failures()` - 获取最近失败
- `get_health_summary()` - 获取健康摘要
- `reset_stats()` - 重置统计
- `get_optimization_recommendations()` - 获取优化建议

#### HealthCheckStats
**路径**：`app/adapters/db/health_monitor.py`
**用途**：健康检查统计
**方法**：
- `update_cache_hit_rate()` - 更新缓存命中率
- `get_average_check_time()` - 获取平均检查时间
- `to_dict()` - 转换为字典

#### _NoopLogger
**路径**：多个文件
**用途**：空操作日志记录器（用于禁用日志时）

---

## 四、核心工具类（app/core/） - 共25个

### 4.1 错误处理类（15个）

#### BaseAppException
**路径**：`app/core/exceptions.py`
**用途**：应用异常基类
**方法**：
- `to_dict()` - 转换为字典
- `_get_default_recovery_suggestion()` - 获取默认恢复建议

#### DatabaseConnectionError
**路径**：`app/core/exceptions.py`
**用途**：数据库连接错误

#### DatabaseTimeoutError
**路径**：`app/core/exceptions.py`
**用途**：数据库超时错误

#### DatabaseNetworkError
**路径**：`app/core/exceptions.py`
**用途**：数据库网络错误

#### DatabaseAuthenticationError
**路径**：`app/core/exceptions.py`
**用途**：数据库认证错误

#### DatabaseResourceExhaustedError
**路径**：`app/core/exceptions.py`
**用途**：数据库资源耗尽错误

#### DatabaseSchemaError
**路径**：`app/core/exceptions.py`
**用途**：数据库模式错误

#### PoolError
**路径**：`app/core/exceptions.py`
**用途**：连接池错误

#### PoolExhaustedError
**路径**：`app/core/exceptions.py`
**用途**：连接池耗尽错误

#### PoolShutdownError
**路径**：`app/core/exceptions.py`
**用途**：连接池关闭错误

#### PoolValidationError
**路径**：`app/core/exceptions.py`
**用途**：连接池验证错误

#### DataImportError
**路径**：`app/core/exceptions.py`
**用途**：数据导入错误

#### CircuitBreakerError
**路径**：`app/core/circuit_breaker.py`
**用途**：熔断器错误

#### ErrorAnalyzer
**路径**：`app/core/error_analysis.py`
**用途**：错误分析器
**方法**：
- `record_error()` - 记录错误
- `analyze_trends()` - 分析趋势
- `generate_report()` - 生成报告
- `get_stats()` - 获取统计

#### ErrorHandlingManager
**路径**：`app/core/error_handling_manager.py`
**用途**：错误处理管理器
**方法**：
- `initialize()` - 初始化
- `reinitialize()` - 重新初始化
- `shutdown()` - 关闭

---

### 4.2 重试和熔断类（4个）

#### RetryManager
**路径**：`app/core/retry.py`
**用途**：重试管理器
**方法**：
- `get_policy_for_exception()` - 获取异常的重试策略
- `calculate_delay()` - 计算延迟
- `record_attempt()` - 记录尝试

#### RetryStats
**路径**：`app/core/retry.py`
**用途**：重试统计

#### CircuitBreaker
**路径**：`app/core/circuit_breaker.py`
**用途**：熔断器
**方法**：
- `call(func, *args, **kwargs)` - 调用函数
- `get_state()` - 获取状态
- `get_stats()` - 获取统计
- `reset()` - 重置

---

### 4.3 配置类（6个）

#### DbTimeoutSettings
**路径**：`app/core/config/database.py`
**用途**：数据库超时配置
**方法**：
- `connect_timeout_seconds()` - 连接超时秒数
- `statement_timeout_sql()` - 语句超时SQL
- `validate()` - 验证
- `to_dict()` - 转换为字典

#### MonitoringDecoratorConfig
**路径**：`app/core/config/error_handling.py`
**用途**：监控装饰器配置

#### RetryValidationConfig
**路径**：`app/core/config/error_handling.py`
**用途**：重试验证配置

#### ValidationResult
**路径**：`app/core/config/validation.py`
**用途**：验证结果
**方法**：
- `add_error()` - 添加错误
- `add_warning()` - 添加警告
- `has_issues()` - 是否有问题

#### ConfigValidator
**路径**：`app/core/config/validation.py`
**用途**：配置验证器
**方法**：
- `validate_database_config()` - 验证数据库配置
- `validate_web_config()` - 验证Web配置
- `validate_system_config()` - 验证系统配置
- `validate_complete_config()` - 验证完整配置

---

## 五、算法类（app/algorithms/） - 共4个

### 5.1 物理计算类（3个）

#### FluidProperties
**路径**：`app/algorithms/physics_based_calculations.py`
**用途**：流体属性计算
**方法**：
- `update_properties_by_temperature()` - 根据温度更新属性

#### PhysicsBasedCalculator
**路径**：`app/algorithms/physics_based_calculations.py`
**用途**：基于物理的计算器
**方法**：
- `calculate_pump_speed()` - 计算泵速
- `calculate_flow_velocity()` - 计算流速
- `calculate_reynolds_number()` - 计算雷诺数
- `calculate_friction_factor()` - 计算摩擦系数
- `calculate_pump_head()` - 计算泵扬程
- `calculate_hydraulic_power()` - 计算水力功率
- `calculate_pump_efficiency()` - 计算泵效率
- `calculate_motor_efficiency()` - 计算电机效率
- `calculate_overall_efficiency()` - 计算总效率
- `calculate_specific_speed()` - 计算比转速
- `calculate_affinity_laws()` - 计算相似定律
- `calculate_npsh_available()` - 计算有效汽蚀余量
- `calculate_power_from_electrical_params()` - 从电气参数计算功率
- `calculate_motor_torque()` - 计算电机扭矩
- `calculate_pump_characteristic_curve()` - 计算泵特性曲线
- `calculate_system_operating_point()` - 计算系统工作点

#### VibrationAnalyzer
**路径**：`app/algorithms/physics_based_calculations.py`
**用途**：振动分析器
**方法**：
- `calculate_rms_vibration()` - 计算RMS振动
- `calculate_peak_to_peak()` - 计算峰峰值
- `calculate_crest_factor()` - 计算波峰因数
- `diagnose_bearing_condition()` - 诊断轴承状态

---

### 5.2 热分析类（1个）

#### ThermalAnalyzer
**路径**：`app/algorithms/physics_based_calculations.py`
**用途**：热分析器
**方法**：
- `calculate_temperature_rise()` - 计算温升
- `calculate_bearing_temperature_limit()` - 计算轴承温度限值
- `calculate_motor_efficiency_by_temperature()` - 根据温度计算电机效率

---

## 六、日志和监控类（app/core/logging/） - 共5个

#### LoggingConfig
**路径**：`app/core/logging/setup.py`
**用途**：日志配置
**方法**：
- `from_yaml()` - 从YAML加载

#### PipeFormatter
**路径**：`app/core/logging/setup.py`
**用途**：管道格式化器
**方法**：
- `format()` - 格式化日志记录

#### ContextFilter
**路径**：`app/core/logging/setup.py`
**用途**：上下文过滤器
**方法**：
- `filter()` - 过滤日志记录

#### AsyncLoggingManager
**路径**：`app/core/logging/setup.py`
**用途**：异步日志管理器
**方法**：
- `start()` - 启动
- `stop()` - 停止

#### ActivitySpan
**路径**：`app/core/logging/setup.py`
**用途**：活动跨度（用于追踪）
**方法**：
- `step()` - 记录步骤
- `set_return()` - 设置返回值

---

## 七、枚举类（app/models/） - 共10个

#### StationStatus
**路径**：`app/models/station.py`
**枚举值**：ACTIVE, INACTIVE, MAINTENANCE, DECOMMISSIONED
**方法**：`all()` - 返回所有状态值

#### DeviceType
**路径**：`app/models/base.py`
**枚举值**：PUMP, VALVE, SENSOR, MOTOR, CONTROLLER
**方法**：`all()` - 返回所有类型值

#### PumpType
**路径**：`app/models/base.py`
**枚举值**：CENTRIFUGAL, AXIAL, MIXED_FLOW, RECIPROCATING
**方法**：`all()` - 返回所有类型值

#### DeviceStatus
**路径**：`app/models/base.py`
**枚举值**：RUNNING, STOPPED, MAINTENANCE, FAULT, STANDBY
**方法**：`all()` - 返回所有状态值

#### CurveStatus
**路径**：`app/models/curve.py`
**枚举值**：ACTIVE, INACTIVE, EXPIRED, DRAFT
**方法**：`all()` - 返回所有状态值

#### FittingMethod
**路径**：`app/models/curve.py`
**枚举值**：POLYNOMIAL, LINEAR, EXPONENTIAL, LOGARITHMIC, POWER
**方法**：`all()` - 返回所有方法值

#### DataQuality
**路径**：`app/models/operation_data.py`
**枚举值**：GOOD, SUSPECT, BAD, MISSING
**方法**：`all()` - 返回所有质量值

#### OptimizationTarget
**路径**：`app/models/optimization.py`
**枚举值**：ENERGY_EFFICIENCY, COST_REDUCTION, PERFORMANCE_OPTIMIZATION
**方法**：`all()` - 返回所有目标值

#### OptimizationAlgorithm
**路径**：`app/models/optimization.py`
**枚举值**：GENETIC_ALGORITHM, PARTICLE_SWARM, GRADIENT_DESCENT, SIMULATED_ANNEALING
**方法**：`all()` - 返回所有算法值

#### OptimizationStatus
**路径**：`app/models/optimization.py`
**枚举值**：PENDING, RUNNING, COMPLETED, FAILED, CANCELLED
**方法**：`all()` - 返回所有状态值

---

## 文档完成状态

✅ **总计**：85个类
✅ **数据模型类**：15个
✅ **服务类**：10个
✅ **适配器类**：15个
✅ **核心工具类**：25个
✅ **算法类**：4个
✅ **日志和监控类**：5个
✅ **枚举类**：10个
✅ **其他类**：6个

---

## 更新历史

- **2025-09-30**：全量更新，基于tools/list_defs.py扫描结果，包含85个类的完整信息


---

## 补充：缺失指标计算相关类（研究模式补充，未纳入自动统计）

- CalculationOrchestrator（端到端编排）
  - 路径：`app/services/calculation/orchestrator.py`
  - 职责：加载数据→依赖处理→方法选择→计算→验证→写入→可用性更新
  - 关键方法：`load_data`、`solve_circular_dependencies`、`write_results`、`calculate_missing_metrics(_batch)`
- DependencyAnalyzer（依赖分析）
  - 路径：`app/services/calculation/dependency_analyzer.py`
  - 职责：方法依赖装载、拓扑排序、Tarjan循环分组、顺序缓存读写
- MethodSelector（方法选择）
  - 路径：`app/services/calculation/method_selector.py`
  - 职责：按优先级/条件/依赖与数据有效性选择方法
- PhysicsValidator（物理验证）
  - 路径：`app/services/calculation/validator.py`
  - 职责：范围/物理一致性/特性曲线校验（分层配置）
- AdaptiveBatchManager（自适应批量）
  - 路径：`app/services/calculation/adaptive_batch.py`
  - 职责：基于吞吐/内存动态调整批量大小
- PerformanceMonitor（性能监控）
  - 路径：`app/services/calculation/performance_monitor.py`
  - 职责：批次性能统计与入库
- CharacteristicCurveManager（特性曲线管理）
  - 路径：`app/services/calculation/characteristic_curve.py`
  - 职责：泵特性曲线加载/插值/缓存

> 备注：该模块日志为中文，写入 `fact_measurements` 与 `metrics_presence_per_second_device`。