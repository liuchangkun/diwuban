# .memory/ 文档更新总结

> 更新时间：2025-09-30
> 执行者：AI
> 状态：部分完成（数据层核心文档已更新）

---

## 已完成的工作

### 1. 数据库信息收集
✅ 已查询的表结构：
- fact_measurements（13个字段，10个索引）
- dim_stations（4个字段）
- dim_devices（7个字段）
- dim_metric_config（11个字段）
- quality_code_dict（5个字段）
- metric_quality_rules（18个字段）
- mv_device_running_1s（6个字段）

✅ 已查询的函数签名：
- api schema：6个函数（完整签名和参数）
- reporting schema：8个函数（完整签名和参数）

### 2. 代码文件扫描
✅ 已读取的代码文件：
- app/services/data_import.py（405行）
- app/adapters/db/pool.py（1377行）
- app/services/curve_fitting.py（519行）
- app/main.py（部分）

✅ 已扫描的目录结构：
- app/api/v1/（路由和端点）
- app/models/（数据模型）
- app/services/（服务层）

### 3. 已创建的完整文档
✅ `.memory/数据层/数据库函数清单_完整版.md`
- 包含api schema的6个函数的完整签名、参数说明、返回值结构、使用示例
- 包含reporting schema的8个函数的完整签名和说明
- 总计300行，格式规范

### 4. 备份工作
✅ 已备份旧文档到：`.memory/_backup_20250930_105438/`

---

## 待完成的工作

### 数据层文档（优先级：高）

#### `.memory/数据层/数据表清单.md`
**当前状态**：部分更新（fact_measurements已更新为详细版本）  
**待补充内容**：
1. 质量管理表的完整字段列表：
   - quality_code_dict
   - quality_diagnosis_log
   - quality_eval_by_device_metric
   - quality_profile_log
2. 规则配置表的完整字段列表：
   - metric_quality_rules（已查询，待整理）
   - metric_rule_auto_baseline
   - device_running_thresholds
3. 物化视图表的完整字段列表：
   - mv_device_running_1s（已查询，待整理）
   - mv_metric_60s_stats
   - mv_presence_1s
4. 分区表的说明（~160个分区表）
5. 临时表的说明（staging_raw、staging_rejects）

**建议模板**：
```markdown
### 表名
**用途**：...  
**Schema**：public  
**大小**：...  
**数据量**：...

#### 字段详细列表
| 字段名 | 数据类型 | 可空 | 默认值 | 约束 | 功能说明 |
|--------|---------|------|--------|------|---------|
| ... | ... | ... | ... | ... | ... |

#### 索引列表
| 索引名 | 索引类型 | 索引字段 | 说明 |
|--------|---------|---------|------|
| ... | ... | ... | ... |

#### 外键关系
- ...

#### 使用示例
```sql
...
```
```

#### `.memory/数据层/视图清单.md`
**当前状态**：简略版本  
**待补充内容**：
1. 每个视图的完整字段列表（字段名、类型、说明）
2. 视图的SQL定义
3. 视图的使用示例
4. 视图的性能特征

**建议查询**：
```sql
-- 获取视图定义
SELECT definition
FROM pg_views
WHERE schemaname = 'public' AND viewname = 'mv_device_running_1s';

-- 获取视图字段
SELECT column_name, data_type
FROM information_schema.columns
WHERE table_schema = 'public' AND table_name = 'mv_device_running_1s'
ORDER BY ordinal_position;
```

#### `.memory/数据层/Schema说明.md`
**当前状态**：基本完整  
**待补充内容**：
1. 每个schema的权限说明
2. 每个schema的使用规范（更详细）

---

### 代码层文档（优先级：高）

#### `.memory/代码层/应用函数清单.md`
**当前状态**：简略版本  
**待补充内容**：
1. 每个函数的完整签名（从实际代码中提取）
2. 每个函数的参数类型和返回值类型
3. 每个函数的详细用途说明
4. 每个函数的调用示例

**需要读取的文件**：
- app/services/data_import.py（DataImportService类的所有方法）
- app/services/curve_fitting.py（CurveFittingService类的所有方法）
- app/services/data_completion.py
- app/services/quality/*.py
- app/services/reporting/*.py

**建议模板**：
```markdown
### 函数名
**文件**：app/services/xxx.py  
**类**：XxxService  
**完整签名**：
```python
async def function_name(
    self,
    param1: Type1,
    param2: Type2 = default_value
) -> ReturnType:
```

**参数说明**：
- `param1`: 参数1的说明
- `param2`: 参数2的说明

**返回值**：ReturnType - 返回值说明

**用途**：详细的功能描述

**调用示例**：
```python
result = await service.function_name(
    param1=value1,
    param2=value2
)
```
```

#### `.memory/代码层/类清单.md`
**当前状态**：简略版本  
**待补充内容**：
1. 每个类的完整属性列表
2. 每个类的完整方法列表（包含签名）
3. 类之间的继承关系
4. 类之间的依赖关系

**需要读取的文件**：
- app/models/*.py（所有模型类）
- app/services/*.py（所有服务类）
- app/adapters/db/*.py（数据库适配器类）

**建议模板**：
```markdown
### 类名
**文件**：app/xxx/yyy.py  
**继承**：BaseClass  
**用途**：...

#### 属性列表
| 属性名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| ... | ... | ... | ... |

#### 方法列表
| 方法名 | 签名 | 返回值 | 说明 |
|--------|------|--------|------|
| ... | ... | ... | ... |

#### 依赖关系
- 依赖：ClassA、ClassB
- 被依赖：ClassC、ClassD

#### 使用示例
```python
...
```
```

#### `.memory/代码层/API接口清单.md`
**当前状态**：简略版本  
**待补充内容**：
1. 每个API端点的完整路径
2. 每个API端点的请求方法（GET/POST/PUT/DELETE）
3. 每个API端点的请求体结构（Pydantic schema）
4. 每个API端点的响应体结构
5. 每个API端点的查询参数
6. 每个API端点的使用示例（curl或Python requests）

**需要读取的文件**：
- app/api/v1/router.py
- app/api/v1/endpoints/*.py
- app/schemas/*.py（请求和响应模型）

**建议模板**：
```markdown
### API端点名称
**路径**：`/api/v1/xxx/yyy`  
**方法**：POST  
**用途**：...

#### 请求体
```json
{
  "field1": "value1",
  "field2": 123
}
```

#### 响应体
```json
{
  "success": true,
  "data": {...}
}
```

#### 查询参数
| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| ... | ... | ... | ... | ... |

#### 使用示例
```bash
curl -X POST http://localhost:8000/api/v1/xxx/yyy \
  -H "Content-Type: application/json" \
  -d '{"field1": "value1"}'
```
```

---

### 架构层文档（优先级：中）

#### `.memory/架构层/系统架构概览.md`
**当前状态**：简略版本  
**待补充内容**：
1. 详细的系统架构图（使用Mermaid或ASCII art）
2. 技术栈的详细说明（版本号、配置）
3. 部署架构说明
4. 数据流和控制流的详细说明

#### `.memory/架构层/功能模块清单.md`
**当前状态**：简略版本  
**待补充内容**：
1. 每个模块的完整功能描述
2. 每个模块的具体动作和步骤
3. 模块之间的调用关系图
4. 模块的工作流程图

#### `.memory/架构层/服务清单.md`
**当前状态**：简略版本  
**待补充内容**：
1. 每个服务类的完整方法清单（包含方法签名）
2. 每个方法的详细描述和用途
3. 服务的工作逻辑和业务流程
4. 服务之间的调用关系和依赖

---

### 业务层文档（优先级：中）

#### `.memory/业务层/业务流程.md`
**当前状态**：基本完整  
**待补充内容**：
1. 每个流程的详细步骤（带序号）
2. 每个流程的流程图（Mermaid）
3. 每个流程的异常处理说明

#### `.memory/业务层/算法说明.md`
**当前状态**：基本完整  
**待补充内容**：
1. 每个算法的数学公式
2. 每个算法的伪代码
3. 每个算法的性能特征

#### `.memory/业务层/质量检查规则.md`
**当前状态**：基本完整  
**待补充内容**：
1. 每个质量码的检查算法（伪代码）
2. 每个质量码的阈值配置说明

---

## 执行建议

### 方案1：逐个文档更新（推荐）
**优点**：内容详细、准确  
**缺点**：耗时较长

**步骤**：
1. 先完成数据层文档（最重要）
2. 再完成代码层文档
3. 最后完成架构层和业务层文档

### 方案2：批量更新（快速）
**优点**：快速完成  
**缺点**：可能不够详细

**步骤**：
1. 使用PowerShell批量查询数据库
2. 使用codebase-retrieval批量读取代码
3. 使用模板批量生成文档

---

## 后续维护建议

1. **定期更新**：每次代码或数据库结构变更后，及时更新对应文档
2. **版本控制**：在变更日志中记录每次更新
3. **自动化**：考虑编写脚本自动从数据库和代码中提取信息更新文档
4. **验证机制**：定期验证文档内容与实际代码/数据库的一致性

---

## 需要的SQL查询

```sql
-- 获取所有表的字段信息
SELECT 
    table_name,
    column_name,
    data_type,
    is_nullable,
    column_default
FROM information_schema.columns
WHERE table_schema = 'public'
ORDER BY table_name, ordinal_position;

-- 获取所有索引信息
SELECT
    tablename,
    indexname,
    indexdef
FROM pg_indexes
WHERE schemaname = 'public'
ORDER BY tablename, indexname;

-- 获取所有外键信息
SELECT
    tc.table_name,
    kcu.column_name,
    ccu.table_name AS foreign_table_name,
    ccu.column_name AS foreign_column_name
FROM information_schema.table_constraints AS tc
JOIN information_schema.key_column_usage AS kcu
  ON tc.constraint_name = kcu.constraint_name
JOIN information_schema.constraint_column_usage AS ccu
  ON ccu.constraint_name = tc.constraint_name
WHERE tc.constraint_type = 'FOREIGN KEY'
  AND tc.table_schema = 'public';

-- 获取所有函数的完整定义
SELECT 
    n.nspname AS schema_name,
    p.proname AS function_name,
    pg_get_function_arguments(p.oid) AS arguments,
    pg_get_function_result(p.oid) AS return_type,
    pg_get_functiondef(p.oid) AS definition
FROM pg_proc p
JOIN pg_namespace n ON p.pronamespace = n.oid
WHERE n.nspname IN ('api', 'reporting', 'public')
ORDER BY n.nspname, p.proname;
```

---

## 需要读取的代码文件

```
app/services/data_import.py
app/services/curve_fitting.py
app/services/data_completion.py
app/services/quality/*.py
app/services/reporting/*.py
app/services/rules/*.py
app/api/v1/router.py
app/api/v1/endpoints/*.py
app/models/*.py
app/schemas/*.py
app/adapters/db/*.py
```

