请按照以下步骤执行数据质量检查流程的分析和验证工作：数据库表

## 第一步：数据库表执行完整的数据质量检查
1. 在项目根目录 `d:\Augment\diwuban` 下执行数据质量检查命令 `run-all`
2. 等待命令完全执行完成（不要中断），观察并记录执行过程中的所有输出和日志信息
3. 记录命令执行的结果状态（成功/失败）和任何错误信息

## 第二步：数据库表深入分析 `metrics_presence_per_second_device` 表的处理逻辑
针对 `metrics_presence_per_second_device` 表，需要验证以下关键点：数据库表

### 2.1 定位相关代码
- 使用 codebase-retrieval 工具搜索所有涉及 `metrics_presence_per_second_device` 表的代码文件
- 特别关注在 presence 阶段（对应配置文件 `configs\merge.yaml` 中 `presence: true` 设置）处理该表的代码逻辑

### 2.2 验证数据处理流程
确认该表的处理流程是否符合以下预期模式：数据库表
- **预期流程**：数据库表清空表（TRUNCATE 或 DELETE）→ 重新计算数据 → 写入新数据
- **需要排除的错误流程**：数据库表备份表数据（backup）→ 处理数据 → 恢复表数据（restore）

### 2.3 关键检查点
- [ ] 该表在 presence 阶段开始时是否被完全清空？
- [ ] 该表的数据是否通过重新计算生成并写入？
- [ ] 代码中是否存在针对该表的备份（backup）操作？
- [ ] 代码中是否存在针对该表的恢复（restore）操作？
- [ ] 如果存在备份/恢复操作，这些操作是否是导致数据问题的根源？

## 第三步：数据库表问题诊断和修复建议
如果发现 `metrics_presence_per_second_device` 表存在不合理的备份/恢复逻辑：数据库表
1. 明确指出问题代码的位置（文件路径和行号）
2. 说明当前逻辑为什么不正确
3. 提供修复方案：数据库表移除备份/恢复逻辑，改为直接清空表后重新计算写入
4. 在实施修复前，先向我确认修复方案是否正确

## 输出要求
请在完成分析后提供：数据库表
- `run-all` 命令的执行结果摘要
- `metrics_presence_per_second_device` 表的当前处理流程描述
- 发现的问题清单（如果有）
- 建议的修复方案（如果需要修复）





D:\Augment\diwuban\app\services\rules\auto_baseline_b.py:229: FutureWarning: 'S' is deprecated and will be removed in a future version, please use 's' instead.
  df = df.resample("60S").mean().interpolate(limit_direction="both")
D:\Augment\diwuban\app\services\rules\auto_baseline_b.py:229: FutureWarning: 'S' is deprecated and will be removed in a future version, please use 's' instead.
  df = df.resample("60S").mean().interpolate(limit_direction="both")
D:\Augment\diwuban\app\services\rules\auto_baseline_b.py:229: FutureWarning: 'S' is deprecated and will be removed in a future version, please use 's' instead.
  df = df.resample("60S").mean().interpolate(limit_direction="both")
D:\Augment\diwuban\app\services\rules\auto_baseline_b.py:229: FutureWarning: 'S' is deprecated and will be removed in a future version, please use 's' instead.   

 


请按照以下步骤执行完整的测试和验证流程，并在每个阶段提供详细的验证报告：数据库表

## 第一阶段：数据库表测试执行
1. **启动测试**
   - 使用 `launch-process` 工具运行 `run-all` 命令
   - 设置合理的超时时间（建议至少 10 分钟）
   - 捕获完整的标准输出和错误输出

2. **监控执行过程**
   - 实时读取命令执行日志
   - 记录所有警告、错误和异常信息
   - 注意程序执行的各个阶段和时间点

## 第二阶段：数据库表功能验证
3. **模块功能检查**
   - 确认所有功能模块是否成功启动和完成
   - 验证模块间的调用顺序是否符合预期
   - 检查是否有模块执行失败或被跳过

4. **业务逻辑验证**
   - 对照业务需求文档，验证核心业务流程是否正确执行
   - 检查条件分支、循环逻辑是否按预期运行
   - 确认异常处理机制是否正常工作

## 第三阶段：数据库表数据库验证
5. **数据完整性检查**
   - 使用 `query_PostgreSQL` 工具查询所有相关数据库表
   - 验证每个表中是否有数据写入
   - 检查是否存在空表或缺失的必要数据

6. **数据准确性验证**
   - **数值准确性**：数据库表
     - 抽取样本数据，对比源数据文件和数据库中的数值
     - 验证计算字段（如总和、平均值、百分比等）的计算逻辑是否正确
     - 检查数值的精度、舍入方式是否符合业务要求
   
   - **数量正确性**：数据库表
     - 统计每个表的记录总数
     - 对比源数据的记录数和数据库中的记录数
     - 验证是否存在重复记录或遗漏记录
     - 检查关联表之间的记录数量关系是否合理（如外键关系）

7. **数据质量检查**
   - 验证数据类型是否符合表结构定义（如 INTEGER、VARCHAR、TIMESTAMP 等）
   - 检查数据格式是否正确（如日期格式、数值格式）
   - 验证约束条件（NOT NULL、UNIQUE、CHECK 约束等）是否被满足
   - 检查是否存在异常值或超出合理范围的数据

## 第四阶段：数据库表参数和阈值验证
8. **参数验证**
   - 列出各个数据导入阶段生成的所有参数
   - 验证参数值是否符合业务规则和配置要求
   - 检查参数的数据类型和取值范围

9. **阈值验证**
   - 识别所有计算得出的阈值（如统计阈值、业务阈值等）
   - 验证阈值的计算公式和逻辑是否正确
   - 确认阈值是否在合理的业务范围内
   - 检查阈值的应用是否符合预期

## 第五阶段：数据库表问题分析（如发现异常）
10. **问题定位和分析**
    - **根因分析**：数据库表
      - 使用 `codebase-retrieval` 和 `view` 工具检查相关代码逻辑
      - 追溯数据处理流程，定位问题发生的具体位置
      - 分析是否为代码逻辑错误、配置问题或数据源问题
    
    - **影响范围评估**：数据库表
      - 确定问题影响的功能模块和数据表
      - 评估问题的严重程度（致命、严重、一般、轻微）
      - 判断是否影响后续流程或其他模块
    
    - **数据流追溯**：数据库表
      - 从源数据开始，逐步追踪数据的转换和处理过程
      - 识别数据在哪个环节出现异常
      - 检查中间处理步骤的输入输出

## 第六阶段：数据库表报告输出
11. **验证结果总结**
    - 提供结构化的验证报告，包含：数据库表
      - 测试执行概况（开始时间、结束时间、总耗时）
      - 功能模块验证结果（通过/失败）
      - 数据库验证结果（表名、记录数、数据质量评估）
      - 参数和阈值验证结果

12. **问题清单**
    - 按严重程度分类列出所有发现的问题
    - 对每个问题提供：数据库表
      - 问题描述（现象）
      - 根本原因（代码位置、逻辑错误）
      - 影响范围（受影响的模块、表、数据）
      - 修复建议（具体的代码修改方案或配置调整）
      - 优先级（紧急、高、中、低）

13. **后续行动建议**
    - 如果所有验证通过，确认系统可以进入下一阶段
    - 如果存在问题，提供优先修复顺序和预估修复时间
    - 建议是否需要重新执行测试

## 执行要求
- 使用任务管理工具（`add_tasks`、`update_tasks`）跟踪验证进度
- 每完成一个阶段，更新任务状态并请求用户确认
- 所有数据库查询结果需要保存并展示关键数据样本
- 发现问题时立即报告，不要等到所有验证完成
- 使用表格、列表等结构化格式呈现验证结果          



 


 1：数据库表quality_eval_by_device_metric  字段没有备注和注释。  什么功能需要这个表，每个字段是否都被使用，功能是什么。
 2：数据库表quality_profile_log 字段没有备注和注释，表的用途说明不够丰富。什么功能需要这个表，每个字段是否都被使用，功能是什么。
4：数据库表mv_presence_1s  表是空的，为什么是空的，表有什么用，作用是什么，什么功能需要这个表，每个字段是否都被使用，功能是什么。
   5：数据库表mv_metric_60s_stats 表是空的，为什么是空的，表有什么用，作用是什么，什么功能需要这个表，每个字段是否都被使用，功能是什么。记忆都是错的，只能依靠代码、脚本、数据库分析，你可以运行相关cli命令运行相关功能进行测试分析。
 6：数据库表device_running_thresholds_shadow 表是空的，为什么是空的，表有什么用，作用是什么，什么功能需要这个表，每个字段是否都被使用，功能是什么。记忆都是错的，只能依靠代码、脚本、数据库分析，你可以运行相关cli命令运行相关功能进行测试分析。


 


 你好，请确认你已加载 RIPER-5 协议。
 数据库表dim_metric_metadata 表是空的，为什么是空的，表有什么用，作用是什么，什么功能需要这个表，每个字段是否都被使用，功能是什么。记忆都是错的，只能依靠代码、脚本、数据库分析，你可以运行相关cli命令运行相关功能进行测试分析。

 

















 7：数据库表calculation_validation_config 表里只有全局参数，没有设备参数，是否合理？记忆都是错的，只能依靠代码、脚本、数据库分析，你可以运行相关cli命令运行相关功能进行测试分析。
 

 10：数据库表dim_device_capabilities  表是空的，为什么是空的，表有什么用，作用是什么，什么功能需要这个表，每个字段是否都被使用，功能是什么。记忆都是错的，只能依靠代码、脚本、数据库分析，你可以运行相关cli命令运行相关功能进行测试分析。
 11：数据库表completion_runs 表有什么用，作用是什么，什么功能需要这个表，每个字段是否都被使用，功能是什么。记忆都是错的，只能依靠代码、脚本、数据库分析，你可以运行相关cli命令运行相关功能进行测试分析。
 12：数据库表dim_metric_metadata_override   表是空的，为什么是空的，表有什么用，作用是什么，什么功能需要这个表，每个字段是否都被使用，功能是什么。字段没有备注和注释，表的用途说明不够丰富。记忆都是错的，只能依靠代码、脚本、数据库分析，你可以运行相关cli命令运行相关功能进行测试分析。
 13：数据库表metric_quality_rules  表是空的，为什么是空的，表有什么用，作用是什么，什么功能需要这个表，每个字段是否都被使用，功能是什么。记忆都是错的，只能依靠代码、脚本、数据库分析，你可以运行相关cli命令运行相关功能进行测试分析。
 14：数据库表quality_eval_by_device_metric  表是空的，为什么是空的，表有什么用，作用是什么，什么功能需要这个表，每个字段是否都被使用，功能是什么。字段没有备注和注释，表的用途说明不够丰富。记忆都是错的，只能依靠代码、脚本、数据库分析，你可以运行相关cli命令运行相关功能进行测试分析。
 15：数据库表metric_rule_auto_baseline  表是空的，为什么是空的，表有什么用，作用是什么，什么功能需要这个表，每个字段是否都被使用，功能是什么。记忆都是错的，只能依靠代码、脚本、数据库分析，你可以运行相关cli命令运行相关功能进行测试分析。






 