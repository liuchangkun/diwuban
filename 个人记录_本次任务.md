











 你好，请确认你已加载 RIPER-5 协议。

 根据   特性曲线开发\开发文档 目录下的文档，排查、检查、检索、分析，还有没有完成的功能模块、代码、没有集成的代码等工作吗？
 需要你结合实际落地的代码和数据库内容确定。
 不要重复造轮子。

  
  

  进入执行模式，按照 `.tasks\泵组直接拟合-进度跟踪-2025-12-09.md` 中的任务清单开始逐项实施开发工作。

**执行要求**：

1. **严格按照顺序执行**：从 D1（数据结构）开始，按 D1 → D2 → D3 → ... → D12 的顺序执行

2. **逐步完成并标记**：
   - 完成每个步骤后，立即更新进度跟踪文档，将 `[ ]` 改为 `[x]`
   - 完成每个阶段（D1-D12）后，更新总体进度表中的状态为 ✅
   - 每完成一个步骤后，请求用户确认再继续下一步

3. **强制复用现有代码**（禁止重复造轮子）：
   - 在编写任何新代码前，必须先使用 `codebase-retrieval` 搜索是否已有类似实现
   - 必须复用以下已验证的组件：
     - `BaseMethod._calculate_metrics()` - 用于计算 R²、RMSE、MAE、MAPE
     - `ParameterOptimizer.cross_validate()` - 用于交叉验证
     - `FrequencyDataProvider.get_latest_frequencies()` - 用于获取频率数据
     - `ConnectionLease` - 用于长时间数据库操作
     - `get_connection()` - 用于数据库连接
   - 参考技术规范文档第6节"复用组件验证清单"，在使用前必须用 `view` 验证函数签名

4. **严禁使用默认值和回退值**：
   - 禁止使用 `||`、`??`、`or` 等运算符提供回退值
   - 禁止在函数参数中使用默认值（除非技术规范明确要求）
   - 禁止使用硬编码的默认值
   - 所有配置必须从 YAML 文件或数据库读取
   - 数据缺失时必须抛出明确的异常，不能静默使用默认值

5. **验证每个步骤**：
   - 每完成一个步骤后，使用技术规范文档第8节"验证方法"中的命令进行验证
   - 使用 `diagnostics` 检查语法错误
   - 使用 `view` 确认代码已正确添加

6. **参考文档**：
   - 主要参考：`.tasks\泵组直接拟合-技术规范-2025-12-09.md`（1402行完整技术规范）
   - 进度跟踪：`.tasks\泵组直接拟合-进度跟踪-2025-12-09.md`（任务清单和进度表）

7. **错误处理**：
   - 所有异常消息必须使用中文
   - 参考技术规范文档第9节"错误处理规范"
   - 直接拟合失败不能影响合成曲线的执行

现在开始执行 D1（数据结构定义）的步骤1.1。






   1： 补齐HighResPlotter，以及其他未实现的功能代码。
   2：实施D10图片生成功能，不要重复造车。

   - 禁止在函数参数中使用默认值（除非技术规范明确要求）
   - 禁止使用硬编码的默认值
   - 所有配置必须从 YAML 文件或数据库读取
   - 数据缺失时必须抛出明确的异常，不能静默使用默认值






















基于已完成的特性曲线拟合系统代码，创建完整的端到端演示：

**目标**：生成模拟数据并执行完整的曲线拟合流程，覆盖所有支持的场景。

**需要覆盖的场景**：
1. **单泵场景**：
   - 软启动单泵（SOFT_START_SINGLE）
   - 变频单泵（VFD_SINGLE）

2. **泵组场景**：
   - 同构泵组（HOMOGENEOUS_GROUP）：同型号泵，频率差<2%，功率差<10%
   - 异构泵组（HETEROGENEOUS_GROUP）：纯VFD，功率差≥10%
   - VFD频率异构（VFD_HETEROGENEOUS_FREQ）：纯VFD，频率差≥2%
   - 混合泵组（MIXED_GROUP）：VFD+SS混合，功率差<10%
   - 混合异构（MIXED_HETEROGENEOUS）：VFD+SS混合，功率差≥10%

**需要生成的曲线类型**：
- Q-H曲线（流量-扬程）
- Q-P曲线（流量-功率）
- Q-η曲线（流量-效率）

**完整流程步骤**：
1. 生成模拟数据（插入到fact_measurements表）
2. 执行P0管道（单泵曲线拟合）：阶段1-13
3. 执行P2管道（泵组曲线合成）：阶段14-16
4. 将拟合结果写入数据库（curve_fit_results、curve_fit_params、curve_fit_metrics表）
5. 生成可视化报告：
   - 拟合曲线图（原始数据点+拟合曲线）
   - 残差分析图
   - 质量评估报告（R²、RMSE、MAE等指标）
6. 输出结果文件（JSON格式和图片格式）

**技术要求**：
- 使用已实现的CurveFittingPipeline类
- 使用已实现的P2Pipeline类（泵组场景）
- 调用CLI命令或直接调用Python API
- 确保数据符合物理约束（Q-H单调递减、Q-P单调递增、Q-η单峰）

**输出要求**：
- 每个场景生成独立的演示脚本
- 包含数据生成、执行、验证、可视化的完整代码
- 生成演示报告文档，展示所有场景的拟合结果